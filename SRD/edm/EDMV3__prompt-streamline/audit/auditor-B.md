# SRD Audit Report: EDMV3 -- EDM Plugin Hardening and Prompt Streamline (Auditor B: factual accuracy + diagrams)

**SRD**: `SRD/edm/EDMV3__prompt-streamline/srd.md` v1.0.0, 2413 lines, 111 requirements
**Companions audited**: `architecture.md` (829 lines), `planning.md`, `decisions.md`
**Ground truth**: `plugins/edm/`
**Date**: 2026-07-25

---

## Findings

### P0

B-01 | P0 | high | Competing Requirements / Delivery Plan | srd.md:2228 (Sec 11.1), EDMV3-17, EDMV3-18, EDMV3-19, EDMV3-21, EDMV3-22 | Wave A requirements depend on Wave B requirements; the wave table is unbuildable as written. Evidence: EDMV3-18 (E2, Wave A) declares "Dependencies: EDMV3-41, EDMV3-42" -- both in E4 (Wave B, srd.md:2335). EDMV3-17 (Wave A) declares "Dependencies: EDMV3-11, EDMV3-16, EDMV3-18, EDMV3-36" -- EDMV3-36 is E4/Wave B. EDMV3-22 (Wave A) depends on EDMV3-36. EDMV3-21 and EDMV3-19 (Wave A) depend transitively on EDMV3-18. Sec 11.2 "Ordering constraints" lists none of these. Sec 11.1's Wave-A exit criterion ("The three-command bypass fails") therefore cannot be met in Wave A. architecture.md:759,776 does acknowledge this ("B5 ... completes the archive precondition from A8") -- srd.md does not. Fix: either (a) move the PARTIAL-closure half of EDMV3-17/18 into Wave B and land a reduced Wave-A archive check (gates + current_phase + phase-6 completed_at + converged), or (b) pull EDMV3-41/42/36 into Wave A. Add the resulting edges to Sec 11.2 and restate the Wave-A exit criterion.

B-02 | P0 | high | Factual Mistakes | srd.md:120 (Sec 4.1), srd.md:86 (Sec 3.3), srd.md:1351 (EDMV3-59), srd.md:1719,1721 (EDMV3-81), decisions.md:5 (D1) | Agent roster is stated as 26; the tree contains 30 agent files, and the stated family breakdown sums to 29. Evidence: `ls plugins/edm/agents/*.md` returns 30 files. srd.md:120's own breakdown = 29, not the 26 it states. architecture.md:203 D-1 says "30 agent prompts" -- architecture.md is correct and srd.md contradicts it. EDMV3-81 makes "coverage widened from 1 agent to 26" a merge-request acceptance criterion, so the wrong number becomes a testable falsehood. Fix: change 26 -> 30 at srd.md:120, :86, :1351, :1719, :1721 and decisions.md:5; correct the family breakdown to 11 lenses + 1 synthesizer + 10 test agents + 4 writer/auditor + explorer/implementer/qc-auditor/architect.

### P1

B-03 | P1 | high | Diagram Errors / Competing Requirements | srd.md:247-276 (Sec 5.3, diagram 2) vs architecture.md:278-312 (D-3) and EDMV3-33/34 | Sec 5.3 shows the synthesizer writing the JSONL to bin/edm-state and rendering the markdown itself -- the exact dual-output pattern EDMV3-33 declares a failing condition. Evidence: srd.md:258 `SYN->>ST: write findings-ledger.jsonl` and :259 `SYN->>ST: render findings-ledger.md from the JSONL` (ST = `bin/edm-state`). EDMV3-33 AC2: "The synthesizer does not write `findings-ledger.md` directly ... A synthesizer that writes both is a failing condition." EDMV3-34 AC6: "The code-audit skill calls `render-ledger` immediately after the synthesizer returns." architecture.md D-3 has it right. Sec 5.3 is cited as "the normative sequence" by EDMV3-20 AC2. Fix: redraw Sec 5.3 to add an `LG as findings-ledger.jsonl` participant, route `SYN->>LG` for the write, and make the render a `CA->>ST: edm-state render-ledger PREFIX` message.

B-04 | P1 | high | Diagram Errors | srd.md:230-231 (Sec 5.2) and srd.md:269-272 (Sec 5.3) | Both SRD diagrams invert the permission-layer relationship: they show bin/edm-state calling the permission layer, after the kernel has already been reached. Evidence: srd.md:229-232: `PHASE -->|bash| KERNEL`, `KERNEL --> ASK`, `ASK -->|human click| U`, `KERNEL --> STATE`. srd.md:269-272: `CA->>ST: edm-state approve-gate ...`, then `ST->>PM: Bash matches the ask rule`, `PM-->>ST: proceed`. The permission layer intercepts the Bash tool call *before* `edm-state` executes -- that is the entire point of Sec 5.1 Tier 1. architecture.md D-1 and D-3 both draw it correctly. As drawn in the SRD, T1 is downstream of T2 and buys nothing. Fix: in 5.2 change to `PHASE -->|bash| ASK`, `ASK -->|human click| U`, `ASK -->|allow| KERNEL`. In 5.3 change to `CA->>PM: Bash edm-state approve-gate ...`, `PM->>H`, `H-->>PM: allow`, `PM->>ST: execute`.

B-05 | P1 | high | Competing Requirements | srd.md:283-304 (Sec 5.4) vs architecture.md:445-462 (D-8) and EDMV3-17 | The two "normative" archive-lifecycle diagrams disagree on what prototype mode exempts and on whether current_phase is mode-derived. Evidence: Sec 5.4 puts `C{"mode == prototype?"}` as the SECOND check, branching straight to the move -- prototype skips gates, current_phase, phase-6 completion, and PARTIALs. architecture.md D-8 puts prototype only at `C5 -->|"true, or mode is prototype"| MV`, so a prototype still must pass gates, phase, completed_at and PARTIAL checks. Sec 5.4 also hardcodes `F{"current_phase == 6?"}` while D-8 uses `C2{"current_phase equals the terminal phase for this mode"}` and EDMV3-17 AC3 requires mode-derivation. srd.md:29 declares architecture.md canonical "on diagram detail" while srd.md:613 declares "Section 5.4 is the normative decision flow" -- two normative sources that contradict each other. Fix: pick one. Recommend adopting D-8's shape in Sec 5.4 and update EDMV3-17 AC1 from "current_phase == 6" to "current_phase equals the mode's terminal phase".

B-06 | P1 | high | Feature Gaps / Factual Mistakes | EDMV3-43 (srd.md:1067), EDMV3-39 (srd.md:1001), srd.md:98 (Sec 3.4) | The vocabulary checker's scan scope excludes two trees that actually contain the prohibited tokens, so the grep-clean acceptance criterion cannot pass. Evidence: EDMV3-43 AC1 scans `plugins/edm/skills/`, `agents/`, `docs/`, `CLAUDE.md`, `README.md`. `grep -rn -i defer plugins/edm/` returns 44 hits across 14 files, including `plugins/edm/hooks/hooks.json:117` and `plugins/edm/bin/tests/wave4b-smoke.sh` (2 hits) -- neither tree is scanned, and no EDMV3 requirement edits hooks.json's prompt text. EDMV3-39 AC6 nonetheless requires `grep -rn 'defer' plugins/edm/` to return only three allowlisted classes. Sec 3.4 further says the checker "exits 0 over plugins/edm/" (whole tree), contradicting EDMV3-43's narrower enumeration. Fix: add `plugins/edm/hooks/hooks.json` and `plugins/edm/bin/` to EDMV3-43's scan scope; add `hooks/hooks.json:117` to EDMV3-40's Target Components as an edit site; reconcile Sec 3.4's whole-tree wording with EDMV3-43 AC1.

B-07 | P1 | high | Feature Gaps | EDMV3-43 (srd.md:1065,1071), EDMV3-40 (srd.md:1019), architecture.md:696 (R-H), srd.md:2276 (RK-8), srd.md:47 | "Two currently-green assertions" undercounts: at least three wave4b assertions go red, and the sweep's enumerated edit sites miss implement/SKILL.md:200. Evidence: `bin/tests/wave4b-smoke.sh:36` asserts `deferred-to-runtime`; `:38` asserts the literal string `do not require remediation` (which EDMV3-40 AC2 removes from `skills/implement/SKILL.md:105`); `:40` asserts `deferred-to-runtime: call the endpoint`, whose target text lives at `skills/implement/SKILL.md:200` -- a line outside every range EDMV3-40 lists. `skills/implement/SKILL.md` also carries `## Deferred Work` (:121) and a `Deferred-to-runtime note` table header (:130), neither enumerated. Fix: change "two" to "three" at srd.md:47, :1065, :2276 and architecture.md:696; add `wave4b-smoke.sh:38` and `skills/implement/SKILL.md:121,130,200` to EDMV3-40/43 Target Components.

B-08 | P1 | high | Competing Requirements | srd.md:100 (Sec 3.4), EDMV3-90 AC2 (srd.md:1855) vs EDMV3-16 AC5 (srd.md:601), EDMV3-17 AC7 (srd.md:621) | Two Must-Have acceptance criteria cannot both pass: a repo-wide grep must find zero `--force` while two other requirements mandate tests that contain `--force`. Evidence: Sec 3.4 and EDMV3-90 AC2 both require `grep -rn -- '--force\|--accept-partials' plugins/edm/bin plugins/edm/skills plugins/edm/agents` to return zero results, "asserted in CI". EDMV3-16 AC5 and EDMV3-17 AC7 mandate negative tests containing those literal strings inside `plugins/edm/bin/tests/`. Fix: scope the grep to exclude `plugins/edm/bin/tests/` (or restrict to `bin/edm-*` executables and prompt files) and add an explicit allowlist entry for the negative tests in EDMV3-43's allowlist file.

B-09 | P1 | medium | Competing Requirements | EDMV3-09 AC5 (srd.md:488), EDMV3-94 AC4 (srd.md:1926) vs bin/edm-state:1243-1256 | The requirement that new anomalies "not change the exit code of validate" is impossible given cmd_validate's current contract. Evidence: `cmd_validate` at `bin/edm-state:1247-1253` returns 3 whenever `state_anomalies` emits any line. EDMV3-09 AC2 requires `state_anomalies` to emit `PERM_RULES_MISSING`; AC5 requires that absence "does not change the exit code of validate; it is a warning only." Both cannot hold without restructuring `cmd_validate` to distinguish informational from blocking anomalies -- a change no requirement asks for. EDMV3-94 AC4 compounds it. Fix: add an explicit sub-requirement: `state_anomalies` emits a severity marker per code and `cmd_validate` exits 3 only on blocking anomalies, 0 (with output) on informational ones. Name `cmd_validate` in EDMV3-09 and EDMV3-94 Target Components.

B-10 | P1 | medium | Competing Requirements | EDMV3-27 (srd.md:789-794) vs architecture.md:174 (Plane 4) vs architecture.md:485 (D-9) | The eval's five scoring dimensions have three different memberships across the two documents, and the diagram shows only four. Evidence: EDMV3-27 dimensions: requirement-ID coverage, AC testability, Mermaid parse success, coverage-map bidirectionality, lens JSONL/prose agreement. architecture.md:174: swaps the fifth for "artifact presence and section floors". architecture.md D-9 node E2 lists only four. EDMV3-31 AC3 and R-D depend specifically on dimension 5 being the lens-agreement check. Fix: make EDMV3-27's five the single definition; update architecture.md Plane 4 and the D-9 E2 node label to match, or add a sixth dimension explicitly.

B-11 | P1 | medium | Competing Requirements | architecture.md:174,486-488 (D-9, scorer) vs EDMV3-27, EDMV3-52 AC2, EDMV3-28 AC2 | The scorer's pass/fail threshold differs between documents: exact baseline vs baseline minus observed variance. Evidence: architecture.md Plane 4: `score-artifacts.sh` "exit 1 when total drops below `evals/baseline/scores.json`". EDMV3-52 AC2: "at or above baseline minus the observed run-to-run variance recorded in EDMV3-28." EDMV3-27 gives the scorer no baseline-comparison responsibility at all. With three baseline runs and non-zero variance, the architecture's strict threshold will red the pipeline on noise. Fix: add an AC to EDMV3-27 stating the scorer emits `scores.json` only (no baseline comparison); move the comparison into the CI job per EDMV3-52's variance rule; update architecture.md Plane 4 and D-9's CMP label.

B-12 | P1 | medium | Competing Requirements | EDMV3-76 AC1 (srd.md:1629) vs architecture.md:133 (Plane 1) and architecture.md:578 (Integration Point 7) | The two documents name different default insertion headings for `update-patterns`. Evidence: EDMV3-76 AC1: "`## Anti-Patterns` is the default target." architecture.md:133/:578: "`## Top Recurring Findings`; if absent the command skips with a message." Verified current behavior: `bin/edm-state:1668-1675` appends at EOF with no heading logic. Fix: decide once and state it in both files. Note EDMV3-55 places the Mermaid entries under `## Anti-Patterns`, so Anti-Patterns as default is the self-consistent choice for srd.md; architecture.md then needs correcting.

B-13 | P1 | medium | Specification Quality | EDMV3-54 (srd.md:1256-1269), EDMV3-58 AC2 (srd.md:1333), srd.md:40, srd.md:1330 | "Eleven touch points" is asserted four times but EDMV3-54 enumerates only nine, and EDMV3-58 calls it twelve. Evidence: EDMV3-54's title says "Eleven touch points"; its ACs cover only 3 agents + 2 auditors + 4 skills = 9 files; the two pattern-library docs are a separate requirement (EDMV3-55). EDMV3-58 AC2 says "the eleven touch-point files enumerated in EDMV3-54" -- a list that does not exist at that cardinality -- while EDMV3-58's Description says "eleven of the twelve touch points are prompt text". Fix: state the canonical touch-point list once, as a numbered table in EDMV3-54 with exactly N rows and file paths, and have EDMV3-58 reference that table. Reconcile 11 vs 12.

B-14 | P1 | medium | Competing Requirements / Feature Gaps | EDMV3-49 Description (srd.md:1171) | The stated safety justification for deleting the weak gate protocol is false under the SRD's own threat model. Evidence: EDMV3-49: "This is safe to delete now because EDMV3-08 means a drifted skill cannot silently record an approval anyway." EDMV3-08 only *documents* a `permissions.ask` setting the plugin cannot ship (EDMV3-08 AC4, R-C, RK-3 rated Medium likelihood). Nothing in T2 prevents `cmd_approve_gate <PREFIX> 2` from succeeding when called by a drifted skill -- verified at `bin/edm-state:590-609`. On any install without the ask rules, a drifted skill records approvals exactly as before. Fix: rewrite the justification: deletion is safe because the phase skills stop containing approval-recording text at all (EDMV3-48 AC3). Optionally surface the `enforcement: prose-only` tag (EDMV3-10) at gate presentation.

B-15 | P1 | medium | Feature Gaps | EDMV3-16 (srd.md:597), architecture.md:126 (Plane 1), architecture.md:420,436-437 (D-6, D-7), architecture.md:552 | architecture.md gives `phase-complete 6` a zero-open-PARTIALs precondition that no SRD requirement specifies. Evidence: architecture.md:126: "Phase 6 additionally requires zero PARTIAL entries." D-7 draws the branch; D-6:420 and error-paths:552 repeat it. EDMV3-16's per-phase map stops at "phase 6 -> qc/qc-summary.md" and its ACs never mention PARTIALs; EDMV3-18 puts the PARTIAL check only on `cmd_archive`. Since srd.md:29 declares architecture.md canonical on diagram detail, an implementer reading D-7 will build a check the SRD does not authorize -- and it interacts with EDMV3-70's ordering. Fix: either add the phase-6 PARTIAL precondition as an explicit AC on EDMV3-16, or delete it from architecture.md Plane 1, D-6, D-7 and the error-paths table.

B-16 | P1 | medium | Feature Gaps | EDMV3-45 (srd.md:1106,1112,1114) vs bin/edm-state:1194-1237 | Step 0's gate-check cannot satisfy two of its own ACs, and bin/edm-state is missing from Target Components. Evidence: `cmd_gate_check` recognizes only `srd|audit-srd`, `tickets`, `audit-tickets|implement`; every other token falls through to `*) return 0`. EDMV3-45 AC1 requires Step 0 in `plan`, `code-audit` and `verify-runtime` too -- for those it is an unconditional no-op. AC7 requires mode-aware passes -- `cmd_gate_check` has zero `skipped_phases` or mode awareness today. Target Components lists only the eight SKILL.md files and `hooks/hooks.json:13-78`; `bin/edm-state` is absent. Fix: add `plugins/edm/bin/edm-state` (`cmd_gate_check` at 1194-1237) to EDMV3-45 Target Components and add ACs for the new command tokens and skipped-phase/mode awareness. Reconcile with EDMV3-108 AC3 ("cmd_gate_check's numeric comparison logic is unchanged").

B-17 | P1 | medium | Factual Mistakes | EDMV3-17 AC3 (srd.md:617), EDMV3-16 AC4 (srd.md:600) vs bin/edm-state:344-346 and architecture.md:464-468 | `gated_phase_for_gate` alone cannot derive mode-required gates; the mode -> terminal-phase table it needs does not exist and is not required anywhere. Evidence: `gated_phase_for_gate` is a gate-to-phase map only, entirely mode-blind. architecture.md:466-467 supplies the missing half ("the mode's terminal phase (prototype -> 2, everything else -> 6)") -- which makes mini-srd terminal phase 6, directly contradicting EDMV3-16 AC4's example. The SRD also conflates the orthogonal `mode` and `lifecycle_mode` axes (verified at `bin/edm-state:1437-1467`). Fix: add an AC creating an explicit `terminal_phase_for_mode()` helper with the full enum and state whether `lifecycle_mode` also participates. Fix the mini-srd example in EDMV3-16 AC4.

B-18 | P1 | medium | Feature Gaps | EDMV3-33 AC4 (srd.md:896), architecture.md:148,161 | The `deferred` status value that D13 abolishes survives in the SRD's ledger requirements. Evidence: `agents/edm-audit-synthesizer.md:137` contains a worked ledger row `| CA-003 | P2 | deferred | ...`; `:140` states "Status values: open, fixed, deferred. ... A deferred finding is excluded from the convergence blocking set"; `:157` prints "{D} deferred". architecture.md:148 explicitly requires "Status enum is open | fixed | noted -- deferred is deleted (D13)" and :161 names `:116,137,140`. srd.md's EDMV3-33 AC4 instead says "Cross-round semantics are preserved exactly as today" and EDMV3-39 AC4 names only the worked example at `:116`. `:137,140,157` are never named as edit sites. Fix: add an AC to EDMV3-33 stating the status enum is exactly `open | fixed | noted` and name `agents/edm-audit-synthesizer.md:137,140,157` (and its local severity table at `:60-61`) as edit sites in EDMV3-39.

B-19 | P1 | medium | Diagram Errors / Competing Requirements | srd.md:194 (Sec 5.2), srd.md:327, EDMV3-46 AC1 vs architecture.md:157, architecture.md:357 (D-5), planning.md:104 | The dispatcher's line budget is 220 in srd.md and ~200 in architecture.md and planning.md, including inside diagram node labels. Evidence: srd.md:194 node ORCH label "max 220 lines"; EDMV3-46 AC1 "at most 220 lines, asserted by a smoke test". architecture.md:157 "(645 -> approx 200 lines)"; D-5 node D label "approx 200 lines"; planning.md:104 "~200 lines". EDMV3-46 AC1 is a hard numeric test threshold. Fix: fix the number in one place. Recommend 220 everywhere (srd.md wins on requirement wording) and update architecture.md:157, D-5's node label, and planning.md:104.

B-20 | P1 | medium | Competing Requirements | srd.md:266 (Sec 5.3) and EDMV3-20 AC1 vs architecture.md:302 (D-3) | The convergence gate's AskUserQuestion header string differs between the two documents, and a smoke assertion depends on the exact string. Evidence: srd.md:266 header "Convergence" (EDMV3-20 AC1, AC8 assert it). architecture.md D-3:302: header "Code audit". Fix: change architecture.md D-3:302 to `AskUserQuestion header Convergence`.

B-21 | P1 | medium | Factual Mistakes | srd.md:134 (F1), EDMV3-17 | The F1 description of `cmd_archive` omits the `product_name` condition, which is a second, larger hole than the one described. Evidence: `bin/edm-state:887`: `elif [[ "$converged" == "false" && -n "$product_name" ]]; then die "archive refused: ..."`. A flat-layout initiative with `code_audit_converged=false` archives with no refusal at all. srd.md:134 describes only "checks one boolean". EDMV3-17's ACs do not mention removing the `product_name` coupling. Fix: add the coupling to F1's evidence and add an explicit EDMV3-17 AC: the refusal is unconditional on `product_name`; a smoke case covers a flat-layout initiative.

B-22 | P1 | low | Factual Mistakes / Additional Concerns | EDMV3-08 (srd.md:468), architecture.md:182 | The `permissions.ask` pattern is stated as covering "any caller" but only matches the literal Bash prefix; no requirement addresses trivially equivalent invocations. Evidence: rules are `Bash(edm-state approve-gate*)` / `Bash(edm-state archive*)`. `bash -c "edm-state archive X"`, an absolute path, or `sh -lc '...'` would not match, and every audit lens and the code-audit skill hold broad `Bash(edm-state *)` (verified `skills/code-audit/SKILL.md:8`). Sec 5.1 claims T1 "cannot be bypassed by ... side entry paths". architecture.md:811 rejects a PreToolUse hook, leaving no compensating control. Fix: add an AC documenting the matcher's literal-prefix limitation, consider adding equivalent absolute-path and `bash -c` patterns to the documented rule set, and downgrade the Sec 5.1 claim to "cannot be bypassed by prompt drift, compaction, or a persuasive transcript".

### P2

B-23 | P2 | high | Factual Mistakes | srd.md:119 (Sec 4.1) | Skill count stated as 12; the enumeration lists 13 and the tree contains 13. Fix: change 12 -> 13 at srd.md:119 and :1351 (14 after verify-runtime).

B-24 | P2 | high | Factual Mistakes | srd.md:123 (Sec 4.1) | Hooks stated as "6 event families in one file"; `hooks/hooks.json` has 7 (SessionStart, UserPromptExpansion, PreToolUse, Stop, PreCompact, SubagentStop, TaskCompleted). Six is the post-EDMV3-82 count. Fix: change to 7 and note that EDMV3-82 reduces it to 6.

B-25 | P2 | high | Factual Mistakes | EDMV3-72 (srd.md:1559,1568) | `get_session_tokens_since` range is 206-231; the function is 206-225. Fix: correct to 206-225.

B-26 | P2 | high | Factual Mistakes | EDMV3-73 (srd.md:1575,1584), srd.md:140 (F6) | `compute_cost_usd` range is 233-267; the function is 233-262. Fix: correct.

B-27 | P2 | high | Factual Mistakes | EDMV3-74 (srd.md:1600) | `human_cost_for_phase` range is 268-290; the function is 268-281 -- the cited range covers an unrelated function. Fix: correct.

B-28 | P2 | high | Factual Mistakes | EDMV3-71 (srd.md:1542,1552) | `cmd_audit_round_start` range is 1394-1411; the function is 1394-1406. architecture.md:130 has it correct. Fix: correct in srd.md.

B-29 | P2 | high | Factual Mistakes | EDMV3-42 (srd.md:1048,1058), EDMV3-18 (srd.md:630,639), srd.md:141 (F7) | `cmd_record_partial_verdict` range is 1412-1432; the function is 1412-1424. Cited three times. Fix: correct in all three places.

B-30 | P2 | medium | Factual Mistakes | EDMV3-111 AC1 (srd.md:2206) | State-derived path resolution cited as `bin/edm-state:131-186`; `initiative_dir_for` begins at 125, `state_file_for` at 142, `session_dir_for_cwd` at 194. Fix: correct to 125-192.

B-31 | P2 | medium | Factual Mistakes | EDMV3-65 (srd.md:1444) | `skills/plan/SKILL.md:116` is quoted verbatim but the file says something different. The claim presents a shared literal string across two files; only orchestrator:305 matches. Fix: quote each site separately or paraphrase.

B-32 | P2 | medium | Diagram Errors | srd.md:286 (Sec 5.4), srd.md:304 | The diagram has three non-refusing early exits; the prose says two, and node Z1 is a dangling terminal. Node D's label says "lifecycle fields absent?" while the prose says the signal is an absent `schema_version`. Fix: add `Z1 --> M`, change "two" to "three" (or drop the B branch), and align node D's label with `schema_version`.

B-33 | P2 | medium | Diagram Errors | srd.md:309-319 (Sec 5.5) | Diagram omits the render-ledger step and orphans the prose-report node; `LJ --> UP` asserts a JSONL->update-patterns data path no requirement specifies (`bin/edm-state:1678` reads `### ` headings from the audit report). Fix: insert a render-ledger node between LJ and LM, add a dotted `P -.-> SY` edge, and either add an AC making update-patterns JSONL-aware or drop the `LJ --> UP` edge.

B-34 | P2 | medium | Diagram Errors | srd.md:233,235 (Sec 5.2) | Two edges contradict 5.5 and the requirement text on producer/direction: `AGENTS -->|one JSON line per finding| JSONL` (per EDMV3-30/33 the lenses write `lens-L{N}.jsonl`; only the synthesizer writes `findings-ledger.jsonl`); `CONV --> JSONL` reversed vs 5.5's `LJ --> AC`. Fix: split the JSONL node into per-lens and ledger nodes in 5.2, and settle one convention for "reads" edges.

B-35 | P2 | medium | Diagram Errors | architecture.md:485-489 (D-9) | D-9 chains all stages linearly, contradicting EDMV3-103 AC2 ("Stages run in parallel where they have no dependency"); node V1 is a job EDMV3-23 never specifies; V2's conditionality contradicts EDMV3-23 AC4. Fix: redraw with parallel lint jobs converging on test, and either add V1 and the conditionality to EDMV3-23 or remove them from D-9.

B-36 | P2 | medium | Diagram Errors | architecture.md:371-388 (D-5) | Seven of the eight phase-skill nodes are terminal; only S1 connects to the Step 0 / body / return chain, so the diagram literally shows six phase skills that do nothing. Fix: draw `S1 & S2 & ... --> PRE` or collapse S1..S8 into one representative node with a note.

B-37 | P2 | medium | Diagram Errors / Specification Quality | architecture.md:429-431 (D-7) | The skipped-phase branch bypasses the phase-6 PARTIAL check, creating an unrecorded exemption: a phase 6 entered into `skipped_phases` would complete with open PARTIALs. Fix: route the skipped branch past the artifact check only, keeping the phase-6 PARTIAL gate: `SK -->|yes| P6`.

B-38 | P2 | medium | Competing Requirements | architecture.md:128,551 vs EDMV3-36, EDMV3-100 AC4 | `cmd_audit_converged`'s exit-2 semantics exist only in architecture.md and clash with the SRD's exit-code convention (2 = usage/environment error). EDMV3-11 AC4 never mentions a `ledger: absent` record that architecture.md:551 specifies. Fix: add explicit exit-code ACs to EDMV3-36 and add the `ledger: absent` field to EDMV3-11 if wanted.

B-39 | P2 | medium | Competing Requirements | EDMV3-53 AC1 (srd.md:1242) and EDMV3-54 AC5 (srd.md:1265) vs architecture.md:163 | The canonical Mermaid section has two different names across the documents, and a smoke test asserts the exact heading: srd.md `## Mermaid diagram conventions (canonical)` vs architecture.md `## Mermaid diagram rules`. Fix: change architecture.md:163 to match srd.md.

B-40 | P2 | medium | Competing Requirements | EDMV3-25 (srd.md:756), EDMV3-27 (srd.md:799), EDMV3-43 (srd.md:1076) vs architecture.md:172,174,139 | Three new-file paths differ between the documents: fixture (`evals/fixture/` vs `evals/fixtures/tiny-svc/` + `expected.json`), vague-AC regex source, vocabulary list paths/polarity. Fix: reconcile all three path pairs in one pass.

B-41 | P2 | medium | Specification Quality | srd.md:104 (Sec 3.4) | A Definition-of-Done bullet is written as a shell command that cannot be executed (grep cannot scope to fence regions -- that is why EDMV3-56 exists). Fix: rewrite as "`edm-lint-artifacts --all` exits 0 with class 4 enabled over the repository's tracked artifact trees".

B-42 | P2 | medium | Specification Quality | EDMV3-56 AC7 (srd.md:1301) vs EDMV3-96 AC2 (srd.md:1961) | Two requirements prescribe incompatible fixes for the same `usage()` line; the truncation is already live (`bin/edm-lint-artifacts` header runs to :22, `sed -n '2,19p'` cuts :20-22). Fix: delete EDMV3-56 AC7 in favour of EDMV3-96, and note the pre-existing truncation.

B-43 | P2 | low | Reuse Opportunities | EDMV3-07, EDMV3-15, EDMV3-43, EDMV3-79 | Four new grep-and-report scripts each re-implement the file-walk, ignore-list and `path:line: <class>: <detail>` reporting that `bin/edm-lint-artifacts` already provides (`report_violation` :59-63, `build_ignore_set` :69-109, `is_ignored_line` :112). Fix: direct the new scripts to source or mirror those helpers, or land as additional `edm-lint-artifacts` classes where the target tree is the plugin itself.

B-44 | P2 | low | Feature Gaps | EDMV3-17, EDMV3-36, EDMV3-11 | A `fast-track` / `mini-srd` / `fix-pack` initiative that never runs a code audit can never archive: EDMV3-17 AC1 makes convergence an unconditional precondition (only prototype exempted); EDMV3-36 AC4 makes a missing ledger non-zero for any non-legacy initiative. Fix: add an AC to EDMV3-17 stating which modes/lifecycle modes require convergence, and an AC to EDMV3-36 for the "no audit is required for this mode" case.

B-45 | P2 | low | Additional Concerns | EDMV3-05, EDMV3-93, EDMV3-31 | The 11 lenses gain `Write` with `Bash` still denied, yet writing requires the pass directory to exist. This works only because `skills/code-audit/SKILL.md:40` runs `mkdir -p "${OUTPUT_DIR}"` before spawning -- an unstated dependency. Fix: add a one-line AC to EDMV3-05 noting the lenses rely on the skill having created `${OUTPUT_DIR}` and do not need a Bash grant.

B-46 | P2 | low | Specification Quality | EDMV3-101 (srd.md:2048-2052), EDMV3-102 (srd.md:2064-2067) | Latency budgets reference an unnamed "reference environment". Fix: define it in the SRD or restate budgets as relative to a same-machine baseline (EDMV3-102 AC2 already does this correctly).

B-47 | P2 | low | Factual Mistakes | srd.md:28 (Sec 1) | The document asserts every path:line reference "was verified by direct read before being written here"; eight ranges are demonstrably wrong (B-25..B-31, B-21). Symbol names are correct in all cases. Fix: soften to "symbol names are authoritative; line ranges are indicative and were spot-checked", or fix the eight ranges and keep the claim.

B-48 | P2 | low | Diagram Errors | srd.md:283-302 (Sec 5.4) | Inline node definition chained onto a labelled edge (`C -->|yes| W1["..."] --> M`) is unusual syntax some Mermaid versions render inconsistently; not used anywhere in architecture.md's nine diagrams. Fix: split into two statements per edge.

B-49 | P2 | low | Feature Gaps | EDMV3-11, EDMV3-36, Sec 5.3 | Sec 5.3 omits the re-check-inside-the-command step that EDMV3-11 AC4 requires and architecture.md D-3 draws (TOCTOU guard). Fix: add the re-check message between `PM-->>ST: proceed` and the state write.

### NOTED

- Sec 5.2 and 5.3 use `#59;` correctly inside node/message text (srd.md:204, :219). architecture.md uses it correctly at :269, :299, :346, :386, :422, :433. No violation found -- the document eats its own dog food.
- Both srd.md and architecture.md are 100% ASCII (ripgrep [^\x00-\x7F]: zero matches in srd.md, architecture.md, planning.md). The pass is genuine, not an artifact of the ignore set.
- The 645-line orchestrator claim is correct.
- "36 subcommands" (srd.md:121, EDMV3-97) is correct: dispatch case at bin/edm-state:1981-2016 contains exactly 36 entries.
- The 13-agent grant class is correct: 15 agents carry `disallowedTools: Write, Edit, NotebookEdit`, but edm-srd-auditor and edm-ticket-auditor have no write instruction in any of the three sources, so the class is the 11 lenses + explorer + qc-auditor = 13. edm-audit-synthesizer already has Write. EDMV3-07's "exactly 13" holds.
- EDMV3-53's claim that tree-wide greps for "#59", "&#" and "semicolon" return zero matches in plugins/edm/ is verified.
- EDMV3-88 / D8 (no Mermaid renderer validation spike) is an explicit, recorded user decision. Not re-raised.
- RK-9 / R-G (removing override flags pushes users to hand-edit state) is documented as knowingly accepted. Not re-raised.
- R-D / EDMV3-31's residual risk (count match does not imply content match) is documented as accepted. Not re-raised.
- architecture.md D-1's hook list omits TaskCompleted -- correct: D-1 depicts the post-EDMV3-82 target state.
- Node-id collision across documents (LJ, LM meaning different things) is confusing on side-by-side read but not a rendering or correctness defect.

---

## Verification tally

**Anchors spot-verified: 96. Passed: 85. Failed or materially imprecise: 11** (B-02 agent count, B-23 skill count, B-24 hook families, B-25..B-29 five function ranges overrunning 5-9 lines, B-30 path-resolution range, B-31 plan/SKILL.md:116 misquote, B-21 cmd_archive behaviour under-described).

Passing highlights include every load-bearing anchor: `cmd_set:479` boolean allowlist, `:491-494` generic branch, `cmd_approve_gate:590-609` with `:597-602` Gate 3.5 branch, `cmd_archive:860-903`/`:877-889`, `gated_phase_for_gate:344-346`, `cmd_gate_check:1194-1237`, `cmd_update_patterns:1576-1692`/`:1668-1675`, dispatch `:1980-2023`, `sed -n '2,39p'` at `:2018` (header ends :39), `edm-init:139`/`:148-168`, `hooks.json:13-78`/`:86`/`:117`/`:122-131`, wave4b-smoke `:36,:38,:40`, EDMV2 archived state (`6_phase` started_at with no completed_at, `audit_rounds.code=2`, `code_audit_converged: true`), README.md:11,14, both binaries present in plugins/edm/.

## Diagram verdict

**13 diagrams audited (4 in srd.md Sec 5.2-5.5, 9 in architecture.md D-1..D-9).**

- **Semicolon rule: PASS, zero violations.** No raw `;` inside any node, edge, or message label across all 13 diagrams; every literal semicolon written `#59;` with no leading ampersand.
- **Syntax: PASS.** All fences `mermaid`; headers well-formed; subgraph/end and alt/else/end balance everywhere. One low-confidence style flag (B-48).
- **Logic and prose fidelity: FAIL on 4 of 13.** Sec 5.2/5.3 invert the permission layer (B-04); Sec 5.3 shows a render the requirements forbid (B-03); Sec 5.4 contradicts D-8 on prototype scope and terminal phase (B-05, B-32); Sec 5.5 omits render-ledger and draws an unspecified data path (B-33, B-34). D-5 leaves seven phase-skill nodes terminal (B-36); D-7's skip branch bypasses the PARTIAL check (B-37); D-9 serialises stages and shows four of five dimensions (B-35). D-1, D-2, D-3, D-4, D-6, D-8 individually sound.
- **Cross-diagram naming: MIXED.** Consistent within each document; node-id semantics collide across documents.

## Overall verdict

**FAIL** -- two P0 and twenty P1 findings must be closed before Phase 4. This is a genuinely strong SRD: 111 well-formed requirements with unique IDs, exact priority arithmetic (76/25/5/5), complete epic map, explicit non-goals, full traceability. File:line discipline is unusually good -- 85 of 96 anchors exact, including every load-bearing one. The document obeys the Mermaid rule it establishes: zero raw semicolons across all 13 diagrams. What blocks Phase 4: (1) a delivery plan whose Wave A cannot be built because five Wave-A requirements depend on Wave-B requirements the plan never acknowledges, and (2) an agent-count error repeated in a merge-request acceptance criterion, plus a divergence cluster between srd.md and architecture.md (archive prototype semantics, gate header string, dispatcher line budget, canonical section name, update-patterns heading, eval fifth dimension and threshold, three file paths, phase-complete-6 PARTIAL precondition) that guarantees a ticket writer reading both documents produces contradictory tickets. Fix the two P0s, reconcile the cross-document set in one editing pass, close the vocabulary-scope and --force-grep contradictions, and correct the four diagram logic errors; the eleven anchor imprecisions can be swept in the same pass.
