# EDMV3 - Session Handoff

> **Last updated**: 2026-08-17T03:55:10Z by darryl.porter  
> **To resume**: `/edm:orchestrator EDMV3`

## Current Status

- **Phase**: Phase 6 - Implementation
- **Gates approved**: 3 of 3
- **Last gate**: Gate 3 - approved 2026-07-27T01:43:50Z by darryl.porter
- **Product**: edm
- **Description**: prompt-streamline
- **Next action**: Phase 6 in progress - run `/edm:orchestrator EDMV3` to continue, then `/edm:test EDMV3` when all tickets pass QC

## Resume Point

- **Phase**: Phase 6 - Implementation
- **Step**: 6.stage-d-spec-sweep-complete
- **Last decision**: Stage C complete: 13 P2s fixed (CA-401 false-pass site, 408, 418, 419, 454, 458, 463, 464, 465/D59, 466, 468, 469, 470; CA-407 found already fixed). Nine test-infrastructure P2s tagged as candidate Stage E debt set (D60): CA-401 remainder, 402, 403, 404, 405, 453, 455, 456, 457, 459. Suite 2182/0. Next: Stage D round-9 full audit under remediation freeze.

**Pending artifacts for Phase 6 - Implementation**:

_(implementation in progress -- track individual ticket status)_

> Copy-paste to resume: `/edm:orchestrator EDMV3`

## Lifecycle & Mode

- **Mode**: standard
- **Lifecycle mode**: standard
- **Compliance**: false
- **Implementation mode**: standard

## Open Code-Audit Findings

not converged: 52 blocking finding(s) for EDMV3 (P0=0 P1=7 P2=45) of 512 considered, 68 NOTED excluded:
  CA-401 P2 NEW in round 7 (L4-03 and L4-04 merged -- they overlap at :1186 and one edit fixes both): two defects in the same count-extraction block. (a) Unguarded TERMINAL grep in a command substitution under set -euo pipefail: in t66ac3_claude_count=$({ grep -oE '[0-9]+ subcommands' FILE || true; } | head -1 | grep -oE '^[0-9]+') and t40_heading_order at :1911 the FIRST grep is || true-guarded and the terminal one is not, so rewording the 'N subcommands' phrase or the 'Severity vocabulary' heading makes the assignment exit 1 and kills the suite at line 1186, discarding ~1,400 later assertions in the largest suite. Same class with no || true anywhere on the pipeline at :2018, :2019, :358, :2392, :2393 (grep piped to sort -u to wc -l, and ls piped to wc -l -- a zero-match grep poisons the pipeline under pipefail even though wc -l exits 0). The file documents the correct convention itself at :1236-1238 and violates it two lines of context away. (b) Guard asymmetry: [[ dispatch_count == claude_count ]] compares two independently-extracted values with neither validated numeric or non-empty, so the pass message renders as 'count () matches the dispatch table ()' in the both-empty case; the assertion has no floor. The dispatch regex also undercounts by design (an alternation arm counts as one subcommand while --help lists two; an arm whose cmd_ body wraps counts as zero), so merging two subcommands into one arm and decrementing CLAUDE.md keeps it green. Fix: || true on every terminal grep, ^[0-9]+$ floors on both sides before comparing, and count arms by parsing cmd_ function definitions rather than case arms. ROUND 8: STILL OPEN, and now CORROBORATED INDEPENDENTLY BY L1 (L1-004), which extends the class to a SECOND suite -- multi-lens, higher confidence. L4 re-verified 15 unguarded sites in wave7-smoke.sh (:1195, :1937, :358, :373, :1322, :1501, :1504, :2044, :2045, :3621, :3628, :3696, :4296, :4300, :4394) plus the unfloored comparison at :1196. L1 independently found the same class in a SECOND SHAPE -- a bare capture with no guard at all -- at wave6-smoke.sh:285, :291, :892, :3248, :3255, :3261, :3267 and wave7-smoke.sh:3621, :3628, :3696, :4296, :4300. grep -c always prints a count so the VALUE is fine; the exit status is 1 on zero matches, the assignment inherits it, and both suites run under set -euo pipefail, so deleting or renaming BLOCKING_FILTER or schema_at_least kills the suite at that line and discards every later assertion instead of producing the one-line fail count_matches exists to guarantee. Ten of L1's twelve can genuinely reach zero; wave6:291 and wave6:3261 are synthetic controls with guaranteed matches (same shape, benign). wave7-smoke.sh:6875 is NOT in this set -- its capture sits inside a double-bracket test where errexit does not apply. The file documents the correct convention itself at wave7:1253-1254 and violates it. Fix, now covering both suites: route every count capture through count_matches or count_matches_strict, using the strict form and asserting its exit status wherever a fixed path's disappearance must not read as a passing zero (the shape wave7:4319 already uses after the CA-392 fix), and add numeric floors on both sides of :1196. See CA-459 for the false-pass direction of the same bare-capture class.
  CA-402 P2 NEW in round 7 (L4-05): repo-wide grep -r needles that match the assertion's own source, plus three exclusion vocabularies for one check class. All three sites walk ${PLUGIN_DIR}/, which contains bin/tests/wave7-smoke.sh where the needle literal is defined. :2742 and :2798 are 'this string must be gone' assertions whose baseline is therefore permanently at least 1 -- either the threshold was tuned to absorb the self-hit (fragile: it silently allows one real violation) or it accounts for it some way not confirmed; :2018's distinct-form count is inflated by its own regex literal. The correct exclusions exist elsewhere in the same file and diverge from each other: :1444 uses grep -v tests/ (unanchored), :1817 uses grep -v ${PLUGIN_DIR}/bin/tests/ (absolute), and :2742, :2798 and :2018 use none. Fix: one shared harness_grep_plugin_excluding_tests() helper in _harness.sh, called by every repo-wide scan. Folded sub-item from L4's sweep: :2098 uses grep -qi for the Mermaid heading reference where the eight sibling assertions on the same reference (:1980, :1993, :2003, :2108) are case-sensitive, which would accept a lowercased heading; one character, same consistency class. ROUND 8 (L4): STILL OPEN, all three halves unchanged, and L4 supplied the CONCRETE ONE-LINE CORE of the self-match half (raised as L4-11 and folded in here rather than given its own ID): wave7-smoke.sh:2001 defines MERMAID_QUOTED, and grepping the whole tree finds exactly one occurrence of that identifier -- its own definition. It has NO readers. Its only observable effect is that its literal value is precisely what the repo-wide scan 43 lines below (:2044, :2045, scanning the plugin directory) searches for, making it a guaranteed self-hit inside both the raw floor of 11 and the distinct-form count of 1. Deleting :2001 is the fix for that half. The exclusion-vocabulary half is unchanged: five divergent live forms and still no shared harness helper, with none at all at :2044/:2045. The case-sensitivity outlier at :2124 is unchanged. L4 CLEARED :2768 and :2824 as already handled -- both build their needles from two runtime halves so the contiguous phrase never appears in this file's own source and cannot self-match; the self-match half is live only at :2044/:2045.
  CA-403 P2 NEW in round 7 (L4-06): six positive controls were derived from the pattern rather than from the real regression. The CA-037 control convention itself is good and merits credit, but in these six the control string was written to satisfy the regex (for example 'synthetic control: code_audit_converged true' against the flag-leak pattern, and 'a row mentioning missing version header' against the wrong-classes pattern), so each proves only 'the regex compiles and matches a string invented for it' -- not 'the regex matches the shape the real violation had'. A regex loosened during a later edit keeps both the control and the live scan green while no longer detecting anything specific. :2826's control (reproducing real prose, 'synthetic control: Ask: Do you approve of this plan?') is the counter-example done right. Fix: copy control strings verbatim from the pre-fix text each finding was raised against (git history has it) and say so in a comment beside each control. ROUND 8 (L4): STILL OPEN. All six cited controls are unchanged and none carries a provenance comment: wave7-smoke.sh:963, :1137, :1284, :1294, :1304, :1442. Each proves only that the regex matches a string invented to match it, not that it matches the shape the real violation had, so a regex loosened in a later edit keeps both the control and the live scan green. :2852's control remains the in-file counter-example done right, reproducing real retired prose. Fix unchanged. Distinguish from CA-457, which is the ARM-COVERAGE axis of the same control-quality class rather than the PROVENANCE axis.
  CA-404 P2 NEW in round 7 (L4-07): the eval scorer's exit status is discarded and two failed runs prove determinism. The four dimension extractions run bash "$SCORE_ARTIFACTS" run-dir > out-dimN.json 2>/dev/null and then jq the file with 2>/dev/null, so a crash yields an empty JSON file and a jq failure swallowed into an empty dN -- the scorer's exit code is never checked and its stderr is thrown away. The same file demonstrates the right pattern at :563 (jq -e with an explicit ERR sentinel) and does not apply it to the four extractions. Related, :556: 'if diff -q out-a.json out-b.json' establishes run-to-run determinism, but two identically EMPTY outputs from two crashed runs also satisfy diff -q. Partially mitigated by the separate dim_count assertion at :563; a non-empty and valid-JSON precondition on both files before the diff would close it outright. Fix: check the scorer's exit status at all five sites, capture stderr into the failure message, and add the validity precondition before the determinism diff. ROUND 8 (L4): PARTIALLY FIXED, NARROWED. The determinism half is CLOSED -- wave7-smoke.sh:548-554 captures rc_a and rc_b on their own statements and asserts both are 0 before the diff at :556, and :563-580 additionally require 5 dimensions and non-null scores for dimensions 1-4, so two identically-empty crashed outputs now fail three assertions before reaching the diff. WHAT REMAINS OPEN is diagnosis at the four scorer-dimension extraction sites (:607, :635, :648, :678), which all run the scorer with stderr discarded and no rc capture, then read the result with jq, also stderr-discarded. Because each site asserts a literal expected value (50/100/0/80) a crash does FAIL, so the residual is diagnosis rather than a false pass: the failure message reads 'dimension 2 scored , expected the hand-computed value 50' with the scorer's own stderr thrown away. Fix: capture rc on the same statement at all four sites and interpolate the captured stderr into the failure message, using the shape :548-554 and :563 already demonstrate in this same file.
  CA-405 P2 NEW in round 7 (L4-08): three meta-assertions count test scaffolding or markdown shape rather than behavior, using grep -c, which counts LINES and not occurrences -- so two hits on one line count once and any 'expect N' threshold is off-by-one-per-shared-line the moment formatting changes. :2521 counts check_fails or check-quote occurrences inside an extracted block; :1693 counts markdown bullets of the form dash-backtick in an Output section. :1701's grep -ci 'defer' against the synthesizer agent is additionally unanchored and case-insensitive: it fires on 'deferred', 'deferral' and any future word containing the trigraph, which is conservative for an absence check but wrong for any threshold other than zero. Fix: assert on behavior or on named anchors instead of counts where possible; where a count is genuinely wanted, use grep -o piped to wc -l for occurrences and anchor the needle. ROUND 8 (L4): NARROWED to ONE surviving sub-item; the other two are FALSE ALARMS and are recorded here so they are never re-investigated. (a) SURVIVING: the T44 AC8 meta-assertion, now at wave7-smoke.sh:2547, counts harness call sites inside an extracted block against a greater-than-zero threshold, so it passes with 39 of 40 assertions deleted -- a presence-only assertion over test scaffolding. It also counts lines rather than occurrences, though at a zero-adjacent threshold that is immaterial. Fix: drop it (T44's real cases already prove harness use by running) or pin an exact expected count derived from the block. (b) FALSE ALARM, :1710: the pattern is anchored at line start so at most one match per line -- a line count and an occurrence count are identical here and no off-by-one is possible. (c) FALSE ALARM, :1718: the grep is unanchored and case-insensitive but the threshold is exactly zero, so over-matching can only produce a false FAILURE, never a false pass, and the target file's existence is proven by the positive assertion at :1721.
  CA-414 P2 NEW in round 7 (L8 P2-2): echo "$staged" plus git's default core.quotePath is interpreter-dependent. Two hazards compound. First, git diff --cached --name-only C-quotes any path containing non-ASCII or backslash bytes by default, so a path arrives as a literal double-quoted, backslash-escaped string. Second, echo's treatment of backslash is not portable: bash's builtin leaves it alone without -e, but dash's and BusyBox ash's builtins expand escapes unconditionally -- and the interpreter here is chosen by the host, not by a shebang. On an ash-family host an embedded escape becomes a real newline and one staged path becomes two lines; the prefix-extraction awk then sees a truncated first fragment, the grep -E allowlist drops it, and if that was the only staged file from that initiative the hook exits 0 without linting. Not scored as an exploit (the actor who controls staged filenames is the committer, who can bypass with --no-verify) but a real portability defect, and multiple interpreters are a live condition given test:smoke-bash32 and the Alpine CI images. Fix: git -c core.quotePath=false -c diff.relative=false diff --cached --name-only, and printf '%s\n' "$staged" in place of echo -- the file already uses printf correctly for the $out diagnostics two statements later, so this is an internal-consistency fix too.
  CA-416 P1 NEW in round 7 (L9, structural residue of its scope-creep sweep): remediation waves are ledger-tracked and never AC-tracked, so no acceptance criterion anywhere obliges the same-commit doc sweep -- which is precisely why the stale-citation class has now recurred for five rounds (CA-385 is round 7's instance, authored inside round 6 itself). A grep for with_state_lock or write_atomic across SRD/edm/EDMV3__prompt-streamline/tickets/ returns zero matches: neither Wave 3 (commit 2d83898, shared-lint-library extraction) nor Wave 4a (commit 4022300, concurrency hardening) is covered by any epic AC; both appear only in decisions.md, architecture.md, HANDOFF.md, the ledger and per-pass audit artifacts. That absence is NOT scope creep -- findings-ledger.md is this initiative's documented work-item source for audit remediation -- but it leaves the doc-sweep obligation with no owning artifact. Fix: add a mandatory 'spec/AC text swept in same commit: yes/no/n-a' field to the findings-ledger entry template (and to the synthesizer's remediation format) so the obligation lives on the same artifact that authorizes the code change, and have the remediation gate refuse to mark a wave done while any fixed entry carries 'no'. ROUND 8 (L9-001): STILL OPEN, nothing built, and ESCALATED FROM P2 TO P1. Rationale for the escalation: this is the named structural root cause of the stale-citation class, it is now in its second round unbuilt, and the class produced FOUR fresh instances this round -- CA-468 (T66 AC3 cites a case label that exists nowhere), CA-469 (T30 AC4 stale in both halves), CA-470 (T29 AC12 unsatisfiable as written) and CA-431 (five new state fields absent from the state-field table) -- for the fifth consecutive round. Verified this round: NO 'spec/AC text swept in same commit' field, or any equivalent, exists anywhere in plugins/edm/. The synthesizer's Findings Ledger Format field rules (agents/edm-audit-synthesizer.md:159-168) enumerate only id, status, confidence, lenses, raised_round and resolved_round; the Remediation Plan Format (:87-147) carries Problem, Fix, Verification and Files affected and no doc-sweep obligation. A repository-wide grep for the token finds it only inside restatements of the finding itself (HANDOFF.md:83, this ledger, findings-ledger.md, pass-7/REMEDIATION.md:275, pass-7/lens-L9.md:43). Fix unchanged and now urgent: add the mandatory field to the ledger entry template AND to the synthesizer's remediation format, and have the remediation gate refuse to mark a wave done while any fixed entry carries 'no'.
  CA-424 P1 NEW in round 8 (L9-003): T28 AC12 is now FALSE for one of its three input classes. The AC states that cmd_approve_gate <PREFIX> code-audit 'calls the check and refuses when it fails, and cmd_archive calls it as part of lifecycle verification', and the blocking set is defined once at bin/edm-state:1496 (BLOCKING_FILTER) and includes P2. cmd_approve_gate still calls the check but no longer UNCONDITIONALLY refuses when it fails: :2178-2195 inspects the refusal and, when the exit code is 1 with P0 and P1 both zero, P2 greater than zero, and --accept-p2-debt passed, proceeds to record convergence anyway. AC12 was not amended and NO acceptance criterion anywhere describes the new branch, so the ticket pack's statement of cmd_approve_gate's contract is now false. The flag-absent path is unchanged and still refuses correctly, which is precisely why every existing smoke case stays green. Fix: amend T28 AC12 to describe all three input classes explicitly -- (i) refuses without the flag; (ii) refuses WITH the flag when the refusal is not a severity refusal (partial or unknown round type, invalid JSONL, invalid status lines); (iii) converges with the flag when the blocking set is open-P2-only -- and land the negative ACs together with the tests that are missing today (CA-425). Sequence with CA-423: if the SRD change request is refused and the flag withdrawn, this AC needs no amendment at all.
  CA-453 P2 NEW in round 8 (L1-001): CA-389 and CA-390 shipped their CODE HALVES and NONE of their prescribed tests. CA-389 prescribed three wave6-smoke.sh assertions and CA-390 prescribed one; NONE OF THE FOUR EXIST. Verified three ways: a repo-wide grep for CA-389 and CA-390 outside SRD/ returns exactly two hits, both source comments in bin/edm-state; a grep for the rendered string 'Gates approved' across bin/tests/ returns ONE hit and it is a COMMENT (wave6-smoke.sh:644), not an assertion; and required_gate_count appears NOWHERE in any test file. So the rendered gates-approved line emitted at bin/edm-state:5531 -- the line BOTH findings are about -- has NO ASSERTION ANYWHERE IN THE SUITE, exactly as CA-389's own closing sentence warned ('No test anywhere asserts this line today, so the whole CA-335 fix is uncovered'). Both fixes are one careless edit away from silently regressing, and THE CLASS HAS NOW REGRESSED TWICE (CA-335 to CA-389/CA-390). This is the COMPLETENESS HALF of two findings whose code halves are correct and verified, not a re-flag of the code halves; contrast CA-391, which landed code AND check_refuses_and_leaves_state cases in the SAME COMMIT and is the model. Fix: add the four prescribed cases beside the existing G13/CA-391 block -- (i) a prototype initiative renders a denominator of 1; (ii) a fast-track initiative with phases 1, 2, 3 and 5 recorded skipped renders a denominator of 0, NOT 3; (iii) a gate re-approved after approve-gate 1, 2, 1 renders a numerator of 2 of 3, counting DISTINCT gates rather than approval events; (iv) the reconciling note text '3 approval event(s) recorded above for 2 distinct gate(s)' appears in the same HANDOFF.
  CA-455 P2 NEW in round 8 (L4-09): NINE conditional SKIP paths print a SKIP line and increment NEITHER PASS NOR FAIL. The suite summary at :7954 carries no skip term, and run-all.sh:108 parses exactly a two-number Results line, so a SKIPPED CASE IS INDISTINGUISHABLE FROM A CASE THAT NEVER EXISTED. This is not hypothetical: three of the nine (:6589, :7649, :7737) are gated on the presence of flock(1), and macOS -- THE DOCUMENTED PRIMARY DEVELOPMENT PLATFORM per CLAUDE.md Sec.'Testing changes' -- ships none. So on a developer's own machine the G12/CA-345 lock-contention hook contract, the G49 live-timeout case and the G13/CA-347 unremovable-marker case NEVER EXECUTE, and run-all.sh still prints ALL SUITES PASSED. Two more (:7055, :7085) vanish under root, the default user in most container images. Fix: add a SKIP counter and a skip() function to _harness.sh, call it at all nine sites, emit a three-term Results line, and extend run-all.sh's summary regex and its table with a SKIP column, so a silently degraded environment is VISIBLE IN THE AGGREGATE rather than invisible. Note this compounds CA-471: a round can currently be reported green while both a lens's authoritative artifact and three lock-contract test cases are missing.
  CA-456 P2 NEW in round 8 (L4-10): four assertions run against a fixture THE TEST ITSELF WROTE THREE LINES EARLIER. t25_ledger_case writes three literal JSONL lines at :1663-1667, then asserts against them: every line is valid JSON (:1670-1675); every id matches the CA-NNN shape and carries confidence and lenses (:1677); ids are unique (:1681); every status is in the open/fixed/noted enum (:1685). NO PLUGIN FILE IS READ; nothing under test produces or consumes this fixture. These four assertions CANNOT FAIL for any change to any EDM source file -- only if jq itself broke -- and they inflate the pass count without covering anything. The block header at :1626-1632 explains why the synthesizer has no directly-invokable entry point but does not acknowledge that the resulting assertions are vacuous. REAL COVERAGE IS AVAILABLE AND ALREADY PRECEDENTED: edm-state render-ledger consumes exactly this file shape, and wave6-smoke.sh:2875-2906 already drives it. Fix: pipe this fixture through edm-state render-ledger and assert the RENDERED MARKDOWN -- ID ordering, severity grouping, status rendering -- converting four tautologies into real assertions about shipped code. While there, add the CA-378 durability case the round-6 fix never got: a title containing a pipe character must still render a row with exactly eight cells.
  CA-457 P2 NEW in round 8 (L4-12): a SIX-ARM detection regex is proven by a control exercising ONE ARM. T61_BASH4_RE (:951) has six alternation arms guarding the bash-3.2 compatibility constraint -- the associative-array declaration, two array-read builtins, the uppercase and lowercase parameter-expansion forms, and the named-file-descriptor form. Its positive control at :963 feeds a single synthetic line exercising ONLY the first arm. The parameter-expansion and named-fd arms carry NON-TRIVIAL ESCAPING that no control validates; if any of them were mis-escaped, :967's 'zero bash-4-only constructs found' assertion would stay PERMANENTLY GREEN over a live violation while the control still passed on the first arm. This is ADJACENT TO CA-403 BUT A DISTINCT AXIS: CA-403 is about control PROVENANCE (controls authored to satisfy the regex rather than copied from real pre-fix text), this is about control ARM COVERAGE. Fix: loop the control over one synthetic line PER ARM and require EVERY arm to match its own line, failing BY ARM NAME so a mis-escaped arm is named rather than silently tolerated.
  CA-459 P2 NEW in round 8 (L10-206), CA-037's L10 half VERIFIED FOR THE FIRST TIME IN THREE ROUNDS: the positive-control idiom has a canonical harness helper and roughly 17 hand-rolled copies, and they HAVE DIVERGED on the exact defect the helper exists to close. CANONICAL: assert_tree_absent (_harness.sh:235-254) routes BOTH the real and the control haystack through count_matches_strict (:198-207), asserts count_matches_strict's own EXIT STATUS alongside its printed value, and additionally guards EVERY SCAN PATH with an existence test before the needle check (:239-241); 41 call sites (wave7 33, wave6 3, harness-smoke 5). COPIES: wave6-smoke.sh:291, :995, :2805, :3261 and wave7-smoke.sh:409, :940, :963, :1137, :1284, :1294, :1304, :1442, :2852, :3007, :4149, :4206, :7924; eleven of these are the SAME FIVE-LINE SHAPE to within the needle. EXACTLY ONE (wave7-smoke.sh:4204-4208, T47 AC5) uses count_matches_strict and captures its exit code, matching the canonical contract. THE OTHER SIXTEEN use a bare grep count with a true fallback (or grep -l / grep -n variants) and no path-existence guard, violating the harness's own docstring rule at :179-185 verbatim -- 'Any caller whose expected count is 0 MUST pair this with a positive control ... OR use count_matches_strict' -- and, critically, THE POSITIVE CONTROL DOES NOT SUBSTITUTE FOR THE SECOND PROTECTION: it proves the pattern CAN match, not that the path was READ. CONCRETE LIVE FALSE-PASS INSTANCE at wave7-smoke.sh:1438: a wrong plugin directory, a renamed skills layout, or a glob expanding to nothing yields grep exit 2, the stderr redirect discards the diagnostic, the true fallback swallows the status, awk prints 0, and the assertion passes as 'the leak is absent' -- while the control at :1442 still passes, because it is built from a synthetic string rather than from the tree. False-alarm filter: the sixteen differ from the one only by not having been converted; the genuine structural gap is that assert_tree_absent expresses only 'expect zero' while several sites assert 'expect exactly 1', which argues for a SECOND HELPER rather than sixteen hand-rolls; both sides are test code, but prior rounds have consistently treated _harness.sh duplication as in scope (CA-042, CA-145, CA-037 are all test-side); every one of these files already sources _harness.sh. Fix: (1) add assert_count_with_control beside assert_tree_absent sharing its count_matches_strict plus path-existence contract, so the expect-exactly-N family has a canonical form at all; (2) convert the eleven identical-shape sites, and make the remaining non-count variants (:4149, :7924, :7863, :6621) at minimum stop discarding grep's exit 2; (3) add a durability pin of the shape wave6-smoke.sh:781-784 already uses, asserting the bare-grep-with-true-fallback shape adjacent to a control assignment occurs ZERO times in bin/tests/, using count_matches_strict rather than count_matches. RELATED TO CA-401, which is the SUITE-ABORT direction of the same bare-capture class; this is the FALSE-PASS direction. Fix both in one test-hygiene pass.
  CA-473 P1 NEW in round 9, INTRODUCED BY THE CA-440 FIX (L1-001 + L3-3 merged -- same root cause, same files, two lenses independently): CA-440 correctly moved the hook-spawned QC auditors off the single qc/qc-summary.md path and onto qc/qc-shard-{NN}.md, but the pre-existing threshold-shard mechanism ALREADY OWNS that namespace with an INCOMPATIBLE key. hooks.json:117 and SKILL.md:81-83 key NN on the LOWEST TICKET NUMBER in the implementer's range; SKILL.md:101 keys it on the SHARD ORDINAL; SKILL.md:97 keys it on the ticket number again. All are two-digit zero-padded, written into one directory with full-file Write semantics by concurrently running agents, and SKILL.md:103 merges both sets through a single qc-shard-*.md glob while SKILL.md:87 states the two mechanisms run separately from each other. Two deterministic collisions, both routine rather than exotic: (a) SMALL INITIATIVE (ticket_count at or below qc_shard_threshold, default 20) -- SKILL.md:97 uses the lowest-ticket key, BYTE-IDENTICAL to the hook's own key, so the skill's single orchestrated auditor overwrites the hook shard for the implementer that owned that same lowest ticket, every time; (b) LARGE INITIATIVE -- threshold shard 1 writes qc-shard-01.md and collides with the implementer whose range begins at T01 (essentially always present), shard 2 with T02, and so on for every index coinciding with a range-leading ticket number. Last writer wins and the loser's FAIL verdicts never reach SKILL.md:119's compile step; only PARTIAL verdicts reach state through the correctly-locked record-partial-verdict, so PASS and FAIL live ONLY in these markdown files. This is the same harm CA-440 was rated P1 for, relocated rather than removed, introduced by CA-440's own remediation, at reduced blast radius (one shard rather than all of them). CLAUDE.md's hooks table repeats only the hook half of the convention, so nothing documents the overlap either. Fix: disjoint namespaces -- hooks.json:117 writes qc/qc-shard-impl-{ticket}.md; SKILL.md:97 and :101 write qc/qc-shard-pass-{i}.md so the two branches of one mechanism also agree with each other; widen SKILL.md:103's merge glob to both prefixes; state both prefixes and their required non-overlap at SKILL.md:81-85 and in CLAUDE.md's SubagentStop hooks-table row; and add a wave7 assertion that the shard token in hooks.json's SubagentStop prompt and the shard token in implement/SKILL.md's pseudo-code are NOT equal, with a positive control that sets them equal and turns red.
  CA-474 P1 NEW in round 9, INTRODUCED BY THE CA-437 FIX (L8 P1-1 + L1-002 merged; L8 rates P1 and L1 P2 -- taken at P1 on multi-lens corroboration plus the blocking-job blast radius): lint:hooks-shell iterates the new NUL-delimited extraction with a 'while IFS= read -r -d ...' loop. The -d option is a bash extension with no POSIX equivalent -- dash has no -d, and BusyBox ash, the shell in the alpine:3.20 images every alpine_edm consumer uses, supports -r -n -p -s -t -u but NOT -d. A sweep of every script body in the file for bash-only constructs (double-bracket tests, local, here-strings, mapfile, declare, array syntax and read -d) returns EXACTLY ONE HIT: line 301. The file states the opposing constraint three times, once inside this very job's own header comment: :274-276 ('process substitution needs bash itself as the invoking shell, which is not guaranteed for the runner script stage'), :517-519 and :566-568. Installing bash in before_script does not change which interpreter the runner already selected for the script stage. Failure mode on an sh runner: read rejects -d, the while condition is false immediately (errexit does not apply to a loop condition), the body never executes, COUNT stays 0, and the CA-437 cross-check at :320 fails the job with a message naming an extraction or splitting regression in hooks.json -- fail-closed but ACTIVELY MISDIAGNOSING, on a needs-nothing, no-allow_failure job that gates the plugin's most privileged shell surface and has never had a green run against a live runner fleet. Fix: drop the delimiter question entirely by indexing with jq -- derive EXPECTED_COUNT once from the hooks.json command list, then loop an index from 0 while it is below EXPECTED_COUNT, writing the indexed .command to its own temp file and incrementing COUNT, which makes COUNT equal to EXPECTED_COUNT structurally rather than by post-hoc check and is immune to every splitting question including the multi-line .command case CA-437 was about; keep the :320 cross-check and retarget its message at a jq or index mismatch. Alternative, only if the runner fleet is confirmed to resolve bash for the script stage: pin the shell on the alpine_edm anchor AND delete the three POSIX-sh-safe comments as false. Do not leave the file asserting one thing and doing another.
  CA-475 P1 NEW in round 9, RESIDUAL OF THE CA-462 FIX (L6-01 + L9's T23 partial-implementation finding merged -- one root cause: the sixth scorer dimension shipped without its same-commit doc and AC sweep, which is CA-416's class measuring itself). score-artifacts.sh:145 now declares six DIM_NAMES ending in known-gap-recall, with SCORER_VERSION 1.1.0 at :137 and compute_dim6 at :546-574 gated only on run.json's fixture field equalling tiny-svc -- and run-eval.sh writes that field on BOTH the complete path (:675) and the partial path (:152). A wave-A capture therefore scores dimensions 1, 2, 3, 4 and 6 and skips only 5, so dimensions_scored is 5. TWO ARTIFACT FAMILIES ARE NOW FALSE. (a) THE BASELINE-CAPTURE RUNBOOK, evals/baseline/README.md, was edited TWICE in this same batch (CA-461 at :107-112, CA-464 at :11-14) without sweeping the dimension count: :50-51 is the file's one gating instruction, telling the operator to confirm dimensions_scored is 4 before committing a baseline, and it can no longer be satisfied; :56 heads a section 'Why four dimensions, not five'; :62-63 calls it a four-dimension baseline, deliberately; :63-67 says a later run establishes a five-dimension baseline (it would now be six); :77-84's variance table has rows for dimensions 1-5 only; :95-105's per_dimension_range example omits known-gap-recall; :116 says five mechanical dimensions; :119 says all four (or five) dimensions. evals/README.md WAS swept correctly (:197-199, :217-220), which is what makes this a miss rather than a convention. The failure mode is silent and expensive: an operator spends three live model runs (the README's own estimate is roughly 10-25 dollars and 30-60 minutes each), reads dimensions_scored 5, and concludes the capture is broken or hand-corrects it. (b) EDMV3-T23's ACCEPTANCE CRITERIA: AC1 requires 'exactly five' and verifies with a jq length equality on 5, which now returns FALSE; AC2 enumerates only dimensions 1-5; AC8 requires a committed four-dimension baseline with dimensions_scored 4 and one skipped dimension plus a grep for the literal 'four-dimension' in the runbook, and is UNSATISFIABLE BY CONSTRUCTION because dimension 6 scores on any tiny-svc run; AC9 and AC10 both say five. No AC anywhere owns known-gap-recall, expected.json's new srd_match patterns, fixture_version 1.1.0 or run.json's new fixture field. score-artifacts.sh:38-39 ACKNOWLEDGES the AC conflict in a code comment while no ticket, AC or decisions.md entry records it. BLOCKING COUPLING: wave7-smoke.sh:830 and :834 assert the literal string 'four-dimension' against this README, so the stale text is currently pinned green by the suite and the doc fix cannot land without the test edit in the same commit. Fix, one commit: rewrite the runbook section as a five-scored / one-skipped wave-A baseline (dimensions 1-4 and 6 score; dimension 5 is null on every wave-A run because run-eval.sh stops after audit-srd), add the known-gap-recall row to the variance table and to the per_dimension_range example, correct :116 and :119; amend T23 AC1 to exactly six with the new verify literal, add dimension 6's definition to AC2, rewrite AC8 as a five-dimension wave-A baseline, correct AC9 and AC10; add an AC on T22 or T23 owning srd_match, fixture_version and run.json.fixture; and update wave7-smoke.sh:830 and :834 to the new literal. See CA-484 for the score-artifacts.sh:4 header, which belongs in this same sweep.
  CA-476 P1 NEW in round 9 (L11 Finding 1): cmd_update_patterns -- the sole mechanism by which the cross-initiative pattern library accumulates knowledge, called from four skills (code-audit step 9a, audit-srd, audit-tickets, test-coverage) and read by all eleven lens agents and the four audit skills -- extracts finding titles with a grep for third-level markdown headings, and NO report format this methodology produces puts finding titles at that level. For the code arm, skills/code-audit/SKILL.md's own Remediation Plan Format puts finding titles at the SECOND level and reserves the third for the Problem / Fix / Verification / Files sub-blocks; on the round-8 report that actually shipped, the third-level set is the document's structural scaffolding. THE LIVE CONSEQUENCE IS COMMITTED AND VISIBLE: plugins/edm/docs/audit-patterns/code-audit.md:101-322 now holds 22 machine-appended entries, every one tagged source EDMV3, date 2026-08-16, status pending-review, with titles such as 'Syntax / static', 'Test suites', 'Lint / contract checks', 'Integration / manual smoke', 'Targeted re-audit (round 9)', 'WORK ITEM 4 onward -- P2 detail' and 'P0 (0)'. They sit under the Anti-Patterns heading at :70 alongside the five real hand-authored entries at :72-96, so the library's largest section is now roughly 80 percent non-patterns -- in a shipped plugin asset that thirteen prompt surfaces are pointed at as accumulated wisdom. The srd (audit-srd.md), ticket (tickets/audit.md) and qc (qc/qc-summary.md) arms contain ZERO third-level headings of any kind, so those three arms record new_findings 0 on every run, permanently, and print 'no novel findings to append' -- indistinguishable from a genuinely clean round, which is the same false-record ambiguity CA-355's fix eliminated one layer down at heading resolution and which survives one layer up at extraction. False Alarm Filter fails on all three clauses: docs/audit-patterns/README.md's Append Schema (:30-35) specifies only the DESTINATION shape, so the source-side heading-level rule is an undocumented implementation choice mandated by no SRD or ticket text; the stop-word list at :5126 shows the author anticipated structural leakage but it is a six-string blocklist against an unbounded space and covers none of the 22 that landed nor the template's own Problem, Fix, Verification and Files headings; and this is the only extractor. Fix: make the extraction heading level per audit type rather than one hardcoded level -- for code, second-level filtered to the finding-block shape and excluding the six fixed structural headings the Remediation Plan Format defines (Context, Findings Summary, Detailed Findings, Decisions / Non-Findings, Rollout Order, Verification Plan); for srd, ticket and qc the reports are TABLES not headings, so a heading-based extractor cannot work for them at all -- either read the table or make those three arms fail loudly rather than record 0. Revert the 22 polluted entries out of docs/audit-patterns/code-audit.md in the same commit, and add a wave7 case asserting a synthetic report in each of the five formats yields the finding titles and NOT the structural headings; the existing coverage (CA-002, CA-056, CA-355) exercises insertion MECHANICS and never asserts WHAT gets extracted. Related non-finding: L11 N4 records that the three-entries-per-convergence-gate curation drain at skills/code-audit/SKILL.md:467-468 is what makes this backlog pathological, and becomes benign again once this is fixed.
  CA-477 P1 NEW in round 9 (L4 P1), covering bin/edm-state:4449-4472: the brand-new CA-471 lens-JSONL completeness gate survives TWO independent one-line mutations with all six of its own assertions -- and the whole 2182-assertion suite -- green. (a) UNCONDITIONAL DOWNGRADE: moving the partial assignment at edm-state:4469 out of the missing-lens guard while leaving it inside the manifest guard makes EVERY code round that has a pass directory record round_type partial, so no initiative can ever converge again -- and nothing fails, because the only fully-backed case (CA471OK) is started with --lenses L1,L2 at :875 so its round is ALREADY partial, and its sole assertion at :877 is a check_absent on the warn text, never on round_type. There is NO case anywhere that completes a FULL code round with all its JSONL present and asserts the round is still full afterwards; T27's three round_type=full assertions (:3234, :3251, :3259) are all taken immediately after audit-round-start and never after audit-round-complete, so they cannot catch it either. (b) DELETED JSON-VALIDITY ARM: deleting the jq-empty conjunct from the condition at edm-state:4463 also leaves all six assertions passing, because the only miss fixture (:884) is an EMPTY file, which the sibling non-empty test catches on its own. Of the three input classes the gate's own comment claims at edm-state:4442 -- missing, empty, unparseable -- only EMPTY is exercised, and the class that MOTIVATED the guard is MISSING: pass-7 of this initiative shipped eleven prose reports and zero JSONL files. Distinct from CA-453 (CA-389/CA-390's unwritten tests) and from CA-425 (the accept-p2-debt guard): different gate, different code, new in this batch. Fix: (i) add a case that runs audit-round-start with no --lenses, lands a parseable JSONL for every lens named in the manifest, completes the round, and asserts round_type equals full AFTER completion; (ii) split the miss fixture into three lenses -- one file absent, one empty, one present-but-unparseable -- and assert all three lens IDs appear in the single warn's list.
  CA-478 P2 NEW in round 9, DEFECT INSIDE THE SHIPPED CA-471 GATE (L1-003 + L3-1 merged): the completeness backstop reads the manifest with an unguarded read loop, and read returns non-zero at EOF on a final line with no terminating newline, so the loop body NEVER EXECUTES for that line. Why this input class differs from every other file-redirected reader in the tree: lenses-run.txt is the ONE manifest in this codebase authored by an LLM agent through the Write tool (skills/code-audit/SKILL.md:78 -- 'one lens ID per line'), whereas edm-check-grants:562 reads a file the plugin itself generated with printf, edm-check-vocabulary:158 and :233 read repo-committed config with a guaranteed trailing newline, and score-artifacts.sh:500 reads jsonl. A missing trailing newline is implausible for those and entirely plausible for a model-written text file. The consequence sits at the worst possible spot: on a FULL round the manifest's last line is L11, and a round that truncated its delivery is exactly the round most likely to be missing its LAST lens. A round whose lens-L11.jsonl never landed and whose manifest ends with an unterminated L11 reports no misses, leaves the downgrade variable empty, records round_type full, and can converge -- reproducing the pass-7 failure CA-471 was filed to prevent, at the boundary. SECONDARY TRIGGER: a CRLF manifest fails the lens-ID regex on EVERY line and disables the gate wholesale with no diagnostic. Not detectable by the current suite: wave6-smoke.sh:870 and :881 both seed the manifest with a printf that ends in a newline. This is NOT a re-flag of CA-471, which is closed on the gate's existence; it is a defect inside the shipped gate. Fix: add the standard non-empty fallback to the read condition so an unterminated final line is still processed, strip a trailing carriage return before the regex, and add a wave6 case seeding one manifest with no final newline and asserting the gate still names the last lens in its warn.
  CA-479 P2 NEW in round 9 (L3-2), second defect inside the shipped CA-471 gate: pass-directory resolution walks a date-suffixed glob and silently keeps the LAST match with no diagnostic. A round re-run across a date boundary, a hand-copied scratch directory, or a partially-created retry leaves TWO pass-N directories, and both directions fail silently -- if the lexicographically last is a scratch or partial copy without lens JSONL, the gate fires SPURIOUSLY and downgrades a legitimately complete round to partial, which CA-506 shows is irreversible and therefore permanently non-convergent; if the last is a stale copy that happens to hold JSONL, the gate passes VACUOUSLY for the round that actually ran. The C-4 carve-out at :4445-4447 deliberately tolerates a MISSING pass directory and says nothing about an AMBIGUOUS one. The round number itself is safe here: the glob prefix is fully quoted, so state-file metacharacters are not glob-active. Fix: collect matches into a counted list; when the count exceeds one, emit a warning naming ALL of them and select by mtime, or refuse outright -- do not resolve silently.
  CA-480 P2 NEW in round 9 (L3-4): a state lockdir that exists with NO pidfile is never reclaimed, and on the mkdir branch that bricks the initiative's lock permanently. The entire reclaim apparatus -- both the age-gated invalid-PID path (:1350-1383) and the dead-PID path (:1385-1398) -- lives inside a single 'pidfile is a regular file' guard at :1348, so a lockdir without one skips the whole block on every iteration, falls through to _lock_retry_or_die, exhausts the roughly 100-try / 10-second budget and dies. Nothing on this invocation or any later one can ever reclaim it. Three ways to reach that state: (a) a crash, kill -9 or power loss in the window between mkdir succeeding at :1343 and the pidfile write at :1404 -- and the trap layer is not installed until :1425, so even a CLEAN interrupt in that window leaves the lockdir with no pidfile AND no cleanup; (b) the pidfile write failing at all, since :1404 ends with a silencing redirect and an unconditional true, swallowing EROFS, ENOSPC and quota, which holds the lock with no pidfile for the process lifetime so the first ungraceful exit orphans it permanently; (c) any external process creating the directory name. IMPACT: mkdir is the macOS branch and CLAUDE.md names macOS the primary development platform, so once bricked, every edm-state mutation for that initiative dies -- including the Stop and PreCompact checkpoint-if-active hooks that fire on every session -- until a human runs rm -rf by hand. CORROBORATING TELL that the author intended coverage: the invalid-PID branch already prints a holder-PID-or-missing label at :1375, :1377 and :1379, and that 'missing' arm is UNREACHABLE as written, because the guard means the holder PID can only be empty when the pidfile exists and is empty. Fix: hoist the age-gated reclaim OUT of the pidfile guard so an absent pidfile takes the same age-gated path an empty or invalid one does, and make the :1404 write failure fatal (remove the lockdir, then die) rather than unconditionally true. L3 records the :1343-to-:1425 untrapped acquisition window as the MECHANISM behind this rather than an independent defect -- fixing the reclaim path makes that window self-healing, and filing both would double-count one defect.
  CA-481 P2 NEW in round 9, UNSWEPT SITES OF THE JUST-CLOSED CA-446/CA-447 CLASS (L3-5b + L7 P2-2 + L8's CA-447-partial line merged; L5 N1 DISSENTS and would hold it under CA-291's do-not-re-file disposition -- OVERRULED at synthesis because the CA-446/CA-447 remediation CHANGED THE PREMISE: edm-check-grants:124-130 now states the four-signal set EXIT INT TERM HUP with signal-shaped exits as a PLUGIN-WIDE convention, and nine sibling sites were converted, so this is no longer an unremarkable outlier but the single declared exception to a declared rule, and L5 itself records it as 'the last trap in the plugin omitting HUP'). Three parts, one commit. (a) evals/tiering-matrix.sh:154 arms one body on RETURN EXIT INT TERM with no exit -- CA-446's cleanup-then-resume shape -- AND omits HUP entirely, which was the other half of CA-447; evals/*.sh is inside both findings' declared scope and this file was simply never visited by either sweep, while its own comment at :148-153 explains the RETURN reasoning, predates the convention, and therefore signals nothing about the divergence being deliberate. (b) evals/score-artifacts.sh:756-757 still has an untrapped one-mktemp window -- the exact gap edm-lint-artifacts:133-137 just closed -- so the first staging file is unprotected until the second is created. (c) CA-447's PRESCRIBED DURABILITY PIN NEVER LANDED: bin/tests/ contains no CA-446 or CA-447 assertion at all, so the convention is stated in one comment in one file and enforced nowhere, which is precisely why (a) and (b) survived a sweep that named them in scope. Fix: convert :154 to the canonical four-arm form (EXIT cleanup-only; INT then exit 130; TERM then exit 143; HUP then exit 129), keeping RETURN on the EXIT-equivalent arm; arm a first-stage trap immediately after score-artifacts.sh's first mktemp; and land the cross-file sweep assertion CA-447 asked for -- scan bin/, bin/tests/ and evals/ for cleanup trap lines and assert each names HUP and each real signal exits after cleanup, with a positive control.
  CA-482 P2 NEW in round 9, INTRODUCED BY THE CA-447 FIX (L3-5a + L7 P2-4 merged; L1 dispositioned the same site NOTED under False Alarm Filter 2 as deliberately transitional and L5 re-verified it as landing exactly as CA-447 prescribed -- the disagreement is recorded and resolved TOWARD FILING because the fix is one line, two lenses filed it, and the site's own comment twelve lines below contradicts it): the new first-stage trap is one body on all four signals with no exit -- the exact CA-446 cleanup-then-resume shape closed at :149-152 in the SAME FILE, directly above the :145-148 comment declaring that the three real signals now exit with signal-shaped codes after cleanup, and a direct contradiction of the contract bin/edm-state:687-691 states file-wide ('INT/TERM/HUP must actually terminate the process after cleanup -- a trap that only cleans up and returns lets the interrupted caller resume'). A signal delivered in the covered window (the second mktemp at :144) cleans up and RESUMES, and this binary runs on every git commit through the PreToolUse hook, so the interrupt an operator sends is the one that gets swallowed. The window is one mktemp wide, which is L1's Filter-2 argument and the reason this is P2 rather than P1. THE SAME SHAPE AT FIVE MORE SITES, folded in from L7 P2-4 rather than filed separately: bin/tests/_harness.sh:85, _harness.sh:114, harness-smoke.sh:264, wave6-smoke.sh:34 and wave7-smoke.sh:25 all use the single-body form while timing.sh:49-52 in the same directory uses the four-line form, so the exit-arm half of the convention is applied at 7 of 13 sites; for wave7-smoke.sh:25 the resume is concretely wrong, because a Ctrl-C deletes the scratch root and the suite then continues running assertions against a tree that no longer exists. L3 N3 confirms the three bin/tests/ sites were named by CA-447 and that its bin/ half landed while its test half did not. Fix: convert all six residual sites to the canonical four-arm split, in the same pass as CA-481, and let CA-481's sweep assertion cover them.
  CA-483 P2 NEW in round 9, RESIDUAL OF THE CA-431 VERSION BUMP -- FOUR-LENS CORROBORATION, the highest-confidence finding of the round (L6-06 + L7 P2-6 + L9 stale-spec 4 + L11 N3, all independently derived): the repository-root CLAUDE.md plugin registry entry still reads edm v3.1.0 while every version source of truth says 3.2.0 -- .claude-plugin/marketplace.json:35, plugins/edm/.claude-plugin/plugin.json:4 and plugins/edm/CHANGELOG.md:7. CA-431's sweep covered three of the four sites. This is a REPEAT of CA-127, which filed and fixed this exact row at round 3 (then stuck at v2.1.0 against a 3.1.0 manifest) and left no mechanism behind, so it re-staled on the very next bump -- the clearest single instance in the ledger of why CA-416's enforcement half matters. It is a SPECIFIED DELIVERABLE, not incidental doc drift: T21 AC2 (epics/03:237-243) makes the root CLAUDE.md Current Plugins entry an EDMV3 deliverable and verifies it by grep, while T66 AC1's CA-431 amendment (epics/11:648-654) covers only the two manifests and does not name this file. The skill inventory on the same line is still accurate (14 commands matching the 14 on-disk SKILL.md files), so only the version token drifted. Scope note: root CLAUDE.md is outside this round's enumerated file list for L6 and L7 but is precedented as in-scope for this initiative by CA-127, and it is the first file a contributor to this repository reads. Fix: set the row to v3.2.0; AND, because this is the second occurrence of the same row going stale with nothing pinning it, add a wave7 case asserting the version token in root CLAUDE.md's edm bullet equals the version jq reads from marketplace.json's edm plugin entry, in the same computed-count shape CA-460's fix already uses; and extend T66 AC1 to name the third site so the obligation lives on the ticket as well as in the test.
  CA-484 P2 NEW in round 9, RESIDUAL OF THE CA-462 SWEEP (L6-02 + L11 N6; L9 names the same contradiction inside its T23 finding): the file-header comment says the scorer produces a scores.json with exactly five dimensions, while the same file's help block at :22 announces the six dimension definitions, :38-41 states 'The six dimensions, in fixed order (T23 AC1 originally fixed this at exactly five; CA-462 added the sixth -- known-gap-recall)', and DIM_NAMES at :145 lists six. Line 4 sits OUTSIDE the EDM-HELP-BEGIN sentinel that opens at :8, which is exactly why the CA-462 sweep of the help text missed it -- that sweep was scoped to the sentinel block, so the miss is structural rather than careless. It is nonetheless the first sentence a contributor opening the file reads and it contradicts three other statements in the same header. Fix: change :4 to say six dimensions, or better, drop the count from :4 entirely and let :38 own it, which is the safer shape given :38 already carries the history. Belongs in the same commit as CA-475.
  CA-485 P2 NEW in round 9, UNSWEPT SIBLINGS OF THE CA-463 FIX (L6-03): CA-463's remediation corrected evals/fixtures/tiny-svc/README.md:38-46 to the shipped mechanism -- a git-tracked byte sum with runs/ excluded, re-verified correct this round against .gitlab-ci.yml:356-374 -- and left two other sites asserting the retired claim. (a) .gitlab-ci.yml:353-355, the comment DIRECTLY ABOVE the code that implements it, still says plugins/edm/evals/ has a documented 100KB DIRECTORY-SIZE ceiling and cites the tiny-svc README as its authority; that README now says the opposite, and the code at :356-364 sums byte counts over git-tracked files under plugins/edm/evals and ceiling-divides, with :374 announcing that runs/ output is deliberately excluded. (b) plugins/edm/CLAUDE.md:1020, the lint:file-type-ban CI-table row, repeats 'the documented 100KB directory-size ceiling on plugins/edm/evals/'. This reproduces exactly the failure CA-463 described: a contributor measuring directory size locally after any eval run counts untracked evals/runs/ output, gets a number far over 100KB, and concludes they breached a budget that is not breached. Fix: replace 'directory-size ceiling' at both sites with the wording CA-463 already standardised in the tiny-svc README -- a 100KB tracked-bytes budget over git-tracked files only, with untracked eval output under evals/runs/ excluded.
  CA-486 P2 NEW in round 9 (L6-04): the lint:shellcheck CI-table row says the job runs shellcheck over bin/*, bin/tests/*.sh and evals/*.sh excluding *.txt, while the shipped job excludes BOTH extensions -- .gitlab-ci.yml:244-250 carries an awk-or-txt continue arm with a G24/CA-233 comment explaining that edm-mermaid-rules.awk is plain awk source and is deliberately skipped. The sibling lint:bash-syntax row at :1015 states both exclusions correctly, which is what makes this a miss rather than a convention. A contributor reading the table concludes bin/edm-mermaid-rules.awk is shellchecked as bash and may chase nonexistent findings in it, or add a second .awk helper expecting coverage that does not exist. Fix: change the row to exclude both *.awk and *.txt, matching the lint:bash-syntax row and the shipped case arm.
  CA-487 P2 NEW in round 9, RESIDUAL OF THE CA-407 FIX (L6-05): CA-407's honesty caveat landed and its ORIGINAL defect -- the paragraph claiming the assertion could detect a call site that was merely COMMENTED OUT -- is closed, which is why CA-407 is recorded fixed this round rather than reopened. Two factual errors that CA-407's own round-8 addendum named survive, and are re-filed here under a new ID per the ledger's CA-434/CA-435/CA-438 precedent. (1) WRONG FILE: the paragraph says wave6-smoke.sh carries the computed assertion, but wave6-smoke.sh contains exactly one occurrence of the helper name, at :2492, and it is prose inside a comment; the real computed assertion is at wave7-smoke.sh:4441-4456. A contributor sent to wave6-smoke.sh to update the pin after adding a call site will not find it, and the drift guard the paragraph exists to advertise goes unmaintained. (2) WRONG GREP: the pattern the paragraph names matches the definition at bin/edm-state:1497 plus four comment mentions -- five hits, none of them call sites -- while the six real call sites contain no literal open-paren and the shipped assertion finds them with a different needle. The substantive counts the paragraph states are CORRECT and were re-derived this round (six call sites; five canonical version comments; the two call sites without one are cmd_approve_gate's precheck and cmd_audit_converged), so only the pointer and the pattern are wrong. Fix: change wave6-smoke.sh to wave7-smoke.sh and quote the pattern the assertion actually uses, or drop the parenthetical entirely and cite the case by its shipped label, which is the durable shape CA-368's verbatim-label rule already prescribes.
  CA-488 P2 NEW in round 9 (L6-07): the README lists decisions.md among 'optional on-demand files' alongside ROLLBACK.md, exec-report.md and post-deploy/, but plugins/edm/CLAUDE.md's layout block annotates decisions.md as Must / always-present, alongside planning.md, srd.md, architecture.md and explorers/. It is also load-bearing at runtime: skills/code-audit/SKILL.md:204-209 requires every convergence approval be appended to it, and CLAUDE.md's D15 section requires scope changes recorded there. Not cleared by the simplification filter: three of the four items in the same parenthetical are correctly classified, so this is a miss rather than a deliberate simplification for non-technical readers, and README is the user-facing document a new adopter reads BEFORE CLAUDE.md. Fix: move decisions.md out of the parenthetical -- for example, name the always-present files this summary omits (architecture.md, explorers/, decisions.md) separately from the optional on-demand files (ROLLBACK.md, exec-report.md, post-deploy/).
  CA-489 P2 NEW in round 9 (L6-08): the documented pending-entry count command does not produce a count. The README states that the pending count is always a bare grep -c for the pending-review status marker against the docs/audit-patterns/*.md glob, computed at read time. But grep -c against a multi-file glob prints one file-and-count line PER FILE (five library documents), not a total, so an operator or agent running the documented command to answer 'how many entries are pending' gets five prefixed lines; and if only one file happened to match, they get a bare number that UNDER-REPORTS the real total. The suite's own correct usage at wave7-smoke.sh:3805 runs the same command against a SINGLE file, which is why nothing catches this. Same bare-grep-c class this plugin has been actively remediating -- CA-392 and commit dfa71d3 replaced four bare grep -c captures with count_matches and count_matches_strict -- so it is in-family rather than a stylistic quibble. Fix: state a command that actually totals (grep -o for the marker across the glob piped to wc -l), and update the verbatim string assertion at wave7-smoke.sh:3474, which pins this exact sentence, in the same change.
  CA-490 P2 NEW in round 9, RESIDUAL OF THE CA-452 FIX -- THE CORE SYMPTOM SURVIVES (L2-001): the CI rewiring half of CA-452 landed and is correct (.gitlab-ci.yml:738-746 no longer aborts on run-eval exit 4, score-artifacts.sh re-derives complete:false from run.json at :644 and :657, and :769-775 guarantees a partial run still fails the job), but the defect CA-452 was actually filed for is still live. edm-compare-eval tests for the committed baseline FIRST -- an exit-3 not-armed return at :58-62 -- and plugins/edm/evals/baseline/scores.json does not exist anywhere in the tree; evals/baseline/ holds only README.md, whose own :3-19 records the absence as the deliberate current state. Refusal condition 3, the complete-not-true check at :67-75 that :22 documents as one of the three named refusal conditions, sits BELOW that early return, as do conditions 1 and 2. So on the partial path the sole automated caller (.gitlab-ci.yml:762, one-argument form, default baseline) still prints 'eval comparison: NOT ARMED' and exits the arm at :765; the complete:false handshake NEVER EXECUTES. Two remediation comments now assert an outcome that cannot occur in any pipeline runnable at HEAD -- .gitlab-ci.yml:736-737 ('edm-compare-eval REFUSES a complete:false candidate by name, exit 2') and plugins/edm/CLAUDE.md:1029 ('visibly refused by edm-compare-eval'). CROSS-LENS RESOLUTION: L6 N10 cleared the CLAUDE.md wording under False Alarm Filter 1 on the ground that the mechanism is approximated but the operator-visible outcome matches; that clearance is OVERTURNED here, because L2 establishes the outcome is UNREACHABLE, not merely differently mechanised. Fix, one small reorder: condition 3 reads only the candidate and needs nothing from the baseline, so move :64 (candidate JSON validation) and :67-75 (condition 3) ABOVE the baseline-existence check at :58-62. That makes the handshake fire today, on an un-armed tripwire, which is exactly the state CI is in and will stay in until the live baseline capture happens, while exit 3 keeps its meaning for a candidate that is complete but has nothing to compare against; it also un-blocks conditions 1 and 2, which CA-462's SCORER_VERSION bump to 1.1.0 makes newly relevant. If the reorder is rejected, both comments must be qualified ('once a baseline is committed') so they stop describing a path that does not exist.
  CA-491 P2 NEW in round 9 (L4 P2 with two L1 noted items folded in), covering bin/edm-check-grants:506-517: the CA-441 missing-task-grant rule shipped with NO must-fail case, and its only coverage is an assertion the rule cannot influence. The established must-fail shape sits 130 lines above at t03_ac6_case (:364-396) -- copy the tree to scratch, strip the grant from one skill's allowed-tools, then assert exit 1 plus the file name and the class name at :388-391 -- and it exercises the AskUserQuestion rule ONLY; nothing was added for Task. Because CA-441's own fix granted Task to all twelve skills that need it, the rule's trigger condition at :515 is false everywhere and mark_and_maybe_report is NEVER REACHED on the live tree, so the family of 'edm-check-grants exits 0 against the live tree' assertions (wave7:339, :2860, :3034, :3171) passes identically whether the rule works or does not exist. A mis-escaped detection regex at :509 (two alternatives, a case-insensitive extended grep, non-trivial escaping) or a has_tool prefix bug would leave the rule silently protecting nothing, permanently, and the next skill added would reproduce exactly the CA-441 defect it was built to prevent. This is CA-441's own prescription landing its code half without its test half -- the CA-453 shape at a new site. TWO KNOWN LIMITS OF THE RULE, folded in from L1's noted set rather than filed separately so the test written for this finding covers them: (a) :511 derives the reported line from a whole-file grep while the trigger is derived from the body, so a reported line can fall in frontmatter -- byte-identical to the pre-existing AskUserQuestion rule at :492-494, hence a shared quirk rather than a new defect; (b) :509's spawn alternative requires the spawn verb and the agent-name prefix on ONE line, so a multi-line spawn instruction escapes the rule -- no live instance today, since metrics and verify-runtime, the only two skills without Task, contain no spawn instruction in any form; and the line-anchored agent-name alternative is currently carried entirely by the first alternative, with no control proving it can match at all. Fix: clone t03_ac6_case for the Task rule (strip the Task grant from a scratch copy of one testing skill, assert exit 1 and the reported class name), plus one synthetic control line per regex alternative, each failing by alternative name.
  CA-492 P2 NEW in round 9 (L4 P2): the CA-426 stream-separation regression fixture POSITIVELY PINS that an initiative with zero recorded audit rounds converges via --accept-p2-debt, and no case asserts any boundary on it. PDEBT6 (:800-815) is init-ed and given a ledger, then approve-gate with --accept-p2-debt is run with NO audit-round-start and NO audit-round-complete at all -- which is the point, it reproduces the zero-recorded-rounds stderr warn CA-426 was about. But cmd_audit_converged's zero-rounds arm warns on stderr and PROCEEDS, so stdout carries the not-converged prefix, the override engages, :812 asserts exit 0 and :814-815 asserts the success message. The suite now positively certifies that an initiative which has never run a code-audit round can be converged. The test-quality defect is the ASYMMETRY: CA-425's negative set added in the same batch covers a partial round (:774), an invalid ledger (:786), the wrong gate (:792) and an unrecognized argument (:795) but has NO zero-round member, even though zero-round is the one class the CA-426 fix deliberately RE-ENABLED; the narrowing comment at edm-state:2236 enumerates a partial round, invalid JSONL and an invalid status line and silently omits the arm that proceeds; and CA-471 landed in the same batch specifically to make round completeness verifiable. Whether zero-round convergence is INTENDED is an L9 or spec question this finding does not decide -- what L4 owns is that the behaviour is now locked in by a passing assertion with no companion case stating the boundary either way. Fix: rebuild the CA-426 regression on a fixture with a real completed round plus an independently-induced stderr warn, so the regression no longer depends on the zero-round shape; then add an explicit zero-round case asserting whichever behaviour is decided, with a comment naming the decision and its authorizing artifact.
  CA-493 P2 NEW in round 9 (L4 P2): test:state-validate (:508-550) is a BLOCKING job that prints OK after validating nothing, with no floor. The script runs under set -u only (:516, deliberately POSIX-sh-safe, no -e) and the 'edm-state list --paths' redirection at :524 has its exit status DISCARDED. If enumeration fails or returns nothing -- a broken EDM_SRD_ROOT, a relocated SRD/ tree, a jq or layout regression in list --paths -- the while loop body never executes, COUNT stays 0, FAIL stays 0, and the job prints 'state-validate: 0 initiative(s) checked' at :545 followed by 'test:state-validate: OK' at :550 and exits 0. A blocking gate reports green having checked zero initiatives, and the printed count is the operator's ONLY signal with nothing asserting it. This plugin already closed the identical class one directory over: CA-016 added the minimum-suite-count floor plus the missing-preferred-suite check to bin/tests/run-all.sh:70-91, whose own comment states that a zero-suite guard catches total deletion but does nothing if a named suite is deleted. No equivalent floor exists here. Fix: capture and check the exit status of 'edm-state list --paths' on the same statement, and fail the job when COUNT is 0 or below a small committed floor, naming enumeration as the cause instead of printing OK.
  CA-494 P2 NEW in round 9, RESIDUAL OF THE CA-443 FIX (L4 P2): eval:nightly still skips scoring AND the baseline comparison silently when the run directory is absent. Both blocks -- :747-753 scoring and :758-768 comparison -- are guarded on a non-empty RUN_DIR with NO else arm, and RUN_DIR is re-derived independently at :749 and :759 from a directory listing piped to head. CA-443 named this exact downstream consequence ('scoring and the baseline comparison are skipped WITH NO MESSAGE, and eval:nightly reports success having evaluated nothing') and its remediation went entirely into run-eval.sh, clamping a zero keep-runs setting to 1 -- which closes ONE CAUSE, not the class. Any other reason the directory is absent (a changed output root, an artifact-cleanup step, a run written outside plugins/edm/evals/runs/) still yields a job that scores nothing, compares nothing, reports neither, and reaches the end without incident. Filed as a residual rather than a re-file of CA-443, matching the ledger's own precedent for CA-434, CA-435 and CA-438. Held at P2 because the job carries allow_failure, so the loss is an advisory tripwire silently reporting nothing rather than a blocking gate. Fix: derive RUN_DIR once, assert it non-empty and that scores.json exists, and give both blocks an else arm that echoes the cause and exits non-zero, so an unscored or uncompared run is a NAMED failure.
  CA-495 P2 NEW in round 9 (L5-001 and L5-002 merged -- one file, one token per site, one commit): scratch trees in the largest smoke suite sit outside the suite's trap-covered root, and one of them is never removed on any path. (a) UNCONDITIONAL LEAK, the more serious half: the G22b scratch tree at :7272 is created under the system temp directory, and a grep of the entire 8000-plus-line suite for its variable returns exactly three hits (:7272, :7273, :7274), NONE of them a removal -- no rm, no trap (:7271 explicitly clears all four dispositions), and it is not under the suite scratch root that the top-level trap at :25 owns. Its otherwise character-for-character identical G22a sibling at :7241 and :7255 DOES carry the tail removal, so this is a dropped line rather than a project pattern; the remediation that added G22b copied G22a's preamble and dropped its last statement. It leaks on the SUCCESS path every single time: bin/tests/run-all.sh discovers smoke suites automatically, so every developer honouring CLAUDE.md's run-before-you-push instruction mints one, and CI runs the aggregator twice per pipeline (test:smoke and test:smoke-bash32). The false-alarm filter is satisfied on no clause -- not gitignore-covered (it never reaches a worktree), not deleted after use (it is never deleted), and not tmpfs-backed, since macOS resolves the temp directory to a per-user path that survives reboots and a long-lived Linux runner sweeps on a timer at best. Small per run, which is what holds it at P2. (b) CONDITIONAL LEAK, eleven more sites: :5116, :5154, :5190, :5217, :5242, :5282, :5331, :5386, :5535, :7241 and :7826 are rooted at the system temp directory instead of the suite's own scratch root, inside subshells that CORRECTLY clear all four inherited trap dispositions -- the comment at :5105-5114 explains why, since restoring the inherited copy of the parent's cleanup would delete the shared scratch root out from under every later test -- and then leave a tail-position removal as the sole cleanup for a resource that now has no handler at all. Two concrete abort paths, not hypotheticals: sourcing edm-state RE-ENABLES errexit inside these subshells (the suite states this itself at :5163-5165) and several later statements are unguarded, including :5251's bare cat of a stderr file that exists only if the code under test failed as expected, so a regression in edm-state -- precisely what the harness exists to detect -- aborts the subshell before its own cleanup, which is CA-450's aggravator verbatim; and a Ctrl-C leaks these eleven while the parent trap reclaims the roughly 45 siblings, with :7241 additionally leaving a mode-555 directory behind if interrupted mid-case. The consistent-project-pattern filter fails cleanly: roughly 45 mktemp calls in this same file use the suite-scratch-nested form, and wave6-smoke.sh:456-458 states the rule outright. Fix: change the template root from the system temp directory to the suite scratch root at all twelve sites and keep the trap-clearing lines exactly as they are, so the parent's four-signal trap reclaims them on every path and the existing tail removals remain valid as a fast path; then consider a bin/tests tripwire asserting every mktemp -d under bin/tests/*.sh either interpolates the suite scratch root or is paired with a trap, since the T61 AC11 sweep at :1137 scans bin/ and evals/ only and explicitly excludes the tests directory.
  CA-496 P2 NEW in round 9 (L7 P2-1 + L11 N2): the CA-005/CA-154/CA-365 CLI-family durability loop hardcodes NINE bin/ helpers and omits edm-lint-staged-artifacts, the tenth -- created by CA-436's own remediation and now the delegate for the plugin's most privileged hook. The comment at :5924-5926 still claims all 12 print_help call sites in the plugin are covered; there are 13. The new file IS compliant with both contracts today (:29 sources the shared library, :32 uses the caller-name convention), so there is no live defect -- the finding is that the one property this loop exists to provide, DURABILITY, is exactly what the newest and most privileged member lacks. Third instance of a single shape: CA-231 (library header undercounted its consumers by one), CA-270 (boundary loop undercounted by one), now the CLI-family loop. Note the asymmetry that makes it invisible: lint:bash-syntax's CA-005 ban at .gitlab-ci.yml:126 DERIVES its file set from the tree, so the negative rule covers the new file while the positive assertion does not. L11 additionally records that the G21 die-map at :7978 was not extended either. Fix: add edm-lint-staged-artifacts at :5900 and correct :5925's count to 13; better, derive the list from the bin/edm-* glob minus the underscore-prefixed libraries, the way .gitlab-ci.yml:106 derives its sweep, so the eleventh helper cannot repeat this.
  CA-497 P2 NEW in round 9 (L7 P2-3, independently corroborated by L8's noted set which reached the same conclusion from the portability axis): this is the only set-uo-pipefail site in the plugin with NO rationale comment, and the omission is load-bearing. Nine of the ten PATH-exposed bin/ scripts use the full set -euo pipefail (edm-state:54, edm-lint-artifacts:62, edm-check-grants:61, edm-check-vocabulary:56, edm-init:16, edm-validate-prefix:15, edm-compare-eval:34, edm-check-skill-sync:25, edm-sync-canonical-sections:39). Round 8 dispositioned the set-uo divergence NOTED under L7-N01 EXPLICITLY on the ground of 'documented rationale at each site' -- evals/tiering-matrix.sh:61-64, evals/run-eval.sh, evals/score-artifacts.sh and bin/tests/run-all.sh:10-16 each carry a CA-074 note immediately above the line. That predicate does not hold here, and the consequence is not cosmetic: at least five statements are bare test-and-assignment lists that return non-zero on their ORDINARY path (:41, :42, :107, :110, :124), and three command substitutions have load-bearing non-zero status (:44, :106, :131-132). Adding -e to 'match the nine siblings' makes the script exit 1 at :41 whenever neither EDM_SRD_ROOT nor CLAUDE_PLUGIN_OPTION_SRD_ROOT is set -- the DEFAULT case -- and under the hook's own contract exit 1 is NON-BLOCKING, so commit-time artifact lint would stop enforcing silently, for everyone. That is the same fail-open outcome CA-410 and CA-413 were each filed for, reachable by a one-token change that looks like a consistency improvement. Fix: add the CA-074-shaped rationale comment above :25 naming the test-and-assignment dependency, the three load-bearing command substitutions and the fail-open direction, so the divergence is intentional on the page rather than incidental.
  CA-498 P2 NEW in round 9 (L7 P2-5): scan_skill_tool_usage (:480-518) now carries TWO positive rules -- AskUserQuestion at :502 and the CA-441 Task rule at :515 -- but not the Skill rule the same contract names. plugins/edm/CLAUDE.md Sec.'Skills are the source of truth for orchestration' states TWO caller obligations, the first being that Skill must appear in the caller's allowed-tools, and grants are explicitly NOT inherited from a caller, which is the identical non-inheritance failure mode CA-441 was filed on. No live gap today: only skills/orchestrator/SKILL.md instructs a Skill-tool invocation (:27, :144, :149, :169) and it grants Skill, while implement/SKILL.md:194 and plan/SKILL.md:31 mention the tool descriptively. But 'no live gap today' is PRECISELY the argument the CA-441 remediation comment at :474-478 rejected for Task -- 'otherwise the next skill added has the same hole'. Fix: add a third positive rule so that a skill body instructing a Skill-tool invocation whose allowed-tools lacks Skill is a missing-skill-grant violation, and give it the must-fail case CA-491 prescribes for the Task rule, in the same commit, so the new rule does not repeat the code-without-test shape.
  CA-499 P2 NEW in round 9 (L8 P2-1): the one script the git-commit hook depends on is the only file in the plugin with a hardcoded interpreter path. Every other production and eval script uses the env-resolved bash shebang -- 12 of 12: edm-state, edm-lint-artifacts, edm-check-grants, edm-check-vocabulary, edm-check-skill-sync, edm-compare-eval, edm-init, edm-sync-canonical-sections, edm-validate-prefix, _edm-cli-lib.sh, _edm-lint-lib.sh and all three evals drivers. The sole hardcoded-path outlier is the brand-new file that hooks.json:86 now routes ALL commit-time enforcement through. Two concrete consequences. (1) FAIL-OPEN on a host with no bash at the hardcoded path (NixOS, distroless or minimal containers, some hardened images): command -v succeeds because the file is on PATH and executable, the hook invokes it, exec fails with status 126 and a bad-interpreter line, and since the hook's contract blocks only on exit 2, status 126 is NON-BLOCKING -- the commit proceeds and artifact enforcement is off. This is the CA-186 fail-open class re-entering through the interpreter rather than through srd_root. (2) SPLIT BASH VERSIONS INSIDE ONE CHECK on macOS, the project's stated primary development platform (bin/edm-state:653): with Homebrew bash installed, the wrapper runs under the system bash 3.2 while the edm-lint-artifacts it calls at :131 runs under the env-resolved bash 5.x, so one commit-time check straddles two language versions with nothing recording that. Fix: change to the env-resolved form for family consistency; or, if pinning bash 3.2 was the intent, say so in a comment at :1 and add the shebang form to the T61 AC11 divergence sweep at wave7-smoke.sh:1137 so the choice is pinned rather than incidental.
  CA-500 P2 NEW in round 9, INTRODUCED BY AND RESIDUAL OF THE CA-448 FIX (L8 P2-2 merged with L8's CA-448-partially-fixed line -- one function, one edit). (a) The new project-root resolution at :1056-1064 accepts CLAUDE_PROJECT_DIR on the SOLE test that it is a directory, with no cross-check against the git toplevel, the initiative directory or SRD_ROOT. check_permission_rules produces the enforcement tag stamped onto every gate approval (:2173, :2306, :2317, :2338) -- the plugin's only auditable record that a human-approval control was armed. An env-prefixed invocation pointing CLAUDE_PROJECT_DIR at an attacker-or-operator-chosen directory does two things at once: the env-prefixed form is one of the documented shapes that does NOT match the literal-prefix approve-gate ask matcher (README's Matcher limitation, echoed at edm-state:1029-1032), so no permission prompt fires; and it simultaneously points the settings scan at that directory, which need only contain the two literal patterns to stamp the approval permission-ask. The result is a bypass THAT RECORDS ITSELF AS ENFORCED -- exactly the direction the function's own AC6 docstring at :1047-1051 forbids ('a false present (permission-ask) is not harmless, since it would overstate the tier that actually enforced the approval'). Pre-CA-448 the equivalent needed a cd-and-run compound, itself a documented bypass shape; the fix reduced it to a one-token env prefix and moved it onto a host-supplied variable. (b) CA-448's own prescription had a second half that did not land: PERM_RULES_MISSING at :1697 still names bare relative settings paths, so a prose-only enforcement tag remains unattributable to any resolved root. Fix: accept CLAUDE_PROJECT_DIR only when it agrees with the repository -- resolve both physically and require equality with, or containment of, the git toplevel; on disagreement prefer the toplevel and say so -- then name the resolved root in the PERM_RULES_MISSING diagnostic, and add the smoke case CA-448 prescribed, asserting the enforcement tag against a scratch settings tree.
  CA-501 P2 NEW in round 9, RESIDUE OF THE CA-436 EXTRACTION (L8 P2-3 + L11 Finding 4): commit-time enforcement now depends on an executable bit that nothing asserts, on a delegate that nothing ever executes. Before the extraction the enforcement body lived inline in hooks.json:86 and could not fail to be present; it is now a PATH-resolved script behind a 'command -v ... or exit 0' guard, and command -v succeeds only for an EXECUTABLE file on PATH -- so a mode-0644 file makes the guard fire and the hook exit 0, which a PreToolUse hook reads as 'proceed', i.e. the same signal as 'commit is clean'. NOTHING IN THE REPOSITORY VERIFIES THE PRECONDITION: lint:bash-syntax (.gitlab-ci.yml:114) and lint:shellcheck (:252) pass the file as an ARGUMENT to bash -n and shellcheck, which need no execute bit; every T67 AC8 assertion (wave7-smoke.sh:4708-4739) and :2546 cat the file and grep substrings, both of which hold on a 0644 file; the only executable-bit test in the entire bin/tests/ tree is :4570, for bin/tests/timing.sh; and no test or CI job ever RUNS edm-lint-staged-artifacts, so there is no execution path that would incidentally catch it. Realistic rather than theoretical: a file created by an agent's Write tool, a patch applied with git apply on a checkout with file-mode tracking disabled, or an archive export can all land the file without the bit, and the failure is indistinguishable from 'no violations found'. The degrade-to-exit-0 idiom is the consistent project pattern across all nine command hooks and is NOT the finding; the finding is the UNVERIFIED NEW DEPENDENCY -- the other hooks' targets are covered by dozens of direct executions in the smoke suites and this one has none. NOTE: the mode bit itself was not observed this round (no Bash in any lens's delivered tool set), so the claim is that nothing PINS it, not that it is currently wrong. Fix: assert the executable bit in the T67 AC8 block (git tracks the mode bit, so this is a real regression guard) and ideally loop it over every non-underscore-prefixed entry in plugins/edm/bin/; add a positive control that copies the script to a scratch directory at mode 0644, puts that directory first on PATH and asserts command -v fails, so the silent-degradation path is exercised rather than assumed; and give the delegate at least one behavioural case -- a scratch repo with a staged violating artifact asserting exit 2, and a clean one asserting exit 0.
  CA-502 P2 NEW in round 9 (L10 Finding 1): the plugin's definition of 'what is a bash source file' is written THREE times and has ALREADY DIVERGED, while the CA-233 consistency pin covers only the exclusion arm. Copy A is lint:bash-syntax (.gitlab-ci.yml:104-124), iterating bin/*, bin/tests/*.sh and evals/*.sh with an awk-or-txt continue arm; copy B is lint:shellcheck (:240-263), the same 20-line block VERBATIM, differing only in the tool invoked, the job name in two verdict strings and the trailing verb; copy C is the T61 AC10 in-suite twin (wave7-smoke.sh:1081-1087), which OMITS evals/*.sh. So a syntax error introduced into evals/run-eval.sh, evals/score-artifacts.sh or evals/tiering-matrix.sh is caught by CI but NOT by a local bin/tests/run-all.sh -- which plugins/edm/CLAUDE.md Sec.'Testing changes' step 5 presents as 'the same command CI runs and the fastest way to catch a regression before opening an MR'. It is not the same command for this class. Not a false alarm: this exact enumeration has diverged and shipped before -- CA-162 in round 2 filed lint:shellcheck covering only bin/* while lint:bash-syntax covered more, and CA-233 spent four rounds resynchronising the EXCLUSION arm across the same three loops. CA-233's fix added a real three-way consistency assertion at wave7-smoke.sh:1099-1115, but it checks ONLY the exclusion string (:1101-1104 counts occurrences in .gitlab-ci.yml, :1112-1115 extracts the twin's loop block); nothing compares the glob lists, and the comment at :1092-1096 states the intent as 'so a future edit to just one of them cannot silently re-diverge the other two', which is true of the exclusion and false of the file set sitting one line above it. Fix: (a) hold the glob triple in one place -- a YAML anchor or referenced snippet consumed by both CI jobs -- and have the in-suite twin read the same list, since it already reads .gitlab-ci.yml at :1100 for the exclusion check so the machinery exists; (b) extend the CA-233 assertion to require the three iteration lines' glob lists to be identical, with a positive control; (c) either add evals/*.sh to the twin or state in its label that it is deliberately bin-only, so a reader is not misled about local-versus-CI parity.
  CA-503 P2 NEW in round 9 (L10 Finding 2): the code-srd-tickets audit-type enum is the one enum in bin/edm-state that does not follow the file's own documented single-source convention. It is spelled out SIX times -- two case patterns (cmd_audit_round_start :4357-4360 and cmd_audit_round_complete :4505-4508, byte-identical apart from the subcommand name), two die-message literals and two usage strings (:4353, :4502) -- plus two help-header lines at :39-40. The file states the opposite rule in its own words at :799-803: enums live in one space-separated constant consumed by the space-padded membership idiom, so no caller re-encodes the enum as a second literal. Four constants follow it -- MODE_ENUM_LIST (:804), LIFECYCLE_MODE_ENUM_LIST (:805), ALL_LENS_IDS (:1528, with a self-check at :1530) and SETTABLE_KEYS (:1923) -- and every one of their consumers derives its diagnostic from the constant by substituting spaces for pipes rather than retyping it (:827, :835, :947, :1970, :4823, :4845). Divergence today: none. That IS the finding, per the CA-343 framing -- the file explicitly documents the opposite convention and then breaks it here. Cost if a fourth audit type is ever added (plausible: this plugin already has a testing layer and a test-coverage skill): six in-code edits, and missing the complete half means audit-round-start succeeds while audit-round-complete dies, leaving a round that can be OPENED AND NEVER CLOSED, which surfaces only as the informational OPEN_AUDIT_ROUND anomaly. Fix: add AUDIT_TYPE_ENUM_LIST beside the other four constants, convert both validators to the membership idiom with the pipe-joined form in the diagnostic, derive the two usage strings from it, and add a count_matches_strict single-definition pin in the shape wave6-smoke.sh:1067-1077 already uses so the convention is enforced rather than merely stated.
  CA-504 P2 NEW in round 9, RESIDUAL OF THE CA-419 FIX (L10 Finding 3): the structural half landed -- _cmd_set_provenance_field at :4903-4912 with two one-line wrappers at :4914 and :4916 -- but the BEHAVIOURAL DIVERGENCE half, which CA-419's own text called 'the more interesting half', did not. Four subcommands write a cross-initiative prefix link into state; only TWO refresh the committed handoff document. cmd_set_parent (:4938-4948) and cmd_add_related (:4951 onward) both validate that the target initiative exists AND call write_handoff_internal (:4947); _cmd_set_provenance_field does neither. _write_handoff_body reads BOTH un-refreshed fields into the rendered artifact -- supersedes at :5318 and forked_from at :5319 -- so after set-supersedes or set-forked-from the committed cross-user resume document keeps showing the STALE value until some unrelated command happens to rewrite it. The validation asymmetry is at least consistent with plugins/edm/CLAUDE.md's state-field table, which claims existence validation for parent_prefix only; the HANDOFF-refresh asymmetry is documented nowhere. Fix: add the write_handoff_internal call as the last line of _cmd_set_provenance_field -- ONE LINE, since the pair is now a single function, which is precisely what the extraction was supposed to make cheap. Decide the validation half explicitly in the same commit: either add the state-file existence check to match the sibling pair, or record in CLAUDE.md's state-field table why provenance links are deliberately allowed to name a not-yet-created initiative.
  CA-505 P2 NEW in round 9, RESIDUAL OF THE CA-418 FIX (L10 Finding 4): the DRY half landed -- :1116-1118 defines three shared header constants referenced from all four sites (:2705, :2717, :3559, :3568) and metrics-report's inline width specs are gone, so ONE mechanism now owns the widths -- but the Coverage column width is still declared TWICE and the two declarations DISAGREE BY ONE COLUMN. Declaration A is the jq pad ladder in the row defs: COVERAGE_LAYER_ROW_JQ_DEF at :1097 pads by magnitude in a 9/8/7 ladder, COVERAGE_EPIC_ROW_JQ_DEF at :1106 in an 8/7/6 ladder. Declaration B is the printf field spec in the new header constants: a 10-wide layer field at :1116 and a 9-wide epic field at :1118. For an integer percentage the row emits an 11-column Coverage field where the header allocates 10, and a 10-column field where the header allocates 9 -- both off by one in the same direction, so the trailing Measured At header sits one column left of its data; for the FRACTIONAL percentage the state schema actually documents, the offset widens to three columns. COVERAGE_EPIC_HEADER at :1117 is correct because Coverage is its last column and needs no width. This is L10's mandate rather than a cosmetic nit because the extraction's own comment at :1108-1115 asserts that widths derive from the row clamps -- true of the 14-wide layer and 15-wide epic clamps, which genuinely do match, and FALSE of the Coverage width, which is a hand-chosen second specification of a number the row def already owns and is wrong. That is the CA-343/CA-420 class -- a comment asserting single-sourcing over a site that is not single-sourced -- at a fourth site, introduced by the fix for the finding that named the class. CONFIDENCE MEDIUM: derived by hand from the format strings, because no Bash was delivered to any lens this round, so the rendered offsets were not observed; CA-418 asked the next round to confirm by running both commands and that request is STILL OUTSTANDING. Fix: emit each header from the same constants the row def pads with, or at minimum widen the two printf specs by one column and add a comment naming the row def as the number's owner; then extend wave5-smoke.sh:111-125, which today compares only the DATA row byte-for-byte between get-coverage and metrics-report, to compare the header and underline pair too -- the assertion CA-418 prescribed and that did not land.
  CA-506 P2 NEW in round 9 (L11 Finding 2): CA-471's round_type partial downgrade is IRREVERSIBLE, but step 9a prescribes a repair loop with no exit. round_type has exactly two writers -- _cmd_audit_round_start_body:4345 for a new round and the CA-471 arm at :4488 -- it is NOT in SETTABLE_KEYS so edm-state set refuses it, and re-running audit-round-complete is refused outright by the once-only guard at :4424-4425. Yet SKILL.md:147-149 tells the operator that if the warn fires they should go back to step 8a, persist the missing halves, record the miss in tooling-notes.md, and not proceed to the convergence gate on a downgraded round. Persisting the halves CANNOT restore the round: it is permanently non-convergent, the only real exit is a fresh audit-round-start plus a full eleven-lens re-run, and that is stated NOWHERE. An operator who follows the instruction literally repairs the artifacts, re-reads the gate refusal, and has no documented next step. Partial credit on False Alarm Filter 2: bin/edm-state:4443-4444 explains the downgrade's INTENT ('a round whose authoritative artifacts are missing cannot converge'); what survives is that the SKILL -- the operative surface -- describes a repair without naming its cost or its terminus. Related to but distinct from CA-510, which is the missing acceptance criterion for the same gate, and from CA-479, whose ambiguous-glob defect can trigger this irreversible downgrade SPURIOUSLY. Fix: one sentence at SKILL.md:147-149 stating that persisting the halves preserves the artifacts for the record but does not clear the recorded downgrade, that the round stays non-convergent, and that reaching convergence requires a new audit-round-start and a fresh full round; pin it with a wave7 check on that clause so a future rewrite cannot drop it; and echo the same sentence from the :4468 warn text so the operator reads it at the moment it fires.
  CA-507 P2 NEW in round 9, RESIDUAL OF THE CA-466 FIX (L11 Finding 3): the tooling-notes.md consumer is one elision away from being unwired again. The synthesizer now reads the file when present and carries a delivery-degradation note into REMEDIATION.md, but that instruction lives ONLY in the agent definition -- agents/edm-audit-synthesizer.md:25-26 as a Mission input and :194-198 as Process step 2. The skill's step-9 bullet list (skills/code-audit/SKILL.md:123-129) and the fenced Synthesizer Phase spawn prompt (:345-362) enumerate the lens reports and the ledger and NEVER NAME tooling-notes.md. Why this matters at this specific site: CA-164's whole lesson was that the skill is the OPERATIVE instruction at spawn time; CA-193's remediation moved the lens JSONL schema INSIDE the fenced launch template for precisely this reason; and CA-130 records delivered agent definitions arriving stale for seven-plus consecutive rounds, including definitions missing four on-disk sections -- a condition reproduced again this round across ALL ELEVEN lenses, which is the strongest available evidence that the agent definition is the wrong single home for a structural instruction. The sibling artifact lenses-run.txt sits in the same structural position but is protected by wave7-smoke.sh:1507-1510's T24 AC0; tooling-notes.md has no structural assertion at all. Partial credit on False Alarm Filter 1: CA-466's ledger entry sanctions the file as on-demand and human-read; what survives is that the fix placed the STRUCTURAL consumer only in the surface with a documented staleness history. Fix: add one line to the fenced spawn prompt at :347-361 instructing the synthesizer to read the pass directory's tooling-notes.md if it exists and carry a one-line delivery-degradation note into REMEDIATION.md, plus one wave7 check asserting that clause is inside the fence, mirroring the G13/G6 fence-body extraction at :6930-6941.
  CA-508 P2 NEW in round 9, RESIDUAL OF THE CA-430 FIX (L9 stale-spec 1): EDMV3-T68 was added to the ticket index and to the EDMV3-90 boundary row, but the pack's own bookkeeping was not swept, so NINE pack-level statistics still assert 67 tickets and the range EDMV3-T01 through EDMV3-T67. Sites: :12 (the Tickets row, 67 and the T01..T67 range); :232 (E4 shown as 10 tickets in wave B, while T68 is E4 wave C); :240 (Total 67); :257-266 (sizing S=31, total 67, and the arithmetic 4 + 31 + 26 + 6 = 67, which becomes 32 and 68 with T68); :365-384 (the wave-C Mermaid subgraph lists T45 through T67 with NO T68 node and NO edge from T28, while :221 declares T28 as its dependency); :519-521 (class lines cover T45..T67 only); :527-531 (all 67 nodes appear in exactly one class line, with a six-term sum equalling 67); :545 (123 edges over 67 nodes); :586-588 (every ticket T01..T67 appears in at least one coverage row); :737-741 (a 115-ID union, all 67 tickets carrying at least one SRD Refs entry and all 67 appearing in at least one coverage-map row). SEPARATE CONVENTION CONFLICT in the same finding: :590-596 states that a Won't Have is not delivered by anything, records EDMV3-90 as STRIPPED from SRD Refs, and explains that listing one 'made the pack's own statistics table contradict its ticket field tables in five places' -- yet :221 and epics/04:1200 now put EDMV3-90 (amended) BACK into a ticket's SRD Refs while :723-729 still reports Won't-Have-covered-by-a-ticket as 0. That is the exact contradiction the convention was written to remove, reintroduced. Fix: sweep all nine statistics to 68 and T01..T68, add the T68 Mermaid node, its dependency edge from T28 and its class line, and resolve the Won't-Have convention explicitly -- either amend :590-596 to sanction an amended SRD Ref and update :723-729's count, or strip EDMV3-90 from T68's SRD Refs and record the authorization in the Depends On or rationale text instead. Sequence with CA-509, which is the srd.md half of the same amendment.
  CA-509 P2 NEW in round 9 (L9 stale-spec 2): the ticket pack asserts an SRD amendment that DOES NOT EXIST in srd.md. tickets/README.md:689 and epics/04:1200 and :1207 say EDMV3-90 is amended by D57/D58 for exactly one sanctioned flag, and T68's SRD Refs reads EDMV3-90 (amended, D57/D58). srd.md's EDMV3-90 is BYTE-UNCHANGED: AC1 at :3836-3837 still states that no --force, --accept-partials, --skip-checks, --yes or equivalent bypass flag exists on phase-complete, archive, approve-gate or audit-converged, and AC4 at :3846-3848 still bans a recorded-exemption category. srd.md is still v1.3.0 with no change-request entry, in direct contrast to the pack's own documented amendment route CR1 through CR6 at tickets/README.md:785-795, each of which landed as a versioned srd.md change. EXPLICITLY NOT A RE-FILE OF CA-423, which is NOTED under human override D58 -- no claim is made here that the flag is unauthorized. The defect is the missing PAPERWORK half: D58 enumerated four consequences (the ticket, the tree-wide relabel, the T28 AC12 amendment and the README boundary row) and srd.md was not among them, leaving one governing artifact asserting a fact about another that the other contradicts, with the SRD -- the higher authority -- being the one that is wrong on the record. Fix: land the srd.md amendment D57/D58 already authorize -- amend EDMV3-90 AC1 and AC4 to carve out the single sanctioned flag by name, bump srd.md's version and add the CR entry -- or, if the pack's claim is to be withdrawn instead, strip the amended annotation from both pack sites. Do not leave the two artifacts disagreeing.
  CA-510 P2 NEW in round 9, SCOPE CREEP INTRODUCED BY THE CA-471 FIX (L9 scope-creep 1): the round_type downgrade inside cmd_audit_round_complete (:4438-4468), together with the new refuse-to-proceed gate at skills/code-audit/SKILL.md:104-113, is a CONVERGENCE-BLOCKING REFUSAL with no acceptance criterion, and both candidate owners explicitly disclaim it. T27's Out of Scope (epics/04:445) assigns audit-round-complete to EDMV3-T51 in wave C; T51's Out of Scope (epics/08:203) assigns round_type recording to EDMV3-T27 in wave B; T51's AC1 through AC10 (epics/08:145-186) cover only timestamp, duration, tokens, cost, the OPEN_AUDIT_ROUND anomaly, metrics, C-4, double-completion and atomicity; and T27 AC1 (epics/04:375-380) derives round_type at audit-round-START only, from lens-set equality. The pack's own rule at tickets/README.md:66-68 requires any ticket adding a gate, a refusal, an allowlist or a permission boundary to carry AT LEAST ONE POSITIVE AND ONE NEGATIVE AC; this refusal has neither. L9's recommendation, adopted at synthesis: the mechanism is a genuine improvement over CA-471's own prescription -- do NOT remove it -- give it an owner. Fix: add an AC to T51 with a positive branch (a complete round with all lens JSONL present stays full), a negative branch (a missing, empty or unparseable lens-L{N}.jsonl records partial and audit-converged then refuses) and the C-4 branch (no pass directory or no lenses-run.txt leaves the round exactly as before), and delete the contradicting Out-of-Scope line in BOTH T27 and T51 in the same commit. Sequence with CA-477, which is the missing test half of the same gate, and CA-506, which is its operator-facing terminus half.
  CA-511 P2 NEW in round 9 (L3-6): nothing couples the eval's INNER per-phase timeout budget to the CI job's OUTER timeout, and the inner budget already consumes 90 percent of the outer. eval:nightly sets a 150-minute (9000-second) job timeout; run-eval.sh runs exactly three phases -- the plan, srd and audit-srd invocations at :500, :533 and :571 -- each bounded by PHASE_TIMEOUT_SECONDS with a 2700-second default, plus a 60-second auth probe at :343, before the npm install, fixture provisioning, scoring and the baseline comparison. Worst case from the inner timeouts alone is 3 times 2700 plus 60, or 8160 seconds (136 minutes), against a 9000-second ceiling. Two consequences. (1) THE OUTER TIMEOUT DEFEATS THE CA-452 HANDSHAKE IN EXACTLY THE CASE IT MATTERS: when the job timeout wins, GitLab kills the job and the TRAILING script steps -- score-artifacts.sh at :748-753, edm-compare-eval at :758-768 and CA-452's own 'a partial run always fails the job' step at :771-775 -- never run at all, so the partial-run contract CA-452 was filed to make live is bypassed precisely when the run was partial. (2) CA-444 VALIDATED THE KNOB'S TYPE BUT LEFT IT UNBOUNDED ABOVE: a 3600-second EDM_EVAL_PHASE_TIMEOUT_SECONDS passes validation cleanly and makes three phases 180 minutes against a 150-minute ceiling, i.e. the phase timeout becomes DEAD WIRING -- the classic inner-exceeds-outer shape. Neither file references the other, and the knob's own documentation at run-eval.sh:34-35 does not mention the CI ceiling. Fix: derive one from the other -- refuse at run-eval.sh startup when three times PHASE_TIMEOUT_SECONDS plus a fixed overhead exceeds CI_JOB_TIMEOUT where that variable is present -- and add a comment at .gitlab-ci.yml:713 naming the three-phase arithmetic the 150-minute figure is derived from, so a future edit to either number sees the other.

## Gates

- Gate 1 - approved 2026-07-25T20:40:25Z by darryl.porter
- Gate 2 - approved 2026-07-25T22:30:47Z by darryl.porter
- Gate 3 - approved 2026-07-27T01:43:50Z by darryl.porter

## Artifact Checklist

| Artifact | Status |
|----------|--------|
| `./SRD/edm/EDMV3__prompt-streamline/planning.md` | [present] |
| `./SRD/edm/EDMV3__prompt-streamline/srd.md` | [present] |
| `./SRD/edm/EDMV3__prompt-streamline/audit-srd.md` | [present] |
| `./SRD/edm/EDMV3__prompt-streamline/tickets/README.md` | [present] |
| `./SRD/edm/EDMV3__prompt-streamline/tickets/audit.md` | [present] |
| `./SRD/edm/EDMV3__prompt-streamline/architecture.md` | [present] |
| `./SRD/edm/EDMV3__prompt-streamline/decisions.md` | [present] |
| `./SRD/edm/EDMV3__prompt-streamline/ROLLBACK.md` | [absent] (on-demand) |
| `./SRD/edm/EDMV3__prompt-streamline/exec-report.md` | [absent] (on-demand) |

## Key Decisions Made

_(none recorded yet - decisions are captured at Gate 1)_

-> See full decision ledger: `./SRD/edm/EDMV3__prompt-streamline/decisions.md`

## How to Resume

1. Pull the latest branch - all EDM artifacts are committed
2. Open Claude Code in the project root
3. Run: `/edm:orchestrator EDMV3`
4. The orchestrator detects the existing initiative and resumes from **Phase 6 - Implementation**

## Notes

_(Add anything a teammate should know before resuming - context, blockers, preferences)_
