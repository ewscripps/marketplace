# Key Decisions

| # | Decision | Chosen | Rationale | Date |
|---|----------|--------|-----------|------|
| D1 | Streamline scope | Large (~50 files) | Medium scope plus structural/governance edits across all 30 agents (count corrected per audit finding B-02) | 2026-07-24 |
| D2 | Convergence-gate schema | Reuse the field | `edm-state approve-gate <PREFIX> code-audit` sets `code_audit_converged` directly; field removed from `cmd_set` allowlist | 2026-07-24 |
| D3 | `edm-init` branch fix | Post-checkout correction | Safer than reordering on the warn-and-continue failure path | 2026-07-24 |
| D4 | Grandfathering | Grandfather existing converged initiatives | No forced re-approval through the new gate | 2026-07-24 |
| D5 | `edm-explorer` write access | Grant `Write` tool | Agent persists its own findings, orchestrator stops proxying | 2026-07-24 |
| D6 | Code-audit deferral policy | No deferral, any severity | P0/P1/P2 all block convergence; NOTED (non-actionable) unaffected | 2026-07-24 |
| D7 | Audience | Team adoption planned | Enforcement kernel (R1) is top priority alongside mechanical fixes | 2026-07-25 |
| D8 | Mermaid `#59;` validation | Skip -- known working | User has seen `#59;` render correctly in org tooling; no fixture-MR ticket | 2026-07-25 |
| D9 | EDMV2 Phase 6 cost root cause | `phase-complete 6` never called | `phase-start 6` fired 2026-06-08T08:49:54Z, complete never did; fix = wire the call + archive lifecycle check; attribution scoping is a separate small ticket | 2026-07-25 |
| D10 | R4 orchestrator refactor | Full dispatcher | Skill-to-skill invocation confirmed working (git plugin precedent); phase procedures live once; gate protocol written once | 2026-07-25 |
| D11 | Platform support | macOS/Linux only | Stated constraint in README/CLAUDE.md; no porting work | 2026-07-25 |
| D12 | `lifecycle_mode=partial` | Dead -- delete | Joins delete list with `TaskCompleted` hook and `record-task-duration` no-op | 2026-07-25 |
| D13 | Universal no-deferral policy | Nothing deferred, ever | Broadens D6 to the whole methodology: all FAIL severities remediated; every PARTIAL closed via mandatory runtime verification before archive; no `--accept-partials` or equivalent escape hatches; no generic `--force` override on phase-complete artifact checks (mode-aware skip-phase records are the only exemption path); no deferral vocabulary in any prompt or template | 2026-07-25 |
| D14 | Phases-as-data | Out of EDMV3 scope | Scope boundary, not a deferral: a separate future initiative decided on its own merits after WS1-WS5 prove out through one real initiative | 2026-07-25 |
| D15 | Unverifiable AC (PARTIAL dead-end, audit finding C-01) | AC is a spec defect: rework it | No BLOCKED verdict, no exemption record. An AC whose runtime environment does not exist is a specification defect: remediation reworks the AC into something verifiable today, or moves the unverifiable clause to a follow-on initiative as a recorded scope boundary (D14 framing) via gate change control. verify-runtime records PASS/FAIL only; archive stays hard-blocked until every AC is closed | 2026-07-25 |
| D16 | Model/effort assignments (Gate 3 revise) | 3 safe downgrades now + measured tiering matrix for everything else | Immediate: edm-explorer and edm-test-coverage-auditor opus/max -> sonnet/high (scan/list work), edm-architect max -> high (writing work). Everything else (11 lenses, SRD/ticket auditors, QC auditor, synthesizer) UNCHANGED until the wave-C tiering matrix runs each agent against the wave-A eval fixture at candidate (model, effort) pairs; cheapest config wins only if it holds 100% P0/P1 recall and >= 80% total recall vs the opus/max baseline; any miss reverts to opus. Results recorded; matrix re-run per model generation. Rationale: hand-picked tiers are unmeasured judgment -- the same prose-not-mechanism sin this initiative exists to fix | 2026-07-26 |
| D17 | AC2 scoped-grant spike (EDMV3-T02) | Bare `Bash` token retained; scoped `Bash(edm-state *)` rejected for agent frontmatter | Spike: added `Bash(edm-state *)` to `agents/edm-test-planner.md` `tools:` (scratch, reverted) and ran `claude plugin validate plugins/edm/` -- it accepted the syntax and passed with the same single pre-existing warning as the unmodified baseline (`CLAUDE.md at the plugin root is not loaded as project context`), so the validator does not distinguish scoped from bare Bash grants in agent `tools:` -- passing validation is not evidence of runtime-enforced scoping. Combined with zero existing precedent (every one of the ten prior Bash-holding agents, e.g. `edm-implementer.md`, `edm-test-coverage-auditor.md`, uses a bare `Bash` token) and the fact that `Bash(pattern)` scoping is documented only for `skills/*/SKILL.md` `allowed-tools` (e.g. `skills/code-audit/SKILL.md:8`), not for agent `tools:`, the scoped-grant branch of AC2 is not taken: `edm-qc-auditor.md` is granted a bare `Bash` token, with the limitation recorded as a frontmatter comment on that agent | 2026-07-26 |
| D18 | lens Write grant -- rejected alternative (EDMV3-T02, AC11) | Grant `Write` directly to all 11 lenses + synthesizer, bounded by `Edit`/`NotebookEdit` deny and named output contracts | Rejected alternative: keep all 13 F3-class agents fully read-only and have `skills/code-audit/SKILL.md`'s orchestrating context persist each lens's chat-returned report to `${OUTPUT_DIR}/lens-L{N}.md` on the agent's behalf (a proxying model). Rejected because: (1) it does not fix the underlying contradiction -- the agent bodies and the skill's launch template both already instruct the lens to write its own file (`skills/code-audit/SKILL.md:44,99`), so proxying is a workaround for a spec defect, not a fix of it; (2) it silently caps report size at the orchestrator's context/response budget for 11 parallel lenses, which the direct-write model does not; (3) it was reproduced live during this initiative -- both EDMV3 explorers, which share the same defect class, returned their reports as chat text with "I have no Write tool" apologies instead of files, which is the exact failure a proxying model still requires the orchestrator to catch and relay perfectly, every time, for every lens. Accepted residual risk (per `architecture.md` R-E): granting `Write` to 11 previously read-only agents is the largest new attack surface this ticket introduces. Bounded by three controls delivered in EDMV3-T02: `Edit` and `NotebookEdit` stay denied for all 12 code-audit agents (AC6), each `## Output` section names exactly two permitted write paths and calls anything else a contract violation (AC7), and audits run against a committed branch so a stray write is visible in `git status` before it can propagate | 2026-07-26 |
| D19 | Wave-A QC pack-level AC contradiction (EDMV3-T21 AC5 vs EDMV3-T61 AC13 / EDMV3-T01 AC9) | T21 AC5 wins: zero literal wave-suite tokens in `.gitlab-ci.yml` | T21 AC5 demands `grep -cE 'wave(3\|4a\|4b\|5\|6\|7)-smoke' .gitlab-ci.yml` = 0; T61 AC13 and T01 AC9 each demanded a durable literal pointer (`wave7-smoke`, `wave6-smoke`) naming a specific suite file in `.gitlab-ci.yml`, which is the exact enumeration T21 AC5 forbids. T21 AC5 wins -- a pipeline file that never hand-names an individual suite cannot silently drift when a suite is renamed or added, which is the stronger invariant. T61 AC13 and T01 AC9 are AMENDED to assert the suite runs via `run-all.sh` auto-discovery instead, verified by `run-all.sh`'s own per-suite output naming the suites it discovered (the aggregator's own receipt is the durable pointer, not a second, hand-maintained one in the pipeline file). `.gitlab-ci.yml`'s `test:smoke` job comment and `wave7-smoke.sh`'s T21 AC5 case were updated to match. **Extended 2026-08-08 per CA-163: four more ACs hit the identical T21 AC5 contradiction this remediation cycle and were reworked the same way with no record until now** -- `tickets/README.md:64-65`'s "through gate change control, never recorded as accepted" rule applies to all six, not just the original two. **T16 AC10** (`epics/02-enforcement-kernel.md`): before -- "Verify: `grep -n '<wave-N>-smoke' .gitlab-ci.yml`" (a literal suite-file pointer, the same shape T61 AC13/T01 AC9 had); after -- "Verify: `grep -n 'run-all.sh' .gitlab-ci.yml plugins/edm/bin/tests/run-all.sh`" (auto-discovery, no suite literal). **T42 AC12** (`epics/06-mermaid-rule.md`): before -- a literal suite-file pointer naming the Mermaid rule-presence test's specific suite; after -- "Verify: `grep -n 'run-all.sh' .gitlab-ci.yml`". **T44 AC6** (`epics/06-mermaid-rule.md`): before -- a literal suite-file pointer naming the fixture-corpus test's specific suite; after -- "Verify: `grep -n 'run-all.sh' .gitlab-ci.yml`". **T56's CI row (AC6)** (`epics/09-pattern-library-curation.md`): before -- a literal suite-file pointer for the four-heading contract test; after -- "Verify: `grep -n 'four-heading\|contract check' plugins/edm/bin/tests/wave7-smoke.sh` ... and `grep -n 'run-all.sh' .gitlab-ci.yml`". All four "after" forms are what ships in the current ticket pack; all four match the exact reasoning already recorded above for T61 AC13/T01 AC9, so no new rationale is introduced, only the missing record of which ACs it also applies to | 2026-07-27 (extended 2026-08-08) |

| D20 | Eval driver auth gate (T22 AC8 rework per D15) | CLI auth accepted alongside ANTHROPIC_API_KEY | The env-var-only credential gate was a false precondition: headless `claude -p` runs on the CLI's subscription/OAuth login with no ANTHROPIC_API_KEY, verified live in this environment (claude 2.1.220, one-token probe returned OK). Per D15, an AC whose stated precondition does not match the runtime environment is a spec defect: run-eval.sh now accepts either auth path and still refuses to start with no working auth at all (AC8's substance -- no half-run against a dead backend -- preserved). Wave-B autonomous completion authorized by the user 2026-07-27 ("complete all waves without getting a confirmation from me"), which includes the API/subscription spend for the baseline captures | 2026-07-27 |
| D21 | Skill-tool composition depth spike (EDMV3-T34) | **GO** | Live two-level chain (`spike-caller -> spike-mid -> spike-leaf`) run via `claude --plugin-dir <scratch> -p` against a real (non-development-tree) plugin cache, per `spike-skill-composition.md`. All six questions resolved favorably: invocation succeeds; `$ARGUMENTS` propagates and composes across two hops; caller-side context (a secret word never written to a file) is visible to the callee at both hops -- the callee runs in the same conversation, not an isolated sub-agent; each skill's own `allowed-tools` governs its own actions (a Bash-less caller's callee ran Bash successfully); an unknown target skill fails with a clean, nameable `tool_use_error: Unknown skill: <name>` rather than hanging or silently continuing; and the caller retains its own context after the callee returns. No rescope of EDMV3-T35 through EDMV3-T38 is triggered | 2026-07-28 |

| D22 | CLAUDE.md by-name reference resolution from an installed plugin cache (EDMV3-T41) | Negative -- does not resolve deterministically; generated docs/canonical-sections.md (negative branch, EDMV3-T41 AC4) | Claude Code 2.1.220. Checked twice against two independent sources of evidence: (1) `claude plugin validate plugins/edm/`, run twice in separate invocations with byte-identical output both times: "root: CLAUDE.md at the plugin root is not loaded as project context. To ship context with your plugin, use a skill (skills/<name>/SKILL.md) instead." This is a deterministic tool statement, not a probabilistic model guess. (2) An actual marketplace-installed cache exists on this machine at `~/.claude/plugins/cache/stg-marketplace/edm/2.0.0/` (installed cache, pre-existing from prior plugin usage, not `--plugin-dir ./plugins/edm`) -- CLAUDE.md IS present there (file-presence holds, confirming EDM-REVIEW.md:159's inferred note), and its "Severity vocabulary (canonical)" section exists at that version but "Mermaid diagram conventions (canonical)" does not (added to the dev tree after that cache was populated) -- a second, independent resolution failure mode (stale install) on top of the mechanism question. Reproduced the "retrieved the wrong file" failure mode named in AC1: from a scratch project directory disjoint from `~/.claude/plugins/cache/...` (no shared filesystem ancestor reachable by a cwd-relative walk), `find . -iname CLAUDE.md` resolves to that project's OWN CLAUDE.md, not the plugin's -- exactly what an agent's own file search would do if it does not already know the plugin's cache path. Zero of the ~20+ existing `` `CLAUDE.md Sec."..."` `` references across agents/skills carry any plugin-relative anchor (`$CLAUDE_PLUGIN_ROOT` or similar) that would let an agent construct the correct absolute path itself. Negative branch (AC4) implemented: `plugins/edm/bin/edm-sync-canonical-sections` deterministically, one-directionally extracts the two canonical sections verbatim from CLAUDE.md into `plugins/edm/docs/canonical-sections.md` (byte-identical, AC5 -- guarded by a smoke-test case in `bin/tests/wave6-smoke.sh`, not `wave7-smoke.sh` as the ticket's Target Components names, because this wave's file-ownership split assigned wave6-smoke.sh, not wave7-smoke.sh, to the implementing agent). **AC7 (ordering) already violated, and this is a REALIZED risk, not a hypothetical one**: `git log` shows `fa108ee feat(edm): EDMV3-T42 -- eleven Mermaid touch points reference the canonical rule` landed, and was merged into this branch at `424f3dc`, BEFORE this T41 check ran -- wave-B's parallel-agent scheduling did not honor AC7's stated landing order. All eleven touch points (`wave7-smoke.sh`'s "T42 AC10" case: "all eleven touch points carry the reference or pattern-library entry") already carry the bare `CLAUDE.md Sec."..."` form this D22 finding just confirmed does not resolve from an installed cache. **Handoff, urgent, not yet done**: updating those nine prompt-surface files (`agents/*.md`/`skills/*/SKILL.md`) to reference `docs/canonical-sections.md` instead of the bare `CLAUDE.md Sec."..."` form requires editing files outside this agent's file boundary this wave (owned by whichever agent handles `agents/*.md`/`skills/*.md`). This is no longer a "before T42 lands" preventive measure -- it is a fix for eleven touch points that already shipped pointing at a confirmed-broken mechanism, and should be picked up as its own immediate follow-up ticket. | 2026-07-28 |
| D23 | EDMV3-T39 eval gate on the dispatcher refactor | Shipped T37/T38 WITHOUT the eval comparison; tripwire built, not armed | The wave-A eval baseline does not exist. Seven capture attempts surfaced and fixed six real driver defects (env-var-only auth gate; `--bare` isolating stored credentials; $2 then $6 per-phase budget caps against measured plan $1.49 / srd $5.42 / audit >$6; nested Claude Code session env leaking cumulative spend into the child; background subagents dying at headless turn-end leaving no audit-srd.md; 900s timeout against a 913s audit phase) and one org monthly spend limit; no run has yet completed all three phases. Per D15, an AC whose stated precondition does not exist is not silently passed and not faked. What SHIPPED: `bin/edm-compare-eval` implementing AC2's threshold (candidate >= baseline_total - variance.total_range) and all three AC3 refusal conditions (scorer_version mismatch, dimensions_scored mismatch, complete:false), each verified live against hand-built fixtures, plus a distinct exit 3 for "no baseline -- tripwire not armed" so an absent baseline can never read as a pass; AC4's per-dimension delta table; AC7's fallback `bin/edm-check-skill-sync`, verified in both directions and wired into run-all.sh so the not-taken branch is still exercised every run; the CI comparison step in `.gitlab-ci.yml`. What is OPEN: AC1 (a post-refactor run artifact) and the baseline data itself. Closing command: export a working auth, then `bash plugins/edm/evals/run-eval.sh --out <dir-outside-plugins/edm>` three times, score each, commit the middle scores.json to `plugins/edm/evals/baseline/` with the max-min variance table per `evals/baseline/README.md`, then re-run `bash plugins/edm/bin/edm-compare-eval <post-refactor>/scores.json`. Risk accepted knowingly: T37/T38 landed with 1124/1124 suite coverage and the T2-09 two-commit reviewable structure, but without the empirical regression number the review named as their mitigation | 2026-07-28 |
| D24 | `monitors/watch-impl` lifecycle finding (EDMV3-T59 AC7 precondition) | **Host-managed (Branch A)** -- `monitors/monitors.json` and `cmd_watch_impl` are kept as-is; documented, not deleted | Claude Code 2.1.220 (`/Users/darryl.porter/.local/share/claude/versions/2.1.220`). Evidence extracted directly from the installed CLI binary's embedded string table (`strings -a`), not inferred from external docs (none reachable from this sandbox): (1) a plugin-manifest schema description states "Background watch scripts the host arms as persistent Monitor tasks (unsandboxed, same trust tier as hooks) so plugins need not instruct the model to arm them. When omitted, monitors/monitors.json at the plugin root is loaded if present." (2) The monitor's `when` field schema: `"always" arms at session start and on plugin reload. "on-skill-invoke:<skill>" arms the first time that skill is dispatched (via Skill tool or slash command)` -- `edm-impl-progress`'s `on-skill-invoke:implement` therefore arms once, on the first `/edm:implement` dispatch of the session. (3) The monitor identifier field is described as "Used to dedupe so re-arming (plugin reload, repeat skill invoke) does not spawn duplicates" -- the host tracks armed-monitor identity itself, confirming host-side lifecycle management rather than a fire-and-forget shell spawn. (4) The underlying persistent-task mechanism (the same one plugin monitors are armed as) is documented as "Run for the lifetime of the session (no timeout). Use for session-length watches like PR monitoring or log tails. Stop with TaskStop" -- i.e., a persistent monitor is NOT scoped to the lifetime of the triggering skill invocation; it runs until the session ends or is explicitly stopped via the host's `TaskStop` mechanism, not by the skill returning. No string ties monitor lifecycle to `PreCompact`/compaction, so compaction (a conversation-context operation) does not appear to stop or restart a running monitor process. Conclusion: the "unkillable-looking loop with no documented owner" framing in the review was the correct concern about *documentation*, not about the mechanism -- the host does own, dedupe and can stop the process; EDM's plugin manifest simply never said so. Branch A taken per EDMV3-T59 AC8: `CLAUDE.md` gains a subsection naming the arm trigger, the 5-second poll interval (`cmd_watch_impl`'s `sleep 5`), and `TaskStop`/ending the session as the two ways to stop it. No deletion of `cmd_watch_impl`, `monitors/monitors.json`, or the `watch-impl` dispatch entry follows from this finding | 2026-07-28 |

| D25 | token attribution -- EDMV3-72/EDMV3-T52 AC1 | Branch (a): scope to the driving session | `get_session_tokens_since` previously summed every session JSONL under the project's `~/.claude/projects/<encoded-cwd>/` directory unconditionally, so a second Claude Code window open on the same project inflated a phase's or audit round's token/cost figures. Branch (a) chosen over relabelling (branch b) because a real per-phase Claude cost is the more useful number this initiative is trying to make honest, and a workable identification mechanism exists without new instrumentation: the driving session -- the one this process is actually running inside -- is the single most-recently-modified `*.jsonl` file in the sessions directory at call time, because Claude Code appends to its own session's JSONL as the conversation proceeds, making it always the most recently touched file on disk. `ls -t` breaks mtime ties deterministically (by name). AC2's fallback (sum the whole directory, the pre-T52 behaviour) applies only if the sessions directory has no readable jsonl files at read time; the resulting figure carries `attribution_mode: "scoped"` or `"whole-directory"` on `phase_durations[phase].attribution_mode` and (T51) each completed audit-round entry, so a reader can always tell which mode produced a given number. Same commit also refreshes the pricing table (EDMV3-73): Opus 4.7/Sonnet 4.6/Haiku 4.5 (one generation stale, per this ticket's own description) become Opus 4.8/Sonnet 4.7/Haiku 4.6, verified 2026-07-28; the previous generation's identifiers and rates are preserved (frozen, non-overridable) in `compute_cost_usd` so an already-recorded state file is never silently repriced on a later read (AC9), and a model identifier matching neither generation nor the `unknown` no-session sentinel produces an explicit stderr warning plus a Sonnet-tier placeholder cost rather than a silent, possibly-wrong figure (AC10) | 2026-07-28 |

| D26 | EDMV3-T67 measured budget misses (AC5, AC6, AC10) | AC5 fixed and now PASS; AC6 re-measured and worse, and its budget *shape* is the real defect; AC10 unchanged and still open. All remaining work carried as named follow-on tickets, not candidates (D29) | **Amended 2026-07-28 after commit `d591b92`. The original entry's figures and root cause were wrong and are superseded here rather than left standing.** All figures measured live on this machine with the committed `bin/tests/timing.sh`. **AC5 (commit-path lint budget, 3,000 ms) -- RESOLVED, PASS.** Originally recorded at 39,872 ms p95; re-profiling on a 30-file / 9,990-line fixture put the true pre-fix figure at 70,168 ms. **The root cause this entry originally named was wrong.** It blamed `build_line_classes()`; that function cost 46 ms. The cost was class 1 (attribution), which ran a bash per-line loop forking `echo \| tr` twice per line to lowercase -- roughly 20,000 subprocesses on a 10,000-line tree. Classes 2 and 3 already ran one `grep` per file; class 1 now matches them, using a single `grep -niF -f` per file against a temp pattern file. `build_line_classes()` was separately rewritten from a per-line bash loop into one awk pass (46 ms -> 41 ms) -- the smaller win, but it removes the same fork-per-line shape. After: **1,021 ms p95** (samples 1035 / 1021 / 1018) against the 3,000 ms budget, a 42x improvement on the re-profiled figure. All four detection classes produce byte-identical output against the pre-change binary over all 16 committed fixtures. Reproduce: `bash plugins/edm/bin/tests/timing.sh --lint`. **AC6 (Mermaid-class overhead ratio, budget 1.40x) -- number now PASSES at 1.19x (commit `ea31ce8`), budget shape still unresolved.** The figure moved twice. After the AC5 fix it went *worse*, 2.26x -> **3.40x** (baseline 984 ms, with-Mermaid 3,345 ms), **because the baseline got roughly 40x faster while class 4 (Mermaid) did not** -- class 4 became the dominant cost rather than a marginal one. Class 4 was then given the same treatment class 1 got: `mermaid_line_is_violation()` ran per line and forked about ten times each (three `sed` calls to trim, strip entity codes and strip a trailing semicolon, then up to seven `grep`s for the label-span and sequence-diagram rules, plus a `sed -n` to fetch the line), and is now one `awk` process per file in `mermaid_scan_awk()`. Result: **1.19x** (baseline 1,000 ms, with-Mermaid 1,193 ms). The entity-code length bound is walked explicitly rather than written as an ERE interval, because interval support is not universal across bwk awk, mawk and busybox awk -- all three run this plugin in CI. Output is byte-identical to the pre-change binary across all 17 committed Mermaid fixtures, a purpose-built 13-violation adversarial corpus (entity-code bound, tab and space indentation, bare and argument-bearing `classDef`/`style`/`linkStyle`, all five label-span forms, sequence-diagram messages, misused and correctly-scoped ignore markers, non-Mermaid fences, empty fence) and a full `--all` tree scan. That corpus earned its keep: it caught a real regression in the first attempt, where `IFS=$'\t' read` collapsed the field separator and a following literal tab into one delimiter (tab is IFS whitespace) and ate the leading indentation of a tab-indented line in the reported snippet. **The budget itself is still malformed and that is not fixed by a green number.** Both QC shards ruled independently that a bare ratio with no stated input size and no absolute floor measures fixed process overhead rather than the linter, and this AC proves it from both directions: the same budget read miss, worse-miss, then pass across three commits, only one of which touched the code it is supposed to measure. Separately, the original 2.26x was taken on a 5-file fixture, not the 50-initiative fixture AC6's own text names, so it never answered AC6 regardless of its value. Re-deriving it as a sized conditional plus an absolute ceiling, and re-measuring on the named fixture, is the one piece of this entry's work still outstanding (D29 T01). Reproduce: `bash plugins/edm/bin/tests/timing.sh --mermaid-ratio`. **AC10 (four parallel lint jobs) -- RESOLVED, PASS (commit `d36e390`).** `bash -n`, `edm-lint-artifacts --all`, `edm-check-grants` and `edm-check-vocabulary` ran as sequential script lines inside the single `lint:shell-and-artifacts` job, which cost twice over: their wall-clock times summed, and because the block ran under `set -e` the first failure suppressed the results of every check after it. They are now `lint:bash-syntax`, `lint:artifacts`, `lint:grants` and `lint:vocabulary`. Every `lint` job carries `needs: []` so it starts at pipeline start rather than at the head of its stage; the three `test` jobs carry `needs: ["lint:bash-syntax"]`, since a syntax error means the suite cannot run meaningfully while the other three checks are orthogonal to whether it passes. Splitting one job into four would have tripled the duplicated image and rules blocks, so those moved into an `.alpine_edm` anchor. Verified: the pipeline parses and the graph is seven independent `lint` jobs plus three `test` jobs gated on the syntax check. **Superseding note on this entry's own remediation plan:** the paragraph above originally carried all three items to D29 as follow-on tickets. Two of those three have since been done in this wave (D29's T02 = this job split, T03 = the class-4 optimization recorded under AC6), so D29 is amended in place rather than left describing work that is finished. Only the AC6 budget *re-derivation* remains open | 2026-07-28 (amended) |
| D27 | EDMV3-T67 AC9/AC13 live-dependency verdicts | verified-locally-pending-pipeline (not faked) | AC9 (blocking pipeline < 5 minutes at each wave boundary) requires a live GitLab runner this sandbox does not have; AC13 (one eval run < 30 minutes, cost documented) requires triggering a real, costed `run-eval.sh` invocation (real API spend, up to an hour of wall time) that this session did not trigger. Per D15's precedent (an AC whose stated precondition/environment does not exist here is not silently passed and not faked), both are recorded as verified-locally-pending-pipeline in `CHANGELOG.md`'s EDMV3-T67 table rather than marked PASS. `evals/README.md` already documents a prior measured eval run (2026-07-27: audit-srd phase alone ran 913s, total range 30-60 minutes), which itself brackets the 30-minute AC13 figure and should be re-measured on the next live eval trigger | 2026-07-28 |
| D28 | Tiering matrix (EDMV3-T48, D16 promotion rule) | Matrix built and unit-verified; NOT run; all 15 contested-agent assignments UNCHANGED | D16 mandates that the eleven lenses, `edm-audit-synthesizer`, `edm-srd-auditor`, `edm-ticket-auditor`, and `edm-qc-auditor` are retiered ONLY by a measured matrix result against the wave-A eval fixture -- hand-picked tiers are exactly the prose-not-mechanism defect this initiative exists to fix, so none is taken here. The matrix cannot run for a reason already recorded once (D23): the wave-A eval baseline does not exist (seven capture attempts fixed six real driver defects and hit one org spend limit; no run has completed all three phases). Building the matrix and measuring against it are two different deliverables, and this ticket ships only the first, honestly: `evals/tiering-matrix.sh` implements the mechanical promotion rule (cheapest configuration holding 100% of the baseline's specific P0/P1 findings by ID -- a strict superset check, not a count comparison -- and >= 80% of total findings wins; any P0/P1 miss disqualifies that configuration outright regardless of its total-findings ratio; no qualifying cheaper configuration leaves the agent at `opus`/`max`) and is verified end to end (`--self-test`) against three hand-built synthetic agents proving each of the three logic branches: a qualifying cheaper configuration wins (`sonnet`/`high` beats the more expensive candidate), a configuration missing one baseline P0 finding is disqualified outright and the next tier is tried instead (`opus`/`high` wins after `sonnet`/`high` is rejected), and an agent with no qualifying candidate is left unchanged at `opus`/`max`. All three assertions pass (`bash plugins/edm/evals/tiering-matrix.sh --self-test` exits 0, 3/3). No agent frontmatter changed as a result of this ticket beyond the three wave-A downgrades EDMV3-T02 already applied on separately-argued merits; `CLAUDE.md`'s "Model and effort assignments" table carries a provenance header stating plainly it is not yet matrix-derived, per D15 (a spec defect is reworked honestly, not faked with placeholder measurements). **Closing command, once the D23 baseline exists**: run the eval fixture per EDMV3-T48's Technical Notes batching (one full code-audit round per candidate configuration covers the 12 code-audit agents; one fixture-document run each for `edm-srd-auditor`/`edm-ticket-auditor`/`edm-qc-auditor` per configuration), build a manifest matching `evals/tiering-matrix.sh --help`'s documented schema from each round's `lens-L{N}.jsonl` finding counts/severities and `edm-state metrics-report`'s per-round cost, then run `bash plugins/edm/evals/tiering-matrix.sh <manifest.json>` and write each agent's DECISION line into its frontmatter | 2026-07-28 |

| D29 | Where do D26's three outstanding items live? D13 forbids deferral; D14 permits a recorded scope boundary but its own framing requires a **named** follow-on, and a "candidate" is not one | A named follow-on initiative -- **EDMV4__lint-and-pipeline-budgets** -- carrying three named tickets: **EDMV4-T01**, **EDMV4-T02**, **EDMV4-T03**. **Amended the same day: T02 and T03 were completed inside this wave (commits `d36e390` and `ea31ce8`) and are CLOSED here, not carried forward. Only EDMV4-T01 -- the AC6 budget re-derivation -- survives into the follow-on initiative, and with a single ticket the initiative reduces to that one ticket.** The scoping argument below is retained because it is what justified naming a follow-on at all, and it still holds for T01: re-deriving a budget and re-measuring it needs a generated 50-initiative fixture this initiative does not have | D26 originally closed with "recorded as its own follow-on ticket candidate". That is the one option D13 forecloses: a candidate is not a follow-on, and "candidate" is deferral vocabulary, which D13 bans from every prompt and template and which therefore cannot stand in the initiative's own decision ledger either. The three items are named as tickets here, each with a scope line and its own closing verify command. **Named follow-on initiative rather than tickets appended to EDMV3**, for three reasons: (1) every one of the three needs a validation surface this initiative does not have -- a generated 50-initiative fixture for T01, a live GitLab runner for T02 (the same missing dependency D27 already recorded for AC9/AC13), and a full commit-path re-baseline for T03; (2) all three change either the commit-path hook binary or the pipeline job graph, and EDMV3 has already converged, re-baselined and closed both out at v3.1.0, so appending them reopens wave C after closeout; (3) appending them would make D13's "remediated before convergence" unsatisfiable rather than satisfied, which is worse governance than an explicitly recorded scope boundary under D14's framing. **EDMV4-T01 -- Re-derive the Mermaid-class budget as an absolute ceiling plus a sized ratio, then re-measure.** Scope: replace EDMV3-T67 AC6's bare 1.40x ratio with a budget that states its input size and carries an absolute millisecond ceiling, and take the measurement on the 50-initiative fixture AC6's own text names (both the 2.26x and the 3.40x figures were taken on small fixtures, so neither answered AC6). Closing verify: `bash plugins/edm/bin/tests/timing.sh --lint` against the generated 50-initiative fixture records baseline ms, with-Mermaid ms, the ratio and the absolute figure, and both the ratio and the absolute figure fall inside the re-derived budget. **EDMV4-T02 -- CLOSED, done in this wave (commit `d36e390`).** Closing verify satisfied: `grep -n 'needs:' .gitlab-ci.yml` returns `needs: []` on all seven `lint` jobs and `needs: ["lint:bash-syntax"]` on all three `test` jobs, with no inter-dependency among the lint jobs; the pipeline parses and the graph was inspected. Original scope, for the record: `bash -n`, `edm-lint-artifacts --all`, `edm-check-grants` and `edm-check-vocabulary` become four independent `stage: lint` jobs that converge on the test stage via `needs:`, satisfying EDMV3-T67 AC10. Closing verify: `grep -n 'needs:' .gitlab-ci.yml` returns the four lint jobs with no inter-dependency among them, and one live pipeline run shows the four starting concurrently. **EDMV4-T03 -- CLOSED, done in this wave (commit `ea31ce8`).** Closing verify satisfied: with-Mermaid p95 is 1,193 ms against a 1,000 ms baseline (ratio 1.19x, inside the 1.40x ceiling), and the Mermaid class produces byte-identical output against the pre-change binary across all 17 committed fixtures plus a 13-violation adversarial corpus and a full `--all` scan. The no-fence short-circuit is retained. Original scope, for the record: apply the class-1 treatment from `d591b92` (one `grep` per file in place of a per-line bash loop) to class 4, which is now the dominant cost -- 3,345 ms with-Mermaid against a 984 ms three-class baseline; the no-fence short-circuit AC6 already requires stays in place. Closing verify: `bash plugins/edm/bin/tests/timing.sh --lint` shows the with-Mermaid p95 inside EDMV4-T01's re-derived absolute ceiling, and the Mermaid class produces byte-identical output against the pre-change binary over all 16 committed fixtures | 2026-07-28 |
| D30 | EDMV3-T57 AC5 deviation: `.gitlab-ci.yml:111` implements the banned-file-type scan as `git ls-files -- plugins`, but AC5 specifies a plain `find` and the ticket's own Technical Notes explicitly rule the `git ls-files` form out. The deviation was justified only in a job comment (`.gitlab-ci.yml:99-101`), with no decision record | **(a) Accept `git ls-files`.** The CI implementation stands unchanged; AC5's verify text and the ticket's Technical Notes are amended to match, and this record -- not the job comment -- is the justification | The control exists to stop F11 recurring: banned binaries and macOS metadata *shipping* inside `plugins/edm/`. Shipping means tracked, and `git ls-files -- plugins` measures exactly that. On the CI surface the two forms are equivalent anyway: a GitLab runner checkout contains the tracked tree and nothing else (`GIT_CLEAN_FLAGS` defaults to `-ffdx`, so untracked leftovers from a prior job sharing the workspace are removed before the job runs), so `find`'s extra reach buys nothing there while adding one failure mode the `git ls-files` form cannot have -- a **blocking** job (this one lost `allow_failure` under AC10) going red on a runner-local artifact that is not and never will be committed. The untracked-becomes-tracked path the Technical Notes worried about is closed twice over: `.gitignore:3` is a bare, unanchored `.DS_Store` pattern, so it matches at every depth under `plugins/` and keeps the file out of the index in the first place; and a deliberate `git add -f` override lands the file in `git ls-files`, which is precisely where this check looks. Measured on the live tree 2026-07-28, both forms return 0 under `plugins/`: the AC's literal `find plugins -name '.DS_Store' -o -name '*.pptx' -o -name '*.docx' \| wc -l` prints 0, and `git ls-files -- plugins \| grep -icE '\.(pptx\|docx)$\|(^\|/)\.DS_Store$'` prints 0. The single `.DS_Store` in the working tree sits at the repository root, outside the `plugins/` scope both forms scan. **No change to `.gitlab-ci.yml` follows from this decision** | 2026-07-28 |
| D31 | EDMV3-T45 AC11: the required grounding inspection of the archived EDMV2 artifacts for filler sections was never recorded anywhere in the tree -- every hit was the requirement or the implemented clause, never a result | Inspection performed 2026-07-28 and recorded here. Result: **all three defect categories AC8's anti-padding clause names are attested in the archive, so the clause is grounded rather than assumed; and nothing in the archive argues for lowering a length floor, so AC7's byte-for-byte preservation of the floors is also correct.** The clause and the floors are compatible, and the change stands as shipped | **What was inspected.** `SRD/.archived/EDMV2/srd.md` end to end (1,522 lines / 1,222 non-blank; all 11 numbered sections plus the three appendix subsections), `planning.md` (190), `audit-srd.md` (127), `HANDOFF.md` (76), `tickets/README.md` (534), `tickets/audit.md` (136), plus the section structure of the eight epic files (6,370 lines) and the two `code-audit/` rounds. AC8's clause names three defects -- filler sections, redundant summaries, boilerplate -- so the inspection was scored against those three. **Filler sections (a section present only to satisfy a template, carrying no initiative-specific content): one found, and it is not in the SRD.** `HANDOFF.md:74-76`'s `## Notes` is an unfilled template stub whose entire body is the placeholder prompt `_(Add anything a teammate should know before resuming -- context, blockers, preferences)_`. It is the only literal unfilled stub in the archive. Every one of `srd.md`'s 14 sections and subsections carries initiative-specific content -- `file:line` evidence, EDMV2-NN requirement IDs, or workstream codes -- and none exists purely to fill a template slot. Two sections are short (Section 8.3 "no new user-facing skill is added"; Section 10.3 "no new external dependency") but each is a negative result stated once and *not* padded out, which is the behaviour the clause wants. **Redundant summaries: three found, one of them exact.** (1) `planning.md:179-190` and `HANDOFF.md:55-66` are byte-identical -- verified with `diff`, zero differing lines across the 12-line decisions block; only the heading differs (`## Decisions Made` vs `## Key Decisions Made`). (2) `srd.md` Section 6.1's schema table restates Section 5.4's annotated target-schema JSON block: same 17 fields, same defaults, same workstream attributions, in a second format; Section 6.2's `validate` anomaly list restates the same list already given in Section 5.4's type-handling note. (3) The staging-copy self-hosting mitigation is stated three times -- Section 3.3 C-5, Section 5.6 Risk 1, and Section 10.4 -- in substantially the same words. `srd.md` Section 9's six subsections also restate Section 4 requirements, but in operator-facing prose for a different reader; that is a deliberate audience shift and is *not* counted as a defect here. **Boilerplate: one found.** `tickets/README.md:11-27`'s `### Size` and `### Priority` legend tables are generic (XS/S/M/L/XL effort bands; Must/Should/Could definitions) and carry nothing EDMV2-specific; the adjacent `### Workstream (WS code -> name)` table in the same Legend section is initiative-specific and is not boilerplate. **Does this support or undercut the decision?** It **supports** it, in both directions. All three named categories are real and attested, so AC8's clause describes defects this methodology actually produces -- it is grounded in the corpus, not an imported generic instruction (explorer 02 F4's concern is answered). At the same time, every instance found is *restatement across or within documents*, not length-driven inflation of any single deliverable: no section in the archive is long because someone was reaching for a floor. So the archive supplies no argument for lowering or removing a floor, which is exactly why AC7's verbatim preservation of the `800+`/`200+`/`50+` figures is the right call and why the ticket's change was purely additive. Nothing in this result reverses any part of EDMV3-T45 as shipped. Note on the archive itself: none of the four instances is remediated here -- `SRD/.archived/` is read-only evidence (EDMV2's own C-7 posture, and EDMV3-T52 AC5 forbids rewriting archived artifacts) | 2026-07-28 |
| D32 | `compute_cost_usd`'s family wildcards defeated EDMV3-T52 AC10 from inside a known model family, and this initiative's own recorded cost figures are the evidence | **Fixed in code**: the current-generation arms now match explicit generation identifiers (`*opus-4-8*`, `*sonnet-4-7*`, `*haiku-4-6*`) instead of bare `*opus*` / `*sonnet*` / `*haiku*`, so an unrecognized generation inside a known family falls through to the warning arm. **Recorded, not fixed**: the pricing table has no row for either model this initiative actually ran on, so its own `estimated_cost_usd` figures are placeholder-rate estimates and are left as recorded rather than retroactively repriced | Found by the wave-C prose-versus-code sweep, not by either QC shard. **The defect.** AC10 promises that a `model_used` matching neither generation nor the `unknown` sentinel "produces an explicit stderr warning ... rather than silently costing zero". The `*opus*` / `*sonnet*` / `*haiku*` arms sat ahead of the `*)` warning arm, so any identifier containing a known family name was priced at *current*-generation rates with no warning, whatever generation it actually named. Measured before the fix: `claude-opus-5-20260501` priced at the Opus 4.8 input rate (6.0000 per 1M) silently; `claude-sonnet-9` at the Sonnet 4.7 rate (4.0000) silently; only a non-Claude string such as `gpt-4o` warned. A family wildcard is not a generation check, and the arm ordering made the guarantee unreachable for exactly the inputs most likely to occur -- a newer Claude generation. **Why this initiative is its own evidence.** `.edm-state.json` records `model_used` as `claude-sonnet-5` for phase 1 and `claude-fable-5` for phases 2 through 5. Neither appears in either pricing table. Under the pre-fix code `claude-sonnet-5` matched `*sonnet*` and was priced at Sonnet 4.7 rates **with no warning at all**, while `claude-fable-5` matched no family and did warn. So four of the five recorded phases were already flagged, and the fifth was silently mispriced -- and the total, **$25.869 across five phases**, is entirely composed of placeholder-rate arithmetic for two models the table does not contain. That figure appears in the CHANGELOG and in `metrics-report` output. This sits inside epic E8, whose stated purpose is economics honesty, which is why it is recorded here in full rather than quietly corrected. **Why the figures are not repriced.** Repricing would require verified per-million rates for Sonnet 5 and Fable 5, which are not available from this environment; inventing them would be strictly worse than an acknowledged placeholder, and EDMV3-T52 AC9 already establishes the principle that a recorded cost keeps the cost it was computed with rather than being silently repriced on a later read. After the fix every affected phase would warn on re-read, which is the intended behaviour: the figure is visibly an estimate instead of invisibly wrong. **What remains open.** Adding verified Sonnet 5 / Fable 5 / Opus 5 rows to the pricing table is real outstanding work and belongs to EDMV4 alongside D29's T01; until it lands, every `estimated_cost_usd` this plugin records on a current-generation session is a warned placeholder. Reproduce: `( source plugins/edm/bin/edm-state; compute_cost_usd claude-sonnet-5 1000000 0 0 0 0 )` -- stderr now carries the warning, and did not before | 2026-07-28 |
| D33 | `cmd_approve_gate`'s `code-audit` branch (`bin/edm-state:1451-1503`) only re-runs `cmd_audit_converged` when `schema_at_least(schema_version, 2)` is true (`:1478`); below that it takes the wave-A permissive branch (`:1482-1484`), records a `degraded_checks` entry, and approves unconditionally with no convergence check at all. EDMV3 itself still had `schema_version: 1` -- the literal `cmd_init` writes and nothing had ever advanced -- despite being a mature wave-B/C initiative using every schema-2 shape (JSONL ledger, `audit_rounds` objects). Live-reproduced 2026-08-01: the human approved the Convergence gate at the round-2 presentation (91 blocking: 1 P0, 13 P1, 77 P2) against the orchestrator's explicit "not recommended, approve-gate will independently re-check and refuse" framing; `approve-gate EDMV3 code-audit` nonetheless exited 0 and set `code_audit_converged: true`, because the precheck at `:1478` was skipped, not because it ran and passed | **Fix applied, not deferred.** (1) Ran `edm-state migrate-schema EDMV3` to advance `schema_version` 1 -> 2, the ordinary supported operation for exactly this situation. (2) Reverted the erroneous approval by direct edit of `.edm-state.json` -- `code_audit_converged` back to `false`, `code_audit_gate_approved_at`/`_approver`/`_enforcement` back to `""`, `code_audit_gate_ledger` back to `"absent"` -- because `code_audit_converged` is deliberately absent from `SETTABLE_KEYS` (by design, per the comment at `:1250`: making it `cmd_set`-settable "would reopen exactly the hand-flip path" the allowlist exists to close), so no CLI command can revert it once wrongly set. (3) Re-ran `approve-gate EDMV3 code-audit`: it now correctly reaches `cmd_audit_converged`, gets exit 1, and refuses with the named 91-finding blocking list. (4) Filed **CA-182** (P0) in the round-2 ledger for the underlying bug in `bin/edm-state` itself | This is not a hypothetical: the exact CA-040 shape ("an edit routing approve-gate through the helper for consistency would open a gate bypass silently") that round 1's own L4 lens warned about, discovered here operationally rather than by lens analysis, on the very state file the lenses were auditing. The precondition is common, not exotic -- `cmd_init` seeds every new initiative at `schema_version: 1` and nothing in the orchestrator flow, any skill, or any hook ever calls `migrate-schema` automatically or prompts a human to. Every initiative that has not had someone manually run `migrate-schema` -- which as far as this ledger's own history shows is every initiative including this one until today -- has an always-bypassable code-audit convergence gate: the wave-A permissive branch was written for genuinely pre-EDMV3 state files that predate the JSONL ledger and have no convergence concept to check, and a current initiative that simply never migrated inherits that exemption it was never supposed to have. **Why a direct file edit rather than a CLI path**: no regression on the human's actual intent -- they approved on the understanding that a real check would gate it, that check did not run, so the state before the click (not-converged) is what should still be recorded; reverting field-by-field to `cmd_init`'s own documented seed values (`""` / `"absent"`) is the same shape `cmd_init` would have produced had the bug never fired, not an invented value. `edm-state validate EDMV3` confirms the corrected file parses clean (one informational `ACTIVE_EXEMPTION` anomaly naming the now-historical `degraded_checks` entry, which is the honest record of what happened, not an error). **Fix scope for CA-182 itself, left to remediation**: either gate `cmd_init` on always seeding `schema_version: 2` for a fresh initiative (there is no legacy shape a brand-new initiative could fail to satisfy), or have `phase-complete`/`gate-check`/`approve-gate` prompt-and-migrate automatically the first time a schema-2 check would otherwise degrade, or at minimum surface the `ACTIVE_EXEMPTION` anomaly as a blocking (not informational) `edm-state validate` finding whenever the exemption applies to an initiative already past Phase 1 | 2026-08-01 |
| D34 | CA-013 (P1, L9+L11): EDMV3-116's negative branch (D22 -- the bare `` `CLAUDE.md Sec."..."` `` form does not resolve from an installed plugin cache) was recorded as a decision but never shipped as a consumer change -- `grep -rn 'canonical-sections\.md' plugins/edm/agents plugins/edm/skills` returned zero hits round-2. `srd.md:2846-2852` still mandated the bare form exclusively and still asked for verification of something D22 had already disproven; `CLAUDE.md:300-302` described the fallback as gated on "once EDMV3-T42 lands" two waves after EDMV3-T42 actually landed | Three edits, per REMEDIATION.md's prescribed order. (1) Added `Read docs/canonical-sections.md` (plugin-root anchored, CA-022's anchor requirement) alongside the existing `CLAUDE.md Sec."..."` citation in `agents/edm-audit-synthesizer.md`, `agents/edm-srd-auditor.md`, and all eleven `agents/edm-audit-*.md` lens definitions, so both forms resolve for a reader of any of these agents. (2) Updated `plugins/edm/CLAUDE.md`'s gating prose to state the plugin-relative path as the shipped default, not a future one, and recorded the residual scope (auditing any remaining untouched prompt-surface files) as a named follow-on ticket, `EDMV4-T04`, rather than an open-ended future reference. (3) Updated `srd.md:2843-2852`'s acceptance criteria to accept either the bare form or the plugin-relative fallback as equivalent (they are byte-identical per EDMV3-T41 AC4/AC5), and replaced the "verified to resolve from an installed plugin cache" criterion (which asked for a positive result D22 had already disproven) with a criterion naming that the negative result is disproven-and-recorded, not re-asserted | The recurring shape this round (CA-005, CA-010, CA-014, CA-016 through CA-019, and 25 more per REMEDIATION.md's root-cause analysis) is a multi-site finding where the code half of a fix lands and the doc/consumer half does not; CA-013 is the purest instance affecting the initiative's own governing documents. Landing this alongside the CA-011/CA-023 hooks.json fix (same wave, same worktree) keeps the two nearby-but-independent findings in one reviewable commit rather than splitting a related SRD/CLAUDE.md/agents edit across two | 2026-07-31 |
| D35 | CA-183 (P0, discovered writing CA-040's regression coverage): `cmd_approve_gate`'s code-audit branch (`bin/edm-state:1455-1471` pre-fix) computed `audit_required` from `mode` only, then -- when `schema_version >= 2` -- delegated its convergence precheck to `cmd_audit_converged`, which shares `convergence_exempt(mode, lifecycle_mode)` with `cmd_archive`. Since `convergence_exempt` returns true for `lifecycle_mode` in `fast-track`/`fix-pack` regardless of `mode`, approve-gate silently inherited that exemption too -- directly contradicting its own code comment ("approving a code-audit gate under a lifecycle mode that has no audit round should stay refused rather than become exempt") and REMEDIATION.md's CA-040 verification text, which assumed the refusal already worked and asked only for a regression test proving it. Live-reproduced at `schema_version=2`, `mode=standard`, `lifecycle_mode=fast-track`: `approve-gate code-audit` exited 0 and set `code_audit_converged=true` with zero code-audit round ever run -- the same bypass shape as CA-182 (a schema_version gate instead of a lifecycle_mode gate), found operationally by writing the very test CA-040 asked for rather than by a lens rereading the same code | **Fixed in code**, not just tested around. Added an explicit `case "$lifecycle_mode" in fast-track\|fix-pack) die ... ;; esac` immediately after the mode-exemption check and before the schema-gated `cmd_audit_converged` delegation, so approve-gate refuses this lifecycle unconditionally rather than reaching the shared-exemption helper at all. Verified empirically both directions: fast-track now refuses citing the lifecycle_mode by name and `code_audit_converged` stays `false`; `mode=standard`/`lifecycle_mode=standard` is unaffected (still reaches the real ledger check). Filed as **CA-183** (fixed same round) rather than silently folded into the CA-040 commit, and `wave6-smoke.sh`'s new CA-040 case block asserts the refusal by name (`check_fails ... "never exempts a lifecycle_mode"`) plus the `code_audit_converged` postcondition, so a future edit routing approve-gate back through the shared helper "for consistency" fails this suite immediately | This is the second gate-bypass this initiative's own operational use has found in the same function this round (CA-182 the first), both in the identical shape the round-1 L4 lens warned about in the abstract ("an edit routing approve-gate through the helper for consistency would open a gate bypass silently") -- confirming that warning was not hypothetical and that `cmd_approve_gate`'s code-audit branch is a recurring risk surface worth a standing regression suite, which CA-040's case block now is. **CA-182 remains separately open**: this fix closes the lifecycle_mode gap only; the schema_version<2 permissive-branch gap CA-182 names is a different code path (the `else` of the same `if [[ "$ag_schema_class2" == "2" ]]` at `:1482`) and needs its own fix, not conflated with this one | 2026-08-01 |
| D36 | CA-163 (P2, round-3 L9): T23 AC13 rework never named in `decisions.md` (round-3 Wave 7f) | Rework confirmed and recorded here; no further code change follows | EDMV3-T23 AC13 was reworked, in an earlier wave, from an impossible temporal precondition into an artifact-provenance check, but `decisions.md` never named T23 or the rework, unlike five similar reworks already recorded in D19. **Before**: "the baseline is captured before the first wave-B commit. This ticket is a wave-A exit criterion. Verify: `git log -1 --format=%cI plugins/edm/evals/baseline/scores.json` predates the first wave-B commit date recorded in `CHANGELOG.md`." This is unverifiable by construction: it asks a wave-A ticket's AC to check a fact (the first wave-B commit date) that cannot exist until wave B starts, and once wave B has started the check is backward-looking history, not a gate. **After** (current, `epics/03-ci-and-fixture-eval.md` T23 AC13, labelled "verifiable today"): "the committed baseline artifact records the wave-A fixture/scorer it was captured from and the variance table that EDMV3-T39 consumes, so later tickets can verify provenance from the artifact itself instead of from a no-longer-live chronology claim. Verify: `grep -n 'wave-A\|variance\|dimensions_scored' plugins/edm/evals/baseline/README.md` shows the provenance and tolerance table, and `jq -e '(.dimensions_scored == 4) and (.complete == true)' plugins/edm/evals/baseline/scores.json`." Per D15, an AC whose stated precondition/environment does not exist is a specification defect reworked into something verifiable, exactly the same class of fix D19 already documents for four other ACs -- this entry closes the one omission. **Extended 2026-08-08 per CA-241 (round-3 G81, same wave):** AC13's `(verifiable today)` label is itself now corrected. `plugins/edm/evals/baseline/` holds only `README.md` -- no `scores.json` exists, so the AC's jq half (`jq -e '(.dimensions_scored == 4) and (.complete == true)' plugins/edm/evals/baseline/scores.json`) cannot run in a clean checkout, which is the same open gap D23 already records (the wave-A baseline has not yet been captured). AC13 is relabelled `(blocked on the D23-documented baseline gap, corrected from "verifiable today")` in `epics/03-ci-and-fixture-eval.md` rather than left claiming a status the tree does not support; capturing the baseline itself (D23's closing command, a live costed `run-eval.sh` invocation) is out of scope for this documentation-only wave | 2026-08-08 |
| D37 | CA-243 (P2, round-3 L9): `CHANGELOG.md` carried no `EDMV3-T39` entry at all, and `EDMV3-T37` was named only inside the wave-B intro summary sentence, never its own bullet | `EDMV3-T39` gets its own new `[3.0.0]` Added bullet (naming `edm-compare-eval`, `edm-check-skill-sync` and the `eval:nightly` CI comparison step); `EDMV3-T37` is covered by a new combined `EDMV3-T37`/`EDMV3-T38` bullet rather than a separate `EDMV3-T37`-only entry, because the two ship as one two-commit merge request per their `Ships-with` pairing and the same is true of their shipped content -- the phase-procedure relocation (T37) and the resulting <=300-line dispatcher (T38) are one narrative, not two | No prior `CHANGELOG.md` entry described either ticket's actual substance: `EDMV3-T38` had only the `current_step` vocabulary migration bullet (itself stale until this same wave's fix, see the `current_step` bullet above), and `EDMV3-T37` had no bullet at all -- only a passing mention in the wave-B intro paragraph's summary sentence. Splitting T37 into its own bullet was considered and rejected: the pack's own `Ships-with` convention (EDMV3-T37/T38, EDMV3-T50 and others) exists precisely because the two tickets are one delivery unit reviewed as two commits, not two independent deliveries, so one combined bullet describing the whole relocated-and-collapsed shape is more accurate than two bullets that would each describe half a mechanism that does not work without the other half | 2026-08-08 |
| D38 | CA-255 (P1, round-4 G10): `epics/05-orchestrator-dispatcher.md` T39 AC5's round-3 Wave 7f rework (re-verifying the driver's stop-before-gate wording against the final Gate PROTOCOL) was never recorded in `decisions.md`, even though the AC's own text asserts "Recorded in `decisions.md`" | Rework confirmed and recorded here; AC5's cross-reference is amended to name this entry instead of asserting a fact with nothing behind it | **Before** (original AC5): "the driver's stop-before-gate instruction is re-verified against the wave-B PROTOCOL wording from EDMV3-T35, and any change to driver behaviour is recorded. If the change is material, the baseline is invalidated and re-captured before the comparison is trusted. Verify: the ticket records the re-verification result, and `grep -n 'stop before gate presentation' plugins/edm/evals/run-eval.sh` matches the final PROTOCOL's terminology." **After** (current, round-3 Wave 7f): "...Re-verified this wave (round-3 Wave 7f): `run-eval.sh`'s driver prompts already use \"STOP and WAIT\" phrasing (e.g. \"no STOP and WAIT text, no waiting for sign-off\"), matching the canonical PROTOCOL's \"**STOP and WAIT for the `AskUserQuestion` response**\" verbatim at `skills/orchestrator/SKILL.md Sec.\"Gate PROTOCOL (canonical)\"`. No material change -- the terminology already agrees, so the baseline is not invalidated. Recorded in `decisions.md` D38. Verify: `grep -n 'STOP and WAIT' plugins/edm/evals/run-eval.sh` matches the final PROTOCOL's terminology." **Reason**: the original verify command's phrase ("stop before gate presentation") never appears verbatim in `run-eval.sh` -- the driver actually carries the canonical PROTOCOL's own "STOP and WAIT" wording at `run-eval.sh:435` and `:500`, matching `skills/orchestrator/SKILL.md`'s "Gate PROTOCOL (canonical)" section verbatim -- so re-verifying against the real, shipped terminology rather than the AC's originally guessed phrase is what the round-3 Wave 7f edit did; this entry supplies the record the AC's own text already claimed existed. **Re-verification result**: `grep -n 'STOP and WAIT' plugins/edm/evals/run-eval.sh` returns matches at lines 435 and 500, both agreeing with the canonical PROTOCOL's verbatim phrase -- no material change, no baseline re-capture required | 2026-08-09 |
| D39 | CA-255 (P1, round-4 G10): `epics/05-orchestrator-dispatcher.md` T39 AC9's walk-back to `epics/03-ci-and-fixture-eval.md` T23 AC11's original naming-convention wording was never recorded in `decisions.md`, even though the AC's own heading asserts "decisions.md records the rework" | Rework confirmed and recorded here; AC9's cross-reference is amended to name this entry instead of asserting a fact with nothing behind it | **Before** (original AC9): "AC9 (trend preserved): the nightly `scores.json` files remain named or tagged such that a simple script can plot total score over time after this comparison job lands. Verify: `ls plugins/edm/evals/runs/` shows timestamp-and-SHA names and the plotting script described in `evals/README.md` runs against them." **After** (current, round-3 Wave 7f, walked back to T23 AC11's wording): "AC9 (trend preserved, walked back to T23 AC11's naming-convention wording -- decisions.md D39 records the rework): the nightly `scores.json` files remain named or tagged such that a simple script **could** plot total score over time after this comparison job lands -- the naming convention exists and is documented; no plotting script is required to exist. This matches the wording T39 AC9 originally escalated from (`epics/03-ci-and-fixture-eval.md` T23 AC11), which asks only that artifacts be named or tagged so a script could be written, not that one already is. `evals/runs/` is gitignored (never committed), so a run-directory listing is never present in a clean checkout regardless of naming; the naming convention itself, not a populated directory, is what this AC verifies. Verify: `grep -n 'timestamp.*git-sha\|<timestamp>_<git-sha>' plugins/edm/evals/README.md` shows the documented run-directory naming convention." **Reason**: the original AC9 demanded a plotting script and a populated `evals/runs/` listing, but `evals/runs/` is gitignored and always empty in a clean checkout regardless of naming, and no plotting script was ever built or required by T23 AC11, the wording T39 AC9 escalated from -- T23 AC11 asks only that artifacts be named or tagged so a script *could* be written. The walk-back removes an unbuilt, unrequired deliverable and an unverifiable-in-a-clean-checkout claim, the same D15 precedent already applied to other ACs whose stated precondition does not hold in this environment. **Re-verification result**: `grep -n 'timestamp.*git-sha\|<timestamp>_<git-sha>' plugins/edm/evals/README.md` returns the documented run-directory naming convention; no plotting script exists and none is required | 2026-08-09 |
| D40 | CA-255 (P1, round-4 G10): `epics/05-orchestrator-dispatcher.md` T37 AC6's rework -- from a per-skill call-count claim to the shipped per-phase ownership contract for `phase-start`/`phase-complete` -- was never recorded in `decisions.md` (this one omits the record rather than falsely claiming one, unlike D38/D39 above) | Rework confirmed and recorded here; AC6 is amended to name this entry | **Before** (original AC6): "AC6 (negative, one call site per phase): `edm-state phase-start` and `phase-complete` calls exist exactly once per phase across the whole plugin. Verify: `grep -rc 'phase-start' plugins/edm/skills/*/SKILL.md` sums to 8 (one per phase skill) and `bash plugins/edm/bin/tests/wave7-smoke.sh` (case \"one phase-start per phase\")." **After** (current): "AC6 (one owning file per phase, phase-complete-6's three-way split sanctioned): for each of the six phases N=1..6, exactly one phase skill's SKILL.md contains `phase-start <PREFIX> N`. The same holds for `phase-complete <PREFIX> N`, except phase 6, where exactly three files contain it: the orchestrator (the owning call, EDMV3-T50), `skills/implement/SKILL.md` and `skills/verify-runtime/SKILL.md` (both documented direct-invocation restatements for a user who invokes the phase standalone, EDMV3-T50 AC5). `skills/tickets/SKILL.md` legitimately contains a second `phase-start <PREFIX> 4` occurrence for its fast-track mode-branch duplicate; both occurrences live in the same file, so phase 4 still has exactly one owning file, not two. Verify: `bash plugins/edm/bin/tests/wave7-smoke.sh` (case \"T37 AC6 -- one owning file per phase-start/phase-complete call...\"), plus the AC's own note explaining why the naive per-skill sum returns 7, not 8, and is not the right check." **Reason**: the original AC's "sums to 8 (one per phase skill)" claim is mechanically false against the shipped tree -- `skills/code-audit/SKILL.md` and `skills/verify-runtime/SKILL.md` are Phase 6 sub-skills that carry no `phase-start` call of their own (Phase 6's one `phase-start` lives in `skills/implement/SKILL.md`), and `skills/tickets/SKILL.md` carries two occurrences for its sanctioned fast-track mode-branch duplicate that both belong to the same phase-4 owner, so the true per-skill sum is 7, never 8, for any tree satisfying the intended per-*phase* contract. The rework replaces the unsatisfiable per-skill count with a per-phase ownership check (plus the documented three-file sanctioned exception for `phase-complete 6`), which is what the shipped tree actually satisfies; this entry is the first record of that rework. **Re-verification result**: `bash plugins/edm/bin/tests/wave7-smoke.sh` (case "T37 AC6...") passes against the current tree -- one owning file per phase for phases 1-5, the documented three-file split for phase 6 | 2026-08-09 |
| D41 | CA-254 (P1, round-4 G9): `epics/11-cross-cutting-delivery.md` T66 AC4 and `epics/06-mermaid-rule.md` T43 AC12 both stated their verify command as grepping `plugins/edm/CLAUDE.md` for the literal phrase "four violation classes" -- but a prior remediation correctly deleted that phrase (the linter now emits seven classes, not four) and rewrote the `bin/` table row to defer to `edm-lint-artifacts --help` instead, so both ACs were permanently unpassable, and `bin/tests/wave7-smoke.sh`'s own green "G9" (`:5475-5478`) and "T43 AC12" (`:2228-2234`) cases assert the exact inverse of what the two ACs' verify text demanded | Both ACs' verify commands are amended to assert the shipped contract instead -- the `CLAUDE.md` `bin/` table row defers to `--help`, and `edm-lint-artifacts --help` enumerates all seven emitted violation classes -- mirroring the smoke suite's own assertions rather than contradicting them | **Before** (T66 AC4): "...Verify: `grep -c 'four violation classes' plugins/edm/CLAUDE.md` is non-zero; `grep -rl 'missing version header\|orphan file\|oversized ticket' plugins/edm/CLAUDE.md \| wc -l` prints 0; and `grep -rl 'TaskCompleted' plugins/edm/CLAUDE.md \| wc -l` and `grep -rl 'lifecycle_mode.*partial' plugins/edm/CLAUDE.md \| wc -l` each print 0." **After** (T66 AC4): "...Verify: `grep -c 'four violation classes' plugins/edm/CLAUDE.md` prints `0`; `grep -n 'edm-lint-artifacts --help' plugins/edm/CLAUDE.md` shows the bin/ table row deferring to `--help`; `bash plugins/edm/bin/edm-lint-artifacts --help 2>&1 \| grep -cE '^#   (attribution\|unicode\|leaked-tool-tag\|mermaid-semicolon\|unterminated-fence\|scan-error\|unreadable)( \|\$)'` prints `7`; and the `TaskCompleted`/`lifecycle_mode.*partial` absence checks are unchanged." **Before** (T43 AC12): "...the `CLAUDE.md` `bin/` table description of `edm-lint-artifacts` is updated to describe four violation classes. Verify: `git diff --stat plugins/edm/hooks/hooks.json` is empty and `grep -n 'four violation classes' plugins/edm/CLAUDE.md`." **After** (T43 AC12): "...the `CLAUDE.md` `bin/` table description of `edm-lint-artifacts` no longer hardcodes a violation class count and instead defers to `edm-lint-artifacts --help`. Verify: `git diff --stat plugins/edm/hooks/hooks.json` is empty; `grep -c 'four violation classes' plugins/edm/CLAUDE.md` prints `0`; `grep -n 'edm-lint-artifacts --help' plugins/edm/CLAUDE.md` shows the row; and the same seven-class `--help` count command as T66 AC4 above prints `7`." **Reason**: the linter grew from three classes (attribution, unicode, leaked-tool-tag) to seven (adding mermaid-semicolon, unterminated-fence, scan-error, unreadable) across this initiative, and a hardcoded "four violation classes" phrase in `CLAUDE.md` went stale before either AC's own text was ever satisfied -- the two ACs, as originally written, asserted a fact that the shipped `bin/` table row's own correct fix (deferring to `--help`) makes permanently false. `bin/tests/wave7-smoke.sh`'s "G9" (`:5475-5478`) and "T43 AC12" (`:2228-2234`) cases already assert the shipped, correct contract (`check_absent` on "four violation classes", `check` on "edm-lint-artifacts --help"); the two ACs are amended to match what the suite already, correctly, checks. **Re-verification result**: `grep -c 'four violation classes' plugins/edm/CLAUDE.md` prints `0`; `grep -n 'edm-lint-artifacts --help' plugins/edm/CLAUDE.md` shows the `bin/` table row; `bash plugins/edm/bin/edm-lint-artifacts --help 2>&1 \| grep -cE '^#   (attribution\|unicode\|leaked-tool-tag\|mermaid-semicolon\|unterminated-fence\|scan-error\|unreadable)( \|\$)'` prints `7` | 2026-08-09 |
| D42 | CA-299 (P1, round-5 G7): `srd.md:4076`'s EDMV3-97 Must-Have acceptance criterion still mandated the `edm-lint-artifacts` row describe "four violation classes" -- the exact phrase D41 correctly deleted from the ticket-pack side (T66 AC4, T43 AC12) the same round it was found. Second consecutive round in which a remediation amended a ticket AC without sweeping the SRD requirement that AC implements | `srd.md:4076`'s bullet is amended to the shipped contract: the row no longer hardcodes a count and instead defers to `--help`, which enumerates all seven emitted classes -- matching D41 and the shipped `CLAUDE.md` bin/ table row exactly | **Before**: "The `edm-lint-artifacts` row describes four violation classes." **After**: "The `edm-lint-artifacts` row no longer hardcodes a violation-class count and instead defers to `edm-lint-artifacts --help`, which enumerates all seven emitted violation classes (decisions.md D42; mirrors the ticket-pack side of the same fix, decisions.md D41)." **Reason**: `plugins/edm/CLAUDE.md:763`'s `bin/` table row already deferred to `--help` before this fix -- only the SRD requirement text was stale, asserting the opposite of what the tree and the ticket pack (post-D41) both already state. **Re-verification result**: `grep -n 'four violation classes' SRD/edm/EDMV3__prompt-streamline/srd.md` returns nothing; `bash plugins/edm/bin/edm-lint-artifacts --help 2>&1 \| grep -cE '^#   (attribution\|unicode\|leaked-tool-tag\|mermaid-semicolon\|unterminated-fence\|scan-error\|unreadable)( \|\$)'` prints `7` | 2026-08-09 |
| D43 | CA-300 (P1, round-5 G8): `epics/02-enforcement-kernel.md` T07 AC5 (Must) required exactly three `prototype)` case sites in `bin/edm-state`, naming `cmd_archive`'s waiver as the third. The waiver was consolidated into the shared `convergence_exempt()` helper (which calls `code_audit_required_for_mode()` rather than carrying its own copy), so the tree has exactly two sites -- and `wave6-smoke.sh:246`'s own shipped test asserts exactly two and is green, meaning a passing suite proved a Must-Have AC false with no record of the consolidation | AC5 is amended to the shipped two-site contract, naming the `convergence_exempt()` consolidation by function name so a future reader does not re-derive "where did the third site go" from scratch | **Before**: "Verify: `grep -n 'prototype)' plugins/edm/bin/edm-state \| wc -l` returns exactly 3 -- one inside `terminal_phase_for_mode()`, one inside `code_audit_required_for_mode()`, and one at the documented `cmd_archive` convergence waiver -- ... A fourth hit is a second mode mapping and is a failing condition. Also: `bash plugins/edm/bin/tests/wave6-smoke.sh` (case \"single mode derivation\")." **After**: "Verify: `grep -n 'prototype)' plugins/edm/bin/edm-state \| wc -l` returns exactly 2 -- one inside `terminal_phase_for_mode()` and one inside `code_audit_required_for_mode()`. `cmd_archive`'s own convergence waiver is NOT a third standalone site: it was consolidated into the shared `convergence_exempt()` helper (decisions.md D43), which calls `code_audit_required_for_mode()` rather than carrying its own copy of the `prototype)` case -- one derivation, two consumers, not three derivations. A third hit anywhere in `bin/edm-state` is a second mode mapping and is a failing condition. Also: `bash plugins/edm/bin/tests/wave6-smoke.sh:246` (case \"single mode derivation\")." **Reason**: `convergence_exempt()` (bin/edm-state:871) is the single source `cmd_archive` and `cmd_audit_converged` both call, replacing what used to be independent byte-identical copies of the lifecycle case and mode derivation (its own docstring names this consolidation and the drift it removes); `cmd_archive` no longer carries its own `prototype)` case at all. **Re-verification result**: `grep -n 'prototype)' plugins/edm/bin/edm-state \| wc -l` prints `2`; `bash plugins/edm/bin/tests/wave6-smoke.sh` case "T07 AC5 -- single mode derivation" passes | 2026-08-09 |
| D44 | CA-301 (P1, round-5 G9): `epics/03-ci-and-fixture-eval.md` T21 AC5's second verify half required `grep -c 'bin/tests/' .gitlab-ci.yml` to return 1. It returns 10 after CA-162's round-3 lint-glob widening and Wave 8's G21/CA-233 edit added more `bin/tests/` mentions to the same loops. The AC's substance (no individual wave-suite gets its own CI job) still holds -- only the literal path-token count the AC happened to pick as its proxy went stale | The second verify half is re-expressed as a check for a wave-suite-named job KEY (the thing the AC actually cares about), not a count of how many lines mention the `bin/tests/` path substring anywhere (comments, lint globs, and shellcheck globs all legitimately mention it too) | **Before**: "Verify: `grep -n 'run-all.sh' .gitlab-ci.yml` returns the single invocation, and `grep -c 'bin/tests/' .gitlab-ci.yml` returns 1 (the aggregator only; no individual suite entries)." **After**: "Verify: `grep -n 'run-all.sh' .gitlab-ci.yml` returns the single invocation, and `grep -nE '^[a-z0-9_-]+:wave[0-9]' .gitlab-ci.yml` returns nothing -- no job KEY is named after an individual wave suite. (A bare `grep -c 'bin/tests/' .gitlab-ci.yml` count is not this check: that literal token also appears in unrelated lint-glob lines, shellcheck-glob lines, and prose comments that legitimately mention the path, so its count moves every time one of those unrelated things is edited and is not a stable signal of "no per-suite job exists" -- decisions.md D44.)" **Reason**: the original verify command conflated "the path substring `bin/tests/` appears exactly once" with "no individual suite has its own CI job" -- two different claims that happened to coincide when the AC was written and diverged as soon as an unrelated lint job's glob pattern grew to also mention `bin/tests/*.sh`. The job-key check tests the actual claim directly and is stable under future lint-glob edits. **Re-verification result**: `grep -n 'run-all.sh' .gitlab-ci.yml` shows the aggregator invocation at the `test:smoke` and `test:smoke-bash32` jobs; `grep -nE '^[a-z0-9_-]+:wave[0-9]' .gitlab-ci.yml` returns nothing | 2026-08-09 |
| D45 | CA-308 (P2, round-5 G19, L2+L11): `skills/plan/SKILL.md:53-55` names only three skills (`implement`, `code-audit`, `verify-runtime`) substituting `current_step` = `6`, with no mechanism named and no mention of the other four skills' values at all -- read in isolation, the sentence cannot distinguish "a producer exists for values 2-5" from "it does not." Investigated as a possible missing producer; found to be a documentation-clarity gap, not a missing producer. Every non-`plan` phase skill's own Step 0 section already cross-references `plan/SKILL.md`'s canonical block by name and substitutes its own `<phase-num>` -- the identical convention checks 1-2 in the same block already use for the gate token and branch check, which nobody reads as "producer-less" for the other seven skills. `bin/tests/wave7-smoke.sh`'s round-3 "G15" case (`srd`=2, `audit-srd`=3, `tickets`=4, `audit-tickets`=5, `implement`=6, `code-audit`=6, `verify-runtime`=6) already asserts every one of these substitutions and was green before this investigation started | No code or skill-behavior change -- the producer already exists and is already tested for all six values. `skills/plan/SKILL.md`'s prose is rewritten to name the cross-reference mechanism explicitly and point at the existing G15 test by name, so a future reader (or lens) cannot reach the same "no producer" misreading from the text alone | **Before**: "`implement`, `code-audit`, and `verify-runtime` all substitute `6` here -- `current_step`'s vocabulary is phase-granularity only, not sub-phase, so all three Phase-6 sub-steps record the same value." **After**: "This numbered block, written once here, is what every other phase skill's own Step 0 section cross-references by name (the identical convention checks 1-2 above already use for the gate token and branch check) -- each substitutes its own `<phase-num>` there, giving every value `2`..`6` a real producer the same way this step gives `plan` its `1`, not a special case limited to some subset of skills. G19/CA-308 (round 5): this substitution is asserted for all six remaining skills (`srd`=2, `audit-srd`=3, `tickets`=4, `audit-tickets`=5, `implement`=6, `code-audit`=6, `verify-runtime`=6) by `bin/tests/wave7-smoke.sh`'s "G15" case (round 3) -- if a reader cannot find the producer for a given value by name here, check that test before concluding one is missing. ... `current_step`'s vocabulary is phase-granularity only, not sub-phase, so `implement`, `code-audit` and `verify-runtime` (Phase 6's three sub-steps) all substitute the same value, `6`." **Reason**: the original sentence's scope (naming 3 of 6 non-`plan` skills, for one value only) reads as a special case rather than an instance of the general Step-0-cross-reference pattern, which is exactly the shape that produces a false "missing producer" reading without checking the skill files or the existing test directly. **Re-verification result**: `bash plugins/edm/bin/tests/wave7-smoke.sh` case "G15" (round 3) passes unchanged, confirming the producer was never actually missing | 2026-08-09 |

## Finding-to-Commit Ledger

| Finding | Source | Decision | Ticket | Status |
|---------|--------|----------|--------|--------|
| F11 -- 708KB of binaries plus macOS metadata ship inside `plugins/edm/` | EDM-REVIEW.md, srd.md | Relocate `EDM_Plugin_Presentation.pptx`/`EDM_Plugin_User_Guide.docx` to repo `docs/`; delete/gitignore `.DS_Store`; flip `lint:file-type-ban` to blocking | EDMV3-T57 (`f76129c`) | Closed |
| R2 -- manual grant-check ritual in `skills/implement/SKILL.md` is an instance-fix for a class defect | EDM-REVIEW.md | Delete the ritual now that `bin/edm-check-grants` (EDMV3-T03) is the class-level replacement (1 agent -> 30) | EDMV3-T58 (`ba6b599`) | Closed |
| -- (same ticket) `TaskCompleted` hook / `cmd_record_task_duration` is a wired no-op | EDM-REVIEW.md | Delete the hook family; `bin/edm-state` half of the deletion (the function, its dispatch entry, `--help` line) is owned by a different wave-C agent | EDMV3-T58 (`ba6b599`) | **Closed** -- the `bin/edm-state` half landed. Verified 2026-07-28: `grep -rn 'record_task_duration\|record-task-duration' plugins/edm/bin/edm-state plugins/edm/hooks/hooks.json` returns nothing (exit 1) -- the handler, its dispatch entry and its `--help` line are all gone; `grep -rn 'TaskCompleted' plugins/edm/` survives only in `CHANGELOG.md` history, `README.md` prose and the `wave7-smoke.sh` T66 AC4 regression case that asserts the removal, with zero hits in `hooks/hooks.json`; `jq -r '.hooks\|keys[]' plugins/edm/hooks/hooks.json` prints the six surviving families (`PreCompact`, `PreToolUse`, `SessionStart`, `Stop`, `SubagentStop`, `UserPromptExpansion`), matching EDMV3-T58 AC5's expected count of 6 |
| R8 -- `monitors/watch-impl` lifecycle undocumented, `[inferred]` in review | EDM-REVIEW.md, srd.md EDMV3-84 | Host-managed (Branch A); documented in `CLAUDE.md`, not deleted (D24) | EDMV3-T59 (`0f3079d`) | Closed |
| D12 -- `lifecycle_mode=partial` is a dead enum value, no sub-flow | decisions.md D12, srd.md EDMV3-83 | Narrow `LIFECYCLE_MODE_ENUM_LIST` to `standard`/`fast-track`/`fix-pack` in `cmd_set_mode`, with a C-4 legacy-read anomaly in `state_anomalies` | EDMV3-T59 | **Closed** -- the `bin/edm-state` half landed. Verified 2026-07-28: `plugins/edm/bin/edm-state:470` reads `LIFECYCLE_MODE_ENUM_LIST="standard fast-track fix-pack"`, the single source consumed by the `terminal_phase_for_mode` guard (`:490-492`) and by `cmd_set_mode`'s membership check (`:3214-3216`), so `set-mode <PREFIX> lifecycle_mode partial` now dies with `invalid lifecycle_mode 'partial' (expected: standard\|fast-track\|fix-pack)` (EDMV3-T59 AC2). The C-4 legacy-read anomaly is present at `:908` (`LEGACY_LIFECYCLE_MODE`, informational, reads the removed value as `standard` rather than erroring), and the re-baselined regression assertions landed at `2a5294b` (`wave4a-smoke.sh:186`, `wave6-smoke.sh:2132-2147`) |
| Gate closure -- all three EDMV3-T57/T58/T59 deletions validate cleanly | srd.md EDMV3-85 | `claude plugin validate` run three times (post-T57, post-T58, post-T59): 1 warning each time, unchanged from the v2.0.0 baseline (`.gitlab/edm-validate-baseline.txt`). `marketplace.json` <-> disk skills/agents bidirectional diff: empty both directions. `hooks/hooks.json` and `monitors/monitors.json`: valid JSON, zero dangling `edm-state` subcommand references. `run-all.sh`: 1166/1166 after each of the three commits. `.gitlab-ci.yml`'s `validate:manifest` (tier 1, blocking) already exercises the marketplace.json <-> disk comparison (AC6); no new CI job needed. The two findings above that were open pending `bin/edm-state` (owned by a different wave-C agent this wave) have since landed, and the re-run this row called for is done: verified 2026-07-28 that `hooks/hooks.json` carries six hook families with no `TaskCompleted` entry, that no `edm-state` subcommand referenced by `hooks/hooks.json` or `monitors/monitors.json` is dangling now that `cmd_record_task_duration` and its dispatch entry are gone, and that `LIFECYCLE_MODE_ENUM_LIST` is narrowed at `bin/edm-state:470` with the C-4 legacy-read anomaly in place at `:908`. **Fourth validator run, 2026-07-28, against the current tree** -- this is the run AC1 actually asks for, since the three above and the structural re-verification just described are grep-level checks of the deletions rather than an invocation of the validator itself: `claude plugin validate plugins/edm/` returns `Validation passed with warnings`, 1 warning, the same `root: CLAUDE.md at the plugin root is not loaded as project context` message as the v2.0.0 baseline in `.gitlab/edm-validate-baseline.txt` -- unchanged count, unchanged text. Taken after every wave-C remediation commit through `c661daf`, so it postdates the `bin/edm-state` halves of R2 and D12, the T54 insertion rewrite, the T55 gate-curation additions, both `edm-lint-artifacts` performance rewrites and the `.gitlab-ci.yml` job split | EDMV3-T60 | Closed -- re-run performed against the current tree, not inferred from the deletions |
