# Code Audit Lens L10: DRY & Redundancy

- **Date**: 2026-07-28 | **Round**: pass-1 (full, 11 lenses) | **Branch**: `edm/edmv3-prompt-streamline`
- **Note**: the lens agent had no Write tool at runtime despite its frontmatter granting one, so this file was transcribed by the orchestrator from the agent report. Findings, sites, severities and fixes are unaltered.

| # | Sev | File A | File B | Canonical | Recommendation |
|---|---|---|---|---|---|
| 1 | P1 | `bin/edm-check-grants:132-138` | `bin/edm-lint-artifacts:143` | `edm-lint-artifacts` | "Mirrored VERBATIM" names `build_ignore_set`, which no longer exists in the named file. Second site at `edm-check-vocabulary:50-52,140`. The suite simultaneously asserts the source is gone (`wave7:1651`) and that the mirror of it is present (`wave7:283`) -- and passes. Rewrite the comments to name `build_line_classes` and state the one behavioural difference, or do #3 and delete the claim. Add an assertion that a mirror comment's named symbol exists in the named file |
| 2 | P1 | `edm-check-grants:139-173`, `edm-check-vocabulary:145-179` | `edm-lint-artifacts:150-192` | `edm-lint-artifacts` | The mirrors anchor fences at column 1; the canonical de-indents, with the reason committed inline. The ~60 indented fences are in scope of both mirrors (e.g. `edm-test-coverage-auditor.md:87,89`, and 6-8 per phase skill). Content inside an indented fence is ignored by the linter and **not** by either checker, so a prohibited token there fails the blocking `lint:vocabulary` job. An odd number of undetected markers desynchronises `in_fence` for the rest of the file -- even counts today are luck, not design |
| 3 | P2 | `edm-check-grants:139-178` | `edm-check-vocabulary:145-184` | new `bin/_edm-lint-lib.sh` | Byte-identical 35-line copies of `build_ignore_set` (compared line by line); `is_ignored_line` exists three times; `report_violation` three times. Not test-only, no circular dependency, and the differences are not principled -- the checkers' own comments claim they are supposed to be identical. One ~45-line sourceable file removes ~80 duplicated lines and the entire drift surface behind #1 and #2. `edm-check-vocabulary` already walks `bin/`, so the new file lands in its own scan scope |
| 4 | P1 | `CLAUDE.md:216-220` | `skills/code-audit/SKILL.md:237-242` | `CLAUDE.md` | The skill cites the canonical section by name at `:235` and then restates the table anyway, **diverged**: P0 drops "or architecturally wrong", P1 drops "missing requirement". This is the table a lens or synthesizer reads while in this skill's context, and P2 is in `BLOCKING_FILTER`, so a mis-graded finding changes whether a round converges. Delete `:237-242`; keep the by-name reference plus the plugin-relative fallback |
| 5 | P1 | `agents/edm-audit-synthesizer.md:87-92`, `agents/edm-srd-auditor.md:67-72` | `CLAUDE.md:216-220` | `CLAUDE.md` | Two more copies. The synthesizer's is not a summary of the canonical scale -- it is the **abolished legacy P1/P2/P3 definitions with the labels shifted down one**, matching CLAUDE.md's own legacy-mapping note rather than its definitions. This is the agent that assigns the ledger severity, so its table decides convergence in practice, and a lens-reported P1 "missing requirement" matches no row of it. Replace both with the by-name reference the eleven lenses already use verbatim; `edm-check-vocabulary` is the natural enforcement point for a fifth copy |
| 6 | P1 | `evals/score-artifacts.sh:242-279` | `bin/edm-lint-artifacts:213-283` | `edm-lint-artifacts` | I agree with the prior assessment's three reasons for not calling the linter (dimension 3 is a genuine superset, needs a per-block verdict stream, must never exit non-zero on a low score) but **disagree that the remedy is optional tidying** -- the copies have already diverged and produce wrong answers today, provable against this plugin's own committed fixtures. Seven divergences: `{...}` curly label **absent** (so `invalid/i04-curly-label.md` scores OK); sequenceDiagram message **absent** (so `invalid/i05-sequence-message.md` scores OK, the case CLAUDE.md singles out as most exposed); entity stripping narrower, so `#lt;`/`#gt;`/`#amp;` are unstripped and a legal diagram scores BAD; trailing-terminator strip absent; the three `%%`/`classDef`/`style` exemptions absent, so a `%%` comment with a bracketed `;` scores BAD; fence recognition anchored at column 1, so indented fences and info-string suffixes are invisible; bracket char class narrower. Dimension 3 is the metric used to detect artifact-quality regressions and it awards full marks to two of five conditions the rule exists to catch. Lift the rule body into `bin/edm-mermaid-rules.awk` consumed via a two-`-f` awk invocation by both -- that keeps the scorer's framing, verdict stream and exit-0 contract untouched, and `awk -f` on a data file is not "a bin/ script on the scorer's PATH". Put it in `bin/`, not `evals/`, because of the 100KB ceiling |
| 7 | P1 | `edm-check-grants:66-69`, `evals/score-artifacts.sh:99-101` | six other sites | `edm-state`'s `print_help` | Five implementations of "print my own help" across eight scripts, in three incompatible shapes plus two copies of the `sed -n '2,45p'` anti-pattern EDMV3-T61 AC1 removed. `edm-check-grants --help` is **provably truncated today** -- it omits the Output format, the Exit codes, and the bash-3.2 note, i.e. the exit contract CI depends on. One `print_help` in the shared lib; sentinels on the two `sed` sites; point `wave7:569-574` (which re-implements the extractor to audit help blocks) at the shared function; a two-line CI grep closes the class |
| 8 | P2 | `bin/edm-state:1278-1283` | `bin/edm-state:579-591` | `convergence_exempt` | **Factoring verified complete** -- the helper is defined once, called from exactly two sites, and neither retains a copy; and `cmd_approve_gate`'s mode-only divergence is genuinely intended and documented (exemption from *requiring* convergence is not permission to *record* it without converging; confirmed a fast-track initiative still runs the full precheck). What is a finding: the mode half including the `mode == "null"` legacy default is written a **third** time, and its comment says "mirrors cmd_archive's identical guard" when `cmd_archive` no longer has that guard. Extract `audit_required_for_mode_or_legacy`; four lines added, three removed |
| 9 | P2 | `wave7-smoke.sh:1003, 1809, 1947, 2227, 2336, 2443, 2534` | `wave7-smoke.sh:3014` | the capture | Seven more identical whole-tree `--all` scans beyond the five already collapsed, and **13 identical `edm-check-grants` invocations in one suite** (plus a 14th in `run-all.sh`), two of them in the same T46 section. Nothing between them mutates the tree. T67 rewrote two hot loops to meet a 3000ms budget and this suite then spends 13 whole-tree grant scans and 7 whole-tree lints on the same invariant. The per-ticket ritual also creates false attribution -- a `--all` failure reported under "T36" has nothing to do with T36. Hoist the capture above the first consumer; add a grants capture. Keep the timed run and the one case that asserts on output. ~18 process trees removed per run |
| 10 | P2 | `.gitlab-ci.yml:42` | `:353`; node at `:461`, `:499` | `.alpine_edm` | The anchor's comment claims a digest refresh "stays a single-line change across all seven jobs", but `validate:manifest` spells the digest out again and the node digest is duplicated with no anchor. `before_script: apk add --no-cache bash jq git` is repeated verbatim in five jobs |
| 11 | P2 | `wave7:12`, `wave6:702`, `wave4b:6`, `timing.sh:26` | `_harness.sh:39-40` | `_harness.sh` | Four divergent derivations of `PLUGIN_DIR`, two of `REPO_ROOT`, four copies of the TMP/trap/SRD_ROOT block, and five suites re-computing inline the value assigned on the line above. `wave4b:6` is the only copy using `$0` rather than `${BASH_SOURCE[0]}`, so it breaks if the suite is ever sourced -- exactly the divergence a shared derivation prevents. Export `_HARNESS_PLUGIN_DIR`/`_HARNESS_REPO_ROOT` and add `harness_scratch_srd_root()`; ~10 lines added, ~35 removed |
| 12 | P2 | `agents/edm-audit-dry.md:71` + 10 siblings | `skills/code-audit/SKILL.md:45` | the skill | The shared lens skeleton has **not** drifted -- the output block, the ASCII clause, the JSONL schema and the residual-risk paragraph are identical in all eleven. But one shared line is wrong in all eleven: it cites `SKILL.md:40` for the `mkdir -p`, which is on line 45. The duplication cost made concrete: one fact, eleven places, drifted in all eleven simultaneously. Two more stale citations in `edm-check-grants:12-13` and `:382-384`. Drop the line numbers; cite the section name |
| 13 | P2 | `run-all.sh:98-113` | `run-all.sh:115-130` | one helper | The same 15-line invocation block twice, differing only in script name, variable prefix and label. Borderline at two copies, but an 8-line `_standalone_check` removes ~30 lines and makes a third checker a one-liner |
| 14 | NOTED | `.gitlab-ci.yml:64-78` | `:133-147` | -- | The 12-line file-loop-with-FAIL-flag skeleton appears twice, but the abstraction costs more than the ~10 duplicated lines removes: the file sets differ (`bin/*` + `bin/tests/*.sh` vs `bin/*` only) and so do the commands. Leave as is |

**On whether the ~36-line lens JSONL skeleton should become a single reference** -- deliberately **not** raised as a finding. The mechanism exists (`edm-sync-canonical-sections` + a plugin-relative path), and the JSONL contract is the strongest candidate since it is byte-identical and purely mechanical. But each lens runs in its own context with no guarantee it reads a referenced doc before writing, the cost of a missed reference is a lost finding, and the block shows zero drift. The duplication is doing real work here. Revisit only if drift is observed; the cheap insurance is a smoke assertion that the eleven blocks are byte-identical modulo the L-number, which does not exist today.

## Noted / Not Actionable
- `docs/canonical-sections.md` duplicating two CLAUDE.md sections -- deliberate, generated, one-directional (verified: the generator reads only CLAUDE.md and writes only the destination, with no transformation), and the guard works: `--check` diffs and exits 1 on any difference including a missing destination, and `wave6:3097-3131` verifies all three legs plus byte-identity of both section spans. Runs in CI. Correctly allowlisted so the vocabulary checker does not double-report. **Nothing else duplicates these sections by hand** except the severity tables in #4 and #5 -- the Mermaid rule is referenced by name everywhere it appears.
- `edm-check-grants`'s `report_violation` diverging in output shape -- deliberate, documented, required by its own AC7. Only the ignore-marker helpers should be shared.
- `edm-check-grants:136-138` not applying the ignore set to source 2 -- deliberate and correct; the lens launch template lives inside a fence and skipping it would blind the checker.
- `cmd_approve_gate` applying only the mode half -- deliberate and semantically right (see #8). Only the duplicated legacy default is actionable.
- `lint:pattern-library-contract` duplicating `_t56_four_heading_contract_check` -- deliberate and documented: the CI job is the fast-fail authoritative copy, the smoke twin adds four negative cases. Compared **rule by rule** (exactly-4 headings, the three literals, the fourth-heading regex, the orphan scan, both exemptions) and they agree on every rule, with only message wording differing. No drift -- but the pair has no guard keeping it that way, and if the two ever disagree this becomes a P1.
- The eleven lens and ten test-writer skeletons -- intended by design; verified byte-identical modulo the identifier. Only the stale citation is actionable.
- `_harness.sh` itself -- correctly factored; the residual duplication is only the per-suite preamble (#11), not the assertions.
- `edm-qc-auditor.md:70-73` and `edm-test-coverage-auditor.md:108-116` -- map *conditions* to severities rather than redefining them, which is legitimate specialisation, and both cite the canonical scale first. Two nits for other lenses: the QC list omits `NOTED`, and the coverage table says `Note` rather than `NOTED`.
- `edm-check-vocabulary`'s help text appearing twice in one file -- an abridgement with no contradiction; disappears if #7 lands.

## Not examined
The ten test-writer agent definitions were not diffed line by line -- the largest remaining gap in this lens. `wave3/4a/4b/5-smoke.sh` examined only for preambles and whole-tree scans; `wave6-smoke.sh` shows ~20 near-identical seed blocks that look like a candidate for a `_seed_initiative` helper, but not enough was read to confirm they are interchangeable. `edm-init`, `edm-validate-prefix`, `monitors/monitors.json` not read. `hooks/hooks.json` not read, so duplication between hook prompt text and skill body text is unassessed.
