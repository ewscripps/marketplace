# Lens L6: Documentation Accuracy -- Round 3

Scope audited: `plugins/edm/` (bin/ incl. the new `_edm-cli-lib.sh`, agents/, skills/, docs/, evals/, hooks/, CLAUDE.md, CHANGELOG.md, README.md), repository-root `CLAUDE.md`, `.gitlab-ci.yml` comments.

## Cross-round verdicts: every open ledger entry whose Lens(es) includes L6

| ID | Verdict | Evidence |
|----|---------|----------|
| CA-005 (L1+L2+L6+L7+L10) | **FIXED** | `/Users/darryl.porter/projects/marketplace/plugins/edm/bin/_edm-cli-lib.sh` exists and is sourced by exactly 9 bin/ helpers + 3 evals/ drivers (`edm-state:63`, `edm-lint-artifacts:61`, `edm-check-grants:65`, `edm-check-vocabulary:57`, `edm-check-skill-sync:31`, `edm-compare-eval:37`, `edm-init:20`, `edm-validate-prefix:19`, `edm-sync-canonical-sections:42`; `evals/run-eval.sh:63`, `evals/score-artifacts.sh:111`, `evals/tiering-matrix.sh:69`). All 12 carry `EDM-HELP-BEGIN/END` sentinels. `edm-sync-canonical-sections:36-38` records that it no longer keys on `set -euo pipefail`. The prescribed CI ban **was** added: `.gitlab-ci.yml:112-125` bans a second extractor literal and the `sed -n 'A,Bp' "$0"` form. One residual, filed as L6-009. |
| CA-010 (L6+L7+L10) | **FIXED** | `bin/tests/wave7-smoke.sh:301-309` label rewritten: names the three as *peer consumers*, states `build_ignore_set` "has never existed in this tree post-extraction", and the assertions now check `source .*_edm-lint-lib.sh` presence (`:312`) plus absence of local definitions (`:318-322`). No mirroring claim survives. |
| CA-011 (L1+L3+L6) | **PARTIALLY FIXED** | Code half landed: `hooks/hooks.json:86` resolves a prefix per initiative and `continue`s when `edm-state resolve-dir` fails. Doc half still defective -- filed as L6-010. |
| CA-017 (L1+L6) | **PARTIALLY FIXED** | `edm-lint-artifacts:24-37` now lists six classes incl. `unterminated-fence` and `scan-error` (was four). `:263-266`'s shared-pass comment now says "four derived sets", matching the four arrays built at `:295-298`. Two residues remain: L6-002 and L6-003. |
| CA-018 (L6+L10) | **FIXED** | `agents/edm-audit-synthesizer.md:83-85` and `agents/edm-srd-auditor.md:67-69` both now carry only the canonical pointer plus the `docs/canonical-sections.md` fallback. No lossy four-bullet restatement in either. |
| CA-068 (L6) | **FIXED** | `CHANGELOG.md:203` AC8 row now cites `wave7-smoke.sh`'s "T67 AC8" scoping assertions and explicitly retracts the `git diff --stat` method it previously cited ("which the note above declares an invalid method for this exact case"). |
| CA-069 (L6) | **FIXED** | `bin/edm-state:2378` and `:2382` both interpolate `mode=` and `lifecycle_mode=`; `:2373-2377` explains why naming only `mode` was misleading. `:1653`'s `(${mode} mode)` is correct in place -- that branch is gated on `audit_required_for_mode_or_legacy "$mode"` alone (`:1645-1647`). |
| CA-070 (L6) | **FIXED, all three halves** | (a) `plugins/edm/CLAUDE.md:423` now reads "no bare `cache_write` key exists"; `edm-state:365-369` writes exactly the five documented keys. (b) `CLAUDE.md:337` accounts for all 14 skills; I verified every frontmatter pair -- 10 opus (orchestrator/plan/srd/audit-srd/tickets/audit-tickets/implement/code-audit/test/test-plan), effort high for srd/tickets/test-plan, max for the rest, and sonnet/high for push-jira/metrics/test-coverage/verify-runtime. Exact match. (c) `CLAUDE.md:795-796` now says `_cmd_init_render` "always writes the literal `1`". |
| CA-071 (L1+L6) | **FIXED** | `.gitlab-ci.yml:247-250`: the evals size-budget failure message's colon is now earned -- it prints every tracked file with its byte count, `sort -rn`, before `exit 1`. |
| CA-073 (L6) | **FIXED, both halves** | `bin/tests/timing.sh:317-323` counts initiatives at measurement time via `edm-state list --paths` and prints `${actual_initiatives}`, never `N_INITIATIVES`; `:308-314` explains why. `:263-272` now states the fixture is the worst realistic case, that the short-circuit is deliberately not exercised, and reports against the printed `<= 1.40x` budget. Header claim "no numbers are invented" (`:7`) now holds; the harness's "eight edm-state entry points" claim also checks out (get/resolve-dir/branch-check/gate-check + phase-complete + audit-converged/render-ledger + session-start = 8). |
| CA-127 (L6+L9) | **FIXED for the edm row** | Root `CLAUDE.md:54` now reads `edm (v3.1.0)` matching `.claude-plugin/marketplace.json:35`, and lists `/edm:verify-runtime` -- all 14 skills present. Adjacent rows in the same block are stale -- filed as L6-007. |
| CA-152 (L6) | **FIXED** | `plugins/edm/CLAUDE.md:473-479` now states the invariant instead of a false order claim and says outright "it is arm 6 of 8, between the `*haiku-4-6*` and `*sonnet-4-7*` ... arms". Verified against `bin/edm-state:392-451`: arm order is opus-4-7, sonnet-4-6, haiku-4-5, opus-4-8, haiku-4-6, `unknown`, sonnet-4-7, `*)` -- `unknown` is arm 6 of 8, exactly as documented. |
| CA-153 (L6) | **FIXED** | `bin/_edm-lint-lib.sh:1-67` now documents: the four emitted classes with their record shapes (`:10-32`), the de-indent rationale (`:27-32`), both `report_violation` signatures **including that field order differs, not just arity** (`:47-58`), and the `violations` counter convention with the CA-155 history (`:60-67`). The doc matches the code: `report_violation` at `:182-198` hard-fails (`return 1`) on an undeclared counter, as documented. |
| CA-154 (L6) | **FIXED** | `bin/edm-state:2-9` no longer claims "Mirrored verbatim in edm-lint-artifacts"; it now says every bin/ helper and evals/ driver sources the shared extractor. With one extractor (`_edm-cli-lib.sh:29`) the family can no longer render differently. `wave7-smoke.sh:4793-4821` asserts zero form-B sites and exactly one extractor literal. Minor calling-convention inconsistency noted as N2. |

### Adjacent entries whose documentation half I verified while in scope

| ID | Doc-half verdict | Evidence |
|----|------------------|----------|
| CA-031 (L6, marked fixed) | Confirmed fixed | `evals/README.md:82-83` and `:240-242` both state 2700s / $15, matching `run-eval.sh:248-249`. |
| CA-034 (L9) | **FIXED** | `evals/baseline/README.md:9-12`, `:26-28`, `:32-35` all state both sanctioned auth paths; matches `run-eval.sh:178-192`. |
| CA-066 (L5) | Doc/code now agree | `evals/README.md:43-49` documents the 10-run retention and `EDM_EVAL_KEEP_RUNS`; implemented at `run-eval.sh:543-570`, success-path only, as documented. |
| CA-086 (L8) | Doc half **FIXED** | `run-eval.sh:218-229` no longer claims the run "cannot reach outside the scratch tree"; it now states plainly that a prefix matcher does not bound what follows the binary and that this is "not a hard security boundary". |
| CA-095 (L10+L11) | **FIXED** | All eleven lens agents now cite by section name, not line (`agents/edm-audit-docs.md:69`: "`skills/code-audit/SKILL.md`'s "Operational Orchestration" step ... runs `mkdir -p`"). Verified: that heading exists at `skills/code-audit/SKILL.md:33` and the `mkdir -p "${OUTPUT_DIR}"` is at `:62`. `edm-check-grants:13` and `:328-342` likewise cite "Operational Orchestration" / "Lens Agent Launch Template" / "Synthesizer Phase" by name (present at `:33`, `:197`, `:226`), and the quoted instruction "Launch lens agents in parallel for every lens in LENS_SET" matches `:68`. |
| CA-019 (L7+L10) | Doc half **FIXED** | `evals/score-artifacts.sh:90-93` no longer says the sourceable file is not attempted; it states the scorer loads `bin/edm-mermaid-rules.awk` so "scorer and `bin/_edm-lint-lib.sh`/`bin/edm-lint-artifacts` now agree on what counts as a fence". |
| CA-166 (L11) | Doc half **FIXED** | `skills/code-audit/SKILL.md:63-67` now briefs lenses from the JSONL and explains the stale-render risk by name. |
| CA-023 (L8) | Code **fixed**, ledger summary now stale | `hooks/hooks.json:86` derives `srd_root="${EDM_SRD_ROOT:-${CLAUDE_PLUGIN_OPTION_SRD_ROOT:-./SRD}}"` and passes it to awk as `root`. The ledger row and `edm-lint-artifacts`' `--help` still assert the old hardcoded `^SRD/` -- see L6-001. |

## Findings (L6: Documentation Accuracy)

| ID | Sev | File:Line | Doc Says | Code Does | Fix |
|----|-----|-----------|----------|-----------|-----|
| L6-001 | **P1** | `plugins/edm/bin/edm-lint-artifacts:53-55` (inside the `EDM-HELP` block, i.e. `--help` output) | "The git-commit hook's staged-path matcher is still the literal `^SRD/`, so relocating srd_root drops that automatic hook enforcement unless the hook is updated too." | `hooks/hooks.json:86` derives `srd_root` from `EDM_SRD_ROOT` / `CLAUDE_PLUGIN_OPTION_SRD_ROOT` (default `./SRD`) and matches staged paths against it via `awk -v root="$srd_root"`. `plugins/edm/CLAUDE.md:656-659` states the opposite of the help text: "both honor `${user_config.srd_root}` ... with no separate hook-matcher update required." | Replace the two sentences with the current contract: the hook derives the same `srd_root` the tool does, so a relocated root scopes commit-path enforcement automatically. Highest-priority L6 item because it is the one operator-facing surface (`--help`) that tells an operator working enforcement is broken. |
| L6-002 | P2 | `plugins/edm/CLAUDE.md:744` | `edm-lint-artifacts` "Scan initiative artifact markdown for **four** violation classes -- attribution trailers, non-ASCII bytes, leaked tool-invocation tags, and a literal `;` inside Mermaid label/edge/message text" | Seven class strings reach `report_violation`: `unreadable` (`:275`), `unterminated-fence` (`:309`), `attribution` (`:328`), `unicode` (`:342`/`:355`), `leaked-tool-tag` (`:373`), `scan-error` (`:404`), `mermaid-semicolon` (`:415`). The tool's own help block already lists six. | Update the row to name six documented classes (or say "six classes; see `edm-lint-artifacts --help`"). This is the un-swept half of CA-017: the help block was corrected, this second copy was not. |
| L6-003 | P2 | `plugins/edm/bin/edm-lint-artifacts:24-37` | "Violation classes (all cause a non-zero exit):" then lists six -- `attribution`, `unicode`, `leaked-tool-tag`, `mermaid-semicolon`, `unterminated-fence`, `scan-error` | `unreadable` is emitted at `:275` for any file failing `[[ -r ]]`, increments `violations`, and drives `exit 1` at `:447`/`:477`/`:521` -- identical blocking behaviour to the six listed, undocumented. | Add an `unreadable -- the file could not be read (permissions); reported at line 0` row. Same defect shape CA-017 raised for `unterminated-fence`. |
| L6-004 | P2 | `plugins/edm/docs/audit-patterns/README.md:72-75` | Four `file:line` citations for the `update-patterns` call sites: `skills/audit-srd/SKILL.md:44`, `skills/audit-tickets/SKILL.md:46`, `skills/code-audit/SKILL.md:110`, `skills/implement/SKILL.md:46` | Actual lines: `audit-srd/SKILL.md:46`, `audit-tickets/SKILL.md:48`, `code-audit/SKILL.md:85`, `implement/SKILL.md:46`. Three of four are wrong; the code-audit citation is off by 25 lines and points into the Convergence-gate block, not the `update-patterns` call. | Cite by step name as the CA-095 remediation did elsewhere (e.g. "`skills/code-audit/SKILL.md` step 9a"), or drop the line numbers. This is CA-095's defect class in a file the sweep did not cover. |
| L6-005 | P2 | `plugins/edm/evals/README.md:290` | "See `SRD/edm/EDMV3__prompt-streamline/decisions.md` **D26** for the exact command that closes this gap once the baseline is captured." | D26 (`decisions.md:34`) is the EDMV3-T67 budget-miss record (AC5/AC6/AC10) and contains no tiering-matrix command. The closing command lives in **D28** (`decisions.md:36`, "Closing command, once the D23 baseline exists"), which is what `plugins/edm/CLAUDE.md:324-325` correctly cites. | Change D26 -> D28. |
| L6-006 | P2 | `.gitlab-ci.yml:57-58` | ".alpine_edm ... The pinned alpine image and the shared rule set are identical across **every lint job**. Expressed once so a digest refresh ... stays a single-line change even though the lint stage is now **four jobs** rather than one." | The lint stage has seven jobs (`lint:bash-syntax`, `:artifacts`, `:grants`, `:vocabulary`, `:shellcheck`, `:file-type-ban`, `:pattern-library-contract`), and the anchor is consumed by ten jobs -- the seven lint jobs plus `test:smoke` (`:318`), `test:state-validate` (`:386`) and `validate:manifest` (`:435`). `CHANGELOG.md:205` and `CLAUDE.md` both already say seven. | Say "seven lint jobs plus three non-lint consumers" (or drop the count). Stale since the shellcheck/file-type-ban/pattern-library jobs adopted the anchor. |
| L6-007 | P2 | `/Users/darryl.porter/projects/marketplace/CLAUDE.md:52-57` ("Current Plugins") | git v1.0.8, jira v1.0.0, ada-tablo v1.1.0, web-cms v1.0.0; six plugins listed | `.claude-plugin/marketplace.json` declares git 1.1.0 (`:16`), jira 1.1.0 (`:25`), ada-tablo 1.2.0 (`:89`), web-cms 1.0.19 (`:113`) -- and a seventh plugin, **`bruno` 1.1.0 (`:97-100`), which the registry section omits entirely**. Only the edm row (CA-127) was refreshed. | Refresh the four version strings and add a `bruno` row. Pre-existing and outside EDMV3's blast radius, but the file is in scope and the claim is falsifiable against a manifest in the same repo. |
| L6-008 | P2 | `/Users/darryl.porter/projects/marketplace/CLAUDE.md:19-26` ("Plugin Structure") | Diagram shows `agents/<agent-name>/AGENT.md` as *the* agent layout | edm's 21 agents are flat files (`plugins/edm/agents/edm-audit-docs.md` etc.), and the blocking `validate:manifest` job only globs `"$PLUGIN_DIR"/agents/*.md` (`.gitlab-ci.yml:475`, `:526`). Only web-cms uses the `AGENT.md`-in-a-directory shape. A contributor following this diagram for edm produces a file `validate:manifest` cannot see, failing as "declared but missing on disk". | Show both shapes, and note which one `validate:manifest` enforces for edm. |
| L6-009 | P2 | `plugins/edm/bin/_edm-cli-lib.sh:9-13` | "This file is now the **ONLY** place the extractor awk literal may appear ... lint:bash-syntax greps for and bans a second occurrence of the literal" | The ban it cites carves out `bin/tests/`: `.gitlab-ci.yml:113` filters `grep -v '/tests/'`, and `bin/tests/wave7-smoke.sh:4807` legitimately contains the literal (it is the assertion that enforces the rule). The absolute "ONLY place" claim is contradicted by the mechanism it cites in the next sentence. | One clause: "the only place outside `bin/tests/`, which is exempted because the smoke suite must carry the literal to assert on it." |
| L6-010 | P2 | `plugins/edm/CLAUDE.md:650` (Hooks behavior table, PreToolUse row) | "Exit 1 (a real violation) sets the code that actually blocks the commit; exit 2 (a setup/usage error ...) is reported to stderr but does not block (CA-011)" | Both numbers are `edm-lint-artifacts`' exit codes, but in a hooks table they read as the hook's. The hook's own codes are the inverse: linter exit 1 sets `fail=2` and the hook exits **2** (the blocking code); linter exit 2 leaves `fail=0` and the hook exits **0** (`hooks/hooks.json:86`). A reader taking this row at face value would believe a PreToolUse hook blocks by exiting 1, which is the CA-011 assumption the ledger flags as unsettled. | Disambiguate: "`edm-lint-artifacts` exit 1 -> the hook exits **2**, the PreToolUse blocking code; `edm-lint-artifacts` exit 2 -> the hook exits 0 and does not block." |

## Noted / Not Actionable

| ID | File:Line | Rationale |
|----|-----------|-----------|
| L6-N1 | `plugins/edm/bin/edm-check-vocabulary:2-53` + `:74-82` | Only script in the twelve with two independent help texts (a long header block and a shorter sentinel block below `usage()`). They agree today on every substantive claim (eight scope roots -- verified against `SCOPE_ROOTS` at `:105-114` -- output format, exit codes), so there is no inaccuracy to report; recorded as a drift surface only. |
| L6-N2 | `plugins/edm/bin/edm-state:4599` | Calls `print_help "$0"` where the library's documented convention (`_edm-cli-lib.sh:24`) and all eleven siblings pass `"${BASH_SOURCE[0]:-$0}"`. Correct here: the dispatch is guarded by `[[ "${BASH_SOURCE[0]}" == "${0}" ]]` at `:4554`, so `$0` is necessarily this script. The library's actual requirement (the caller supplies its own path) is satisfied. |
| L6-N3 | `plugins/edm/CLAUDE.md:464-471` | The bulleted arm list groups arms by generation rather than file order (it shows `sonnet-4-7` before `unknown`), but `:473-479` explicitly disclaims file order and states the true position. Intentional grouping, self-corrected in place. |
| L6-N4 | `plugins/edm/CLAUDE.md:318` | "Derived from tiering matrix `<date>`" placeholder -- `:319-325` states "Status: NOT yet matrix-derived", the D23 dependency, and the D28 closing command. Already accepted as CA-174. |
| L6-N5 | `plugins/edm/README.md:234` | "tracks ... in `SRD/{PREFIX}/.edm-state.json`" names only the flat layout; the layout diagram at `:178-200` documents both and marks product-scoped as canonical. Deliberate simplification in a feature bullet. |
| L6-N6 | `plugins/edm/evals/baseline/README.md:11` | Cites `run-eval.sh:181-183`; the auth gate code is `:188-192` and the cited range lands inside the explanatory comment `:178-183`. Resolves to the correct paragraph; same class as L6-004 but not misleading. |
| L6-N7 | `plugins/edm/bin/tests/timing.sh:39` | Leftover `srand()` in the perl-less `_now` fallback. No comment claims it is needed, and the documented invariant at `:32-33` ("must never invent sub-second digits") holds -- only `systime()` is printed. Dead call, L2's territory, not a doc inaccuracy. |
| L6-N8 | `/Users/darryl.porter/projects/marketplace/CLAUDE.md:7` | Describes the pipeline as lint + smoke + two-tier validate, omitting the `eval` stage. No stage count is claimed and `eval:nightly` is non-blocking and manual; `plugins/edm/CLAUDE.md` documents all four stages. Deliberate summary. |
| L6-N9 | `plugins/edm/docs/audit-patterns/README.md:63-68` | The Consumers list maps three loaders and does not claim one for `test-coverage-audit.md`, so it is not inaccurate -- but it corroborates CA-168 (read-orphaned pattern doc), which is L11's open finding. Cross-lens corroboration only. |
| L6-N10 | `plugins/edm/CHANGELOG.md:65-69`, `:244-247` | "The CI lint stage ran four checks as sequential script lines ... now four jobs" -- historical entries correctly framed as history, and `:205` states the current graph as seven lint + three test jobs. Not stale. |

## Summary for the synthesizer

- **Wave 6a's L6 remediation genuinely landed.** All 14 L6-tagged open ledger entries verified against live code: 11 fully fixed, 2 partially (CA-011, CA-017), 1 fixed with a scope gap in a sibling file (CA-127). No L6 entry I checked was falsely marked as remediated.
- **The new shared library is well documented and its doc claims are true**, including the load-bearing ones: the CI ban it advertises exists (`.gitlab-ci.yml:112-125`), the counter contract it states is what `report_violation` enforces, and the field-order divergence between the 4-arg and 5-arg forms is now called out explicitly. Its help output is uniform across all 12 callers because there is exactly one extractor. One overstated absolute claim (L6-009).
- **Dominant residual pattern, unchanged from round 2: multi-site facts where one copy is corrected and the second is not.** L6-002 (class count fixed in the tool, stale in CLAUDE.md), L6-001 (hook fixed in code, stale in `--help`), L6-005/L6-004 (cross-reference drift). Every one of the ten findings is a one-to-three-line edit; none requires a code change.
- **CA-023's ledger row is itself now stale** and should be re-verified by L8: `hooks/hooks.json:86` derives `srd_root`. `edm-lint-artifacts`' `--help` currently agrees with the stale ledger rather than with the code, which is L6-001.
- **CA-130 reproduced a third time**: `Write` was absent from this lens's runtime tool set despite the frontmatter grant, so this report requires orchestrator transcription to `SRD/edm/EDMV3__prompt-streamline/code-audit/pass-3_2026-08-08/lens-L6.md` and `lens-L6.jsonl`.
