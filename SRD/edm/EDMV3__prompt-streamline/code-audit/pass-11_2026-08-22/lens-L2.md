# Lens L2: Dead Code & Unreachable Paths -- Round 11 (full)

Delivered inline: my runtime tool set this round was Read, Glob, Grep, WebFetch, WebSearch,
TaskStop -- no `Write`, no `Bash`, no `LS`. See N01. Both output files are transcribed by the
launching agent.

## Prior-round L2 ledger re-verification

All three L2-tagged entries that were `status:"open"` entering this round are now **FIXED in the
tree**, verified by direct read (not by trusting a commit message):

- **CA-490 -- FIXED.** `bin/edm-compare-eval` now runs refusal condition 3 at `:67-75`
  (`cand_complete != "true"` -> exit 2) **above** the baseline-existence check at `:77-81`, with a
  CA-490 comment at `:62-66` naming the reorder and the reason. Candidate JSON validation moved to
  `:60`. `plugins/edm/evals/baseline/scores.json` still does not exist (`evals/baseline/` holds only
  `README.md`), which is now the *precondition the fix was built for* rather than a preemption.
  Pinned by `bin/tests/wave7-smoke.sh:9241-9253`, a scratch invocation asserting exit 2 and the
  literal `REFUSED -- candidate run is flagged complete: false`. **Recommend `status: fixed` -- but
  with `spec_swept: no`**, because the same-commit sweep of the consumer comments is incomplete: see
  L2-001, which is the residual this fix created.
- **CA-519 -- FIXED, prescription taken exactly.** `mkdir -p "$(dirname "$DST")"` now sits at
  `bin/edm-sync-canonical-sections:85`, one line above the `mktemp` at `:86`, with a CA-519 comment
  at `:81-84`. It is now load-bearing (the script can regenerate into a not-yet-existing `docs/`)
  instead of a preempted no-op. No residual.
- **CA-520 -- FIXED.** `bin/edm-init:212` now reads "the edm-state init call above, before the
  branch block". Re-pointed by name, matching the G10/CA-340 remedy the same file documents at
  `:173-174`. No line-number citation remains in that comment.

**CA-201's G41 deletion records both hold, at the same line numbers as round 10.**
`bin/edm-check-grants:103-106` and `bin/edm-check-vocabulary:184-188` still carry the explicit
"making a repeated guard unreachable" / "making an empty-FILES guard unreachable" comments, and both
guards are still absent.

## Findings (L2: Dead Code & Unreachable Paths)

| ID | File:Line | Why unreachable | Delete or fix |
|----|-----------|------------------|----------------|
| L2-001 | `.gitlab-ci.yml:859-863` (with `:801-808`, `:844-856`) | **Residual of the CA-490 fix -- a safety net that was load-bearing before CA-490 and is now structurally unreachable.** The CA-452 "a partial run always fails the job" final block (`if [ "${RUN_EVAL_EC:-0}" -eq 4 ]` -> `exit 4`) cannot be reached on the only path it exists for. Trace with `RUN_EVAL_EC=4`: (a) if `${RUN_DIR}scores.json` is missing, `:853-855` exits 1 -- never reaches `:859`; (b) if present, `edm-compare-eval` runs, and post-CA-490 the `complete != "true"` refusal at `edm-compare-eval:71-75` fires FIRST, so `rc=2` unconditionally (the stub's `complete:false` is preserved, not repaired -- `evals/score-artifacts.sh:646` re-derives `false` and `:659-660` re-derives it for an absent `run.json`); (c) `rc=2` hits the `*)` arm at `:851`, which runs `exit "$rc"` and terminates the job. The only two arms that do NOT exit are `0)` and `3)`, and **both require `complete: true`**, i.e. `RUN_EVAL_EC != 4` -- so the `:860` guard can never be true at a point where control has survived block 3. Before CA-490's reorder this block was the *only* thing failing the job on a partial run (a partial with no baseline got `rc=3`, the non-exiting "NOT ARMED" arm, and fell through to `:859-863`). Its own comment at `:857-858` names two escape hatches -- "the comparison arm above was skipped (e.g. no scores.json survived)" and "somehow reported clean" -- and both self-exit or are contradicted, so the stated rationale does not hold. Three consequences: the documented partial signal silently changed from exit 4 to exit 2; the diagnostic pointing an operator at `run.json` / the stub `scores.json` in the artifacts never prints (they get only `eval:nightly: FAILED -- eval comparison failed with exit 2`); and `:808`'s comment "The job still fails at the end either way (CA-452's own final step)" now describes a path that cannot execute -- the same defect class CA-490 was itself filed for, one level up. Filters: (1) reachable again only by reverting CA-490, so no; (2) documented as a safety net, but the documentation's own two cases are refuted above; (3) no test invokes the CI job body -- `wave7-smoke.sh:9241-9253` tests the binary directly, not the job's arm ordering, so nothing turns red. | **Fix, do not silently delete.** Preferred (keeps the documented exit-4 partial signal): stop exiting inside the `*)` arm -- record `CMP_EC="$rc"` and let the final block own the exit, preferring 4 over 2 when `RUN_EVAL_EC=4`. Minimum acceptable: delete `:859-863`, re-word `:801-808` and `:857-858` to state that a partial run now fails at the comparison step with exit 2, and move the `run.json`/stub-`scores.json` artifacts pointer into the `*)` arm's message so the diagnostic is not lost. Either way, land it as CA-490's outstanding `spec_swept`, and add one assertion pinning the job's partial-run exit code. |
| L2-002 | `plugins/edm/bin/tests/run-all.sh:145-148` | The `if`'s condition decides nothing for the statement it guards: `:146` and `:148` are **byte-identical** -- `printf '%-28s %8s %8s %8s\n' "$_suite" "$_s_pass" "$_s_fail" "$_suite_status"` on both arms, same format string, same four arguments. Only the *extra* output at `:149-152` differs. This is the CA-054 class, which this project filed under L2 and fixed ("the `prototype)` case arm emits output byte-identical to the `*)` fallback immediately below it -- pure duplication with no behavioural difference"). Nothing is unreachable in the strict sense -- both arms execute -- which is why it is P2 and not higher: it is the "condition that cannot change the outcome" half of the class. | **Fix (2-line change).** Hoist the `printf` above the `if`, then reduce the block to a one-armed `if [[ "$_suite_status" != "PASS" ]]; then _failed_suites+=(...); echo ...; fi`. Output is unchanged byte-for-byte, so no test or fixture changes. |
| L2-003 | `plugins/edm/bin/edm-mermaid-rules.awk:96` | `if (trimmed == "classDef" || trimmed == "style" || trimmed == "linkStyle") return 0` is provably inert: it can never change the function's result. Any line whose trimmed form is exactly one of those three bare keywords contains no `;`, no `[`/`(`/`{`/`|`/`"` span character, and no `->`, so it falls through the `sub(/;[[:space:]]*$/...)` at `:102`, all five span regexes at `:105-109` and the sequence-message check at `:112-116`, and returns 0 at `:117` regardless. Distinguish from `:95` (`^(classDef|style|linkStyle)[[:space:]]`), which IS load-bearing and has a regression fixture behind it (`bin/tests/fixtures/mermaid/valid/v04-classdef-style-linkstyle.md`). Filters: (1) reachable only if the span-check set were widened to flag something a bare keyword contains -- implausible; (2) `:96` carries no comment of its own, and the `:93-94` rationale it inherits ("their trailing `;` is a statement terminator") is inapplicable to a bare keyword that has no `;`, so it is not documented as a deliberate net; (3) no test environment differs. Confidence medium because the harm is nil -- this is an inert line, not a wrong one. | **Fix by merging, not by deleting outright.** Relax `:95` to `^(classDef|style|linkStyle)([[:space:]]|$)` and delete `:96`. That expresses the carve-out once, keeps the (contrived but harmless) bare-keyword case covered by the same rule, and removes a line that reads as a guard while guarding nothing. Plain deletion is the acceptable alternative and matches the CA-140/CA-202/CA-260/CA-309/CA-362/CA-519 precedents. |

## Noted / Not Actionable

| ID | File:Line | Rationale |
|----|-----------|-----------|
| L2-N01 | `plugins/edm/agents/edm-audit-dead-code.md:5` | **CA-130 reproduces for an ELEVENTH consecutive round at this lens.** The frontmatter grants `tools: Glob, Grep, LS, Read, NotebookRead, WebFetch, TodoWrite, WebSearch, Write`; my delivered runtime set was Read, Glob, Grep, WebFetch, WebSearch, TaskStop -- **no `Write`**, so neither `lens-L2.md` nor `lens-L2.jsonl` could be created by me and both halves are transcribed by the launching agent. Improvement on round 10: the delivered prompt DID carry `## Scope` this round. Unchanged: the authoritative JSONL schema came from the launching agent's task message, not from the on-disk definition's own `## JSONL Line Format` section, and I used the task-restated schema per D22/CA-130. Not a repository defect at this lens -- it is CA-130 and (repository-side) CA-193. |
| L2-N02 | `plugins/edm/bin/edm-compare-eval:67-75` | **CA-490 closure record.** Reorder verified in the tree and pinned by `wave7-smoke.sh:9241-9253`. Close it -- but with `spec_swept: no`, since the consumer comments it made stale are only half swept (`.gitlab-ci.yml:805-808` names the reorder; `:857-863` was not touched, which is L2-001). |
| L2-N03 | `plugins/edm/bin/edm-sync-canonical-sections:85` | **CA-519 closure record.** The `mkdir -p` was moved above the `mktemp`, exactly as prescribed, with the CA-519 rationale in place at `:81-84`. No residual; safe to close `spec_swept: n/a`. |
| L2-N04 | `plugins/edm/bin/edm-init:212` | **CA-520 closure record.** Re-pointed by name; no line-number citation remains. Safe to close. |
| L2-N05 | `plugins/edm/bin/edm-lint-staged-artifacts:75` | Re-verified this round; **line drifted from `:57` to `:75`** since round 10, so cite `:75` going forward. `phys_repo_root="$repo_root"` is still a provable dead store -- `:76`'s `[ -n "$repo_root" ] && A || B` reassigns it on every continuation before any read. Zero-cost defensive initialization in CA-436 code. Same disposition, do not escalate. |
| L2-N06 | `plugins/edm/bin/_edm-lint-lib.sh:214`, `:219` | Re-verified this round: `mermaid_line_set` and `marker_line_set` still exist and still have no external consumers (only `ignored_line_set` does). CA-200 closed this by making the docstring honest; `:51-65` states the position explicitly. Filter item 2. |
| L2-N07 | `plugins/edm/bin/_edm-lint-lib.sh:230` | Re-verified this round: `report_violation`'s "expected 4 or 5 args" arm still cannot fire from any static call site. CA-290, accepted deliberate arity defence on a shared function with two incompatible field orders. Do not re-file. |
| L2-N08 | `plugins/edm/bin/edm-check-grants:103-106`; `plugins/edm/bin/edm-check-vocabulary:184-188` | Re-verified this round at the same line numbers: both G41 deletion records intact, both guards still absent. Recorded so the CA-201 closure is not re-derived. |
| L2-N09 | `plugins/edm/bin/edm-check-vocabulary:199` | Re-verified this round: `[[ "$mode" == "$tline" ]] && mode="literal"` is still unreachable against the shipped `vocabulary-prohibited.txt` (every data line carries a `word:`/`literal:` prefix), and the same holds for `is_allowlisted`'s no-pipe tolerance. Both files are declared data-not-code and are designed to be extended with no code change, so a future bare-token line makes the arm live. Filter item 1. |
| L2-N10 | `.gitlab-ci.yml:853-855` | Deliberately NOT filed. The `scores.json missing before comparison` else arm is preempted for the failure its message names -- block 2 at `:834-839` already exits 1 when `score-artifacts.sh` fails -- so it only fires when that script exits 0 without writing the file. That is a cross-process postcondition check on a separate script, Filter item 1. It is also load-bearing to L2-001's analysis: it is one of the two escape hatches, and it self-exits. |
| L2-N11 | `.gitlab-ci.yml:849` | The `0)` arm ("candidate at or above baseline_total minus variance") is environmentally unreachable today, because no `evals/baseline/scores.json` exists and every complete candidate therefore gets exit 3. It becomes reachable the moment the wave-A baseline is captured (D23/CA-106). Filter item 1 -- textbook. Not a finding. |
| L2-N12 | `plugins/edm/evals/run-eval.sh:342-351` | The G45 auth probe is dead on the only automated path: `.gitlab-ci.yml:783-784` makes `eval:nightly` `when: never` if `$ANTHROPIC_API_KEY == null`, so in that job the key is always set and `[ -z "${ANTHROPIC_API_KEY:-}" ]` is always false. It is reachable locally, on a subscription/OAuth machine with no exported key -- exactly the case it was written for. Filter item 1, keep. Recorded because the consequence is that the `+ 60` term in BOTH timeout arithmetics (`run-eval.sh:452`'s `PHASE_TIMEOUT_SECONDS * 3 + 60`, and `.gitlab-ci.yml:776`'s "plus the 60s auth probe") is a phantom in the only environment where the CA-511 guard is live, since `CI_JOB_TIMEOUT` is GitLab-only. The error is in the conservative direction -- the guard refuses 60s sooner than strictly necessary -- so no behaviour change is warranted; noted so it is not re-derived as a defect. |
| L2-N13 | `plugins/edm/evals/run-eval.sh:443-459` | Positively verified rather than filed. The CA-511 inner/outer timeout coupling is real wiring, not a dead guard: `CI_JOB_TIMEOUT` is a GitLab predefined variable in seconds, `eval:nightly` sets `timeout: 150m` (9000s), the default `2700` yields 8160 < 9000 so no refusal at the default, and the refusal arm goes live at `EDM_EVAL_PHASE_TIMEOUT_SECONDS >= 2980` -- reachable by any operator override. Recorded because a guard keyed on a CI-only variable and compared against a hardcoded phase count is precisely the shape that ships inert, and this one did not. |
| L2-N14 | `plugins/edm/bin/tests/_harness.sh:283-286` | `_harness_hash_file`'s "unhashable" arm requires BOTH `shasum` and `sha256sum` to be absent, which no environment this suite runs in produces (macOS ships `shasum`; the pinned alpine and `bash:3.2` images ship `sha256sum`). It is CA-394's own remediation and all four call sites now check the exit status (`:296-297`, `:303-304`, `:325-326`, `:333-334`), which is what CA-394 was for. Filters 1 and 2. Keep. |
| L2-N15 | `plugins/edm/monitors/monitors.json` | **Flagged for L3, not re-filed here -- a reachability premise inside another lens's finding is L2's competence.** `monitors/monitors.json` **exists at HEAD**: `bin/edm-check-vocabulary:103` and `:121` scan it, and `wave7-smoke.sh:2095` and `:5817` assert against it. CA-522's mitigating rationale states the opposite -- "`monitors/monitors.json` is no longer in the tree (T59/EDMV3-84 took the delete branch), so today the reachable caller is the CLI subcommand" -- which is false. `cmd_watch_impl` is therefore reachable via the host-armed monitor as well as the CLI, so CA-522's dropped-commit-notification window is live in the multi-implementer case its own text calls the high-exposure one. This does not change CA-522's P2, but it removes one of its two mitigating clauses; L3 should correct the entry text. |
| L2-N16 | `plugins/edm/bin/edm-state:4837-4839` | `cmd_audit_converged`'s second `invalid JSONL at ...` refusal is preempted for the failure its message names: the slurping `jq` at `:4773-4776` already parsed the same file and returned 1 at `:4777-4778` on a parse failure. It stays reachable for a *different* cause (a jq evaluation error inside `_audit_ledger_breakdown`, e.g. a type error on `.sev`), so it is not dead. Recorded because two arms carrying one identical message for two different causes will read as a duplicate to the next auditor; if anyone touches it, differentiate the second message ("ledger breakdown failed") rather than removing the arm. |
| L2-N17 | `plugins/edm/bin/edm-state:4638-4651` | Positively verified. The CA-479 multi-pass-directory disambiguation is live and correct: the `-gt 1` arm's `[[ -z "$_pass_dir" ]] || [[ "$_cand" -nt "$_pass_dir" ]]` seeds on the first iteration and is not a dead store, and the `-eq 1` arm covers the normal case. Recorded because this is a newly-landed block in the highest-risk region round 10 left unaudited, and it is clean. |
| L2-N18 | `plugins/edm/bin/edm-state:3876-3880` | **Carry-forward of pass-10 L2-N05, NOT re-verified in round 11.** `cmd_gate_check`'s unknown-gated-command `*)` arm is unreachable from every shipped hook caller but is public-CLI input validation, retained on the same reasoning CA-451 used to keep `gated_phase_for_gate`'s `*)` arm. Keep. |
| L2-N19 | `plugins/edm/bin/edm-check-grants:500`, `:513`, `:459-465` | **Carry-forward of pass-10 L2-N08/N09, NOT re-verified in round 11.** Two dead stores (the `ln`/`task_ln` `body_start` fallbacks, which can only fire when the guarded flag is 0 and the variable is never read) plus one intentionally unused loop variable (`ln` read and discarded because the citation deliberately uses `$lnum`). Same disposition. |
| L2-N20 | `plugins/edm/bin/edm-init:125-127` | **Carry-forward of pass-10 L2-N11, NOT re-verified in round 11.** The `[[ -d "$DIR" ]]` collision guard is preempted by the unconditional `edm-validate-prefix` at `:87`, but still narrows a real TOCTOU window containing the unbounded interactive `read` at `:106`. Keep. |
| L2-N21 | `plugins/edm/bin/edm-init:146-159` | Re-read this round: unchanged. `standard|iac|data-ml)` and `mini-srd)` still have byte-identical bodies (`mkdir -p "$DIR/code-audit"`), so the split is commentary only -- adjacent to CA-054 and to L2-002 above, but both arms execute and each comment carries distinct mode documentation. L4/L10's call. The empty `prototype)` arm is load-bearing (the `case` has no `*)` and `MODE` is pre-validated at `:59-62`). |
| L2-N22 | `plugins/edm/bin/edm-check-skill-sync:64` | **Carry-forward of pass-10 L2-N13, NOT re-verified in round 11.** `[ -f "$f" ] || continue` cannot fire today (all eight phase skills exist), but skipping a missing phase skill is correct for a moved-procedure tripwire and manifest completeness belongs to `validate:manifest`. Filter item 1. |
| L2-N23 | `plugins/edm/bin/edm-lint-artifacts:137`, `:392-429` | **Carry-forward of pass-10 L2-N14/N15, NOT re-verified in round 11.** The first-stage single-file `trap` is reachable and load-bearing (CA-447 window between the two `mktemp` calls); class 4's mermaid loop still omits the `UNREADABLE_FLAGS` guard but is behaviourally equivalent, accepted in the CA-376 bundle. Do not re-file. |
| L2-N24 | `plugins/edm/bin/edm-check-grants:546-551` | **Carry-forward of pass-10 L2-N07, NOT re-verified in round 11.** The `Edit` arm reaches the same `_has_instruction=0` as its `else` (no `Edit_instructed.txt` producer exists), so it is behaviourally redundant -- but it executes, is documented at `:530-537`, and hedges a future producer. Filters 1 and 2. L10's call. |
| L2-N25 | `plugins/edm/hooks/hooks.json:19`, `:32`, `:45`, `:58`, `:71` | **Carry-forward of pass-10 L2-N16, NOT re-verified in round 11.** Positive verification: the CA-409 rewrite of all five `UserPromptExpansion` command hooks onto exit-code 3 is not an inert enforcement layer -- `cmd_gate_check` genuinely returns 3, so the blocking `exit 2` arm is reachable. |

## Sweep coverage notes (honest, per CA-377's precedent)

**Fully read this round:** `bin/edm-compare-eval`, `bin/edm-mermaid-rules.awk`, `bin/_edm-cli-lib.sh`,
`bin/tests/run-all.sh`, `bin/tests/_harness.sh`, `agents/edm-audit-dead-code.md` (frontmatter and
`## Scope` only).
**Substantially read this round:** `bin/edm-init:100-229`, `bin/edm-sync-canonical-sections:70-127`,
`bin/edm-state:4589-4948` (the whole `audit-round-complete` / `audit-converged` pair, including the
CA-471 completeness gate and the CA-479 disambiguation), `bin/edm-check-grants:95-118`,
`bin/edm-check-vocabulary:178-203`, `bin/edm-lint-staged-artifacts:70-80`,
`bin/_edm-lint-lib.sh:204-232`, `evals/run-eval.sh:296-520`, `evals/score-artifacts.sh:636-780`
(via targeted grep), `bin/tests/timing.sh:370-440`, `.gitlab-ci.yml:700-868` and its
timeout/budget/retry surface tree-wide.
**Verified by existence check only:** `plugins/edm/evals/baseline/` (no `scores.json`),
`plugins/edm/monitors/monitors.json` (present).

**NOT AUDITED this round -- report as unaudited, not clean:** `bin/edm-state` outside `:4589-4948`
(five of its ~5,945 lines were read elsewhere via the function index only), all of
`bin/tests/wave3/4a/4b/5/6/7-smoke.sh` and `harness-smoke.sh` bodies (grep-only access this round --
the four L4 findings CA-523 through CA-528 all live in territory L2 has never swept), the bulk of
`bin/tests/timing.sh`, `evals/tiering-matrix.sh`, `evals/score-artifacts.sh` outside `:636-780`
(including `cmd_compare` at `:598-643`, CA-120's noted second comparison implementation),
`.gitlab-ci.yml` outside `:700-868`, all of `skills/**/SKILL.md`, all `agents/*.md` other than my
own frontmatter, `docs/**`, `plugins/edm/CLAUDE.md`, `README.md`, `CHANGELOG.md`, repo-root
`CLAUDE.md`.

**Round 12's highest-value L2 re-sweep targets, in order:** (1) `bin/tests/wave7-smoke.sh` and
`wave6-smoke.sh` bodies -- roughly ten thousand lines of assertion code this lens has never read,
and four round-10 L4 findings establish that vacuous-assertion and dead-control shapes are live
there; (2) `bin/edm-state` outside the audit-round pair, especially `state_anomalies` (`:1706-1956`,
twelve anomaly classes) and the `update-patterns` family (`:5257-5690`, where CA-517 and CA-533
already point at unreachable-arm risk); (3) `evals/score-artifacts.sh` in full.

Round 10's own coverage note contained one error worth correcting: it listed
`monitors/monitors.json` as "fully read this round" while round 10's CA-522 asserts the file is not
in the tree. It is in the tree (L2-N15).
