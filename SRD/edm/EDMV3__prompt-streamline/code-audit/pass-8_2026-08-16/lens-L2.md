## Findings (L2: Dead Code & Unreachable Paths)

## Prior-round re-verification

**CA-393 -- FIXED.** `plugins/edm/evals/score-artifacts.sh:420-427`. The `denom -eq 0` skip branch is gone; `denom` is computed at `:426` and passed straight to `score_from_ratio` at `:427` with no guard. The replacement comment records both halves of the rationale (srd_count proven non-zero by the `:387` early return; `target_count == 0` deliberately scores a hard 0 per G53/CA-286).

**CA-394 -- FIXED, both halves.** `_harness_hash_file`'s unhashable arm now `return 1` at `plugins/edm/bin/tests/_harness.sh:271-272`. Both callers check the function's own exit status, not just its printed value: `check_state_unchanged` at `:283-284` and `:290-291`, `check_refuses_and_leaves_state` at `:312-313` and `:320-321`. Each of the four sites uses the `|| { fail ...; return; }` form, so a hasher-less host now fails loudly instead of comparing `"unhashable" == "unhashable"`.

**CA-395 -- FIXED at the named site, but the cleanup left live scaffolding behind (raised as L2-001 below).** `assert_absent_with_control` no longer exists in `_harness.sh` (only two prose mentions survive, at `:183` and `:210`), and its three self-test cases were deleted from `harness-smoke.sh`, replaced by the record at `:182-185`.

| ID | File:Line | Why Unreachable | Delete or Fix |
|----|-----------|------------------|----------------|
| L2-001 | plugins/edm/bin/tests/wave7-smoke.sh:195-211 | CA-395 deleted `assert_absent_with_control` from `_harness.sh` and deleted its `harness-smoke.sh` self-tests, but this tripwire guarding it survived intact. Its pass message at `:210` asserts the function "is called only from harness-smoke.sh's self-tests" -- there are now zero self-tests and zero call sites anywhere, and no function to call. Its fail arm at `:211` guards a class that cannot be reintroduced by the edit it scans for: a new `assert_absent_with_control ...` line in any suite would be a call to an undefined function, which aborts the suite under `set -euo pipefail` with exit 127 before this assertion is ever evaluated. The one edit that WOULD genuinely reintroduce the class -- re-adding the function definition to `_harness.sh` -- is explicitly excluded from the scan at `:208` (`grep -v '/_harness\.sh$'`). | Delete `:195-211` entirely (the guarded subject no longer exists), OR repoint it at `_harness.sh` alone as a "this function stays deleted" tripwire and rewrite the pass message, which is currently false as written. |
| L2-002 | plugins/edm/bin/edm-state:831 (twin at :902) | `[[ "$gated_phase" == "null" ]] && continue` is structurally unreachable at both sites. `gated_phase_for_gate` (`:774-776`) has explicit arms for exactly `1`, `2`, `3` and returns `"null"` only from its `*)` default; both guarded loops are the hardcoded `for g in 1 2 3` (`required_gates_for_mode:829`, `phase_start_prerequisite_gate:900`), so the `*)` arm is never taken from any of the five call sites in this file (`:830`, `:901`, `:5221`, `:5222`, `:5223` -- the last three pass the literals `1`/`2`/`3`). This is the identical shape CA-140 already deleted one function over, and that deletion is recorded in-tree at `:1452-1454`. | Delete both `== "null"` guards. Keep `gated_phase_for_gate`'s `*)` arm as the helper's own out-of-range contract. If a fourth gate is ever added, the `*)`-plus-guard combination would silently DROP it rather than fail loudly, so removing the guards also removes a future silent-skip hazard. |
| L2-003 | plugins/edm/bin/edm-state:2178 | The `--accept-p2-debt` acceptance arm (`:2179-2189`) is environmentally unreachable whenever `cmd_audit_converged` writes any stderr line before its `not converged:` stdout line. `conv_out` is captured at `:2168` with `2>&1`, and the arm is gated on the anchored prefix `"$conv_out" == "not converged: "*`. `cmd_audit_converged:4476` emits `edm-state audit-converged: [warn] no code-audit round has ever been recorded ...` to stderr and then does NOT return -- control falls through to the blocking-set computation and prints `not converged:` at `:4522`. In that state `conv_out` begins with the warn text, the prefix test fails, `p2_debt_accepted` stays 0, and the `elif` at `:2195` fires instead, dying with `... even with --accept-p2-debt (P0=0 P1=0 must both be 0 ...)` -- where the quoted `P0=0 P1=0` are the UNINITIALIZED locals from `:2177`, not the real breakdown, so the refusal reports the exact condition it claims is unmet. Trigger state is supported and documented: `rounds_count == 0` covers a legacy bare-integer `audit_rounds.code` (coerced to `rounds: []` by `AUDIT_ROUND_COERCE_JQ_DEF`, `:1484`, AC1a) and any initiative whose ledger exists but whose round was never recorded. Untested: every `--accept-p2-debt` case in `bin/tests/wave6-smoke.sh:657-745` runs `audit-round-start` + `audit-round-complete` first, so `latest_round_type` is `full` and no warn line is ever emitted. | Fix. Either capture stdout and stderr separately at `:2168` and prefix-test only stdout (G15/CA-353 already split the two streams at `:4514-4522` for exactly this reason), or match with `*"not converged: "*` instead of an anchored prefix. Separately, populate `p2_debt_p0`/`p2_debt_p1` before the `:2199` message or drop the misleading numbers from it. |
| L2-004 | plugins/edm/evals/run-eval.sh:150-154 | `write_partial_artifacts`'s stub `scores.json` (`complete: false`) exists, per its own docstring at `:131-135` and the exit-code contract at `:52-55`, "purely so CI can see complete: false and refuse the baseline comparison." No CI path can read it. `run-eval.sh` exits 4 on a partial run; `.gitlab-ci.yml:720` runs `bash ... run-eval.sh || { echo "eval:nightly: FAILED -- run-eval.sh"; exit 1; }`, so the job terminates before the scoring step (`:722-727`) and the comparison step (`:732-742`). Consequence: `bin/edm-compare-eval`'s refusal condition 3 (`:70-75`, the `complete != "true"` refusal, documented at `:22` as one of the three named refusal conditions) is unreachable from the only automated caller in the repository -- the sole CI invocation at `.gitlab-ci.yml:736` runs only after a run that exited 0, whose `run.json` always carries `complete: true`. | Fix the wiring, not the stub: let `eval:nightly` distinguish run-eval exit 4 (partial -- proceed to score + compare so the handshake actually fires) from exits 1/2 (fail the job). Alternatively delete the stub and the `complete:false` handshake framing from `run-eval.sh:52-55` / `:131-135` and rely on the artifact upload alone, so the documented mechanism matches the shipped one. |

## Noted / Not Actionable

| ID | File:Line | Rationale |
|----|-----------|-----------|
| L2-N01 | plugins/edm/bin/tests/_harness.sh:183 | CA-395 residue: `count_matches`'s docstring still instructs callers to "pair this with a positive control (assert_absent_with_control below)" -- a function deleted from this file. Mirrored at `harness-smoke.sh:135` and `:137`, whose section header and echo still name it as something that section tests. Live misdirection for a future contributor, but doc-accuracy, not unreachable code -- L6's call. Fix alongside L2-001 as one CA-395 cleanup. |
| L2-N02 | plugins/edm/bin/_edm-lint-lib.sh:199,204 | `mermaid_line_set` / `marker_line_set` have zero callers anywhere in the tree (verified: only `ignored_line_set` is consumed externally, by edm-check-grants, edm-check-vocabulary and edm-state). Explicitly documented at `:56-65` as retained for API completeness after `project_class` made the extension free, not because a caller needs them. False Alarm Filter item 2. |
| L2-N03 | plugins/edm/bin/edm-state:3094, :3518 | The `${_dst_lockbase}.lock.timeout.*` sweep loops cannot match anything a current-version run creates -- G17/CA-305 moved the flock-timeout marker under TMPDIR (`:1226`), and `:1223` already calls this glob "dead". Kept deliberately as backward-compat cleanup for a marker left by a not-yet-upgraded run, and exercised by `wave7-smoke.sh:5852-5900` with a planted synthetic marker. Filter items 1 and 2 both apply. |
| L2-N04 | plugins/edm/bin/edm-state:802-804 | `lifecycle_mode == "partial"` handling for an enum value removed by D12/EDMV3-T59. Unreachable for anything `set-mode` can write today, but reachable from a state file written before the removal, which is precisely the documented C-4 read-compat intent. |
| L2-N05 | plugins/edm/evals/score-artifacts.sh:186 | `score_from_ratio`'s `if (d <= 0) { printf "%d", 0; exit }` cannot fire from any of the six current call sites (dim1's three denominators are all proven >= 1, dim2 and dim3 both early-return on a zero denominator, dim4's `denom` is >= 1 per the CA-393 comment, dim5 guards `bigger -eq 0` separately). Retained: it is a general helper whose documented contract at `:181` names the behavior, and it prevents an awk divide-by-zero for a future caller. Deliberately NOT the CA-393 shape -- that was a caller-side skip that contradicted a stated design intent; this is a library guard. |
| L2-N06 | plugins/edm/bin/edm-state:1033 | `[[ -n "$f" && -f "$f" && -r "$f" ]]` -- the `-n "$f"` conjunct can never be false; the loop's three literals are all non-empty, and the third degrades to `/.claude/settings.json` when `HOME` is unset. Zero-cost dead conjunct. |
| L2-N07 | plugins/edm/evals/score-artifacts.sh:225 | `[[ -z "$max_num" || "$max_num" -le 0 ]]` -- the `-z` half is unreachable (`unique_ids` is proven non-empty at `:208`, and the sed at `:222` emits a line per input line), but the `-le 0` half IS reachable via a `#### PREFIX-00` heading, so the statement is not dead. |
| L2-N08 | plugins/edm/bin/tests/run-all.sh:145-148 | Both arms of `if [[ "$_suite_status" == "PASS" ]]` open with a byte-identical `printf`. Nothing is unreachable -- this is duplication that should collapse into one unconditional printf plus a `!= "PASS"` block. L4's call, out of L2's mandate. |
| L2-N09 | plugins/edm/evals/score-artifacts.sh:484; .gitlab-ci.yml:389, :406 | `${md_count:-0}` / `${count:-0}` / `${orphan_count:-0}` fallbacks after a `grep -c` that always prints a digit. Same class CA-140/CA-202/CA-260 already accepted in-tree and that `run-eval.sh:193-196` documents as deliberately omitted. `score-artifacts.sh:484` additionally stays reachable via grep exit 2 on a present-but-unreadable file. |
| L2-N10 | plugins/edm/bin/edm-init:121 | The `[[ -d "$DIR" ]]` collision guard is preempted for both layouts by `edm-validate-prefix` (`:52-54` flat, `:59-71` product-scoped), but that call is itself conditional on `command -v edm-validate-prefix` (`edm-init:81`), so the guard is reachable on a PATH without it, and it also closes a real TOCTOU window. |
| L2-N11 | .gitlab-ci.yml:58, :80 | Cross-lens, not L2: `lint:hooks-shell` (CA-380) landed after round 6 verified these counts, so "ten consumers total (`grep -c '<<: \*alpine_edm'`)" is now 11 and "seven lint jobs total today" is now 8. Mirrored at `plugins/edm/CLAUDE.md:1036` ("all ten consumers"). L6/L7's call. |

## Sweep coverage notes

Function-definition/call analysis was run across all of `plugins/edm/bin/` (non-test): every function in `edm-check-grants` (23), `edm-lint-artifacts`, `edm-check-vocabulary`, `edm-sync-canonical-sections`, `edm-compare-eval`, `edm-check-skill-sync`, `edm-init`, `edm-validate-prefix`, `_edm-cli-lib.sh` and `_edm-lint-lib.sh` has at least one live caller **except** `mermaid_line_set` / `marker_line_set` (L2-N02). In `edm-state` I traced the helper layer (`to_int`, `read_bool`, `schema_at_least`, `present_or_absent`, `phase_name_for`, `artifact_hash`/`record_artifact_hash`, `human_cost_for_phase`, `_unpack_token_fields`, `format_anomaly_line`, `git_aware_mv`, `skipped_phases_str`, `_restore_trap`, `_print_line`, `_git_lock_age_bucket_label`, the pattern-library trio) -- all reached. Also swept: `hooks/hooks.json` (all 9 command hooks, including the PreToolUse path-normalization chain -- the `""|.` and `*/..` arms are both reachable), `monitors/monitors.json`, `.gitlab-ci.yml` (all 13 jobs), `bin/tests/run-all.sh`, `bin/tests/timing.sh`, `skills/code-audit/SKILL.md` step 10's three-branch gate (verified exhaustive over P0/P1/P2), and my own agent definition (the `## JSONL Line Format` section is present and matches the task's fallback schema, so no D22/CA-130 cache staleness this round).
