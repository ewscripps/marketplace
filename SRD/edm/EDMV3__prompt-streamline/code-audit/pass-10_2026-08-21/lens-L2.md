# Lens L2: Dead Code & Unreachable Paths -- Round 10 (full)

Delivered inline: my runtime tool set this round was Read, Grep, Glob, WebFetch, WebSearch, TaskStop -- no `Write`, no `Bash`. See N01.

## Prior-round L2 ledger re-verification

Every ledger entry naming L2 was cross-checked. All L2-tagged entries carry `status:"fixed"` or `status:"noted"` except one: **CA-490**.

**CA-490 -- STILL OPEN, verbatim reproduction, no change since round 9.** `plugins/edm/bin/edm-compare-eval:61-65` still tests the committed baseline FIRST and returns exit 3 ("NOT ARMED") before refusal condition 3 (the `complete != "true"` check) is reached at `:70-78`:

```
61  if [ ! -f "$BASELINE" ]; then
62    echo "edm-compare-eval: no committed baseline at $BASELINE" >&2
...
64    exit 3
65  fi
...
74  if [ "$cand_complete" != "true" ]; then   # refusal condition 3 -- below the early return
```

`plugins/edm/evals/baseline/scores.json` still does not exist anywhere in the tree (a `**/scores.json` glob returns nothing; `plugins/edm/evals/baseline/` holds only `README.md`). `:56` resolves `BASELINE` to that default path for the one-argument form, which is the only form CI uses. So all three of the refusal conditions this script's own header documents at `:18-25` -- including condition 3, which reads nothing from the baseline -- remain unreachable from any pipeline runnable at HEAD, and the two remediation comments asserting the outcome (`.gitlab-ci.yml` eval:nightly comment; `plugins/edm/CLAUDE.md`'s "visibly refused by edm-compare-eval" line) still describe a path that cannot execute. Fix unchanged and still one small reorder: move `:67` (candidate JSON validation) and `:70-78` (condition 3) above `:61-65`. Round 9's prescription was not applied; do not close on the c3467cb batch.

**CA-451, CA-472, CA-434, CA-393, CA-394, CA-395, CA-460 -- re-confirmed fixed, no regression.** `_edm-lint-lib.sh:103-107`/`edm-lint-artifacts:207-208` still carry the one-shot `_BLC_PROG_CACHE`/`_MSA_PROG_CACHE` form with no dead branch; `assert_absent_with_control` exists nowhere in `bin/tests/`; `_edm-lint-lib.sh:229-232`'s arity `else` is CA-290's accepted arm; the two `gated_phase == "null"` caller-side guards remain deleted with their deletion record in place.

**CA-201's G41 deletion records both hold.** `edm-check-grants:103-106` and `edm-check-vocabulary:184-188` still carry the explicit "no further guard here, it would be unreachable" comments and the guards are still absent.

## Findings (L2: Dead Code & Unreachable Paths)

| ID | File:Line | Why unreachable | Delete or fix |
|----|-----------|------------------|----------------|
| L2-001 (**CA-490, STILL OPEN -- re-open, do not close**) | `plugins/edm/bin/edm-compare-eval:61-65` (vs `:67`, `:70-78`, `:80-97`) | Unchanged from round 9. The `[ ! -f "$BASELINE" ] -> exit 3` early return sits above all three documented refusal conditions, and the default baseline (`:56`) does not exist in the tree, so on the only automated caller (`.gitlab-ci.yml` eval:nightly, one-argument form) the script always exits 3 and refusal conditions 1, 2 and 3 are environmentally unreachable. Condition 3 needs nothing from the baseline. CA-462's `SCORER_VERSION` bump makes conditions 1 and 2 newly relevant, and they are starved by the same return. | **Fix (reorder, do not delete).** Move `:67` and `:70-78` above `:61-65`. Exit 3 keeps its meaning for a *complete* candidate with nothing to compare against. If the reorder is rejected, qualify both remediation comments with "once a baseline is committed". |
| L2-002 | `plugins/edm/bin/edm-sync-canonical-sections:120` | `mkdir -p "$(dirname "$DST")"` can never create anything. `:81` already ran `tmp="$(mktemp "${DST}.tmp.XXXXXX")"` **inside that same directory**, under `set -euo pipefail` (`:39`): if `${PLUGIN_ROOT}/docs` did not exist, mktemp fails and the assignment aborts the script 39 lines earlier. By the time control reaches `:120` the directory provably exists, so the guard is a preempted no-op -- the same "coercion/guard ordering leaves the guard dead" class this project has filed and **deleted** five times (CA-140 schema_at_least, CA-202 .gitlab-ci.yml:245, CA-260 run-eval.sh:188, CA-309 timing.sh:327, CA-362 run-eval.sh:616-624). No comment anywhere claims it is a deliberate safety net. | **Fix, do not delete: move it.** Relocate `mkdir -p "$(dirname "$DST")"` to just above `:81`. That makes it load-bearing (the script can then regenerate into a missing `docs/`, which is what the line was clearly written to allow) instead of unreachable, and it is a one-line move. Deleting it is the acceptable alternative and matches the five precedents. |

## Noted / Not Actionable

| ID | File:Line | Rationale |
|----|-----------|-----------|
| L2-N01 | `plugins/edm/agents/edm-audit-dead-code.md:5` | **CA-130 reproduces for a TENTH consecutive round, both halves, at this lens.** (a) Tool set: the frontmatter grants `Write` (and `LS, NotebookRead, TodoWrite, KillShell, BashOutput`); my delivered runtime set was Read, Grep, Glob, WebFetch, WebSearch, TaskStop -- no Write, so neither output file could be created and both halves are transcribed by the launching agent. (b) Stale definition: my delivered prompt is missing the on-disk file's `## Scope` (`:17-19`), `## Output` (`:54-64`), the `## Output Format` table example (`:70-82`) and the whole `## JSONL Line Format` section (`:84-109`); the task-restated schema was used as the authoritative fallback per D22/CA-130. Not a repository defect -- it is CA-130 and (repository-side) CA-193. |
| L2-N02 | `plugins/edm/bin/edm-lint-staged-artifacts:57` | Carry-forward of pass-9 L2-N05 / pass-8 L2-N06, unchanged: `phys_repo_root="$repo_root"` is a provable dead store -- all three continuations of `:58`'s `[ -n ... ] && A || B` reassign it before any read. Zero-cost defensive initialization in CA-436 code. Same disposition, do not escalate. |
| L2-N03 | `plugins/edm/bin/_edm-lint-lib.sh:214-222` | Carry-forward of pass-9 L2-N07 / pass-8 L2-N02, unchanged: `mermaid_line_set` and `marker_line_set` still have zero callers tree-wide (only `ignored_line_set` has external consumers -- edm-check-grants, edm-check-vocabulary, edm-state's pattern machinery). CA-200 closed this by making the docstring honest, and `:51-65` now states the real position explicitly ("remain for API completeness ... not because a caller currently needs them"). False Alarm Filter item 2. |
| L2-N04 | `plugins/edm/bin/_edm-lint-lib.sh:229-232` | `report_violation`'s "expected 4 or 5 args" arm still cannot fire from any static call site. CA-290, accepted as deliberate arity defence on a shared function with two incompatible field orders. Do not re-file. |
| L2-N05 | `plugins/edm/bin/edm-state:3876-3880` | `cmd_gate_check`'s unknown-gated-command `*)` arm is unreachable from every shipped caller -- all five UserPromptExpansion command hooks pass a hardcoded literal (`srd`, `audit-srd`, `tickets`, `audit-tickets`, `implement`) that the arms above cover, and `plan` is handled at `:3867-3871`. It is a public-CLI input-validation arm (operators can invoke `edm-state gate-check` directly), so Filter item 1 and the same reasoning CA-451 used to *retain* `gated_phase_for_gate`'s `*)` arm apply. Keep. |
| L2-N06 | `plugins/edm/bin/edm-check-vocabulary:199` | `[[ "$mode" == "$tline" ]] && mode="literal"` is unreachable against the shipped `vocabulary-prohibited.txt`, whose seven data lines all carry a `word:`/`literal:` prefix. But that file is explicitly **data, not code** (AC3, stated at `vocabulary-prohibited.txt:11-12`) and is designed to be extended with no code change, so a future bare-token line makes the arm live. Filter item 1. Same disposition for `is_allowlisted`'s `[[ "$asub" == "$aline" ]] && asub=""` at `:141`, unreachable against the shipped allowlist (every data line carries a `\|`) but a malformed-line tolerance on a data file. |
| L2-N07 | `plugins/edm/bin/edm-check-grants:546-551` | The `if [[ "$_class" == "Edit" ]]` arm sets `_has_instruction=0` and the `else` arm reaches the same value by construction: `${WORKDIR}/Edit_instructed.txt` is never created (`:241-242` create only the Write and Bash files), so the `grep -qxF` in the else would exit 2 and leave the variable at 0 regardless. Behaviourally redundant, but the arm **executes** (not unreachable), it is explicitly documented at `:530-537` as the intentional construction that surfaced the synthesizer's unexplained Edit grant, and it hedges against a future `Edit_instructed.txt` producer. Filter items 1 and 2. L10's call if anyone wants it merged. |
| L2-N08 | `plugins/edm/bin/edm-check-grants:500`, `:513` | Two dead stores: `[[ -z "$ln" ]] && ln="$body_start"` and `[[ -z "$task_ln" ]] && task_ln="$body_start"` can only fire when `needs_ask`/`needs_task` is 0, in which case the variable is never read (the guarded `if` at `:502`/`:515` is false). When the flag *is* 1, the `grep -n` over the whole file necessarily matched (the body is a subset of the file), so the value is already non-empty. Never affects a reported line number. Free hedge, correct in direction; same disposition as L2-N02. |
| L2-N09 | `plugins/edm/bin/edm-check-grants:459-461`, `:463-465` | `while IFS= read -r ln; do assert_agent_grant ... "$lnum"; done` -- `ln` is read and never used (the citation deliberately uses the hooks.json physical line `$lnum`, per the `:436-441` rationale). The loop still needs to iterate once per detected instruction, so this is an intentionally unused loop variable, not a dead path. |
| L2-N10 | `plugins/edm/bin/edm-init:146-159` | The `standard|iac|data-ml)` and `mini-srd)` case arms have byte-identical bodies (`mkdir -p "$DIR/code-audit"`), so the split is commentary only -- `tickets/` is created on demand by phase 4 in both. Adjacent to CA-054's class (an identical case arm the project chose to merge), but here the two comments carry real, distinct mode documentation and nothing is unreachable: both arms execute. L4/L10's call; recorded so it is not re-derived. The `prototype)` empty arm is load-bearing (the `case` has no `*)` and `MODE` is pre-validated at `:59-62`). |
| L2-N11 | `plugins/edm/bin/edm-init:125-127` | Carry-forward of pass-9 L2-N04 -- the `[[ -d "$DIR" ]]` collision guard is preempted by the unconditional `edm-validate-prefix` call at `:87` in **both** layouts, but the surviving leg still holds: it narrows a real TOCTOU window that includes the unbounded interactive `read` at `:106`. Keep; do not re-file. |
| L2-N12 | `plugins/edm/bin/edm-init:212` | Not my lens, flagged for L6: the post-checkout-correction comment cites "`edm-state init` (line 139, above)" and the call is actually at `:165`. A second stale line-number citation in the same file whose `:173-174` comment records that exactly this class of citation had already drifted once and was re-pointed by name. Cheap fix: re-point by name. |
| L2-N13 | `plugins/edm/bin/edm-check-skill-sync:64` | `[ -f "$f" ] || continue` inside the eight-phase loop cannot fire today -- all eight of `plan srd audit-srd tickets audit-tickets implement code-audit verify-runtime` exist under `skills/`. Deliberate: skipping a *missing* phase skill is the correct behaviour for a tripwire whose job is to detect a procedure that *moved*, and the `--check`-style hard failure belongs to `validate:manifest`, not here. Filter item 1. |
| L2-N14 | `plugins/edm/bin/edm-lint-artifacts:392-429` | Class 4's mermaid loop still omits the `UNREADABLE_FLAGS` guard its four siblings carry, and is still behaviourally equivalent (`MERMAID_SETS[$i]=""` at `:303` plus `[[ -z "$mermaid_set" ]] && continue` at `:395`). Already accepted in the CA-376 bundle; do not re-file. |
| L2-N15 | `plugins/edm/bin/edm-lint-artifacts:137` | The first-stage single-file `trap` is fully replaced by the four-signal two-file traps at `:149-152`, so its body never runs in the normal path -- but it is exactly the CA-447/L8-006 remediation: it covers the window where the *second* `mktemp` at `:144` dies with the first file already on disk. Reachable, load-bearing, documented at `:134-136`. Not a finding. |
| L2-N16 | `plugins/edm/bin/edm-state:3859-3932` with `plugins/edm/hooks/hooks.json:19`, `:32`, `:45`, `:58`, `:71` | Positively verified rather than filed. The CA-409 rewrite of all five UserPromptExpansion command hooks to `edm-state gate-check ...; ec=$?; if [ "$ec" -eq 3 ]; then exit 2; fi; exit 0` does **not** create an inert enforcement layer: `cmd_gate_check` returns `$GATE_CHECK_REFUSED` at `:3914` (gate not approved) and `:3926` (Gate 3.5 compliance), and that constant resolves to 3 -- so the blocking `exit 2` arm is genuinely reachable, and the non-refusal non-zero paths (`die`/`require_jq`/`read_state`, `:3879`'s unknown-token `return 1`) correctly fall through to `exit 0`. Recorded because a rewrite of a blocking hook onto a single magic status is precisely the shape that produces an environmentally dead enforcement arm, and it did not this time. |

## Sweep coverage notes (honest, per CA-377's precedent)

Finalized early on coordinator instruction, mid-sweep. **Fully read this round:** `bin/edm-compare-eval`, `bin/edm-sync-canonical-sections`, `bin/edm-lint-artifacts`, `bin/edm-lint-staged-artifacts`, `bin/edm-check-grants`, `bin/edm-check-vocabulary`, `bin/edm-check-skill-sync`, `bin/edm-init`, `bin/edm-validate-prefix`, `bin/_edm-cli-lib.sh`, `bin/_edm-lint-lib.sh`, `bin/vocabulary-prohibited.txt`, `bin/vocabulary-allowlist.txt`, `hooks/hooks.json`, `monitors/monitors.json`, `agents/edm-audit-dead-code.md`. **Partially read:** `bin/edm-state` (full command-function index; `:3859-3978` in detail for the gate-check/exit-3 contract). **NOT AUDITED this round -- report as unaudited, not clean:** `bin/edm-state` outside `:3859-3978`, `bin/edm-mermaid-rules.awk`, all of `bin/tests/**` (seven smoke suites, `_harness.sh`, `run-all.sh`, `timing.sh`), all of `evals/**` except the CA-490 cross-references, `.gitlab-ci.yml`, `skills/**/SKILL.md`, `agents/*.md` other than my own, `docs/**`, `plugins/edm/CLAUDE.md`, `README.md`, `CHANGELOG.md`, repo-root `CLAUDE.md`. Round 11 should treat `bin/edm-state`, `bin/tests/**` and `evals/**` as the highest-value L2 re-sweep targets. The 2313-assertion suite figure quoted in my task is orchestrator-supplied; I had no `Bash` and did not verify it (CA-331/CA-377 standing recommendation applies again).
