# Lens L9 -- Spec & Ticket Compliance (Round 10, full)

**Initiative:** EDMV3 -- prompt-streamline
**Subject:** `plugins/edm/` (EDM plugin v3.2.0)
**Spec inputs read:** `SRD/edm/EDMV3__prompt-streamline/srd.md` (v1.3.0, EDMV3-01..EDMV3-120), `SRD/edm/EDMV3__prompt-streamline/tickets/README.md` (68 tickets), `tickets/epics/01..11`, `tickets/audit.md`
**Ledger read:** `SRD/edm/EDMV3__prompt-streamline/code-audit/findings-ledger.md` (523 entries) + `.jsonl`

### Delivery caveat (must be recorded, per CA-130 / CA-388)

This lens agent was delivered with **Read, Grep, Glob, WebFetch, WebSearch, TaskStop only** -- no `Write`, no `Edit`, no `Bash`. Consequences for this round, stated so the degradation is measurable rather than anecdotal:

1. The prescribed output files `code-audit/pass-10_2026-08-21/lens-L9.md` and `lens-L9.jsonl` **could not be written by this agent**. Both halves are delivered inline below and must be transcribed by the orchestrator. This is the 8th+ consecutive round of the CA-130 condition and the 2nd consecutive round in which the JSONL half of the lens contract depends on orchestrator transcription (CA-388).
2. No command was executed. Every `Verify:` clause in every AC was checked by **static evidence** (grep/read of the shipped test labels, shipped code and shipped docs), never by running the cited command. Where an AC's satisfaction depends on runtime output (e.g. T23 AC3's `jq -e` expression, T51 AC1's live `duration_seconds`), this lens confirms only that the mechanism and its cited case label exist, at **medium** confidence.
3. Coverage of this sweep: complete requirement-by-requirement pass over the SRD Coverage Map (all 120 requirements, all 68 tickets' `SRD Refs`), complete AC-level pass over the tickets named by the round-brief (T22, T23, T27, T28, T51, T66, T68) and over every ticket implicated by an open or just-closed L9 ledger entry. Remaining epics were swept at the coverage-map/ownership level rather than AC-by-AC.

---

## Verdicts on prior open L9 ledger findings

| Ledger ID | Prior sev | Verdict this round | Evidence (file:line) |
|---|---|---|---|
| CA-416 | P1 open | **FIXED** (with a new residual, see NEW-L9-01) | `plugins/edm/agents/edm-audit-synthesizer.md:122-130` (remediation-format row "Spec/AC text to sweep in the same commit"), `:166` (`spec_swept` in the ledger template line), `:181-184` (field rules: `yes` / `n/a` / `no`); machine enforcement shipped: `plugins/edm/bin/edm-state:1597` (`SPEC_SWEEP_DEBT_FILTER`), `:1841` (`info SPEC_SWEEP_PENDING` validate anomaly), `:2359` (approve-gate refusal, explicitly not waived by `--accept-p2-debt`), `:4767` and `:4802` (audit-converged refusal + appended debt block); tests `plugins/edm/bin/tests/wave7-smoke.sh:8755-8910` (fixed+no refuses and names the ID; `yes` and `n/a` both pass; C-4 absent-field grandfathering; open+no does not block; multi-ID naming; `--accept-p2-debt` does not waive; validate anomaly). |
| CA-424 | P1 open | **FIXED** | `SRD/edm/EDMV3__prompt-streamline/tickets/epics/04-structured-findings.md:547-576` -- T28 AC12 now enumerates all three input classes (i) flag-absent refuses and records nothing, (ii) flag-present non-severity refusal still refuses, (iii) flag-present open-P2-only converges with the five debt fields. Every cited case label verified verbatim in the shipped suite: `plugins/edm/bin/tests/wave6-smoke.sh:3434` ("T28 AC12 -- approve-gate refuses on an open P0"), `:774`, `:786`, `:694`, `:697`, `:700`, `:677`. |
| CA-475 | P1 open | **FIXED** (both halves) | AC half: `tickets/epics/03-ci-and-fixture-eval.md:518-528` (AC1 "exactly six", verify `length == 6`), `:529-541` (AC2 dimension-6 definition + `--describe` prints six), `:553-563` (AC4 refusal on `dimensions_scored` 5 vs 6), `:578-589` (AC8 rewritten as a **five**-dimension wave-A baseline, `dimensions_scored: 5`, `dimensions_skipped | length == 1`, verify greps `'five-dimension'`), `:590-595` (AC9 six per-dimension ranges, dim 5 N/A), `:596-601` (AC10 "six dimensions are proxies"), `:612-624` (AC13 `dimensions_scored == 5`). New-field ownership landed on T22: `:438-447` AC13 owns `srd_match`, `fixture_version` and `run.json.fixture`. Doc half: `plugins/edm/evals/baseline/README.md:50-52` (confirm `dimensions_scored: 5`), `:57` heading "Why five scored dimensions, not six", `:72` "five-dimension baseline", `:86-94` variance table with a dimension-6 row, `:105-116` `per_dimension_range` example including `known-gap-recall`, `:118-123` `total_range` pinned to the comparer's jq path, `:127-130` "Six mechanical dimensions -- five of them scored". Test half re-baselined: `plugins/edm/bin/tests/wave7-smoke.sh:832` and `:836` now assert the `five-dimension` literal. |
| CA-483 | P2 open | **FIXED** | Repo-root `CLAUDE.md` "Current Plugins" now reads `**edm** (v3.2.0)`; its 14-command inventory matches the 14 on-disk `plugins/edm/skills/*/SKILL.md` files. Durability pin landed: `plugins/edm/bin/tests/wave7-smoke.sh:8940-8962` asserts the repo-root CLAUDE.md `edm` version token equals `plugin.json`'s. Ticket half landed: `tickets/epics/11-cross-cutting-delivery.md:645-653` -- T66 AC1 amended to name the current version rather than a frozen `3.1.0` literal. |
| CA-484 | P2 open | **FIXED** | `plugins/edm/evals/score-artifacts.sh:4-5` now reads "exactly six dimensions (five as originally specified plus known-gap-recall, added by CA-462 ...; `--describe` below is the authority on the set)", consistent with `:24`, `:40-43` and `DIM_NAMES`. |
| CA-508 | P2 open | **FIXED** -- all nine pack statistics swept, and the Won't-Have convention conflict resolved | `tickets/README.md:12` (68, `EDMV3-T01 .. EDMV3-T68`), `:232` (E4 = 11 tickets, `B (10), C (1)`), `:240` (Total 68; the per-epic column sums 4+14+5+11+6+5+5+4+3+4+7 = 68), `:257-266` (XS 4 / S 32 / M 26 / L 6, arithmetic `4 + 32 + 26 + 6 = 68`), `:384` (`T68["T68 accept-p2-debt convergence"]` node inside the wave-C subgraph), `:512` (`T28 --> T68` edge), `:523` (T68 in the `waveC` class line), `:529-531` ("All 68 nodes ... 24 + 20 + 18 + 2 + 3 + 1 = 68" -- re-counted the three class lines by hand: 24/20/18 correct), `:548` ("124 edges over 68 nodes" -- re-counted the edge block at `:387-432`, `:434-476`, `:478-512` = 46 + 43 + 35 = 124), `:589-591` (coverage map spans T01..T68), `:750-755` (union check: 115 IDs plus the single `EDMV3-90 (amended)`, all 68 tickets). Convention conflict closed by the new `:601-607` "The one sanctioned exception (D57/D58, post-Gate-3)" paragraph plus `:742-748`, which keep the Won't-Have row at 0 covered / 5 boundary and state that `(amended)` is the only admissible form. |
| CA-509 | P2 open | **STILL OPEN -- confirmed unchanged** | `srd.md:9` is still Version `1.3.0`; the Revision History `:20-25` ends at 1.3.0 with **no** CR entry for D57/D58. `srd.md:3829-3855` EDMV3-90 is byte-unchanged: AC1 at `:3836-3837` still asserts that no "`--force`, `--accept-partials`, `--skip-checks`, `--yes`, or equivalent bypass flag exists on `phase-complete`, `archive`, `approve-gate`, or `audit-converged`"; AC4 at `:3846-3848` still bans a recorded-exemption category. A repository-wide grep of `srd.md` for `D57`, `D58`, `accept-p2-debt` and `T68` returns **zero matches**. Meanwhile `tickets/README.md:601-607` and `:700`, and `tickets/epics/04-structured-findings.md:1215`, `:1222` and `:1260-1261` all assert the amendment exists. Two governing artifacts still disagree, with the higher authority (the SRD) the one that is wrong on the record. |
| CA-510 | P2 open | **STILL OPEN -- confirmed unchanged** | The gate still ships: `plugins/edm/bin/edm-state:4522-4558` (the CA-471 completeness block, `ca471_downgrade="partial"` at `:4558`, written into the round record at `:4577`/`:4585`), and `plugins/edm/skills/code-audit/SKILL.md:104` ("**refuse to proceed**") plus `:147-152`. Neither candidate owner gained an AC: T51's criteria are still AC1..AC10 (`tickets/epics/08-economics-honesty.md:145-186`) with **no** round-type or completeness AC, and its Out of Scope at `:203` still reads "`round_type` recording -- EDMV3-T27 (wave B)"; T27's criteria are still AC1, AC1a, AC2..AC6, AC8 (`tickets/epics/04-structured-findings.md:375-423`) with round_type derived at **round-start only** (`:375-378`), and its Out of Scope at `:445` still reads "`audit-round-complete` and per-round cost -- EDMV3-T51 (wave C)". The mutual disclaim is intact, so a convergence-blocking refusal still has neither the positive nor the negative AC that `tickets/README.md:66-68` mandates. |

---

## Findings (L9: Spec & Ticket Compliance)

### Missing Implementations (P1)

| Requirement | Ticket | What's Missing | Evidence (search results) |
|---|---|---|---|
| EDMV3-90 (Won't Have, "no override flags"), as amended by D57/D58 | T68 (`SRD Refs: EDMV3-90 (amended, D57/D58)`) | The **SRD-side amendment paperwork**. The ticket pack asserts an SRD amendment that does not exist in `srd.md`. `srd.md` is still v1.3.0, EDMV3-90 AC1/AC4 are byte-unchanged and still forbid the shipped flag, and the Revision History carries no CR entry. This is the ledger's CA-509, re-confirmed open with no movement in round 10. Not a false alarm: the pack's own amendment route (CR1-CR6, `tickets/README.md:799-809`) requires a versioned `srd.md` change, and every prior amendment took it. | `srd.md:9` (Version 1.3.0); `srd.md:20-25` (Revision History ends at 1.3.0); `srd.md:3836-3837` (AC1, unchanged); `srd.md:3846-3848` (AC4, unchanged); zero grep hits for `D57|D58|accept-p2-debt|T68` in `srd.md`; contradicted by `tickets/README.md:601-607`, `tickets/README.md:700`, `tickets/epics/04-structured-findings.md:1215`, `:1222`, `:1260-1261` |

### Partial Implementations (P1)

| Ticket | AC | What the Spec Requires | What Code Does | File:Line |
|---|---|---|---|---|
| T27 / T51 (neither owns it) | *No AC exists* -- the gap **is** the finding | `tickets/README.md:66-68`: "Every ticket that adds or changes a gate, a refusal, an allowlist, or a permission boundary carries at least one positive AC (the permitted case succeeds) and at least one negative AC (the forbidden case is refused, and nothing mutates)." | `cmd_audit_round_complete` downgrades `round_type` to `partial` on missing/empty/unparseable lens JSONL, which `cmd_audit_converged` then treats as permanently non-convergent, and `skills/code-audit/SKILL.md` adds a refuse-to-proceed gate. Both candidate owners explicitly disclaim it in their Out of Scope sections. Ledger CA-510, unchanged. | `plugins/edm/bin/edm-state:4522`, `:4558`, `:4577`; `plugins/edm/skills/code-audit/SKILL.md:104`; disclaimers at `tickets/epics/04-structured-findings.md:445` and `tickets/epics/08-economics-honesty.md:203`; rule at `tickets/README.md:66-68` |
| T27 / T51 / T28 (no owner) | *No AC exists* -- **NEW-L9-01** | Same rule, `tickets/README.md:66-68`. The CA-416 remediation shipped a **second** convergence-blocking refusal plus a new `validate` anomaly class plus a new authoritative-ledger field, and repeated CA-510's structural defect one round later inside the very fix whose stated purpose was to stop remediations shipping without their spec sweep. | `spec_swept` now has: a blocking refusal in `audit-converged` (`edm-state:4767`), a blocking refusal in `approve-gate code-audit` that `--accept-p2-debt` explicitly cannot waive (`edm-state:2359`), a new informational anomaly class `SPEC_SWEEP_PENDING` (`edm-state:1841`), a new filter constant (`edm-state:1597`), a new field in the authoritative JSONL ledger schema (`agents/edm-audit-synthesizer.md:166`, `:181-184`), and a new mandatory row in the remediation-plan format (`:122-130`). A grep for `spec_swept` across `SRD/edm/EDMV3__prompt-streamline/tickets/` and `srd.md` returns **zero hits** -- it lives only in `CONVERGENCE-PLAN.md:110`, `HANDOFF.md` and the ledger. T28 AC1-AC13 (the `audit-converged` exit contract, `epics/04:518-579`) enumerate the exit conditions branch by branch and do **not** include the sweep-debt exit; T05 (`state_anomalies` informational/blocking split) does not name the new class; T25/T26 do not name the new ledger field. | `plugins/edm/bin/edm-state:1597`, `:1841`, `:2359`, `:4767`, `:4802`; `plugins/edm/agents/edm-audit-synthesizer.md:122-130`, `:166`, `:181-184`; absence: zero `spec_swept` hits under `SRD/edm/EDMV3__prompt-streamline/tickets/` or in `srd.md`; rule at `tickets/README.md:66-68`; precedent CA-510 |
| T28 | AC13 ("surfaced": the subcommand appears in the `--help` block, dispatch, and the `CLAUDE.md` `bin/` table) -- **NEW-L9-02** | AC13's `Verify:` is `edm-state --help | grep -n audit-converged`, which is satisfied by the *name* alone. AC12 was amended this round to enumerate three input classes; AC13 was not, so the AC set now documents the *behaviour* of the three-class refusal while the "surfaced" AC still only requires the bare subcommand name. The exit-code contract that `edm-state:4615-4620` documents in-code (0/1/3, plus the new sweep-debt arm folded into 1) is asserted by no AC's help/doc clause. | `--help` and the `CLAUDE.md` `bin/` table were not re-swept for the new sweep-debt refusal path added by CA-416 (`edm-state:4767`), so the operator-facing surface for `audit-converged` documents fewer refusal reasons than the code has. Partial: name is present, the new refusal class is not surfaced in the ACs' doc obligations. | `tickets/epics/04-structured-findings.md:577-579` (AC13); `plugins/edm/bin/edm-state:4615-4620` (documented exit contract), `:4767` (undocumented-in-AC refusal arm) |
| T23 | AC13 -- **NEW-L9-03** (medium confidence) | AC13's second half is explicitly recorded as **not verifiable today**: `jq -e '(.dimensions_scored == 5) and (.complete == true)' plugins/edm/evals/baseline/scores.json` "remains blocked until D23's baseline capture lands". AC8's `Verify:` first clause is the same `jq` against the same non-existent file. | `plugins/edm/evals/baseline/` still holds only `README.md`; the runbook's own `:3` heading is "Status: pending the live baseline capture" and `:6-19` records the absence as deliberate. So T23 AC8's first verify clause and AC13's second are both unsatisfiable in a clean checkout, and EDMV3-28 ("Baseline captured on wave-A code and committed") is **not delivered**. This is honestly recorded (D23/D36, and the AC text itself says "not silently passed and not faked, per D15"), which is why it is reported at P1 as a partial rather than as a spec defect -- but the round-10 record should state plainly that the *only* automated prompt-quality tripwire this initiative has is still un-armed, and that `EDMV3-28`'s Must-Have status is therefore not met. | `tickets/epics/03-ci-and-fixture-eval.md:578-589` (AC8), `:612-624` (AC13); `plugins/edm/evals/baseline/README.md:3`, `:6-19`, `:50-55`; SRD Coverage Map row `tickets/README.md:638` (EDMV3-28 Must -> T23) |

### Scope Creep (P2)

| File / Feature | Not Specified In | Recommendation |
|---|---|---|
| `spec_swept` ledger field + `SPEC_SWEEP_PENDING` anomaly + the two new blocking refusals (`plugins/edm/bin/edm-state:1597`, `:1841`, `:2359`, `:4767`, `:4802`; `plugins/edm/agents/edm-audit-synthesizer.md:122-130`, `:166`, `:181-184`; `plugins/edm/bin/tests/wave7-smoke.sh:8755-8910`) | No ticket, no AC, no SRD requirement. Zero hits for `spec_swept` under `SRD/edm/EDMV3__prompt-streamline/tickets/` or in `srd.md`. Authorized only by ledger entry CA-416 and `CONVERGENCE-PLAN.md:110`. | **Do not remove** -- the mechanism is a genuine improvement and closes the initiative's five-round stale-citation root cause. Give it an owner, exactly as CA-510 prescribes for its sibling: add a positive AC (a fixed entry at `yes`/`n/a` converges), a negative AC (a fixed entry at `no` refuses `audit-converged` **and** `approve-gate --accept-p2-debt`, and nothing mutates) and a C-4 AC (an absent field never blocks) to T28 or T25, plus a doc AC covering `CLAUDE.md`'s state/ledger field table. Reported as the same class as CA-510 rather than merged with it: different code, different round, different gate. |
| The CA-471 `round_type` downgrade in `cmd_audit_round_complete` + the code-audit skill's refuse-to-proceed gate | Neither T27 nor T51; both disclaim it in Out of Scope | Ledger CA-510, still open. Add the three-branch AC (positive / negative / C-4) to T51 and delete the contradicting Out-of-Scope line in **both** T27 and T51 in the same commit. |
| `plugins/edm/bin/edm-lint-staged-artifacts` (new file, now the delegate for the `PreToolUse` git-commit hook) | No ticket names it. T67 AC8 covers the hook's *inline* body; the extraction was authorized by ledger CA-436 only. | **NOTED, not filed as creep** -- see Noted section. Recorded here for completeness because it is a new PATH-resolved executable in the shipped surface with no ticket row; the ledger already carries its defects as CA-497/CA-499/CA-501. |

---

## Noted / Not Actionable

1. **CA-416's absence of a ticket for the remediation wave itself is not scope creep.** `findings-ledger.md` is this initiative's documented work-item source for audit remediation (CA-416's own text says so). Only the *new gate* it introduced needs an AC -- that is NEW-L9-01 above, not a complaint about ledger-driven remediation.
2. **EDMV3-86, -87, -88, -89 (Won't Have) have no implementing ticket -- correct by construction.** `tickets/README.md:696-699` records each as a scope boundary with its named negative-enforcement AC (T38 AC12, T04 AC7, none-by-construction, T14 AC3/AC11). False Alarm Filter 1 applies.
3. **EDMV3-88 (no Mermaid renderer spike) is the only requirement with zero ticket references, and that is correct.** `tickets/README.md:698`; decisions.md D8. Already dispositioned as CA-132.
4. **T27 has no AC7 and the numbering skips it.** `tickets/epics/04-structured-findings.md:433-435` records that the former AC7 moved to T48 AC8 because a wave-B ticket cannot verify a wave-C edit. Deliberate and documented -- not a missing AC.
5. **T68's ACs are checked `[x]` while the rest of the pack uses `[ ]`.** T68 retro-documents already-shipped work (`epics/04:1220-1221`), so the boxes record verified delivery. All six of its cited case labels were verified verbatim in `plugins/edm/bin/tests/wave6-smoke.sh` (`:682`, `:677`, `:774`, `:786`, `:792`, `:795`, `:713`, `:725`, `:707`, `:820`). No G40/CA-368 paraphrase defect.
6. **The `Requirement count` column in the Epics Summary does not reconcile with the tickets' `SRD Refs`.** `tickets/README.md:246-253` states this explicitly and instructs the reader not to reconcile them. Filter 1.
7. **T61's L size and the T37/T38 XL-by-arithmetic delivery unit.** Both disclosed with written justifications (`tickets/README.md:268-293`); T38 AC14's two-reviewable-commits requirement is the recorded risk control. Filter 1.
8. **Nine tickets exceed the 6-12 AC band.** All nine carry an `AC-band note` (`tickets/README.md:295-299`; e.g. `epics/03:636-640` for T23, `epics/04:591-593` for T28). Filter 1.
9. **`plugins/edm/bin/_edm-lint-lib.sh` is not scope creep.** T30 AC9 sanctions sourcing `edm-lint-artifacts`' helpers; already dispositioned as CA-175.
10. **`plugins/edm/bin/edm-lint-staged-artifacts` has no ticket row.** Necessary implementation detail of a ledger-authorized extraction (CA-436) rather than new product surface -- Filter 2. Its own defects are already filed (CA-497, CA-499, CA-501). Recommend it be named in T67 AC8's Target Components at the next sweep so the hook's delegate is a specified deliverable, but this does not block ship.
11. **`plugins/edm/monitors/monitors.json` -- not present in the tree.** `Glob` finds no `monitors/` directory under `plugins/edm/`. This matches T59 (EDMV3-84: "the implementation monitor is documented or deleted") having taken the delete branch. Not a missing implementation; the round brief's scope list names a file the pack deliberately removed.
12. **T23 AC3's exact `jq` expression and T51 AC1's live `duration_seconds` were not executed.** No Bash was delivered. Both mechanisms are present in the shipped code and the ACs' cited artifacts exist; the runtime confirmation is outstanding and is recorded here rather than claimed.
13. **CA-492's open question ("whether zero-round convergence is INTENDED is an L9 or spec question")** -- L9's answer: it is **not** currently decided by any AC. T68 AC3 enumerates the non-severity refusals (partial round, invalid ledger, numeric gate, unrecognized argument) and omits the zero-recorded-rounds case; `edm-state`'s narrowing comment omits it too. This is a sub-case of NEW-L9-01/CA-510's class (a shipped branch with no owning AC) rather than an independent finding, so it is folded in here as noted rather than filed separately -- but if the synthesizer prefers a separate ID, the actionable text is: add an AC to T68 stating whether an initiative with zero completed rounds may converge under `--accept-p2-debt`, with a case either way.
14. **Root `CLAUDE.md` is outside the enumerated per-lens scope for some lenses but in scope here** by CA-127/CA-483 precedent; its `edm` row is now correct at v3.2.0 and is pinned by `wave7-smoke.sh:8940-8962`.

---

Files the orchestrator created from the two halves above:
- `SRD/edm/EDMV3__prompt-streamline/code-audit/pass-10_2026-08-21/lens-L9.md`
- `SRD/edm/EDMV3__prompt-streamline/code-audit/pass-10_2026-08-21/lens-L9.jsonl`
