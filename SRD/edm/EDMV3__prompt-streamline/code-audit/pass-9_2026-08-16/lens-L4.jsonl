{"schema":1,"id":null,"lens":"L4","round":9,"round_type":"full","sev":"P1","confidence":"high","file":"plugins/edm/bin/tests/wave6-smoke.sh","line":877,"title":"NEW in round 9: the CA-471 lens-JSONL completeness gate survives two one-line mutations with all six of its own assertions green. (a) Moving ca471_downgrade=\"partial\" (edm-state:4469) out of the `if [[ -n \"$_missing\" ]]` guard downgrades EVERY code round with a pass directory to partial -- no initiative can ever converge -- and nothing fails, because the only fully-backed case (CA471OK) is started with --lenses L1,L2 at :875 so its round is ALREADY partial, and its sole assertion at :877 is check_absent on the warn text, never on round_type. No case anywhere completes a FULL round with all JSONL present and asserts round_type is still full afterwards; T27's three round_type=full assertions (:3234, :3251, :3259) are all taken immediately after audit-round-start, never after audit-round-complete. (b) Deleting `|| ! jq empty \"$_lens_file\"` from edm-state:4463 also leaves all six green, because the only miss fixture (:884) is an EMPTY file (`: >`), which the sibling [[ ! -s ]] catches alone. Of the three classes the gate's own comment claims at edm-state:4442 (missing/empty/unparseable) only empty is exercised -- and the motivating regression was MISSING (pass-7 shipped eleven prose reports and zero JSONL). Distinct from CA-453 and CA-425: different gate, new code this batch. Fix: add a full-round-stays-full case asserting round_type after completion, and split the miss fixture into absent/empty/unparseable lenses all named in one warn.","status":"open"}
{"schema":1,"id":null,"lens":"L4","round":9,"round_type":"full","sev":"P2","confidence":"high","file":"plugins/edm/bin/tests/wave7-smoke.sh","line":364,"title":"NEW in round 9: the CA-441 Task-grant rule added to edm-check-grants:506-517 has NO must-fail case, and its only coverage is an assertion the rule cannot influence. The established shape sits 130 lines above at t03_ac6_case (:364-396) -- copy the tree to scratch, strip the grant from one skill's allowed-tools, assert exit 1 plus the file and the class name (:388-391) -- and exercises the AskUserQuestion rule ONLY. Because CA-441's fix granted Task to all twelve skills, `needs_task=1 && ! has_tool \"$allowed\" \"Task\"` at :515 is false everywhere and mark_and_maybe_report is never reached on the live tree, so the family of `edm-check-grants exits 0` assertions (wave7:339, :2860, :3034, :3171) passes identically whether the rule works or does not exist. A mis-escaped detection regex at :509 (two alternatives, grep -qiE, non-trivial escaping) or a has_tool prefix bug would leave the rule silently protecting nothing, permanently, and the next skill added reproduces the CA-441 defect undetected. The `^Agent: edm-` alternative is line-anchored and currently carried entirely by the first alternative; no control proves it can match at all. This is CA-441's own prescription landing its code half without its test half -- the CA-453 shape at a new site. Fix: clone t03_ac6_case for the Task rule (strip `, Task` from a scratch skills/test/SKILL.md, assert exit 1 and the reported class), plus one synthetic control line per regex alternative failing by alternative name.","status":"open"}
{"schema":1,"id":null,"lens":"L4","round":9,"round_type":"full","sev":"P2","confidence":"medium","file":"plugins/edm/bin/tests/wave6-smoke.sh","line":811,"title":"NEW in round 9: the CA-426 stream-separation regression fixture positively pins that an initiative with ZERO recorded audit rounds converges via --accept-p2-debt, and no case asserts any boundary on it. PDEBT6 (:804-815) is init-ed and given a ledger with no audit-round-start and no audit-round-complete at all; cmd_audit_converged's zero-rounds arm warns on stderr and PROCEEDS, so stdout carries the `not converged: ` prefix, the override engages, and :812 asserts exit 0 while :814-815 asserts the success message. The asymmetry is the finding: CA-425's negative set added in the same batch covers a partial round (:774), an invalid ledger (:786), the wrong gate (:792) and an unrecognized argument (:795) but has NO zero-round member -- even though zero-round is the one class the CA-426 fix deliberately re-enabled. The narrowing comment at edm-state:2236 enumerates `a partial round, invalid JSONL, or invalid status line` and silently omits the arm that proceeds, and CA-471 landed in the same batch specifically to make round completeness verifiable. Whether zero-round convergence is intended is an L9/spec question; what L4 owns is that the behaviour is now locked in by a passing assertion with no companion case stating the boundary either way. Fix: rebuild the CA-426 regression on a fixture with a real completed round plus an independently-induced stderr warn so it no longer depends on the zero-round shape, then add an explicit zero-round case asserting whichever behaviour is decided, with a comment naming the decision.","status":"open"}
{"schema":1,"id":null,"lens":"L4","round":9,"round_type":"full","sev":"P2","confidence":"high","file":".gitlab-ci.yml","line":524,"title":"NEW in round 9: test:state-validate (:508-550) is a BLOCKING job that prints OK after validating nothing, with no floor. The script runs under `set -u` only (:516, deliberately POSIX-sh-safe, no -e) and `edm-state list --paths > \"$LIST_FILE\"` at :524 has its exit status discarded. If enumeration fails or returns nothing -- a broken EDM_SRD_ROOT, a relocated SRD/ tree, a layout regression in `list --paths` -- the while loop body never executes, COUNT stays 0, FAIL stays 0, and the job prints `state-validate: 0 initiative(s) checked` (:545) then `test:state-validate: OK` (:550) and exits 0. The printed count is the operator's only signal and nothing asserts it. This plugin already closed the identical class one directory over: CA-016 added the _MIN_SUITE_COUNT floor plus the missing-preferred-suite check to bin/tests/run-all.sh:70-91, whose own comment states `a zero-suite guard above catches total deletion, but does nothing if a named suite is deleted`. No equivalent floor exists here. Fix: capture and check `edm-state list --paths`'s status on the same statement, and fail the job when COUNT is 0 (or below a committed floor), naming enumeration as the cause instead of printing OK.","status":"open"}
{"schema":1,"id":null,"lens":"L4","round":9,"round_type":"full","sev":"P2","confidence":"medium","file":".gitlab-ci.yml","line":748,"title":"NEW in round 9, RESIDUAL OF THE CA-443 FIX: eval:nightly still skips scoring AND the baseline comparison silently when the run directory is absent. Both blocks (:747-753 scoring, :758-768 comparison) are `if [ -n \"$RUN_DIR\" ] ... ; fi` with NO else arm, and RUN_DIR is re-derived independently at :749 and :759 from `ls -td .../runs/*/ 2>/dev/null | head -1`. CA-443 named this exact downstream consequence (scoring and comparison skipped with no message, the job reporting success having evaluated nothing) and its remediation went entirely into run-eval.sh, clamping EDM_EVAL_KEEP_RUNS=0 to 1 -- that closes ONE cause, not the class. Any other reason the directory is absent (a changed output root, an artifact-cleanup step, a run written outside plugins/edm/evals/runs/) still yields a job that scores nothing, compares nothing, reports neither, and reaches the end without incident. Filed as a residual rather than a re-file of CA-443, matching the ledger's own precedent for CA-434/CA-435/CA-438. Held at P2 because the job carries allow_failure: true, so the loss is an advisory tripwire silently reporting nothing rather than a blocking gate. Fix: derive RUN_DIR once, assert it non-empty and that scores.json exists, and give both blocks `else echo ...; exit 1` arms so an unscored or uncompared run is a named failure.","status":"open"}
{"schema":1,"id":null,"lens":"L4","round":9,"round_type":"full","sev":"NOTED","confidence":"high","file":"plugins/edm/bin/tests/wave7-smoke.sh","line":1684,"title":"NOTED: the CA-467 positive control asserts a literal `ca467_ctl_unique -eq 2` rather than `ca467_unique + 1`, so when the real identity check already fails the control also fails with a misleading `positive control failed` message. Both fail, so nothing passes wrongly -- diagnostics only, below the reporting bar.","status":"noted"}
{"schema":1,"id":null,"lens":"L4","round":9,"round_type":"full","sev":"NOTED","confidence":"high","file":"plugins/edm/bin/tests/wave7-smoke.sh","line":1664,"title":"NOTED: _ca467_extract uses `head -1`, so a second, divergent schema copy inside the SAME file would be invisible to the identity check. Verified against the tree: each of the twelve files carries exactly one occurrence today, so the gap is latent, not live.","status":"noted"}
{"schema":1,"id":null,"lens":"L4","round":9,"round_type":"full","sev":"NOTED","confidence":"medium","file":"plugins/edm/bin/tests/wave7-smoke.sh","line":8227,"title":"NOTED: the CA-472 tripwire filters comments with `grep -v '^\\s*#'`; `\\s` is a GNU extension, so on BSD grep (macOS, the documented primary development platform) the pattern degrades to `^s*#` and filters only column-0 comments. The direction is toward a false FAILURE, never a false pass, and no indented comment in the three scanned files contains the `-f <(cat` needle today.","status":"noted"}
{"schema":1,"id":null,"lens":"L4","round":9,"round_type":"full","sev":"NOTED","confidence":"high","file":"plugins/edm/bin/tests/wave6-smoke.sh","line":869,"title":"NOTED, checked specifically because a date-shaped literal in a new fixture is the classic real-time-dependency shape: the CA-471 fixture's hardcoded `pass-1_2026-08-16` directory name is NOT a time dependency. edm-state:4453 resolves the pass directory by the glob `pass-${round_num}_*`, so the literal date never has to match the run date and the case does not rot tomorrow.","status":"noted"}
{"schema":1,"id":null,"lens":"L4","round":9,"round_type":"full","sev":"NOTED","confidence":"high","file":"plugins/edm/bin/tests/wave6-smoke.sh","line":870,"title":"NOTED (fixture realism, checked and cleared): the CA-471 manifest fixture (:870) and the T24 AC0 fixture (wave7:1620) both use the real lenses-run.txt shape -- a `Round type:` header plus one bare L<N> per line -- byte-compatible with the actual SRD/edm/EDMV3__prompt-streamline/code-audit/pass-7_2026-08-10/lenses-run.txt on disk. The gate's `^L[0-9]+$` line filter is therefore tested against a representative artifact, not a fixture shaped to suit the parser.","status":"noted"}
{"schema":1,"id":null,"lens":"L4","round":9,"round_type":"full","sev":"NOTED","confidence":"high","file":"plugins/edm/bin/tests/wave7-smoke.sh","line":8167,"title":"NOTED, deliberately NOT re-filed per the round-9 mandate and decisions.md D60: the count_matches exit-2-collapse false-pass class (CA-459, CA-401) acquired new sites this batch -- the CA-460 count captures at :8167-8168 and the CA-472 tripwire loop at :8227 all route through count_matches with no path-existence guard, so a renamed or deleted scan target reads as a passing zero. Recorded only so the round notes the documented debt set grew; no new ID requested.","status":"noted"}
{"schema":1,"id":null,"lens":"L4","round":9,"round_type":"full","sev":"NOTED","confidence":"medium","file":"plugins/edm/agents/edm-audit-synthesizer.md","line":162,"title":"NOTED: CA-416's new `spec_swept` ledger field (:162, :177-180) and CA-466's new tooling-notes.md consumer (:25, :195) both landed with no smoke assertion anywhere in bin/tests/. Prompt-surface-only changes with no executable path, and the `prescribed durability pin never landed` class is already tracked in the ledger; folded here rather than filed, since neither produces a falsely-passing test.","status":"noted"}
{"schema":1,"id":null,"lens":"L4","round":9,"round_type":"full","sev":"NOTED","confidence":"high","file":"plugins/edm/bin/tests/wave6-smoke.sh","line":774,"title":"NOTED (verification, no defect): CA-425's four new negative cases (:774 partial round, :786 invalid ledger, :792 wrong gate, :795 unrecognized argument) all use check_refuses_and_leaves_state, which proves non-zero exit AND the message substring AND state byte-identity from a single real invocation. Correct shape; the CA-425 prescription landed as specified.","status":"noted"}
{"schema":1,"id":null,"lens":"L4","round":9,"round_type":"full","sev":"NOTED","confidence":"high","file":"plugins/edm/bin/tests/wave7-smoke.sh","line":679,"title":"NOTED (verification, no defect): the CA-462 known-gap-recall dimension asserts the literal hand-computed 33 (2 of 6 ground-truth gaps) at :679-681 rather than a `!= null` shape, and pairs it with a skip-path assertion at :663 for a run not attributed to the tiny-svc fixture. This is the expected-value shape CA-039 established; no coverage gap.","status":"noted"}
