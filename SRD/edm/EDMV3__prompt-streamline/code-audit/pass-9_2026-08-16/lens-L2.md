I hit my earlier stopping point mid-sweep; here is the completed L2 report. I had no `Write` tool, so both halves are inline.

## Findings (L2: Dead Code & Unreachable Paths)

### Prior-round re-verification (Priority 1 -- the 833a06d..HEAD batches)

**CA-451 -- FIXED, correctly, no regression.** Both `== "null"` guards are gone. `/Users/darryl.porter/projects/marketplace/plugins/edm/bin/edm-state:850-852` and `:926-927` now carry an explanatory deletion record in place of the guard, and each loop goes straight from `gated_phase_for_gate "$g"` to its real test. I re-derived reachability from scratch: `gated_phase_for_gate` (`:794-796`) has arms for exactly `1|2|3`; the five call sites are `:852`, `:927` (both inside `for g in 1 2 3`) and `:5367`, `:5368`, `:5369` (literals `1`, `2`, `3`), so the `*)` arm is still taken from nowhere. No behavioural change slipped in with the deletion -- the deleted lines were mid-body, so neither loop's exit status or last-command semantics moved. The `*)` arm itself was correctly retained per CA-451's own prescription (see N08 below so a future round does not re-derive it).

**CA-426 -- FIXED, both halves.** `edm-state:2238-2239` now captures stdout only (`conv_out="$(cmd_audit_converged "$prefix")" || conv_ec=$?` -- the `2>&1` is gone), with the rationale recorded at `:2229-2237`. I re-walked `cmd_audit_converged` to confirm the fix is structural, not incidental: the zero-rounds warn at `:4622` writes to stderr and falls through, and the one findings-content line reaches stdout at `:4668`, so the anchored prefix test at `:2249` now runs against pure stdout. The second half also landed: `p2_debt_breakdown_read` (`:2248`, set at `:2253`) gates the two die messages at `:2272-2276`, so the P0/P1-naming form is used only when the breakdown was actually read, and every `${conv_out:-...}` fallback now correctly says "no stdout refusal; see the audit-converged diagnostics above" for the stderr-only arms. Both new branches are reachable.

**CA-434 -- FIXED, both halves.** The tripwire at `wave7-smoke.sh:283-288` was retargeted from "no new call site" to "the definition shape stays deleted", scanning every `*.sh` in the directory with no exclusions, with the needle split across two assignments so the file cannot self-match. That is now a tripwire that a real reintroduction trips. The docstring half landed too: `_harness.sh:185` now points callers at `assert_tree_absent`, and `harness-smoke.sh:135`/`:137` name `count_matches, count_matches_strict`. One comment residue is noted at N01.

**CA-460 -- FIXED (L2's corroborating half).** Mechanically re-derived at HEAD: `.gitlab-ci.yml` has exactly 8 `^lint:` job keys (`:88, :157, :178, :197, :230, :278, :336, :384`) and exactly 11 `<<: *alpine_edm` consumers (`:90, :159, :180, :199, :232, :280, :338, :386, :439, :510, :559`). The anchor comment (`:56-62`) and the job-graph note (`:80-84`) both state eight and eleven. It is now self-maintaining: `wave7-smoke.sh:8167-8194` derives both counts and additionally asserts a CLAUDE.md CI-table row per lint job.

**CA-393 / CA-394 / CA-395 -- still fixed** (spot-checked, no regression). `score_from_ratio`'s CA-139 unreachable-negative comment survives at `score-artifacts.sh:206-208`; `assert_absent_with_control` exists nowhere in `bin/tests/` except as prose.

**New code from the batches, swept for fresh unreachability -- clean.** (a) CA-471's completeness backstop (`edm-state:4448-4472`) is genuinely reachable end to end: the pass-dir glob `pass-${round_num}_*` matches the naming `skills/code-audit/SKILL.md:54,:62` builds from `audit-round-start`'s own `N`, and the `^L[0-9]+$` filter matches the real `lenses-run.txt` shape (a `Round type: full` header plus one `L{N}` per line -- verified against both the fixture and pass-8's own manifest). The `-n "$_pass_dir"` conjunct at `:4458` is load-bearing, not dead. (b) CA-472's cached-program conversion (`_edm-lint-lib.sh:103-107`, `edm-lint-artifacts:207`, `score-artifacts.sh:325`) adds a one-shot `if` per process, no dead branch, and its tripwire at `wave7-smoke.sh:8225-8236` ships a positive control. (c) The CA-436 extraction into `bin/edm-lint-staged-artifacts` is fully reachable, including both `""|.` and the four `..` arms of `:84-90` (`EDM_SRD_ROOT="./"` reaches the empty case; `foo/..` survives the trailing-strip loop and reaches the refusal). Its exit-1/exit-2 mapping matches `edm-lint-artifacts`'s documented 0/1/2 contract (`edm-lint-artifacts:42-47`). (d) CA-450's `_timing_cleanup` (`timing.sh:42-52`) and CA-437's NUL-delimited hook extraction (`.gitlab-ci.yml:298-323`) introduce no dead branches.

### Actionable

| ID | File:Line | Why unreachable | Delete or fix |
|----|-----------|------------------|----------------|
| L2-001 (**CA-452, PARTIALLY FIXED -- core symptom survives**) | `/Users/darryl.porter/projects/marketplace/plugins/edm/bin/edm-compare-eval:58-62` (vs `:67-75`), with `/Users/darryl.porter/projects/marketplace/.gitlab-ci.yml:733-737` and `/Users/darryl.porter/projects/marketplace/plugins/edm/CLAUDE.md:1029` | The CI rewiring half of CA-452 landed and is correct: `.gitlab-ci.yml:738-746` no longer aborts on run-eval exit 4, `score-artifacts.sh` preserves `complete:false` by re-deriving it from `run.json` (`score-artifacts.sh:644`, `:657`), and `:769-775` guarantees a partial run still fails the job. **But the defect CA-452 was actually filed for is still live.** `edm-compare-eval` tests for the committed baseline FIRST -- `[ ! -f "$BASELINE" ] ... exit 3` at `:58-62` -- and `plugins/edm/evals/baseline/scores.json` does not exist anywhere in the tree (a `**/scores.json` glob returns nothing; `evals/baseline/` holds only `README.md`, whose own `:3-19` records "**As of this ticket's landing, `scores.json` does not exist yet**" as the deliberate current state). Refusal condition 3 -- the `complete != "true"` check at `:67-75`, documented at `:22` as one of the three named refusal conditions -- sits *below* that early return, as do conditions 1 and 2. So on the partial path the sole automated caller (`.gitlab-ci.yml:762`, one-argument form -> default baseline) still prints `eval comparison: NOT ARMED` and exits the arm at `:765`; the `complete:false` handshake never executes. Two remediation comments now assert an outcome that cannot occur in any pipeline runnable at HEAD: `.gitlab-ci.yml:736-737` ("edm-compare-eval **REFUSES** a complete:false candidate by name, exit 2") and `CLAUDE.md:1029` ("visibly refused by `edm-compare-eval`"). | **Fix, one small reorder.** Condition 3 reads only `$CANDIDATE` -- it needs nothing from the baseline. Move `edm-compare-eval:64` (candidate JSON validation) and `:67-75` (condition 3) *above* the baseline-existence check at `:58-62`. That makes the handshake fire today, on an un-armed tripwire, which is exactly the state CI is in and will stay in until the live baseline capture happens; exit 3 keeps its meaning for a candidate that is complete but has nothing to compare against. If the reorder is rejected, both comments must be qualified ("once a baseline is committed") so they stop describing a path that does not exist. Note this also un-blocks conditions 1 and 2, which the CA-462 `SCORER_VERSION` bump to `1.1.0` (`score-artifacts.sh:137`) makes newly relevant. |

## Noted / Not Actionable

| ID | File:Line | Rationale |
|----|-----------|-----------|
| L2-N01 | `plugins/edm/bin/tests/wave7-smoke.sh:262-270` | CA-434 residue. The retargeted tripwire is correct, but the header comment block directly above it still describes the *deleted* scan -- "Ban any NEW call site outside harness-smoke.sh", "Scoped to every `*.sh` in this directory EXCEPT harness-smoke.sh ... and this file itself" -- which the CA-434 comment three lines below (`:273-282`) and the code at `:285` both contradict (all `*.sh`, no exclusions). Comment accuracy, not unreachable code; L6's call. Fix as the last sliver of the CA-395/CA-434 cleanup. |
| L2-N02 | `plugins/edm/evals/run-eval.sh:134-138`, `:154-158` | CA-452's wiring fix chose option (a), which makes the stub's own docstring rationale obsolete: it says the stub exists "purely so CI can see complete: false ... without needing to invoke the real scorer on a run with nothing to score", and CI now *does* invoke the real scorer on the partial run (`.gitlab-ci.yml:748-753`), overwriting the stub. The stub is still genuinely useful to a human inspecting a hand-run partial, so it is not dead output -- only its stated reason is stale. L6's call; fold into the L2-001 edit. |
| L2-N03 | `plugins/edm/evals/score-artifacts.sh:569-571` (and `:555-557`) | `gap_total -eq 0` reports "pre-1.1.0 fixture shape", but `$expected` is resolved plugin-relative (`:549`, `$SCRIPT_DIR/fixtures/tiny-svc/expected.json`), never from the run directory -- the fixture and the scorer always ship together, so an *older* fixture can never be read by this code. The branch is still reachable via a corrupt or hand-edited committed `expected.json`, so it is a legitimate guard (same disposition as pass-8's L2-N05 on `score_from_ratio`); only the version-compat framing is impossible. Same for the `! -f "$expected"` arm. |
| L2-N04 | `plugins/edm/bin/edm-init:125-127` | **Rationale correction, recorded so it is not re-derived.** Pass-8's L2-N10 justified this collision guard partly on `edm-validate-prefix` being invoked behind a `command -v` test. CA-344 removed that: `:87` now invokes the validator by sibling path, unconditionally, so `[[ -d "$DIR" ]]` is preempted in *every* layout (flat via `edm-validate-prefix:73`, product-scoped via `:78-81` -- and `validate_slug` bans `/` and a leading `.`, so `$PRODUCT` can neither escape nor be `.archived`). The surviving leg still holds: the guard narrows a real TOCTOU window that includes an unbounded interactive `read` at `:106`. Keep. |
| L2-N05 | `plugins/edm/bin/edm-lint-staged-artifacts:57` | `phys_repo_root="$repo_root"` is a dead store -- all three continuations of `:58` reassign it before any read. Zero-cost defensive initialization in new CA-436 code; same disposition as pass-8's L2-N06. |
| L2-N06 | `.gitlab-ci.yml:772` | The `:-0` half of `${RUN_EVAL_EC:-0}` is unreachable: GitLab concatenates a job's `script` entries into one generated shell, so the assignment at `:739` is always in scope. A correct, free hedge against that assumption. |
| L2-N07 | `plugins/edm/bin/_edm-lint-lib.sh:214-222` | Carry-forward of pass-8 L2-N02, unchanged: `mermaid_line_set` / `marker_line_set` still have zero callers tree-wide (only `ignored_line_set` is consumed externally, by edm-check-grants, edm-check-vocabulary and edm-state). Documented at `:56-65` as retained for API completeness after `project_class` made the extension free. Filter item 2. |
| L2-N08 | `plugins/edm/bin/edm-state:795` | The `*)` arm of `gated_phase_for_gate` is now the only unreachable remnant of the CA-451 shape, and that is **by design** -- CA-451 explicitly prescribed keeping it as the helper's own out-of-range contract while deleting the two caller-side guards. Recorded here so a future round does not file it as the leftover. |
| L2-N09 | `plugins/edm/bin/edm-state:4664-4667` | CA-426 residue: the comment still says "Callers that want both ... already capture `2>&1`". After the fix only `cmd_archive:3103` does, and `cmd_approve_gate` deliberately does not. Still true of one caller, so incomplete rather than false. L6's call. |
| L2-N10 | `plugins/edm/bin/tests/run-all.sh:145-153` | Carry-forward of pass-8 L2-N08: both arms of `if [[ "$_suite_status" == "PASS" ]]` open with a byte-identical `printf`. Duplication, nothing unreachable; L4's call. The adjacent minimum-suite-count floor at `:87-91` remains CA-248 (lens disagreement resolved against L2) -- not re-filed. |
| L2-N11 | `.gitlab-ci.yml:320-323` | The `COUNT -ne EXPECTED_COUNT` cross-check cannot fire against the NUL-delimited extraction at `:298` that it accompanies -- but it is reachable under exactly the edit it guards (a revert to newline-delimited output), which is the definition of a working tripwire. Not a finding. |

## Sweep coverage notes

Full reads this round: `bin/edm-init`, `bin/edm-validate-prefix`, `bin/edm-lint-staged-artifacts`, `bin/_edm-lint-lib.sh`, `bin/edm-compare-eval`, `bin/tests/run-all.sh`, `hooks/hooks.json`. Partial/targeted: `bin/edm-lint-artifacts` (`:1-160`, `:160-430`, `:420-531` -- mode dispatch verified exhaustive over the three validated `MODE` values, both `_has_pcre_grep` branches live), `bin/edm-state` (`:788-931`, `:2210-2320`, `:2812-2872`, `:3080-3150`, `:4400-4673`, plus a full `to_int` / `schema_at_least` / `schema_version` call-site sweep hunting the recurring CA-140/CA-451 coerce-then-guard class -- no new instances), `bin/edm-check-grants:100-300`, `bin/tests/_harness.sh:170-230`, `bin/tests/timing.sh:25-85`, `bin/tests/wave7-smoke.sh:240-295` and `:8148-8263`, `evals/run-eval.sh:40-220`, `evals/score-artifacts.sh:120-240, :300-360, :505-594, :596-770`, `.gitlab-ci.yml` (all 15 job keys; `:1-120`, `:275-400`, `:437-560`, `:690-781`). Ledger cross-check: 471 entries scanned by id/sev/status/lenses/component; every L2-tagged NOTED disposition (CA-105, 107, 114, 116, 120, 121, 130, 248, 249, 290, 293, 328, 376, 377) honoured as do-not-re-file. My agent definition's `## JSONL Line Format` section could not be consulted (no `Read` access to the delivered definition was attempted this round); the task-restated schema was used as the authoritative fallback per D22/CA-130. I have no `Write` tool, so no files were created -- both halves are delivered here, as instructed.

