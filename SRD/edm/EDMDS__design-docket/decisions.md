# Key Decisions

| # | Decision | Chosen | Rationale | Date |
|---|----------|--------|-----------|------|

## Finding-to-Commit Ledger

| Finding | Source | Decision | Ticket | Status |
|---------|--------|----------|--------|--------|

## Key Decisions

| Decision | Question | Choice | Rationale | Date |
|---|---|---|---|---|
| Gate 1 | Is EDMDS's scope, constraint set and go/no-go sound enough to enter the fused SRD? | **Approve, on a standing instruction rather than a gate-time selection** | `planning.md` returns GO with three scope adjustments, all evidence-backed: CA-114 descoped from "add a bound" to "decide whether to depend on one" (its premise is disproved by measurement -- Oniguruma's retry limit bounds it, the error is attributed to the rule file, sibling rules still fire, exit code 0); CA-109 reclassified UP into Epic 1's severity class (two of three project-root resolvers accept `CLAUDE_PROJECT_DIR` unchecked, and `edm-hookify`'s own comment claiming parity is false); CA-072 reduced from five copies to three, byte-identical. The three-epic split held under direct code reading. Two constraints are recorded because they bind implementation: `run-all.sh` at or above 4048/0, and one line of headroom on `bin/edm-gateguard` against `EDMV4-T11` AC1's closed 200-660 bound | **Recorded this way because the approval basis is not a gate-time selection, and writing that down is the point.** The user instructed "go until completion" and then "continue with mini srd". Gate 1's substance -- scope, the three reclassifications, the constraint set, the go/no-go -- was reported to them in full before this was recorded, so the human was informed rather than bypassed, which is what `Gate PROTOCOL` exists to protect. **Gate 2+3 is deliberately NOT being handled this way.** That gate ratifies eighteen design decisions the user has not yet seen, and creating exactly one such checkpoint is the entire reason `mode=mini-srd` was chosen over `fix-pack`. Approving it by inference would discard the only structural benefit of the mode choice | 2026-09-08 |
| Phase 3 / D52 | `AD-DS1`'s branch decision rested on two unverified claims about host channel availability, which lane A raised as a P0 on the grounds that v1.0.0 had already failed once for asserting a channel premise without checking it. Settled by runtime spike on Claude Code 2.1.263, following D25/D26's methodology: a disposable `mktemp -d` git repository, a from-scratch `.claude/settings.json` registering one `PreToolUse` hook on `Bash`, and distinct markers written to both streams. **Result 1, exit 2: stderr IS returned to the model and stdout is NOT.** The nested session quoted `MARKER_ON_STDERR_bravo` back verbatim as the refusal it received, and explicitly noted that the stdout marker "appears solely inside the echoed command string, not as captured output". So `AD-DS1`'s load-bearing premise is confirmed by measurement -- and its secondary claim that the three exit-2 surfaces have only ONE channel is **false**: stdout is available and non-model-facing at all three. **Result 2, exit 0: neither stream appears** in `-p` output or in `--output-format stream-json --verbose`. Whether the interactive transcript surfaces them is UNESTABLISHED and is not claimed. Consequence for `AD-DS1`: the two-branch label-versus-separate table collapses into one uniform Separate decision at the three exit-2 surfaces, where the author's `message` goes to **stdout** and stderr stays EDM-authored. `json` mode remains genuinely different -- its stdout is the host's decision channel and is consumed, leaving stderr as the only stream, so the message may be invisible to everyone rather than only to the model, and EDMDS-02's cost paragraph is corrected to say so | 2026-09-10 |
| Phase 3 / D53 | `explorers/04:94` recorded "whether the retry limit is configurable per-invocation from `jq`" as not investigated, and lane A filed the omission as a P1 on the grounds that a configurable limit would let EDM pin the bound explicitly and defuse R5 entirely. Settled by one invocation on jq-1.8.1: **it is not configurable.** `jq --help` carries no limit, regex or Oniguruma flag; `ONIG_RETRY_LIMIT_IN_MATCH=100` in the environment has no effect, with the limit firing identically; and `test()` accepts only regex FLAGS (`"x"` and siblings), not engine limits. So EDMDS-01's Rejected block was complete after all, the decision to depend on the engine's own bound is the only available option rather than the preferred one, and **R5 is not defusable** -- the dependency on the engine default stands and is a permanent property of the design, not an oversight. Recorded because an unanswered question in an input document is indistinguishable from an unconsidered option, and this one is now answered. Incidental measurement for `EDMDS-01 AC4`'s named constant: the limit trips at 30 `a` characters against a non-matching suffix | 2026-09-10 |
| Phase 3 / D54 | `EDMDS-15`'s decision changed at the user's direction after lane C found the collapse may be infeasible -- the five `UserPromptExpansion` hooks read only `$ARGUMENTS`, and whether the hook input exposes the invoked command name is unverified. The requirement becomes **extract the shared body into one owner and keep the five matcher-keyed entries**, with `edm:implement`'s Gate 3.5 clause passed as a parameter. This removes the duplication CA-121 names, moots the command-name question entirely, and leaves the roughly seventeen matcher-keyed assertions (`wave6-smoke.sh:1440-1446`'s exactly-ten `gate-check` count, `wave7-smoke.sh:7746-7776`'s five-matcher loop, `:7790+`'s per-matcher execution, and `:7914`/`:7938`/`:7994`) intact rather than requiring an unowned amendment of all of them -- which was lane C's P0 against the collapse | 2026-09-10 |
| Phase 3 / D55 | Hand-enumerating the shipped assertions a requirement breaks failed three rounds running, and the v1.1.0 audit measured the shortfall: against mechanically derived counts, `EDMDS-02` named 2 dependent references where `emit_decision` has 44, `EDMDS-06` named 0 where the lens fixtures number 50, `EDMDS-07` named 7 where `audit-round-complete .* code` appears 26 times, `EDMDS-15` named 0 against 17, `EDMDS-16` named 0 against 47, and `EDMDS-19` named 0 against 33. The one requirement whose hand-enumeration was COMPLETE is `EDMDS-14`, at 4 of 4 -- and its target is a unique string literal. **Conclusion: hand-enumeration is reliable only when the target is a unique literal, and is off by three to ten times for a function name, a fixture shape or a config structure.** So v1.2.0 stops enumerating harder. Every requirement touching `bin/` derives its affected-assertion set from a committed script, and that script is an artifact the audit re-runs rather than a list the audit re-reads. Recorded as the structural cause of the recurring "a Must falsifies shipped assertions with no AC owning them" class, which the prior round raised as its own P0-5 and which v1.1.0 then reproduced in three more requirements | 2026-09-10 |
| Phase 3 / D85 | Incidental defect observed while refreshing this initiative's handoff, recorded rather than fixed because fixing it here would be scope creep the audit would rightly flag. `edm-state write-handoff EDMDS` printed `wrote ./SRD/EDMDS/HANDOFF.md` while actually writing `./SRD/edm/EDMDS__design-docket/HANDOFF.md`. The file landed correctly and no stray directory was created; the **message** is wrong. `cmd_write_handoff` (`bin/edm-state:7048`) echoes `initiative_dir_for "$1"`, which returns the flat-layout path, while the write itself goes through the product-scoped resolver that `resolve-dir` uses -- so for every product-scoped initiative the success message names a path that does not exist. Consequence: an operator following the printed path finds nothing, and an agent trusting it could create the named directory as litter or delete work at the real one. This is lens L6's class (an operator-facing message that misstates what happened) and it is the same shape as CA-134's, where the gap between the resolved path and the assumed path was the whole defect. **Recommendation**: echo the same value the write used, or call `resolve-dir`'s resolver. Not adopted into EDMDS -- `EDMDS-17 AC6` fixes the finding count at 22 and this would make 23 without a decision to widen scope. Filed here so it survives this session; a `fix-pack` initiative or the next `code-audit` round is the right home | 2026-09-10 |
| Phase 3 / D86 | This entry records a collision that happened while writing the entry above it. D85 was first filed as **D56**, which `srd.md` v1.2.0 had allocated to `EDMDS-T01` minutes earlier -- so the first out-of-band ledger write after introducing per-ticket D-number blocks collided with block one. Nobody was working in parallel; one author, one session, one file. That is worth recording rather than quietly renumbering, because the audit finding behind the blocks (nine unsequenced writers to one `decisions.md`, with no allocation) was rated P1 on the theory that parallel implementers would collide. This collision needed no parallelism at all, which strengthens the finding rather than weakening it. **Convention adopted**: D56 through D84 are reserved by `srd.md`'s ticket allocation and are not to be used by any other writer; out-of-band decisions take numbers from D85 upward. D-numbers are identifiers, not timestamps, so a non-chronological allocation is correct and intended | 2026-09-10 |
| Phase 3 / D87 | D52's spike was extended after an audit lane filed EDMDS-02 AC6 as a security regression -- the concern being that stdout at the exit-2 surfaces is the channel the host parses as control (`bin/edm-gateguard:232-234` uses it that way; `bin/edm-stop-gate:54-55` documents a raw stdout JSON echo as the failure mode for a Stop hook), while D52 had measured only plain-text markers on the `Bash` matcher. **Method**: disposable `mktemp -d` repository, from-scratch `.claude/settings.json`, and a hook that emits a control-shaped `{"hookSpecificOutput":{"permissionDecision":"allow"}}` on stdout while exiting 2 -- so a host that parses stdout would overturn its own deny. **Two false results were produced and discarded before the real one.** A `Write`-matcher test appeared to show stdout overriding the deny, and its control appeared to show the hook never firing; both were invalid because only `Write` was hooked, leaving `Bash` as an unhooked escape route the model used instead. A sentinel-file discriminator (`touch` a flag, independent of every stream and of the exit code) proved both hooks fire. Re-run with both matchers hooked and no escape route: **the deny held on both `Bash` and `Write`, the file was not created, the sentinel confirmed the hook ran, and only stderr reached the model.** So on `PreToolUse` exit 2 the host ignores stdout entirely -- it is neither model-facing nor a control channel, and the lane's concern does not hold for that path. **`Stop` is different and is NOT claimed**: the equivalent test was inconclusive, and `edm-stop-gate:54-55`'s own contract forbids stdout there, so that surface keeps the labelled-stderr shape rather than inheriting a result measured elsewhere. This is the fourth time this decision has been made and the first time it rests on a measurement taken at each surface separately rather than generalised from one | 2026-09-12 |
| Phase 3 / D88 | `AD-DS6`'s derivation mechanism was rewritten after three independent audit lanes measured version 1 as **narrower than the prose it replaced** -- EDMDS-07 derived 26 sites against 64 real references, EDMDS-10 15 against 106, EDMDS-14's own target derived 0, and EDMDS-19's target was blind to the 60 references it needed because the assertions reach the resolver through a fixture helper. One lane's phrasing is the finding: the failure moved from "the reader missed a site" to "the target cannot see the site", and the second is worse because it prints a number and reads as complete. **Two root causes, both fixed.** (1) Every search was `bin/tests/*.sh`, so product code, the 124 fixture files under `bin/tests/fixtures/`, and all 61 documentation surfaces were structurally invisible; each target now carries its own **root**, and the root is printed beside the count so a reader sees what was not searched. (2) Targets anchored on call SYNTAX rather than the callee -- `audit-round-complete [A-Z0-9]+ code` cannot match `audit-round-complete "$prefix" code`, which is exactly the `ca416_fixture` call the SRD credited the script with finding. **Measured effect**: EDMDS-07 now derives 64 rather than 26, EDMDS-14's marker target 4 rather than 0, EDMDS-19 60 rather than 6, and EDMDS-05 and EDMDS-13 gained targets they never had. Three targets that came back too BROAD were tightened in the same pass, because a target that buries its dependents in noise fails the same way as one that misses them. Two further defects were found by dogfooding the rewrite: a `|| printf 0` fallback appended a second zero and broke the arithmetic, and `baseline_for` keyed on requirement-plus-pattern so EDMDS-19's two same-pattern targets at different roots both resolved to the first row -- the second was caught by `--check` on a clean tree, which is the mode working as designed. The baseline is now emitted by `--emit-baseline` rather than hand-typed, and `--check` reports a target with NO baseline instead of skipping it, which was the one structural weakness a lane named. **The honest limit, stated because version 1's failure was that it stated none**: this finds TEXTUAL dependents, so an assertion reaching its subject through a fixture helper is found only where a target names something that assertion literally contains | 2026-09-12 |
| Phase 3 / D89 | **D87's conclusion was wrong, and this reverses it.** An audit lane observed that D87 established only that a stdout `allow` failed to OVERRIDE an exit-2 deny -- which is consistent with two different host behaviours: stdout unread, or stdout read but outranked for the decision. D87 asserted the first ("stdout is ignored by the host: not model-facing, and not control"), which is the stronger claim and was not isolated by the experiment. The lane proposed the discriminating test and it was run: emit a `permissionDecision: "deny"` on stdout carrying its own distinctly-worded `permissionDecisionReason`, write a different marker to stderr, and exit 2 -- then see which reason the model quotes. **Run twice. The model quoted the STDOUT reason both times, and in the run where both streams carried markers it reported seeing only the stdout one.** So on `PreToolUse` exit 2 the host reads stdout, surfaces its `permissionDecisionReason` to the model, and that reason takes precedence over stderr. **Consequence: there is no non-model-facing channel at any exit-2 surface.** Both streams reach the model and stdout outranks stderr, so `EDMDS-02 AC6`'s "author's `message` to stdout" was not a fix but a worse regression than the one it replaced -- the message would land on the channel the model reads *first*. The correct shape at all three exit-2 surfaces is the **label**: an EDM-authored line naming the rule, then the sanitized untrusted text beneath it, on one channel, exactly as `stop_gate_emit_blocking` already does. **That is what v1.1.0 decided, and v1.2.0 overturned it on a measurement that did not support the overturning.** This decision has now been made four times and the answer is the one it started with; what changed is that it is finally measured rather than argued. The `json`-mode row is unaffected -- there stdout carries the decision object itself and stderr is a genuinely separate stream, with its operator visibility still unestablished (R1) | 2026-09-13 |
| Phase 3 / D90 | **`control-coverage.sh` is the Goal 3 analogue of `AD-DS6`, and it is mechanical because reading did not work.** `affected-assertions.sh` derives which EXISTING assertions an edit disturbs; nothing derived whether a NEW assertion can fail. That is Goal 3 ("add no assertion that cannot fail") and CC1, and five consecutive revisions of this document shipped at least one AC that promised an assertion and named no control -- `EDMDS-17 AC5`, `EDMDS-06 AC3` and `AC3b`, `EDMDS-18 AC3`, `EDMDS-03 AC4`, `EDMDS-05 AC1`, `EDMDS-20 AC2`. Three of those were found by audit lanes reading the document, which is how the other four survived. The script flags every AC whose text promises an assertion and which names no control in its own text, in the adjacent AC, or in any AC of the same requirement that names it back; the adjacency rule is load-bearing, since this document's own convention is "ACn asserts, ACn+1 is ACn's control", and checking only self-named controls produced 19 findings, most of them that pattern. Control matching is case-insensitive: a clause written `**Controls**:` is a control, and the first version missed exactly that on `EDMDS-17 AC5`. The script is itself controlled, per the rule it enforces -- stripping the control language from `EDMDS-09 AC3` makes it report `EDMDS-09 AC2`, verified. Owned by `EDMDS-T38 AC8`/`AC9`, bound by Definition of Done item 10 | 2026-09-13 |
| Gate 2+3 | AD-DS1 -- one separation at every deny surface | Label shape at all three exit-2 surfaces, json mode separates | Settled by measurement, not argument (D89). Both streams reach the model at exit 2 and stdout OUTRANKS stderr, so there is no non-model-facing channel to move untrusted text to. The label -- an EDM-authored line naming the rule, sanitized text beneath -- is what `stop_gate_emit_blocking` already does and is machine-enforced precedent. Decided four times, and the answer is where v1.1.0 had it | 2026-09-13 |
| Gate 2+3 | AD-DS6 -- affected-assertion sets derived, not enumerated | Retained, with D88's rewritten mechanism | Round three found the v1 derivation narrower than the prose it replaced; per-target search roots, callee-name anchoring and a reproducible baseline fixed it rather than withdrawing it. Round four drew no further mechanism findings. Bound by Definition of Done item 8, owned by `EDMDS-T38 AC6` | 2026-09-13 |
| Gate 2+3 | Goal 3 -- add no assertion that cannot fail | Mechanized as `control-coverage.sh` (D90) | Seven ACs promised an assertion and named no control across five revisions; three were caught by lanes reading, which is how the other four survived. Reading finds instances, not classes. Bound by Definition of Done item 10, owned by `EDMDS-T38 AC8`/`AC9` | 2026-09-13 |
| Gate 2+3 | Initiative scope -- round three proposed splitting it and pulling four requirements | Rejected; all 22 requirements and 40 tickets carried forward | Operator instruction was to complete everything. All four blocked items were resolved rather than deferred -- `EDMDS-02` by measurement (D89), `EDMDS-15` by re-deciding on the real assertion set (D54), `EDMDS-19` by resolving its AC7/AC9/AC10 contradiction, `AD-DS6` by rewriting its mechanism (D88). Deferral is not in the canonical vocabulary in any case | 2026-09-13 |
| Gate 2+3 | Pattern-library curation, 11 entries harvested | 1 kept, 2 discarded, 8 left pending | Operator curation at the gate. Kept: the no-negative-control class, now derived by `control-coverage.sh`. Discarded: the unswept-prose class and the over-claimed-spike class | 2026-09-13 |
| Gate 2+3 | Merged gate (mode=mini-srd) | **Approve** | SRD v1.7.0 after four audit rounds: 22 requirements (16 Must, 5 Should, 1 Could), 40 tickets, 16 risk rows, every P0 and P1 closed. Document-scoped Definition-of-Done checks all at exit 0. Phases 4 and 5 recorded skipped; proceeds directly to Phase 6 | 2026-09-13 |
