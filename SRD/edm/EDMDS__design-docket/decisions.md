# Key Decisions

| # | Decision | Chosen | Rationale | Date |
|---|----------|--------|-----------|------|

## Finding-to-Commit Ledger

| Finding | Source | Decision | Ticket | Status |
|---------|--------|----------|--------|--------|

## Key Decisions

| Decision | Question | Choice | Rationale | Date |
|---|---|---|---|---|
| Gate 1 | Is EDMDS's scope, constraint set and go/no-go sound enough to enter the fused SRD? | **Approve, on a standing instruction rather than a gate-time selection** | `planning.md` returns GO with three scope adjustments, all evidence-backed: CA-114 descoped from "add a bound" to "decide whether to depend on one" (its premise is disproved by measurement -- Oniguruma's retry limit bounds it, the error is attributed to the rule file, sibling rules still fire, exit code 0); CA-109 reclassified UP into Epic 1's severity class (two of three project-root resolvers accept `CLAUDE_PROJECT_DIR` unchecked, and `edm-hookify`'s own comment claiming parity is false); CA-072 reduced from five copies to three, byte-identical. The three-epic split held under direct code reading. Two constraints are recorded because they bind implementation: `run-all.sh` at or above 4048/0, and one line of headroom on `bin/edm-gateguard` against `EDMV4-T11` AC1's closed 200-660 bound | **Recorded this way because the approval basis is not a gate-time selection, and writing that down is the point.** The user instructed "go until completion" and then "continue with mini srd". Gate 1's substance -- scope, the three reclassifications, the constraint set, the go/no-go -- was reported to them in full before this was recorded, so the human was informed rather than bypassed, which is what `Gate PROTOCOL` exists to protect. **Gate 2+3 is deliberately NOT being handled this way.** That gate ratifies eighteen design decisions the user has not yet seen, and creating exactly one such checkpoint is the entire reason `mode=mini-srd` was chosen over `fix-pack`. Approving it by inference would discard the only structural benefit of the mode choice | 2026-09-08 |
| Phase 3 / D52 | `AD-DS1`'s branch decision rested on two unverified claims about host channel availability, which lane A raised as a P0 on the grounds that v1.0.0 had already failed once for asserting a channel premise without checking it. Settled by runtime spike on Claude Code 2.1.263, following D25/D26's methodology: a disposable `mktemp -d` git repository, a from-scratch `.claude/settings.json` registering one `PreToolUse` hook on `Bash`, and distinct markers written to both streams. **Result 1, exit 2: stderr IS returned to the model and stdout is NOT.** The nested session quoted `MARKER_ON_STDERR_bravo` back verbatim as the refusal it received, and explicitly noted that the stdout marker "appears solely inside the echoed command string, not as captured output". So `AD-DS1`'s load-bearing premise is confirmed by measurement -- and its secondary claim that the three exit-2 surfaces have only ONE channel is **false**: stdout is available and non-model-facing at all three. **Result 2, exit 0: neither stream appears** in `-p` output or in `--output-format stream-json --verbose`. Whether the interactive transcript surfaces them is UNESTABLISHED and is not claimed. Consequence for `AD-DS1`: the two-branch label-versus-separate table collapses into one uniform Separate decision at the three exit-2 surfaces, where the author's `message` goes to **stdout** and stderr stays EDM-authored. `json` mode remains genuinely different -- its stdout is the host's decision channel and is consumed, leaving stderr as the only stream, so the message may be invisible to everyone rather than only to the model, and EDMDS-02's cost paragraph is corrected to say so | 2026-09-10 |
| Phase 3 / D53 | `explorers/04:94` recorded "whether the retry limit is configurable per-invocation from `jq`" as not investigated, and lane A filed the omission as a P1 on the grounds that a configurable limit would let EDM pin the bound explicitly and defuse R5 entirely. Settled by one invocation on jq-1.8.1: **it is not configurable.** `jq --help` carries no limit, regex or Oniguruma flag; `ONIG_RETRY_LIMIT_IN_MATCH=100` in the environment has no effect, with the limit firing identically; and `test()` accepts only regex FLAGS (`"x"` and siblings), not engine limits. So EDMDS-01's Rejected block was complete after all, the decision to depend on the engine's own bound is the only available option rather than the preferred one, and **R5 is not defusable** -- the dependency on the engine default stands and is a permanent property of the design, not an oversight. Recorded because an unanswered question in an input document is indistinguishable from an unconsidered option, and this one is now answered. Incidental measurement for `EDMDS-01 AC4`'s named constant: the limit trips at 30 `a` characters against a non-matching suffix | 2026-09-10 |
| Phase 3 / D54 | `EDMDS-15`'s decision changed at the user's direction after lane C found the collapse may be infeasible -- the five `UserPromptExpansion` hooks read only `$ARGUMENTS`, and whether the hook input exposes the invoked command name is unverified. The requirement becomes **extract the shared body into one owner and keep the five matcher-keyed entries**, with `edm:implement`'s Gate 3.5 clause passed as a parameter. This removes the duplication CA-121 names, moots the command-name question entirely, and leaves the roughly seventeen matcher-keyed assertions (`wave6-smoke.sh:1440-1446`'s exactly-ten `gate-check` count, `wave7-smoke.sh:7746-7776`'s five-matcher loop, `:7790+`'s per-matcher execution, and `:7914`/`:7938`/`:7994`) intact rather than requiring an unowned amendment of all of them -- which was lane C's P0 against the collapse | 2026-09-10 |
| Phase 3 / D55 | Hand-enumerating the shipped assertions a requirement breaks failed three rounds running, and the v1.1.0 audit measured the shortfall: against mechanically derived counts, `EDMDS-02` named 2 dependent references where `emit_decision` has 44, `EDMDS-06` named 0 where the lens fixtures number 50, `EDMDS-07` named 7 where `audit-round-complete .* code` appears 26 times, `EDMDS-15` named 0 against 17, `EDMDS-16` named 0 against 47, and `EDMDS-19` named 0 against 33. The one requirement whose hand-enumeration was COMPLETE is `EDMDS-14`, at 4 of 4 -- and its target is a unique string literal. **Conclusion: hand-enumeration is reliable only when the target is a unique literal, and is off by three to ten times for a function name, a fixture shape or a config structure.** So v1.2.0 stops enumerating harder. Every requirement touching `bin/` derives its affected-assertion set from a committed script, and that script is an artifact the audit re-runs rather than a list the audit re-reads. Recorded as the structural cause of the recurring "a Must falsifies shipped assertions with no AC owning them" class, which the prior round raised as its own P0-5 and which v1.1.0 then reproduced in three more requirements | 2026-09-10 |
| Phase 3 / D85 | Incidental defect observed while refreshing this initiative's handoff, recorded rather than fixed because fixing it here would be scope creep the audit would rightly flag. `edm-state write-handoff EDMDS` printed `wrote ./SRD/EDMDS/HANDOFF.md` while actually writing `./SRD/edm/EDMDS__design-docket/HANDOFF.md`. The file landed correctly and no stray directory was created; the **message** is wrong. `cmd_write_handoff` (`bin/edm-state:7048`) echoes `initiative_dir_for "$1"`, which returns the flat-layout path, while the write itself goes through the product-scoped resolver that `resolve-dir` uses -- so for every product-scoped initiative the success message names a path that does not exist. Consequence: an operator following the printed path finds nothing, and an agent trusting it could create the named directory as litter or delete work at the real one. This is lens L6's class (an operator-facing message that misstates what happened) and it is the same shape as CA-134's, where the gap between the resolved path and the assumed path was the whole defect. **Recommendation**: echo the same value the write used, or call `resolve-dir`'s resolver. Not adopted into EDMDS -- `EDMDS-17 AC6` fixes the finding count at 22 and this would make 23 without a decision to widen scope. Filed here so it survives this session; a `fix-pack` initiative or the next `code-audit` round is the right home | 2026-09-10 |
| Phase 3 / D86 | This entry records a collision that happened while writing the entry above it. D85 was first filed as **D56**, which `srd.md` v1.2.0 had allocated to `EDMDS-T01` minutes earlier -- so the first out-of-band ledger write after introducing per-ticket D-number blocks collided with block one. Nobody was working in parallel; one author, one session, one file. That is worth recording rather than quietly renumbering, because the audit finding behind the blocks (nine unsequenced writers to one `decisions.md`, with no allocation) was rated P1 on the theory that parallel implementers would collide. This collision needed no parallelism at all, which strengthens the finding rather than weakening it. **Convention adopted**: D56 through D84 are reserved by `srd.md`'s ticket allocation and are not to be used by any other writer; out-of-band decisions take numbers from D85 upward. D-numbers are identifiers, not timestamps, so a non-chronological allocation is correct and intended | 2026-09-10 |
