# Key Decisions

| # | Decision | Chosen | Rationale | Date |
|---|----------|--------|-----------|------|
| D1 | Which prefix, given that `EDMV4__lint-and-pipeline-budgets` is named in `plugins/edm/CLAUDE.md:345-346` and in EDMV3's `decisions.md` (D29, D34, D62) but has never existed on disk -- the condition EDMV3's own code audit raised as CA-430 (P2, round 8; L6 flagged the same claim in round 7) | **EDMV4**, with the description slug changed to `ecc-integration`. This initiative absorbs the named-but-never-created one, inheriting its three orphaned ticket IDs: **EDMV4-T01** (re-derive the Mermaid-class `edm-lint-artifacts` budget as an absolute ceiling plus a sized ratio, measured on a generated 50-initiative fixture), **EDMV4-T04** (anchor the eight remaining EDMV3-54 prompt-surface touch points that still carry a bare `CLAUDE.md Sec."..."` reference), **EDMV4-T05** (fix the CA-532 `--allowedTools` argv-splitting bug, run the live wave-A eval capture, commit `evals/baseline/scores.json`, re-verify CA-490's `edm-compare-eval` reorder). T02 and T03 were closed inside EDMV3 and are not carried forward | Three ticket IDs were cited across EDMV3's decision ledger, its ticket coverage map and `CLAUDE.md` as living in a pack that no glob matches. Absorbing the name retires that debt directly rather than leaving a second dangling reference beside it. Cost: the slug no longer reads `lint-and-pipeline-budgets`, so `CLAUDE.md:345-346` goes stale on creation and is itself in scope to fix | 2026-08-30 |
| D2 | Which parts of `plugins/edm/docs/ecc-integration-analysis.md` are in scope | **Part 4 in full (4.1 GateGuard, 4.2 update-patterns read-only defect, 4.3 size classifier, 4.4 lenses L12/L13/L14) plus Part 5 items 5.1 through 5.5**, plus the three inherited tickets from D1. Out of scope: **5.6** (modular install), all of **Part 6** (reject list), and **Part 7** (kernel/methodology split) | 5.6 is conditional on the Part 7 split and the analysis states it is not actionable on its own. Part 7 the analysis itself defers behind everything in Parts 4 and 5 regardless. Part 6 is the document's own reject list. Note per Part 8.3.1 that every "Value" rating in the source is the author's ranking of a structural argument, not a measured outcome -- the structural claims were verified against source, the value claims were not, and 5.5's premise in particular is called out as unmeasured | 2026-08-30 |
| D3 | `/edm:orchestrator` Step 2 could not dispatch any phase: all 14 skills in `plugins/edm/skills/` set `disable-model-invocation: true`, so every `Skill`-tool invocation of a phase skill failed with `Skill edm:<name> cannot be used with Skill tool due to disable-model-invocation`. Hit live during this initiative's intake on the `/edm:plan` dispatch | **CLOSED -- real defect, already fixed upstream; NOT in EDMV4 scope.** Fixed on `origin/main` by commit `bdec805` ("fix(edm): :bug: Remove disable-model-invocation so skills can be Skill-tool dispatched", merged as `bdb5698`, released as 3.2.1), which replaced `disable-model-invocation: true` with `user-invocable: true` on all 14 skills and added a plugin-wide guard to `bin/edm-check-skill-sync` banning the flag anywhere under `skills/`. Re-dispatch after `/plugin` update + `/reload-plugins` succeeded | The defect was not Phase-1-specific -- all six Step 2 dispatches failed identically, as did the `/edm:code-audit` and `/edm:verify-runtime` calls in the Phase 6 entry. It contradicted `CLAUDE.md Sec."Skill-tool composition"` (D21), whose cited in-repo precedent (git `commit` -> `jira:search-jira`) holds only because its target carries `user-invocable: true` and no `disable-model-invocation` key. The upstream guard closes it with a regression test, so EDMV4 inherits the fix rather than re-doing it. Supersedes the "candidate scope addition" framing passed to `/edm:plan` | 2026-08-30 |
| D4 | The initiative branch `edm/edmv4-ecc-integration` was cut from a local `main` that was **2 commits behind `origin/main`**, so its tree still carried `disable-model-invocation: true` on all 14 skills, plugin version 3.2.0, and no `edm-check-skill-sync` guard -- i.e. the exact state D3 fixed. Merging as-is would have silently reverted the D3 fix | **Original decision: rebase onto `origin/main` before Phase 6.** **Superseded during Phase 2 -- the revert hazard is GONE.** Commit `bdec805` ("fix(edm): :bug: Remove disable-model-invocation so skills can be Skill-tool dispatched") is now the tip of this branch, applied directly rather than by rebase. Verified in the working tree: **zero** skill files carry `disable-model-invocation: true`, and `bin/edm-check-skill-sync` carries 4 references to the guard. Root `CLAUDE.md` was updated to match | **What remains is narrower and non-blocking**: the branch is now 3 commits behind `origin/main`, missing the merge commit `bdb5698` and the two version-bump commits (`33d63e0`, `4ad0f35`). `plugins/edm/.claude-plugin/plugin.json` therefore still reads `3.2.0` on this branch while `origin/main` reads `3.2.1` -- a one-line divergence to reconcile at merge, not a functional defect. The residual pre-merge task is reconciling that version bump, not restoring the D3 fix. Still outstanding in the working tree and unrelated to this: the staged EDMV3-archival rename set (~313 paths), and unstaged edits to `bin/edm-state` (an `*opus-5*` arm added to `compute_cost_usd`'s model `case`, local work not present upstream) and `bin/tests/wave6-smoke.sh` | 2026-08-30, amended 2026-08-31 |

| Gate 1 / D5 | Where should 5.1 (bounded implementer/QC remediation loop) live? | **Its own initiative** -- out of EDMV4 scope, carried as a named follow-on rather than an unnamed candidate (D13/D14 precedent) | 5.1 is Large by itself and its cost-ceiling precondition is design work distinct from every other item. Splitting it means the eleven remaining items are not gated behind that design phase. ECC's `gan-harness.sh` provides no cost model to copy -- it has no dollar ceiling at all, only an iteration cap (default 15) plus plateau detection (2 consecutive iterations gaining <=0.2, starting no earlier than iteration 3, `gan-harness.sh:175-259`) | 2026-08-30 |
| Gate 1 / D6 | How should GateGuard (4.1) be implemented -- vendor ECC's 3 Node files or rewrite in bash? | **Deferred at Gate 1 ("Resolve in SRD"); ANSWERED in Phase 2 as architecture decision AD1: bash rewrite, ~250-350 lines.** Independently recommended by the human the same day. Subject to Gate 2 ratification | **There is no bash implementation to adopt anywhere -- both existing ones add a runtime dependency.** Upstream `zunoworks/gateguard` is **Python** (`pyproject.toml`, `src/gateguard/`, `pip install gateguard-ai`, verified by direct inspection of the repo); ECC's vendored copy is **JavaScript** (`gateguard-fact-force.js`, 1,301 lines) -- ECC ported it. EDM's required-binary contract is `bash`/`jq`/`git` only, so either adoption path adds a first. AD1's load-bearing insight is a scope one both explorers treated as fixed: ECC's 741 lines of helper closure (`shell-substitution.js` 482 + `gateguard-heredoc.js` 259) exist **solely** to serve `isDestructiveBash():674-721`. 4.1 is an EDIT gate; descoping the destructive-Bash arm deletes the entire recursive-BFS shell tokenizer explorer 04 called "the harder half to port," collapsing the estimate from 400-600 lines to ~250-350. **Descoping the destructive-Bash gate is itself a scope reduction requiring Gate 2 ratification**, not an implementation detail | 2026-08-30, answered 2026-08-31 |
| Gate 1 / D7 | What rule-file format should hookify (5.3) use? | **JSON rule files** | jq-native, adds no required binary beyond the existing `bash`/`jq`/`git`, and matches how every other structured file in `bin/` is consumed. There is zero YAML parsing anywhere in `bin/` today, so ECC's YAML-frontmatter format would need a from-scratch bash/awk YAML subset parser or a new required binary. ECC compatibility buys nothing: explorer 04 established by exhaustive search that ECC has **no evaluator at all** -- the condition/operator engine in `hookify-rules/SKILL.md` is documentation with no corresponding code, so only the format was ever reusable | 2026-08-30 |
| Gate 1 / D8 | How should L13 (Type Design) handle stack-conditionality, given `round_type=full` is derived by comparing the run set against `ALL_LENS_IDS`? | **Auto-N/A plus a new `round_type` state** | Follows the test layer's N/A-agreement precedent (`agents/edm-test-integration.md:21-25`): one authority determines applicability, consumers agree rather than self-declare, N/A is recomputed each run, absence is authoritative. The mode matrix is the wrong precedent -- it is a user-selected enum, not detected from the codebase. Keeps L13's conditionality framed as genuine **inapplicability**, satisfying the D2 guard, never as cost-driven exclusion of an applicable lens. Phase 2 must design how CA-471's completeness backstop learns a third state distinguishing "legitimately N/A" from "missing JSONL" | 2026-08-30 |
| Gate 1 / D9 | Should EDMV4-T05 spend live API budget to capture `evals/baseline/scores.json`? | **No -- recorded as an explicit scope boundary with a named follow-on** | Both named bugs are already fixed and verified in source (CA-532 at `evals/run-eval.sh:420-421,460-461` -- `CLAUDE_ALLOWED_TOOLS`/`CLAUDE_DISALLOWED_TOOLS` are real bash arrays expanded as `"${ARRAY[@]}"`; CA-490 at `bin/edm-compare-eval:70-75` -- the `complete != true` refusal now runs before the baseline-existence check at `:77-81`), so T05's code work is complete and closes as verification, confirmed by two regression checks in `bin/tests/wave8-smoke.sh` (EDMV4-T05, AC1/AC2, self-proven to discriminate). A third inherited item, CA-537's `.gitlab-ci.yml` exit-code ordering, is dropped as moot: no `.gitlab-ci.yml` exists in this repository (constraint C8 -- no CI pipeline at all), commit `b56558d` removed the pipeline, and stray `.gitlab-ci.yml` copies under `.claude/worktrees/*/` are agent scratch worktrees, not this repository's own files. **Named follow-on (AC9-AC12):** capturing `evals/baseline/scores.json` is out of EDMV4's scope and is a human credential decision -- it is carried as **`EVALB` (eval-baseline-capture)**, a named follow-on initiative reserved and verified free via `edm-validate-prefix EVALB` on 2026-09-02, to be run once a human owning `ANTHROPIC_API_KEY` for wave-A exit is ready. **Naming it is the point (wave-1 QC, shard 1):** the earlier wording, "its own follow-on ticket, to be filed", named nothing and was itself the orphaning defect `EDMV4-T05` exists to prevent -- exactly the failure mode D1 recorded when three EDMV3 ticket IDs were cited across three documents as living in a pack no glob matched. A reserved prefix is checkable; "to be filed" is not, not spent silently by an agent verifying its own ticket. That capture requires three `run-eval.sh` invocations against fresh scratch trees, each scored individually by `score-artifacts.sh`, with the run whose `total` is the **middle** of the three committed as the baseline. The committed `evals/baseline/scores.json` must carry `scorer_version` (currently `"1.1.0"`, `score-artifacts.sh:139`), `dimensions_scored` (**5** for a wave-A `plan -> srd -> audit-srd` capture -- dimension 5, lens-JSONL-versus-prose agreement, has no input without a code-audit round), `dimensions`, `total`, `complete`, and -- only on the committed baseline, never on a candidate run -- `variance.total_range` (the sole baseline field `edm-compare-eval` reads: `jq -r '.variance.total_range // 0'` at `:109`). Until that capture happens, `edm-compare-eval` exits **3**, printing "the eval tripwire is NOT armed" (`bin/edm-compare-eval:77-81`) -- a named, non-crashing outcome, not a silent pass; this record cross-references `evals/baseline/README.md` (`:39-55`, `:104-116`, `:165-171`), which documents the same boundary in its own words, so the two records agree. No agent spends API budget under EDMV4 to capture the baseline | 2026-08-30 |
| Gate 1 / D10 | What is EDMV4-T04's scope -- the 8 files `CLAUDE.md` names, or the verified set? | **All 14 verified files, plus a third generated canonical section** | The verified unanchored set is 5 agents + 9 skills = 14; `CLAUDE.md`'s own list misses `edm-qc-auditor.md`, `verify-runtime/`, `push-jira/`, `orchestrator/`, `metrics/` and `code-audit/SKILL.md`, all carrying the identical D22 defect. Separately, `edm-qc-auditor.md:39` cites `Sec."Unverifiable acceptance criteria (D15)"`, which has **no mirror in `docs/canonical-sections.md`** (that file generates only two sections), so anchoring it the standard way would not fix it -- hence adding D15 as a third generated section, with `edm-sync-canonical-sections` re-run and its `--check` drift assertion re-verified in the same ticket | 2026-08-30 |
| Gate 1 / D11 | How should codemaps (5.5) be approached? | **The `SRD/.codemap.md` interim** -- first explorer of an initiative writes a reusable current-architecture map that later initiatives read and refresh; no generator built | The source document flags its own value claim here as explicitly unmeasured, and its Part 8.2 correction establishes that ECC's generator emits literal template placeholders for its two most valuable sections (`generate.ts:225-231`), so there is nothing worth porting. The interim is the cheapest way to test whether the premise holds before spending medium-large effort on a generator. A codemap is the CURRENT architecture and does not duplicate `architecture.md`, which is the TARGET | 2026-08-30 |
| Gate 1 / D12 | Should Phase 1's eleven verified corrections be written back into `ecc-integration-analysis.md`? | **Yes -- amend the source document in place**, in the Part 8.2 style it already uses to self-correct twice; `planning.md` retains the correction table as the audit record | Leaving the analysis uncorrected means the next reader re-derives the same eleven findings. The corrections are substantive, not cosmetic: ECC's hook count is 23 across 7 events (not 25/8); the seven security triggers live at `orch-pipeline/SKILL.md:100-104`, not `rules/common/security.md` (a citation the analysis repeated without checking); hookify has no evaluator; `GAN_EVAL_CRITERIA` is dead code; `delivery-gate` has an undocumented third disk tier; `silent-failure-hunter`'s body is 44 lines not ~30; `harness-audit.js` consumer mode is 16 checks/39 points not 11/29; the `update-patterns` line cites are stale; `GATEGUARD_DISABLED=1` is a second omitted kill switch; `MultiEdit` needs one retry per unchecked file; codemaps placeholders are at `:225-231` | 2026-08-30 |

| D13 | GateGuard's upstream provenance was the one unresolved licence question. ECC root is MIT (`ECC/LICENSE:1-3`, Affaan Mustafa 2026) but ECC **vendored** the hook from a third party per its own header at `scripts/hooks/gateguard-fact-force.js:19-20` (`skills/gateguard/SKILL.md:5` sets `metadata: origin: community`), so MIT-via-ECC was not sufficient provenance | **CLEARED -- `zunoworks/gateguard` is MIT.** Verified by direct inspection of `https://raw.githubusercontent.com/zunoworks/gateguard/main/LICENSE`, which reads "MIT License / Copyright (c) 2026 Hirokazu Seto / ZUNO WORKS K.K." Vendoring or porting `gateguard-fact-force.js` is permitted with attribution | Direct inspection is the standard `CLAUDE.md Sec."Prompt conventions (house style)"` already applies to the `caveman` and `ponytail` adoptions, both of which record a licence "verified 2026-07-28 by direct inspection" -- an assertion alone would not meet the bar this plugin sets for itself. **Attribution obligation, amended after AD1**: this entry originally reasoned about vendoring, where MIT's attribution and licence-notice requirement genuinely binds because the reuse is verbatim (~2,042 lines). AD1 chose a **bash rewrite with the destructive-Bash arm descoped**, so nothing is copied from either upstream implementation -- what carries over is the concept (deny first touch, demand facts, allow on retry), which is pattern-level adoption matching the clean-room posture `CLAUDE.md Sec."Prompt conventions (house style)"` already records for `caveman` and `ponytail`. **An earlier framing in this initiative's conversation held that a rewrite derived from reading the source is still a derivative work carrying the same obligation; with the tokenizer descoped that is more cautious than the facts warrant.** EDM should still record the source, URL, licence and means of verification in the house-style section, as a matter of convention rather than strict MIT compulsion -- it is free, and it is what that section exists for. If Phase 2 or Gate 2 reverses AD1 back to vendoring, the strict obligation returns in full. **Amended again after Gate 2 (D14, 2026-09-02): AD1 is now ratified, not merely architect-chosen** -- the bash rewrite stands, nothing is copied from either upstream, and `EDMV4-12` stays Should Have with its NOTICE clause dormant. The trigger that would revive the strict obligation is an AD1 reversal to vendoring by any route, not the outcome of `EDMV4-05` or any other single gate question -- so the licence record (this entry) and the architecture record (`architecture.md` AD1) cannot disagree | 2026-08-31, amended 2026-09-02 |

| Gate 2 / D14 | **AD1 itself** (`EDMV4-59`): is GateGuard a bash rewrite scoped to `Edit`/`Write`/`MultiEdit`, or a vendoring of an existing implementation? Gate 1 / D6 deferred this to Phase 2 with "Resolve in SRD"; Phase 2 answered it as AD1, but an architect's answer is not a ratification | **RATIFIED -- bash rewrite, ~250-350 lines, not a vendoring.** Presented at Gate 2 as a decision distinct from `EDMV4-05` and `EDMV4-06`, and approved | Neither upstream is bash: `zunoworks/gateguard` is Python and ECC's copy is JavaScript, so both adoption paths would add a runtime dependency the plugin has never had, breaking the `bash`/`jq`/`git` required-binary contract (`EDMV4-56`). AD1 is also the sole trigger for D13's dormant MIT NOTICE obligation, which is why it needed its own gate decision rather than riding on `EDMV4-05`: ratifying the descope while separately directing vendoring would have left that obligation dormant with nothing to wake it (R17). `EDMV4-12`'s dormant clause and `EDMV4-56`'s dependency-addition clause are both wired to an AD1 reversal by any route | 2026-09-02 |
| Gate 2 / D15 | **`EDMV4-05`**: may GateGuard's destructive-`Bash` arm be descoped, given it is a reduction against Gate-1-approved 4.1 scope? | **RATIFIED -- descope approved.** 4.1 ships as an edit gate over `Edit`/`Write`/`MultiEdit` only | The arm's 741 lines of helper closure (`shell-substitution.js` 482 + `gateguard-heredoc.js` 259) exist solely to serve `isDestructiveBash():674-721` -- descoping deletes the entire recursive-BFS shell tokenizer explorer 04 called the harder half to port, collapsing the estimate from 400-600 lines to ~250-350. Presented rather than absorbed because an implementer cannot descope approved scope (`CLAUDE.md Sec."Unverifiable acceptance criteria (D15)"` applied by analogy; the analogy itself is recorded in SRD Sec.5.2 as a convention gap to open separately). **Independent of D14**: rejecting this would have yielded a larger bash rewrite, never a vendoring. **Scope-boundary record** (EDMV3 D14 scope-boundary framing -- a decision on its own merits, not a postponed finding): the residual destructive-`Bash` exposure is bounded by nothing in this plugin -- `rm -rf`, `git reset --hard`, `git clean -fd`, a force-push (`git push --force` / `--force-with-lease`), and destructive SQL such as `DROP TABLE` are all left entirely unguarded. The named follow-on vehicle is 5.3's `bash`-event rule files (`EDMV4-43`), and that vehicle ships **only if Spike A (`EDMV4-T06`) clears** -- the follow-on route is conditional, not assured | 2026-09-02 |
| Gate 2 / D16 | **`EDMV4-06`**: may 5.4's "phase started with no `completed_at`" anomaly be descoped, given it is a reduction against Gate-1-approved 5.4 scope? | **RATIFIED -- descope approved** | Same change-control principle as D15: a reduction against Gate-1-approved scope is a first-class Must Have requirement with gate-presentation acceptance criteria and explicit accept/reject branches, not a note in a constraints list (R14). Gates `EDMV4-44` and `EDMV4-45`. **Scope-boundary record** (EDMV3 D14 scope-boundary framing): `EDMV4-45` ships the three verified `validate` anomalies only; a future "phase started with no `completed_at`" design would need a time threshold or an explicit wave-in-progress carve-out, since a phase legitimately stays started for hours during a Phase 6 wave. The exposure is non-terminal phases only -- `OPEN_AUDIT_ROUND` is kept informational for the identical block-on-a-normal-state reason (`bin/edm-state:1784-1790`), and `cmd_archive` already refuses when the **terminal** phase has no `completed_at` (`bin/edm-state:3181-3185`) | 2026-09-02 |
| Gate 2 / D17 | Pattern-library curation at Gate 2: 19 entries carried `status: pending-review` across `docs/audit-patterns/` (11 from EDMV4's SRD audit, 8 from EDMV3's code audit) | **Three oldest curated as Keep** -- CA-509, CA-510 and CA-106 (all EDMV3, 2026-08-21) had their `status: pending-review` markers removed, bodies unchanged. The remaining 16 stay pending and are re-offered at Gate 3 | `AskUserQuestion`'s four-question ceiling caps curation at three entries per gate alongside the gate question itself, so the oldest-first rule applies (`skills/audit-srd/SKILL.md Sec."Pending Pattern Entries (gate-time curation)"`). Curation is one-way: de-duplication on the entry title stops a later `update-patterns` re-marking a curated entry. Curation carries no approval weight -- the Gate 2 verdict is independent of it | 2026-09-02 |

| D18 | Phase 5's Step 0 preflight refused to run: `edm-state gate-check EDMV4 audit-tickets` exited 3 demanding Gate 3 -- the gate `/edm:audit-tickets` itself presents. `cmd_gate_check` grouped `audit-tickets` with `implement` on the `required_gate=3` arm, so Phase 5 was unreachable for every standard-lifecycle initiative, through **both** enforcement layers (the Step 0 preflight and the `edm:audit-tickets` `UserPromptExpansion` hook, which greps the same exit code). Neither escape applied -- the legacy branch needs an absent `schema_version`, and `gate_required_and_approved` exempts only skipped or past-terminal phases | **Fixed inside EDMV4 as a scope addition, not deferred and not bypassed.** `audit-tickets` moved to the `required_gate=2` arm beside `tickets`; `implement` keeps its own `required_gate=3` arm. New requirement `EDMV4-60` and ticket `EDMV4-T54` record it; SRD bumped 1.1.0 -> 1.2.0 and the ticket-pack `Generated From` header with it. Implemented before Phase 5 could run, so it is presented at **Gate 3** for ratification rather than approved in advance | The user directed that EDMV4 own the fix rather than treat it as an external blocker. Bypassing Step 0 for one run would have left the deadlock in place for the next initiative and set a precedent for skipping the very enforcement EDMV3-T13 built. Deferring it to a wave was not available: Phase 5 is the gate that schedules the waves. **The invariant, now asserted directly**: a token names the gate a phase *consumes*, never the one it *produces* -- `audit-srd` presents Gate 2 and consumes Gate 1 for the same reason. **Why the suite was green**: `wave6-smoke.sh`'s EDMV3-T13 AC3 loop asserted only `"$tok_out" == *"Gate "*`, so a wrong-but-present gate number passed. The loop is now pinned per token and a separate producer/consumer assertion added, so a ninth token cannot reintroduce the circularity silently. This is the harvested SRD-audit pattern "an anti-regression assertion counts a hardcoded name list rather than the live set" recurring on a new surface -- the second such recurrence inside this initiative, after the stale-branch-premise one | 2026-09-02 |
| D19 | Sec.4.6 C9 and D4 described the initiative branch as 3 commits behind `origin/main`, diverging only in a `plugin.json` version line. At Phase 4 close it was **25 commits behind** and a strict ancestor of `origin/main`, missing the entire VERIF initiative, plugin 3.2.2, and `plugins/edm/docs/ecc-integration-analysis.md` -- the document `EDMV4-54` exists to correct, making that requirement un-executable | **Fast-forwarded the branch to `origin/main` at Phase 4 close**, after the eight epic writers finished (the user's call: let them complete, then reconcile). A redundant local re-append of the eleven SRD-audit pattern entries was discarded in favour of main's copy, which is byte-identical apart from carrying the true 2026-08-31 harvest date. Recorded as a standing note in the ticket-pack `README.md` | Reconciling before the writers finished would have discarded work already most of the way through; reconciling after left only citation drift, which every epic already handles by anchoring on function names and literal strings rather than line numbers. **Consequences carried into the pack**: `plugin.json` now reads 3.2.2 so `EDMV4-03`'s scope shrinks; `SRD/.archived/` now exists so `EDMV4-04`'s Target Components resolve; and VERIF's `VERIF-T09` already shipped a fix in EDMV4's own problem space (read-only verifiers raised from `maxTurns` 25 to 50), which the lens and verifier tickets must check for overlap before implementing. The installed plugin cache also had to be refreshed to 3.2.2 separately -- a repo merge does not update it, and Phase 5's auditors would otherwise have run at the old 25-turn ceiling that truncated this initiative's own SRD audit | 2026-09-02 |

| Gate 3 / D20 | Is the 52-ticket pack implementation-ready, and is `EDMV4-60`/`EDMV4-T54` accepted as a scope addition to the Gate-2-approved requirement set? | **APPROVED.** Pack ratified at Gate 3 on 2026-09-02: 52 tickets, 100% SRD coverage (60/60, no orphans either direction), 0 XL, dependency graph verified acyclic with all 101 declared edges represented in the critical-path diagram. `EDMV4-60` and its ticket `EDMV4-T54` ratified as a scope addition -- implemented before the gate because the circular `audit-tickets` gate mapping made Phase 5 unreachable, so it could not be deferred to a wave | Two-lane audit (structural + content-quality) found **P0: 2, P1: 11, P2: 24, NOTED: 18**; every P0 and P1 remediated before the gate, plus six P2s. Both P0s were schedule-breaking: `EDMV4-T53`, the Definition-of-Done pass, declared no dependencies and sat in wave 1 where its ACs enumerate scripts that do not yet exist; and `EDMV4-T44`/`EDMV4-T46` formed a latent circular dependency undeclared in either direction, broken by making `T46` validate-only and leaving the hookify wiring to `EDMV4-T45`, which already owned it. `run-all.sh` re-run green (2,377 passed, 0 failed) after remediation | 2026-09-02 |
| Gate 3 / D21 | The pack's own "verified anchors" -- the per-epic tables the README and SRD v1.2.0 both delegate `file:line` authority to -- were derived pre-fast-forward and are now wrong where the SRD is right | **Anchors demoted to advisory across all eight epics; `EDMV4-T08` AC8 rewritten to require re-derivation from the tree and to forbid "correcting" `srd.md` toward any number in this pack** | The fast-forward's `6e29dcb` re-inserted four lines at `bin/edm-state:504-507`, restoring the offset the Phase 4 writers had corrected away: `ALL_LENS_IDS` is at 1613 (SRD correct, pack said 1609), `MODE_ENUM_LIST` at 807 (SRD correct, pack said 803), `state_anomalies()` at 1709 (SRD correct, pack said 1705). `EDMV4-T08` AC8 mandated correcting any SRD citation off by more than +/-10 lines, so an implementer following the pack would have edited the SRD's **correct** numbers into wrong ones. **This is the third recurrence of the stale-anchor pattern inside one initiative** -- the SRD branch premise (Phase 4), the epic writers' corrections (here), and `wave6-smoke.sh` shifting under this initiative's own `EDMV4-T54` fix (here). Each earlier fix re-derived numbers that drifted again, so the remediation breaks the loop instead of continuing it: locate by symbol name at edit time | 2026-09-02 |
| Gate 3 / D22 | `EDMV4-T54` AC7 required "a dedicated assertion stating the producer/consumer invariant directly", and the implementation satisfying it used a hardcoded two-entry list -- the exact anti-pattern the AC exists to forbid | **Both the AC and the implementation rewritten to derive both sides live.** Tokens come from `cmd_gate_check`'s own `Valid tokens:` line; the gate each phase **produces** comes from that skill's `## HITL Gate N` heading. The assertion fails on any token whose consumed gate is not strictly lower than its produced gate, and fails loudly if the derivation yields fewer than two gate-presenting skills so it can never pass vacuously | Caught by the content-quality lane as audit finding P1-8. The hardcoded list left a ninth phase-skill token free to reintroduce the circularity while the suite stayed green -- verbatim the outcome the AC's own Technical Note said it prevented -- and was a second independent encoding of one predicate, the CA-409 class this same ticket cites when it leaves `hooks/hooks.json` untouched. The rewrite now covers **3** gate-presenting skills rather than 2 (it picks up `plan` automatically), and was verified to **fail** against a deliberately reintroduced regression rather than only to pass today. The AC text, not just the code, was the defect: "states the invariant directly" was satisfiable by what shipped | 2026-09-02 |
| Gate 3 / D23 | `edm-state update-patterns EDMV4 ticket` harvested nothing, reporting `extraction_status=unsupported-format` | **Recorded as a methodology gap, not remediated in EDMV4.** Surfaced at Gate 3 for a follow-on decision | The ticket-audit report format has no machine-readable per-finding shape, so the Phase 5 arm contributes nothing to the pattern library while the SRD and code-audit arms do. The command fails loudly and names the exact fix (add a deterministic per-finding line to the report format, then the matching arm in `pattern_extract_titles` and the matching row in `docs/audit-patterns/README.md`), which is good design -- it does not report a clean round. Left out of scope because EDMV4's Gate-3-approved pack is already set and this is a distinct piece of design work, recorded as a named follow-on per the D5/D14 precedent rather than an unnamed candidate | 2026-09-02 |
| Gate 3 / D24 | Pattern-library curation at Gate 3: 16 entries pending across `docs/audit-patterns/` | **Three oldest curated as Keep** -- CA-481, CA-493, CA-495 (all EDMV3, 2026-08-21), markers removed and bodies unchanged. Thirteen remain pending for the code-audit convergence gate | Same oldest-first rule as Gate 2's D17, capped at three by `AskUserQuestion`'s four-question ceiling. Curation carries no approval weight; the Gate 3 verdict is independent of it | 2026-09-02 |

| D25 | Spike A (`EDMV4-T06`): what does the host do when two blocking hooks match the same tool call on the same event, and does a homogeneous `command`-plus-`command` pair in one `hooks` array behave like AD4's `Stop` design assumes? Run against `claude --version` = `2.1.246 (Claude Code)`, entirely from scratch `.claude/settings.json` files in disposable `mktemp -d` repositories, never touching `plugins/edm/hooks/hooks.json` (`git status --short` on that path returned nothing before or after -- AC11 satisfied) | **Order is not load-bearing for either event; a deny always wins; every registered command runs.** `PreToolUse`, matcher `Bash` registered twice (block-a exit 0, block-b exit 2), each writing its own marker under one scratch dir: allow-then-deny and deny-then-allow both produced `block-a-ran` AND `block-b-ran` present, and both orderings refused the `Bash` call (`echo spike-test-marker-A1/A2` never ran; the agent's tool result was `PreToolUse:Bash hook error: ... block-b denies this Bash call`). `Stop`, two separate no-matcher blocks (block-a always exits 0; block-b denies once via a marker-gated `if`, then allows): both orderings logged two runs of each block (the Stop event fired twice -- once forced to continue, once allowed) and the session still ended in "DONE" -- again order-independent. AC5's specific homogeneous-pair test (one `Stop` block, `hooks: [entry1, entry2]`, both `"type": "command"`, entry1 always exit 0, entry2 denies once): entry1 and entry2 both ran on both Stop-event firings, and entry2's exit 2 WAS honored on the first firing despite entry1 exiting 0 in the same array -- AD4's second-entry-in-one-array `Stop` design works as assumed. AC6 honestly recorded: `hooks.json:16-25` is command-plus-`prompt`, and a repo-wide `find . -iname hooks.json` returns exactly one file (`plugins/edm/hooks/hooks.json`) outside worktree mirrors of the same repo, so the grep-verified count of homogeneous command-plus-command pairs anywhere in this repository, before this spike, was zero -- the AC5 result stands on the experiment alone. AC7's branch (only one entry executes) did NOT occur, so no gate re-presentation of a second `Stop` matcher block is triggered. AC8: because the `PreToolUse` `Bash`-matcher collision test showed both blocks running and any deny winning regardless of registration order, 5.3 MAY register a second `bash`-event `PreToolUse` block alongside the existing `git commit` block at `hooks.json:80-90` without suppressing it -- AD4's fallback (folding `edm-lint-staged-artifacts` into `edm-hookify`'s `Bash` dispatcher) is not required by this evidence. AC9's trigger condition ("first-registered wins" or "blocks must be consolidated") did not occur, so `EDMV4-43`'s `bash` event is not withdrawn by this finding | Both events' collision behaviour and the homogeneous-pair shape were unverified from source before this spike; EDM had never had two independently authored blocking hooks compete on one call, and `hooks.json`'s five `UserPromptExpansion` blocks are matcher-disjoint so they never exercised it. Markers were written under one `mktemp -d` per experiment run (fresh scratch dir per ordering, per AC3's "distinct marker file" requirement), so "which decision the host took" and "which commands ran" could be read apart directly from the filesystem rather than inferred from a transcript. This is one host version's behaviour (`2.1.246`), not a documented contract -- if `EDMV4-T11`, `EDMV4-T45` or `EDMV4-T46` ship against a materially different Claude Code version, re-run before relying on it | 2026-09-02 |
| D26 | Spike B (`EDMV4-T07`): can a `PreToolUse` hook deny a native `Edit`/`Write`/`MultiEdit`, by which of AD2's two mechanisms, and what should `EDM_GATEGUARD_DENY_MODE` default to? Run against `claude --version` = `2.1.246 (Claude Code)`, from scratch `.claude/settings.json` files in disposable `mktemp -d` repositories; `plugins/edm/hooks/hooks.json` untouched (AC9) | **`EDM_GATEGUARD_DENY_MODE` defaults to `json`, evidence-backed for `Edit` and `Write`; `MultiEdit` is UNTESTABLE on this host, not PASS or FAIL.** AD2 JSON mechanism (stdout `{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"deny","permissionDecisionReason":"..."}}`, exit 0): `Edit` DENIED (target.txt confirmed unchanged by direct file read) with the reason surfaced to the agent as a clean `<error>spike-b1-json-deny-reason</error>` tool result; `Write` DENIED (file confirmed never created by directory listing) with `<error>spike-b2-json-deny-reason</error>` surfaced identically. `MultiEdit`: the tool is **absent from this Claude Code session's toolset entirely** -- not in the loaded tool list and not reachable via the host's own `ToolSearch` deferred-tool lookup (`select:MultiEdit` returned "No matching deferred tools found"), reproduced twice: once under natural instruction and once forcing it via `--allowedTools MultiEdit`. This is a genuine negative finding, not a skipped test -- AD2's `MultiEdit` deny-mode row and AC5's >=3-edit/>=2-file partial-application batch test could not be exercised as written on this host and are recorded as UNTESTABLE, pending a host/config where the tool loads. Exit-2-plus-stderr mechanism, repeated on `Edit` and `Write`: both DENIED (file states confirmed unchanged/uncreated the same way), but the reason text arrives wrapped in a noisier shape that leaks the hook's own shell command verbatim to the agent -- `PreToolUse:Edit hook error: [echo 'spike-b4-stderr-deny-reason' >&2; exit 2]: spike-b4-stderr-deny-reason` -- rather than the JSON mechanism's clean reason-only text. Positive control (AC4): the identical `Edit` prompt against a scratch repo with NO hook registered at all succeeded normally (target.txt content changed from `hello world` to `goodbye world`, confirmed by direct file read), proving the three denials above are not an unrelated tool failure misread as a deny. AC7's precedent comparison: `edm-lint-staged-artifacts:7-10` documents exit 0/1/2 and its loop at `:146-159` sets `fail=2` on a real violation, but it fires on a `Bash`-wrapped `git commit`, so it never itself evidenced the native-tool case -- this spike is the first direct evidence for `Edit`/`Write`. AC8's negative branch (neither mechanism denies) did NOT occur for `Edit`/`Write`, so 4.1 is not sent back to a gate as a no-go on this evidence -- but `MultiEdit` remains genuinely unverified and should be re-tested before GateGuard's `MultiEdit` arm ships against a host where the tool is confirmed present | The JSON mechanism is chosen as the default over the exit-code mechanism because it denies exactly as reliably for both tested tools AND surfaces a clean, purpose-written reason rather than leaking the hook's own shell command text into the agent's transcript -- an information-hygiene concern for a hook that may contain implementation detail not meant to reach the model. `MultiEdit`'s absence from this host's toolset is left as an open question rather than papered over: R2's worst-case framing (GateGuard denying nothing while appearing to work) is precisely why an untestable tool is recorded as untestable, not assumed to work by analogy with `Edit`/`Write` | 2026-09-02 |
| D27 | EDMV4-T04 / EDMV4-51: the full orphaned-citation enumeration, re-derived by content (not by the ticket pack's advisory line numbers) across all 14 target files, since AC5 requires every distinct section name cited under `skills/` and `agents/`, at every site, catching all three citation shapes (backticked, unbackticked, bare `Sec."..."` continuation) | **32 anchorable `CLAUDE.md Sec."..."` citation sites** (12 agent-file sites + 20 skill-file sites = 32 total), classified into four buckets. **Bucket A -- already resolves via the generator, exact-match-modulo-suffix (AC6):** `Severity vocabulary` and `Mermaid diagram conventions`, cited bare everywhere despite the generator's `(canonical)` suffix -- deliberately classified as NOT orphans (`agents/edm-architect.md:31,89`, `agents/edm-srd-writer.md:36,85`, `agents/edm-ticket-writer.md:44,104`, `agents/edm-ticket-auditor.md:63,177`, `agents/edm-qc-auditor.md:70`, `skills/srd/SKILL.md:80,182`, `skills/tickets/SKILL.md:54`, `skills/audit-srd/SKILL.md:105,122`, `skills/audit-tickets/SKILL.md:127,139`, `skills/code-audit/SKILL.md:389,393`). **Bucket B -- Route 1, exact heading match, resolved by adding to the generator:** `Unverifiable acceptance criteria (D15)` (`agents/edm-qc-auditor.md:39`, `agents/edm-ticket-auditor.md:55`, `skills/verify-runtime/SKILL.md:31,39,147` -- five sites, not the four the pre-audit orphan table named; `edm-ticket-auditor.md:55` was missed by that table and is the +1 recorded below), `Project artifact layout` (`skills/orchestrator/SKILL.md:199`), `Optional: Jira synchronization` (`skills/push-jira/SKILL.md:36`). **Bucket C -- Route 2, heading mismatch, resolved via default route (a) (citation text gains the real heading's `(EDMV3-T38)` suffix, then that exact string is added to the generator):** `EDM mode matrix` (`skills/orchestrator/SKILL.md:113,199-200`), `Phase Timing Guidelines` (`skills/metrics/SKILL.md:74`, `skills/orchestrator/SKILL.md:199-200`). **Bucket D -- Route 3, not a `##` heading (bold inline text at `CLAUDE.md:24`), resolved by inlining the rule at the citation site and dropping the `Sec."..."` form** (default's cheaper alternative, chosen over promoting to a real heading to avoid restructuring `CLAUDE.md`'s own section 2 mid-wave while other agents edit neighboring `CLAUDE.md` sections): `Skill-tool composition` (`skills/orchestrator/SKILL.md:28,152,196`) | Reconciled against `EDMV4-50`'s pre-audit 8-site/6-section table (this ticket's own Technical Notes): that table is correct for every site except `agents/edm-ticket-auditor.md`, where the live tree carries a fifth D15 citation at `:55` ("a specification defect resolved only through gate change control") that the table did not name. The pre-audit table also predates the VERIF initiative's `Verifier completion sentinel (canonical)` section, added to `CLAUDE.md` after this ticket pack was written -- see D28 below for its own classification. Recorded per AC8: the discrepancy is this row, not silently absorbed. Net site-count reconciliation against the ticket pack's advisory "31 sites" figure: real anchorable count is 32 (31 + the missed `edm-ticket-auditor.md:55` D15 site), with the ticket's own advisory `edm-ticket-auditor.md` "third site" (`:134`) actually corresponding to the (separately classified, not anchored) sentinel citation now at `:138` in the live tree | 2026-08-31 |
| D28 | `CLAUDE.md` now carries a `Verifier completion sentinel (canonical)` section (added by the VERIF initiative after this SRD/ticket pack was written) whose own text states it is **deliberately not** added to `docs/canonical-sections.md`'s generated set, since "adding a third section to it is out of scope" for that (separate) contract. Four verifier-agent prompts cite it by name (`agents/edm-qc-auditor.md:133`, `agents/edm-ticket-auditor.md:138`, plus the two already-anchored `agents/edm-srd-auditor.md:110` and `agents/edm-test-coverage-auditor.md:228`, the latter outside T04's 14-file scope entirely). `EDMV4-T04`'s three-route orphan taxonomy (Route 1/2/3) has no arm for a section that is cited by name but is *intentionally* absent from the generator -- forcing it into Route 1 would fail (the section will never exist in `docs/canonical-sections.md`); forcing it into Route 3 would misdescribe it (it IS a real `##` heading, just a deliberately-unmirrored one) | **Add a fourth classification: "deliberately unmirrored -- record and exempt."** These sites are not anchored with the `Read docs/canonical-sections.md` sentence (AC10's pattern), because that document will never carry this section by design. Each site already documents, in its own surrounding prose, why: "per D22, a bare section-name citation is not known to resolve from an installed plugin cache, so the literal string is what this agent actually follows, not a paraphrase of it" -- i.e. the D22 resolution problem is already solved for these specific citations by inlining the sentinel's literal grammar immediately after the citation, a different and equally valid resolution to the same underlying defect. `bin/edm-check-grants`'s AC11 orphan-citation check exempts the section name `Verifier completion sentinel`. **Attribution corrected (wave-1 QC, shard 1):** when this decision was written the check did not exist -- `EDMV4-T04` AC11 was reverted rather than shipped broken, so D28 described the behaviour of code that had not been written. The check was subsequently built under `EDMV4-T55`'s remediation of that P0, and on its first run caught the live regression `EDMV4-T34` had introduced (two bare `CLAUDE.md Sec."EDM mode matrix"` citations added to `skills/orchestrator/SKILL.md` after `EDMV4-T04` anchored the tree). It compares on the suffix-stripped heading, so the exemption matches whether or not the `(canonical)` suffix is present so it neither reports these sites as bare/unanchored (failure mode i) nor as anchored-to-a-nonexistent-section (failure mode ii) | 2026-08-31 |
| D29 | `EDMV4-T08`'s merge-time reconciliation of the D4 residual `plugin.json` version divergence -- what genuinely remained open at ticket-close time, re-derived from the tree rather than from the ticket pack's premise (ticket-pack audit P1-2: the pack's own line-number anchors were verified pre-fast-forward and are stale where the SRD is right, per D21 above) | **AC1 (version) was already satisfied, not fixed here.** At ticket-close, `plugins/edm/.claude-plugin/plugin.json:4` and `.claude-plugin/marketplace.json`'s `edm` entry both already read `3.2.2` -- identical to each other and above the `3.2.1` AC1 requires, so `EDMV4-T35`'s manifest-description bump is already reconciled into one final value with nothing further to do. **AC6's two change sets are also already resolved, not merely accounted for.** `git status` on this branch is clean: the ~313-path EDMV3-archival rename set is already committed (`SRD/.archived/edm/EDMV3__prompt-streamline/` exists on disk) and no unstaged `bin/tests/wave6-smoke.sh` edits remain outstanding from D4's snapshot. **What was genuinely open** was AC2/AC3/AC4's regression coverage: the `*opus-5*` arm `compute_cost_usd` already carries (commit `6e29dcb`, landed between D4's snapshot and this ticket's close) had no runnable assertion proving its computed cost, its silence on the unrecognized-model warning, or its ordering ahead of the `*)` fallback. That coverage is added to `bin/tests/wave6-smoke.sh` (the "EDMV4-T08 AC2/AC3/AC4" block) rather than as new pricing logic, since the pricing logic itself needed no change | Re-deriving from the tree rather than trusting the ticket's own narrative (written against an earlier snapshot, per D4's "amended 2026-08-31" note) avoided two failure modes the ticket-pack audit specifically warned about: reintroducing already-fixed regressions by re-doing completed work, and "correcting" `srd.md`'s already-accurate `bin/edm-state` line citations (`ALL_LENS_IDS:1613`, `MODE_ENUM_LIST:807`, `state_anomalies():1709`) toward the pack's own stale pre-fast-forward numbers (AC8) | 2026-09-02 |

| Wave 1 / D30 | Phase 6 wave 1 ran the methodology at full scale for the first time since the agent layer was written. Seven defects surfaced in EDM's own implementation harness, six of them invisible at smaller scale | **Fixed inside EDMV4 as `EDMV4-61`/`EDMV4-T55` before wave 2**, rather than deferred: every subsequent wave pays the same cost otherwise. `edm-implementer` `maxTurns` 60 -> 200 and `edm-qc-auditor` 50 -> 150, both calibrated from measured wave-1 usage; `qc_shard_threshold` 20 -> 6; the `SubagentStop` matcher widened to cover the plugin-namespaced agent name; a new `skills/implement` Step 6a requiring a post-wave shard count; implementers forbidden from running the whole-tree suite; a rebase-before-commit and ancestry-check requirement for worktrees; and eleven malformed shellcheck directives repaired | **The QC defect is the one that matters.** The `SubagentStop` hook fired for nobody across eight implementers, emitted no warning, and the entire automatic QC layer did not run -- the only evidence was an absent `qc/` directory, and `skills/implement` Step 9's "loop until all tickets have PASS verdict" is vacuously satisfiable when no verdict exists. Everything QC later caught, including four orchestrator errors, would have shipped unaudited. **Capacity was unanimous, not marginal**: 8/8 implementers and 2/3 auditors exhausted their ceilings; one implementer stopped between building and committing, leaving two new `bin/` scripts untracked where a worktree cleanup would have destroyed them. **The contention was self-inflicted**: 6-10 parallel agents each running a whole-tree suite produced 38 concurrent processes at load 10, so the suite each waited on was slow *because* the others waited on it. **An eighth candidate was investigated and rejected** -- `run-all.sh` classifies a truncated suite correctly as `CRASH`; that report was an orchestrator misreading of the aggregate `Total:` line, recorded so a later reader does not "fix" working code | 2026-09-02 |
| Wave 1 / D31 | Raising `maxTurns` broke three VERIF-T09 assertions that pinned the old values | **Assertions updated to preserve VERIF's intent, never weakened or deleted.** AC1 pinned the exact `maxTurns: 50`; it now asserts the floor (`>= 50`), which is VERIF's actual intent ("no longer 25", enforced positively there and negatively by AC2) and survives any future evidence-based raise. AC4's `edm-implementer` entry was a scope-containment control proving VERIF touched no producer; its value moved to 200 so the other three producers still catch an accidental change. AC8's revert self-test rewrote the literal `maxTurns: 50`, so the raise turned its `sed` into a no-op and it could no longer construct the reverted copy it exists to test -- now value-agnostic | This chain is the strongest argument in the initiative for writing self-tests. VERIF-T09 AC8 exists solely to prove AC1 is load-bearing rather than passing regardless of file content, and months later it caught a vacuity introduced by a change its author never anticipated. Had AC8 not existed, AC1 would have silently become an assertion that passes on any file with any `maxTurns` value | 2026-09-02 |
| Wave 1 / D32 | `EDMV4-T04` AC11's orphan-citation check was reverted rather than shipped broken, after its sweep repeatedly self-matched the anchor sentences it had just written -- consuming three implementer turn budgets | **Built during QC remediation, using the solution QC shard 1 found: skip the literal three-dot `Sec."..."` placeholder.** Two failure modes -- ORPHANED (cited section has no mirror) and UNANCHORED (mirrored but no read instruction) -- comparing on the suffix-stripped heading and exempting the deliberately-unmirrored verifier sentinel | **On its first run it caught the exact regression it exists to prevent, already live in-wave**: `EDMV4-T34` added two bare `CLAUDE.md Sec."EDM mode matrix"` citations to `skills/orchestrator/SKILL.md` after `EDMV4-T04` anchored the tree. The missing enforcement mechanism had allowed its own failure class to recur within the same wave, undetected until QC. Building it hit two further false-positive classes -- matching `Sec."X"` citations to *other* documents (28 phantom orphans, fixed by anchoring on the `CLAUDE.md` prefix) -- and one gap is recorded rather than papered over: a bare `Sec."X"` continuation whose prefix sits on the previous line is not detected | 2026-09-02 |
| Wave 1 / D33 | `EDMV4-T33` AC9: should `docs/audit-patterns/README.md:137`'s pattern-library index row ("code-audit.md \| 11-lens code audit \| EDMV2 seed (2026-06-08)") be bumped to 14, given the row's own third column dates it to the EDMV2 seed corpus | **Retained as a dated historical seed row, not bumped.** The row describes the lens count in force when the EDMV2 seed corpus was captured (2026-06-08), not the current count; bumping it to 14 would misdate a historical snapshot. `docs/audit-patterns/code-audit.md` itself was inspected per AC10 and carries no current-count assertion -- only dated per-finding entries (`CA-NNN (P#, lens(es) L#...)  (EDMV3, 2026-08-21, ...)`) that name individual lens IDs, none of which asserts a total | Two further sites found during the same sweep and deliberately left: `plugins/edm/CLAUDE.md`'s `audit_rounds.<type>.rounds[].round_type` state-field row (the "full when the lens set equals all eleven lens IDs ... run all 11" description) is a substantive two-way-to-three-way rewrite explicitly owned by `EDMV4-T22` AC12 and `EDMV4-T23` AC12, not a bare count bump, so it is untouched here; and the root-repository `CLAUDE.md`'s edm plugin bullet ("an 11-lens code audit") is a real stale site but sits outside `plugins/edm/` and outside every T29/T33/T16 Target Components list, so it is left unswept pending a ticket that names it. One extra in-scope site was fixed beyond the ticket's explicit citation list: `plugins/edm/CLAUDE.md`'s Verifier-completion-sentinel `maxTurns: 30` floor passage ("the eleven code-audit lens agents' own ceiling") -> fourteen, found by the same grep sweep and squarely a current-count assertion | 2026-09-02 |

| Wave 2 / D34 | `EDMV4-T11` AC11 requires the measured GateGuard allow-path p95 to be recorded in `decisions.md`, but `decisions.md` is not in that ticket's Target Components. The implementer measured the figure and **declined to write it**, flagging the gap rather than writing outside its assigned scope | **Figure recorded here by the coordinator.** Measured p95: **42 ms** on the marker-absent allow path and **38 ms** marker-present, against a 126-byte `Edit` `tool_input` payload, 20 samples, nearest-rank p95, on Apple M4 Max / macOS 26.6.2 (Darwin 25.6.0). Runs varied 25-42 ms. **Budget MET** against SRD Sec.9.1/9.3's 50 ms design target | The refusal was correct: writing outside assigned Target Components is a contract violation, and an implementer silently widening its own scope is worse than a flagged gap. Recording it at the coordinator level closes AC11 without weakening that rule. **The figure is quoted with its fixture size, sample count and host, per `EDMV4-T01`'s governing rule that a budget without its input size is dominated by fixed process overhead and reads differently on every machine.** Note the 50 ms figure was a **design target, not a verified property**, until this measurement -- risk R4 recorded it as "plausible and standard practice, but unmeasured for this plugin". It is now measured, on one host, once | 2026-09-02 |
| Wave 2 / D35 | `EDMV4-T11`'s AC9 smoke test restricts `PATH` to simulate `jq` being absent, then used a `jq`-spy that called `touch` to record invocation -- but `touch` itself needs a `PATH` lookup, so the spy could never fire and the test proved nothing | **Fixed during the ticket's own resume**: the spy now uses the shell builtin `: > file`, which needs no `PATH`. A positive control was added proving the spy DOES fire when a marker is present | A test that disables `PATH` and then depends on a `PATH`-resolved binary is self-defeating in a way that reads as passing. This is the same family as the five self-matching scans and the six `set -e` aborts this initiative has produced: **the assertion machinery breaking in a direction that looks like success**. The positive control is what distinguishes "the spy did not fire because nothing happened" from "the spy could never fire at all" | 2026-09-02 |

| Wave 2 / D36 | `EDMV4-T29` AC12 has two clauses: the closure grep must return zero stale references to either renamed lens-count heading, **and the result must be recorded**. Wave-2 QC (shard 2) independently re-derived the grep -- `The 11 Audit Lenses`, `Why 11 Lenses`, `Audit Lenses`, `Single-Pass Audits Miss` across all of `plugins/edm/` return exactly two hits, both the renamed headings' own definition sites -- so clause 1 genuinely holds. Clause 2 was unmet: no `decisions.md` row, no mention in commit `6e64c86`, no assertion in `bin/tests/` | **Recorded here, closing AC12.** The sweep result: **zero stale references** to `## The 11 Audit Lenses` or `## What Single-Pass Audits Miss (Why 11 Lenses)` anywhere under `plugins/edm/` after `EDMV4-T29` renamed them to their fourteen-lens forms. Re-verified by QC rather than taken from the implementer | The FAIL was correct and the distinction matters: a sweep whose result exists only in the implementer's head is not auditable, and the next reader has no way to tell "verified zero" from "never checked". AC12 pairing the grep with a recording obligation is good AC design, and it caught exactly the gap it was written for | 2026-09-02 |
| Wave 2 / D37 | `decisions.md` D33 recorded the root `CLAUDE.md` edm bullet as a stale site left unswept because it falls outside every Target Components list. Wave-2 QC found that line carries **two** stale facts, and D33 named only one | **Both fixed**: `(v3.2.2)` -> `(v3.3.0)` and `11-lens code audit` -> `14-lens code audit` in the repository root `CLAUDE.md` | The version half is the interesting one: it went stale **because of `EDMV4-T33`'s own AC12 bump** to 3.3.0, so D33's "left unswept pending a ticket that names it" rationale never covered it -- the ticket created the staleness in the same commit that recorded the site as pre-existing. A change that invalidates a fact elsewhere in the tree owns that invalidation, whether or not the other site is in its Target Components. Neither half failed an AC (root `CLAUDE.md` is outside every list and AC12's three conjuncts all hold), so `EDMV4-T33` stands at PASS | 2026-09-02 |
| Wave 2 / D38 | `EDMV4-T27` AC9 forbids copying any text from ECC, while AC2 prescribes a step-4 wording verbatim. Wave-2 QC measured the delivered lens against `ECC/agents/pr-test-analyzer.md`: zero shared 8-grams, and exactly one shared run -- the seven words `prefer meaningful assertions over no-throw checks`, which AC2 itself dictates | **Recorded as a specification tension, not a defect.** Graded PASS because no implementation could satisfy both clauses simultaneously | The clean-room posture in `CLAUDE.md Sec."Prompt conventions (house style)"` is pattern-level adoption, not zero-overlap-of-words, and a seven-word technical phrase mandated by the initiative's own AC is not a copying event. Recording it prevents a later audit re-litigating the same overlap, and flags that an AC dictating verbatim wording should not sit beside an AC forbidding all shared text | 2026-09-02 |
| Wave 2 / D39 | `EDMV4-T31`: re-inventory the lens-count sites `EDMV4-T30` retargeted, per its own three-shape grep (`-eq 11`/`== 11` exact integers; `edm-audit-` 3+ times per line for hardcoded name lists; `-eq 12\|13\|15` offset counts), tree-wide, and record the do-not-touch list and any discrepancy against `EDMV4-T30`'s named site set rather than silently absorbing it (per AC1-AC3, and per the wave-2 QC pattern D36 already harvested: a sweep whose result exists only in an agent's head is unauditable) | **Reconciliation closed with two discrepancies found and fixed, four new do-not-touch categories recorded, and the tree-wide closure grep re-run clean.** (1) **`bin/tests/wave7-smoke.sh:9522`'s `verif_t09_lens_name_count -eq 11` (VERIF-T09 AC3, landed after the ticket pack was written) was not in `EDMV4-T30`'s named nine-site list** -- found by AC1's second pass and fixed to compare against the same `WAVE7_LENS_COUNT` constant AC10 introduces, making the tree-wide exact-integer count **eleven** (T30's nine, plus this tenth in `wave7-smoke.sh`, plus `bin/edm-state:1611`'s own self-check owned by `EDMV4-T21`), not the ten the ticket pack's Technical Notes projected. (2) **Three "30 agent files on disk" baseline assertions** (`wave7-smoke.sh` T03 AC9 and its T66 AC6 mirror, both a bare `ls agents/*.md \| wc -l` baseline, plus T46 AC10's carve-out-heading file count) are a **fourth count shape none of the three named greps can find** -- they count all agent files, not lenses, so they carry no `11`/`eleven`/`-eq 12\|13\|15` token at all; each grew from 30 to 33 as a second-order consequence of the three new lens agent files and is fixed to 33, discovered by re-deriving what each assertion actually counts rather than trusting its label (the same technique D36's grep-and-record discipline models). Both fixes landed inside `EDMV4-T30`'s own commit (serialization rule honored) rather than duplicated here. **`bin/edm-state`'s four AC4 prose sites, fixed:** the `metrics-report` round-type legend (`:3725`, "all 11 lenses" -> "all 14 lenses"), the CA-478 comment naming the last manifest line of a full round (`:4872`, "on a full round that is L11" -> "L14"), `cmd_audit_converged`'s legacy-degradation comment (`:4975`, "a fresh eleven-lens opus round" -> "fourteen-lens"), and its unknown-round-type refusal text (`:5040`, "full (eleven-lens)" -> "(fourteen-lens)"). **`CLAUDE.md`/`README.md` AC5-AC7 sites: already correct, verified not edited.** `CLAUDE.md:118-119` and `README.md:204-205`'s artifact-layout trees already read `lens-L14`; the contested-audit-set row already reads "14 code-audit lenses ... (18 agents)"; the D34 passage already reads "all fourteen" throughout -- all three landed via the concurrent `EDMV4-T29`/prior-wave work this ticket's Technical Notes anticipated might land first, so this ticket's AC5-AC7 close as **verified, not edited** (recorded so "verified matching" is distinguishable from "never checked", per D36). `edm-sync-canonical-sections --check` exits 0 (AC10) -- no hand-edit was needed since neither generated section's live text changed. **Do-not-touch list, closed (AC8), keyed by string per the epic's own instruction, with four categories beyond the epic's own enumeration found live in the tree:** (a) `CLAUDE.md:307`/`docs/canonical-sections.md:88`, the Mermaid touch-point count ("eleven touch points inventoried in `architecture.md`"); (b) `bin/edm-state`'s pass-7 incident comment (`:4832`, "shipped eleven prose reports") and coverage-table column-width comments (`CONSTANT 11 columns`, `(11 and 10)`, both un-matched by the closure grep by shape, exactly as the epic warned); (c) `bin/edm-check-grants:11`'s own rationale comment ("a \`grep every agents/*.md\` check passes green while eleven code-audit lens agents..."), verified still describing the checker's own historical design rationale, not a live count; (d) `CHANGELOG.md` (17 matching lines, not the epic's projected 14 -- historical entries, none edited, a new entry is `EDMV4-T33`'s); (e) `bin/edm-state:2472`'s "does NOT imply \"a full eleven-lens round was run\"" invariant sentence, re-read and confirmed to remain true at 14, not mechanically incremented; (f) `CLAUDE.md:581`'s guard D2 text ("the 11-lens or 2-auditor fan-out"), fan-out policy prose, not a count; (g) **new, found live and not in the epic's list:** `agents/edm-audit-wiring.md`'s own `lens-L11.md`/`lens-L11.jsonl` output-path citations (5 sites) -- L11 is the Wiring lens's own permanent ID and never changes with the total count; (h) **new:** `bin/tests/wave7-smoke.sh`'s `EDMV3-T42`/`T42 AC10` Mermaid-touch-point assertions (3 sites, the same unrelated eleven as (a), independently re-derived in the smoke suite), its `CA-186 G3` "twelve documented srd_root shapes" (2 sites), its `G5/CA-195` "Twelve further sites" (SRD/{PREFIX} spelling sweep, 1 site), its `G21/CA-074` "three shapes across twelve" (`die()` history, 1 site), its `EDMV3-T25` historical batch-scope note listing the original eleven lens-file suffixes as of that historical batch's own file boundary (5 lines), and its `CA-476` "thirteen prompt surfaces read as accumulated wisdom" (prompt-surface count, unrelated domain, matching the identical unrelated-thirteen already found in `bin/edm-state:5502`); (i) **new:** `bin/_edm-cli-lib.sh`'s "twelve pre-existing copies" of the shared `--help` extractor (CA-005, unrelated); (j) **new:** `bin/tests/wave8-smoke.sh`'s own unrelated "Eleven such directives" (shellcheck-suppression count, CA-500-class) and its `EDMV4-T32`-owned positive-control fixture text that deliberately contains the literal word "eleven" to prove its own scan fires. **Out of scope, confirmed and left untouched:** `docs/audit-patterns/README.md:137` and `docs/ecc-integration-analysis.md` (both `EDMV4-T33`'s "documentation and user-facing surfaces", not this ticket's). **Tree-wide closure grep, re-run after the sweep** (`grep -rniE 'eleven\|twelve\|thirteen\|fifteen\|11[- ]lens\|lens-L11\|L1-L11\|all 11\|-eq (11\|12\|13\|15)' plugins/edm/`, excluding `bin/tests/fixtures/` which is `EDMV4-T32`'s fixture-growth surface and `CHANGELOG.md`'s historical entries): **every remaining hit is a member of the do-not-touch list above**; zero survivors outside it | The epic's own Technical Notes predicted line-number drift on `EDMV4-T30`'s citations and explicitly asked this ticket to reconcile rather than assume; both discrepancies found (the VERIF-T09 site and the three-site "30->33" shape) are exactly the two hazards the epic named -- an incomplete inventory, and a shape the named grep patterns structurally cannot see. Recording all four `EDMV4-T30`/`EDMV4-T31` fixes here, plus the do-not-touch list in full with every site newly found live in the tree (not only the epic's own pre-enumerated six), closes AC8/AC9/AC11 the way D36 requires: a later reader can tell this inventory was closed deliberately, not skipped | 2026-09-03 |

## Finding-to-Commit Ledger

| Finding | Source | Decision | Ticket | Status |
|---------|--------|----------|--------|--------|
| EDMV4-50 -- add D15 as a third generated canonical section | SRD Sec.4 | D27 (context) | EDMV4-T04 | Fixed |
| EDMV4-51 -- enumerate and resolve every orphaned by-name citation before anchoring | SRD audit P0-3 | D27, D28 | EDMV4-T04 | Fixed |
| EDMV4-49 -- anchor all 14 by-name reference files | SRD Sec.4 | D10, D27, D28 | EDMV4-T04 | Fixed |
| CA-532 | `SRD/.archived/edm/EDMV3__prompt-streamline/code-audit/findings-ledger.jsonl` (raised_round: 10, resolved_round: 11) | D9 | `EDMV4-T05` | Verified fixed in current source: `evals/run-eval.sh:420-421` declares `CLAUDE_ALLOWED_TOOLS`/`CLAUDE_DISALLOWED_TOOLS` as real bash arrays, `:460-461` expands them `"${ARRAY[@]}"`. No re-fix attempted; pinned by a regression check in `bin/tests/wave8-smoke.sh` |
| CA-490 | `SRD/.archived/edm/EDMV3__prompt-streamline/code-audit/findings-ledger.jsonl` (raised_round: 9, resolved_round: 11) | D9 | `EDMV4-T05` | Verified fixed in current source: `bin/edm-compare-eval:70-75` runs the `complete != true` refusal before the baseline-existence check at `:77-81`. No re-fix attempted; pinned by a regression check in `bin/tests/wave8-smoke.sh` |
| CA-537 | `SRD/.archived/edm/EDMV3__prompt-streamline/code-audit/findings-ledger.jsonl` | D9 | `EDMV4-T05` | Dropped as moot -- no `.gitlab-ci.yml` exists in this repository (constraint C8, no CI pipeline exists at all); commit `b56558d` removed the pipeline; stray `.gitlab-ci.yml` copies under `.claude/worktrees/*/` are agent scratch worktrees, not this repository's own files |
