# Key Decisions

| # | Decision | Chosen | Rationale | Date |
|---|----------|--------|-----------|------|
| D1 | Which prefix, given that `EDMV4__lint-and-pipeline-budgets` is named in `plugins/edm/CLAUDE.md:345-346` and in EDMV3's `decisions.md` (D29, D34, D62) but has never existed on disk -- the condition EDMV3's own code audit raised as CA-430 (P2, round 8; L6 flagged the same claim in round 7) | **EDMV4**, with the description slug changed to `ecc-integration`. This initiative absorbs the named-but-never-created one, inheriting its three orphaned ticket IDs: **EDMV4-T01** (re-derive the Mermaid-class `edm-lint-artifacts` budget as an absolute ceiling plus a sized ratio, measured on a generated 50-initiative fixture), **EDMV4-T04** (anchor the eight remaining EDMV3-54 prompt-surface touch points that still carry a bare `CLAUDE.md Sec."..."` reference), **EDMV4-T05** (fix the CA-532 `--allowedTools` argv-splitting bug, run the live wave-A eval capture, commit `evals/baseline/scores.json`, re-verify CA-490's `edm-compare-eval` reorder). T02 and T03 were closed inside EDMV3 and are not carried forward | Three ticket IDs were cited across EDMV3's decision ledger, its ticket coverage map and `CLAUDE.md` as living in a pack that no glob matches. Absorbing the name retires that debt directly rather than leaving a second dangling reference beside it. Cost: the slug no longer reads `lint-and-pipeline-budgets`, so `CLAUDE.md:345-346` goes stale on creation and is itself in scope to fix | 2026-08-30 |
| D2 | Which parts of `plugins/edm/docs/ecc-integration-analysis.md` are in scope | **Part 4 in full (4.1 GateGuard, 4.2 update-patterns read-only defect, 4.3 size classifier, 4.4 lenses L12/L13/L14) plus Part 5 items 5.1 through 5.5**, plus the three inherited tickets from D1. Out of scope: **5.6** (modular install), all of **Part 6** (reject list), and **Part 7** (kernel/methodology split) | 5.6 is conditional on the Part 7 split and the analysis states it is not actionable on its own. Part 7 the analysis itself defers behind everything in Parts 4 and 5 regardless. Part 6 is the document's own reject list. Note per Part 8.3.1 that every "Value" rating in the source is the author's ranking of a structural argument, not a measured outcome -- the structural claims were verified against source, the value claims were not, and 5.5's premise in particular is called out as unmeasured | 2026-08-30 |
| D3 | `/edm:orchestrator` Step 2 could not dispatch any phase: all 14 skills in `plugins/edm/skills/` set `disable-model-invocation: true`, so every `Skill`-tool invocation of a phase skill failed with `Skill edm:<name> cannot be used with Skill tool due to disable-model-invocation`. Hit live during this initiative's intake on the `/edm:plan` dispatch | **CLOSED -- real defect, already fixed upstream; NOT in EDMV4 scope.** Fixed on `origin/main` by commit `bdec805` ("fix(edm): :bug: Remove disable-model-invocation so skills can be Skill-tool dispatched", merged as `bdb5698`, released as 3.2.1), which replaced `disable-model-invocation: true` with `user-invocable: true` on all 14 skills and added a plugin-wide guard to `bin/edm-check-skill-sync` banning the flag anywhere under `skills/`. Re-dispatch after `/plugin` update + `/reload-plugins` succeeded | The defect was not Phase-1-specific -- all six Step 2 dispatches failed identically, as did the `/edm:code-audit` and `/edm:verify-runtime` calls in the Phase 6 entry. It contradicted `CLAUDE.md Sec."Skill-tool composition"` (D21), whose cited in-repo precedent (git `commit` -> `jira:search-jira`) holds only because its target carries `user-invocable: true` and no `disable-model-invocation` key. The upstream guard closes it with a regression test, so EDMV4 inherits the fix rather than re-doing it. Supersedes the "candidate scope addition" framing passed to `/edm:plan` | 2026-08-30 |
| D4 | The initiative branch `edm/edmv4-ecc-integration` was cut from a local `main` that was **2 commits behind `origin/main`**, so its tree still carried `disable-model-invocation: true` on all 14 skills, plugin version 3.2.0, and no `edm-check-skill-sync` guard -- i.e. the exact state D3 fixed. Merging as-is would have silently reverted the D3 fix | **Original decision: rebase onto `origin/main` before Phase 6.** **Superseded during Phase 2 -- the revert hazard is GONE.** Commit `bdec805` ("fix(edm): :bug: Remove disable-model-invocation so skills can be Skill-tool dispatched") is now the tip of this branch, applied directly rather than by rebase. Verified in the working tree: **zero** skill files carry `disable-model-invocation: true`, and `bin/edm-check-skill-sync` carries 4 references to the guard. Root `CLAUDE.md` was updated to match | **What remains is narrower and non-blocking**: the branch is now 3 commits behind `origin/main`, missing the merge commit `bdb5698` and the two version-bump commits (`33d63e0`, `4ad0f35`). `plugins/edm/.claude-plugin/plugin.json` therefore still reads `3.2.0` on this branch while `origin/main` reads `3.2.1` -- a one-line divergence to reconcile at merge, not a functional defect. The residual pre-merge task is reconciling that version bump, not restoring the D3 fix. Still outstanding in the working tree and unrelated to this: the staged EDMV3-archival rename set (~313 paths), and unstaged edits to `bin/edm-state` (an `*opus-5*` arm added to `compute_cost_usd`'s model `case`, local work not present upstream) and `bin/tests/wave6-smoke.sh` | 2026-08-30, amended 2026-08-31 |

| Gate 1 / D5 | Where should 5.1 (bounded implementer/QC remediation loop) live? | **Its own initiative** -- out of EDMV4 scope, carried as a named follow-on rather than an unnamed candidate (D13/D14 precedent) | 5.1 is Large by itself and its cost-ceiling precondition is design work distinct from every other item. Splitting it means the eleven remaining items are not gated behind that design phase. ECC's `gan-harness.sh` provides no cost model to copy -- it has no dollar ceiling at all, only an iteration cap (default 15) plus plateau detection (2 consecutive iterations gaining <=0.2, starting no earlier than iteration 3, `gan-harness.sh:175-259`) | 2026-08-30 |
| Gate 1 / D6 | How should GateGuard (4.1) be implemented -- vendor ECC's 3 Node files or rewrite in bash? | **Deferred at Gate 1 ("Resolve in SRD"); ANSWERED in Phase 2 as architecture decision AD1: bash rewrite, ~250-350 lines.** Independently recommended by the human the same day. Subject to Gate 2 ratification | **There is no bash implementation to adopt anywhere -- both existing ones add a runtime dependency.** Upstream `zunoworks/gateguard` is **Python** (`pyproject.toml`, `src/gateguard/`, `pip install gateguard-ai`, verified by direct inspection of the repo); ECC's vendored copy is **JavaScript** (`gateguard-fact-force.js`, 1,301 lines) -- ECC ported it. EDM's required-binary contract is `bash`/`jq`/`git` only, so either adoption path adds a first. AD1's load-bearing insight is a scope one both explorers treated as fixed: ECC's 741 lines of helper closure (`shell-substitution.js` 482 + `gateguard-heredoc.js` 259) exist **solely** to serve `isDestructiveBash():674-721`. 4.1 is an EDIT gate; descoping the destructive-Bash arm deletes the entire recursive-BFS shell tokenizer explorer 04 called "the harder half to port," collapsing the estimate from 400-600 lines to ~250-350. **Descoping the destructive-Bash gate is itself a scope reduction requiring Gate 2 ratification**, not an implementation detail | 2026-08-30, answered 2026-08-31 |
| Gate 1 / D7 | What rule-file format should hookify (5.3) use? | **JSON rule files** | jq-native, adds no required binary beyond the existing `bash`/`jq`/`git`, and matches how every other structured file in `bin/` is consumed. There is zero YAML parsing anywhere in `bin/` today, so ECC's YAML-frontmatter format would need a from-scratch bash/awk YAML subset parser or a new required binary. ECC compatibility buys nothing: explorer 04 established by exhaustive search that ECC has **no evaluator at all** -- the condition/operator engine in `hookify-rules/SKILL.md` is documentation with no corresponding code, so only the format was ever reusable | 2026-08-30 |
| Gate 1 / D8 | How should L13 (Type Design) handle stack-conditionality, given `round_type=full` is derived by comparing the run set against `ALL_LENS_IDS`? | **Auto-N/A plus a new `round_type` state** | Follows the test layer's N/A-agreement precedent (`agents/edm-test-integration.md:21-25`): one authority determines applicability, consumers agree rather than self-declare, N/A is recomputed each run, absence is authoritative. The mode matrix is the wrong precedent -- it is a user-selected enum, not detected from the codebase. Keeps L13's conditionality framed as genuine **inapplicability**, satisfying the D2 guard, never as cost-driven exclusion of an applicable lens. Phase 2 must design how CA-471's completeness backstop learns a third state distinguishing "legitimately N/A" from "missing JSONL" | 2026-08-30 |
| Gate 1 / D9 | Should EDMV4-T05 spend live API budget to capture `evals/baseline/scores.json`? | **No -- recorded as an explicit scope boundary with a named follow-on** | Both named bugs are already fixed and verified in source (CA-532 at `evals/run-eval.sh:420-421,460-461` -- `CLAUDE_ALLOWED_TOOLS`/`CLAUDE_DISALLOWED_TOOLS` are real bash arrays expanded as `"${ARRAY[@]}"`; CA-490 at `bin/edm-compare-eval:70-75` -- the `complete != true` refusal now runs before the baseline-existence check at `:77-81`), so T05's code work is complete and closes as verification, confirmed by two regression checks in `bin/tests/wave8-smoke.sh` (EDMV4-T05, AC1/AC2, self-proven to discriminate). A third inherited item, CA-537's `.gitlab-ci.yml` exit-code ordering, is dropped as moot: no `.gitlab-ci.yml` exists in this repository (constraint C8 -- no CI pipeline at all), commit `b56558d` removed the pipeline, and stray `.gitlab-ci.yml` copies under `.claude/worktrees/*/` are agent scratch worktrees, not this repository's own files. **Named follow-on (AC9-AC12):** capturing `evals/baseline/scores.json` is out of EDMV4's scope and is a human credential decision -- it is carried as its own follow-on ticket, to be filed and run once a human owning `ANTHROPIC_API_KEY` for wave-A exit is ready, not spent silently by an agent verifying its own ticket. That capture requires three `run-eval.sh` invocations against fresh scratch trees, each scored individually by `score-artifacts.sh`, with the run whose `total` is the **middle** of the three committed as the baseline. The committed `evals/baseline/scores.json` must carry `scorer_version` (currently `"1.1.0"`, `score-artifacts.sh:139`), `dimensions_scored` (**5** for a wave-A `plan -> srd -> audit-srd` capture -- dimension 5, lens-JSONL-versus-prose agreement, has no input without a code-audit round), `dimensions`, `total`, `complete`, and -- only on the committed baseline, never on a candidate run -- `variance.total_range` (the sole baseline field `edm-compare-eval` reads: `jq -r '.variance.total_range // 0'` at `:109`). Until that capture happens, `edm-compare-eval` exits **3**, printing "the eval tripwire is NOT armed" (`bin/edm-compare-eval:77-81`) -- a named, non-crashing outcome, not a silent pass; this record cross-references `evals/baseline/README.md` (`:39-55`, `:104-116`, `:165-171`), which documents the same boundary in its own words, so the two records agree. No agent spends API budget under EDMV4 to capture the baseline | 2026-08-30 |
| Gate 1 / D10 | What is EDMV4-T04's scope -- the 8 files `CLAUDE.md` names, or the verified set? | **All 14 verified files, plus a third generated canonical section** | The verified unanchored set is 5 agents + 9 skills = 14; `CLAUDE.md`'s own list misses `edm-qc-auditor.md`, `verify-runtime/`, `push-jira/`, `orchestrator/`, `metrics/` and `code-audit/SKILL.md`, all carrying the identical D22 defect. Separately, `edm-qc-auditor.md:39` cites `Sec."Unverifiable acceptance criteria (D15)"`, which has **no mirror in `docs/canonical-sections.md`** (that file generates only two sections), so anchoring it the standard way would not fix it -- hence adding D15 as a third generated section, with `edm-sync-canonical-sections` re-run and its `--check` drift assertion re-verified in the same ticket | 2026-08-30 |
| Gate 1 / D11 | How should codemaps (5.5) be approached? | **The `SRD/.codemap.md` interim** -- first explorer of an initiative writes a reusable current-architecture map that later initiatives read and refresh; no generator built | The source document flags its own value claim here as explicitly unmeasured, and its Part 8.2 correction establishes that ECC's generator emits literal template placeholders for its two most valuable sections (`generate.ts:225-231`), so there is nothing worth porting. The interim is the cheapest way to test whether the premise holds before spending medium-large effort on a generator. A codemap is the CURRENT architecture and does not duplicate `architecture.md`, which is the TARGET | 2026-08-30 |
| Gate 1 / D12 | Should Phase 1's eleven verified corrections be written back into `ecc-integration-analysis.md`? | **Yes -- amend the source document in place**, in the Part 8.2 style it already uses to self-correct twice; `planning.md` retains the correction table as the audit record | Leaving the analysis uncorrected means the next reader re-derives the same eleven findings. The corrections are substantive, not cosmetic: ECC's hook count is 23 across 7 events (not 25/8); the seven security triggers live at `orch-pipeline/SKILL.md:100-104`, not `rules/common/security.md` (a citation the analysis repeated without checking); hookify has no evaluator; `GAN_EVAL_CRITERIA` is dead code; `delivery-gate` has an undocumented third disk tier; `silent-failure-hunter`'s body is 44 lines not ~30; `harness-audit.js` consumer mode is 16 checks/39 points not 11/29; the `update-patterns` line cites are stale; `GATEGUARD_DISABLED=1` is a second omitted kill switch; `MultiEdit` needs one retry per unchecked file; codemaps placeholders are at `:225-231` | 2026-08-30 |

| D13 | GateGuard's upstream provenance was the one unresolved licence question. ECC root is MIT (`ECC/LICENSE:1-3`, Affaan Mustafa 2026) but ECC **vendored** the hook from a third party per its own header at `scripts/hooks/gateguard-fact-force.js:19-20` (`skills/gateguard/SKILL.md:5` sets `metadata: origin: community`), so MIT-via-ECC was not sufficient provenance | **CLEARED -- `zunoworks/gateguard` is MIT.** Verified by direct inspection of `https://raw.githubusercontent.com/zunoworks/gateguard/main/LICENSE`, which reads "MIT License / Copyright (c) 2026 Hirokazu Seto / ZUNO WORKS K.K." Vendoring or porting `gateguard-fact-force.js` is permitted with attribution | Direct inspection is the standard `CLAUDE.md Sec."Prompt conventions (house style)"` already applies to the `caveman` and `ponytail` adoptions, both of which record a licence "verified 2026-07-28 by direct inspection" -- an assertion alone would not meet the bar this plugin sets for itself. **Attribution obligation, amended after AD1**: this entry originally reasoned about vendoring, where MIT's attribution and licence-notice requirement genuinely binds because the reuse is verbatim (~2,042 lines). AD1 chose a **bash rewrite with the destructive-Bash arm descoped**, so nothing is copied from either upstream implementation -- what carries over is the concept (deny first touch, demand facts, allow on retry), which is pattern-level adoption matching the clean-room posture `CLAUDE.md Sec."Prompt conventions (house style)"` already records for `caveman` and `ponytail`. **An earlier framing in this initiative's conversation held that a rewrite derived from reading the source is still a derivative work carrying the same obligation; with the tokenizer descoped that is more cautious than the facts warrant.** EDM should still record the source, URL, licence and means of verification in the house-style section, as a matter of convention rather than strict MIT compulsion -- it is free, and it is what that section exists for. If Phase 2 or Gate 2 reverses AD1 back to vendoring, the strict obligation returns in full | 2026-08-31 |

## Finding-to-Commit Ledger

| Finding | Source | Decision | Ticket | Status |
|---------|--------|----------|--------|--------|
| CA-532 | `SRD/.archived/edm/EDMV3__prompt-streamline/code-audit/findings-ledger.jsonl` (raised_round: 10, resolved_round: 11) | D9 | `EDMV4-T05` | Verified fixed in current source: `evals/run-eval.sh:420-421` declares `CLAUDE_ALLOWED_TOOLS`/`CLAUDE_DISALLOWED_TOOLS` as real bash arrays, `:460-461` expands them `"${ARRAY[@]}"`. No re-fix attempted; pinned by a regression check in `bin/tests/wave8-smoke.sh` |
| CA-490 | `SRD/.archived/edm/EDMV3__prompt-streamline/code-audit/findings-ledger.jsonl` (raised_round: 9, resolved_round: 11) | D9 | `EDMV4-T05` | Verified fixed in current source: `bin/edm-compare-eval:70-75` runs the `complete != true` refusal before the baseline-existence check at `:77-81`. No re-fix attempted; pinned by a regression check in `bin/tests/wave8-smoke.sh` |
| CA-537 | `SRD/.archived/edm/EDMV3__prompt-streamline/code-audit/findings-ledger.jsonl` | D9 | `EDMV4-T05` | Dropped as moot -- no `.gitlab-ci.yml` exists in this repository (constraint C8, no CI pipeline exists at all); commit `b56558d` removed the pipeline; stray `.gitlab-ci.yml` copies under `.claude/worktrees/*/` are agent scratch worktrees, not this repository's own files |
