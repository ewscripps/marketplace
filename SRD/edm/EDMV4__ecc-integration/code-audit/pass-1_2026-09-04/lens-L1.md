# EDM Code Audit -- Lens L1: Logic, Correctness & Completeness

Round 1 (full), pass-1_2026-09-04. Diff under audit: `git diff main..HEAD -- plugins/edm/`.

Scope covered this round: `bin/edm-gateguard`, `bin/edm-hookify`, `bin/edm-stop-gate`,
`bin/edm-bash-gate`, `bin/edm-repo-readiness`, `bin/_edm-datadir-lib.sh`, `bin/edm-check-grants`
(EDMV4-T04 AC11 block), `bin/edm-sync-canonical-sections`, the EDMV4-marked regions of
`bin/edm-state` (marker primitive, `detect-conditional-lenses`, `audit-round-start`/`-complete`,
`gate-check`, `session-start` reconciliation, `update-patterns` delta machinery),
`hooks/hooks.json`, `skills/code-audit/SKILL.md` Step 1/4, `skills/orchestrator/SKILL.md`
Step 1b.5, `skills/plan/SKILL.md` Step 6, the 14 lens agent files, and the EDMV4-T28/T52 bands of
`bin/tests/wave8-smoke.sh` plus `bin/tests/timing.sh --gateguard`.

Note on evidence standard: per D44, six tickets closed `NOT RUNTIME-VERIFIED`, and neither
`/plugin update` nor `/reload-plugins` reads the working tree, so every runtime path below was
read as source rather than observed. Where a finding names a triggering input, that input is
derived from the code, not from an executed reproduction. The single exception is L1-013, which
is settled against the vendor's own published hook contract rather than against this tree.

## Findings (L1: Logic, Correctness & Completeness)

| ID | Severity | File:Line | What's Wrong | Fix |
|----|----------|-----------|--------------|-----|
| L1-001 | P1 | plugins/edm/bin/edm-stop-gate:160 | `_hookify_out="$(printf '{}' \| edm-hookify eval stop 2>&1)"` merges `edm-hookify`'s stderr into the captured stdout, then discards the capture on every path except `rc == 2`. Every `warn`-rule line and every malformed-rule-file setup error is therefore swallowed and never reaches the operator. `warn` is the format's DEFAULT action (`CLAUDE.md` "Hookify rule format": absent `action` reads as `warn`), so the default-action path of the entire `stop` event is non-functional through its only consumer. Demonstrating input: `.claude/edm-hookify/warn-x.json` = `{"name":"warn-x","enabled":true,"event":"stop","conditions":[],"message":"..."}` -- `edm-hookify eval stop` writes `warn-x warn ...` to stderr and exits 0; `edm-stop-gate` prints nothing. Same for an invalid-JSON rule file (hookify exits 1 after naming the path on stderr). The comment at `:163` asserts the opposite ("edm-hookify already warned on stderr for that case"), and both sibling consumers -- `bin/edm-bash-gate:67` and `bin/edm-gateguard:401` -- correctly do NOT redirect stderr, so this file is the lone divergence | Drop `2>&1` so hookify's stderr flows through to the operator, or capture stderr to a separate variable/file and re-emit it unconditionally |
| L1-002 | P1 | plugins/edm/bin/edm-gateguard:222,348 | `GG_MAX_DENIALS="${EDM_GATEGUARD_MAX_DENIALS:-3}"` is never validated, and `:348` feeds it straight into a bash arithmetic context: `[[ "$(gg_denial_count)" -ge "$GG_MAX_DENIALS" ]]`. Bash arithmetic treats a bare identifier as a variable name, so `EDM_GATEGUARD_MAX_DENIALS=none` (or `unlimited`, or any alphabetic word) evaluates to 0, making the test `0 -ge 0` -- true on the very first call and every call after. The gate then prints "denial budget (none) reached" and allows every Edit/Write/MultiEdit, forever, while appearing to be armed. That is exactly risk R2's worst case as recorded in decisions.md D26 ("GateGuard denying nothing while appearing to work"). `bin/edm-state:167` ships `to_int()` for precisely this hazard, and its own docstring records that arithmetic evaluation additionally performs command substitution -- `edm-gateguard` uses neither `to_int()` nor any regex guard | Validate at `:222` with a digit-only regex (`[[ "$GG_MAX_DENIALS" =~ ^[0-9]+$ ]] \|\| { warn; GG_MAX_DENIALS=3; }`), matching the `HUMAN_HOURLY_RATE_USD` guard at `bin/edm-state:143-144` |
| L1-013 | P1 | plugins/edm/hooks/hooks.json:82 | `"matcher": "git commit"` on the `PreToolUse` block is not a tool name, so `edm-lint-staged-artifacts` never runs. Settled against the vendor hook contract (`code.claude.com/docs/en/hooks`, "Matcher patterns"), which states that for `PreToolUse` the matcher filters on **tool name** -- documented matcher values are `Bash`, `Edit\|Write`, `mcp__.*` -- and that filtering on the tool's arguments requires the separate per-handler `if` field using permission-rule syntax (`"if": "Bash(git *)"`). `"git commit"` matches no tool name and is not a regex satisfied by `Bash`, so the block is inert: the unicode / attribution-trailer / leaked-tool-tag / mermaid-semicolon commit-time enforcement that `CLAUDE.md`'s "Hooks behavior" section documents as live has never executed. Two consequences inside this diff: (a) `bin/edm-bash-gate:12-15` and decisions.md D25's AC8 conclusion both assert the new `Bash` block is "matcher-disjoint" from this one and that "both fire on their own matcher independently" -- only one of the two can ever fire, and Spike A never registered a `"git commit"` matcher at all (it registered `Bash` twice), so it does not evidence the claim it was cited for; (b) the fallback D25 AC8 ruled unnecessary (folding `edm-lint-staged-artifacts` into a `Bash` dispatcher) is in fact the shape that works. Deliberately NOT resting on the em-dash-survival passage in `CLAUDE.md`: that passage already explains itself by reach (prefix mode never scans `skills/`/`agents/`) and by `collect_md_files`' `*.md` filter, so it corroborates nothing here. Pre-existing block, but this diff adds two sibling `PreToolUse` blocks beside it and builds a design decision on its behaviour | Change the matcher to `"Bash"` and narrow with a per-handler `"if": "Bash(git commit*)"`; then re-check D25 AC8's collision conclusion, since it would place a second real `Bash`-matcher block beside `edm-bash-gate`'s |
| L1-003 | P2 | plugins/edm/bin/edm-gateguard:300 | `glob_list=($globs)` is an unquoted array assignment, so it performs pathname expansion in addition to the intended word split. The `# shellcheck disable=SC2206` on the line above names only the word-splitting half; SC2206 covers both. Bash 3.2 has no `globstar`, so `**/tests/**` is read as `*/tests/*` and is expanded against the hook's cwd (the project root). Demonstrating input: a repository containing `src/tests/a.js` and `src/tests/b.js` -- the entry `**/tests/**` is replaced by those two concrete paths, and `gg_is_exempt "src/tests/c.js"` then returns false, so a file the exemption list is supposed to cover gets a first-touch denial. The same applies to `**/dist/**`, `**/build/**` and `**/node_modules/**` in any tree where a matching path exists at that depth | Disable globbing around the split (`set -f; glob_list=($globs); set +f`) or replace it with a `while IFS=, read -r g` loop over `printf '%s\n' "$globs" \| tr ',' '\n'` |
| L1-004 | P2 | plugins/edm/bin/edm-state:4822-4830 | When `--lenses` is omitted, `lenses` is materialized to the full `ALL_LENS_IDS` set without subtracting `lenses_na`, and nothing anywhere validates that `lenses` and `lenses_na` are disjoint. Demonstrating input: `edm-state audit-round-start P code --na-lenses L13` (a combination the code's own comment at `:4785-4786` calls "independent optional flags") records `lenses` = all 14 IDs INCLUDING L13, `lenses_na` = `["L13"]`, and `round_type` = `full` (the union rule is satisfied). At completion, check (1) at `:4980-4990` requires a non-empty `lens-L13.jsonl` while check (2) at `:4994-5004` requires that same file to be absent -- mutually exclusive, so the round is downgraded to `partial` regardless of what the agents produced. Per `:4988` and CA-506 the downgrade is irreversible and `audit-round-complete` refuses a second completion, and a `partial` round is never convergent, so the initiative cannot converge on that round. Only `skills/code-audit/SKILL.md:73` is mitigated (it always passes `--lenses`); the CLI itself is a trap | Subtract `lenses_na` from the materialized set in the omitted-`--lenses` branch, and/or `die` at round-start when `lenses` and `lenses_na` intersect (before any state write, alongside the existing `CONDITIONAL_LENS_IDS` membership check) |
| L1-005 | P2 | plugins/edm/bin/edm-state:4602-4616 | The SessionStart Phase-6 marker reconciliation makes its two documented directions mutually exclusive: the stale-marker removal lives in the `if [[ -f "$_ss_marker" ]]` arm and the recreate lives in the `elif`, so a run that removes a stale marker never writes the correct one. Demonstrating input: marker names initiative A (no longer at `current_phase == 6`, e.g. after a branch switch) while initiative B genuinely is at phase 6. `session-start` removes A's marker, prints the `[info] removed stale Phase-6 marker` line, and returns with NO marker on disk -- so `edm-gateguard` takes its marker-absent fast exit and the whole gate is silently off for B until the next SessionStart. The comment at `:4593-4597` states the two behaviours as independent ("...is removed...; an absent marker is recreated when some initiative genuinely is at phase 6") | After `rm -f "$_ss_marker"`, fall through to `[[ -n "$_ss_phase6_first_prefix" ]] && _edm_marker_write "$_ss_phase6_first_prefix" "$_ss_phase6_first_dir"` rather than returning from the `if` arm |
| L1-006 | P2 | plugins/edm/bin/edm-hookify:178-198,227,304 | `validate()` checks unknown top-level keys, required-key presence, the `event` enum, the operator enum and per-event `field` membership -- but never the TYPE of `enabled` nor the value of `action`. `"enabled": "true"` (a JSON string, the single most likely authoring slip in a hand-written JSON rule) makes `$parsed.enabled != true` true at `:227`, so the rule emits `empty` and is silently skipped with no diagnostic on any stream. `"action": "Block"` (or any value other than the literal `block`) is coerced to warn at `:304`, so a rule the author explicitly opted into blocking with only warns. Both are the exact failure shape `CLAUDE.md`'s own "Hookify rule format" section says the per-event field constraint exists to prevent: "reads identically to 'my rule is fine and nothing violated it' -- exactly the failure a rule author cannot detect by inspection" | Add two arms to `validate()`: `($r.enabled\|type) != "boolean"` -> setup error naming the file and the value; `($r\|has("action")) and (["warn","block"] \| index($r.action)) == null` -> setup error naming the offending action |
| L1-007 | P2 | plugins/edm/bin/edm-gateguard:361,365,375,394 | Four `VAR="$(printf '%s' "$PAYLOAD" \| jq ... 2>/dev/null)"` captures on the gated path are unguarded under `set -euo pipefail` (`:48`), unlike every other capture in this file. Demonstrating input: a payload whose `tool_input` is not an object (`{"tool_name":"Edit","tool_input":"oops"}`) makes `.tool_input.file_path` a jq type error, exit 5; the assignment fails and the hook aborts with status 5. The documented exit contract at `:40-46` and in `CLAUDE.md`'s "Hooks behavior" row is 0/1/2 only, and `2>/dev/null` has already discarded jq's explanation, so the operator sees a bare non-zero hook exit with no message. The MultiEdit projection at `:375` and the hookify projection at `:394` are exposed the same way through `.tool_input.edits[0]` when `edits` is not an array | Guard each capture (`\|\| GG_TARGET=""` / `\|\| die "could not read the tool_input payload"`), matching the `\|\| die` form already used for the `tool_name` parse at `:131-133` |
| L1-008 | P2 | plugins/edm/bin/edm-gateguard:254,273-275,280-285 | The denial budget cannot advance when session state is unwritable, so the deny path has no backstop. `gg_record_denial` returns early on an unwritable directory (`:281`), `gg_denial_count` reads an absent file and returns 0 (`:273-275`), so `:348`'s budget test is never satisfied and `gg_maybe_deny` denies every call indefinitely. `gg_mark_checked` also returns without recording, and at `:254` (the empty-`GG_CHECKED_FILE` branch) it does so with no stderr warning at all. Demonstrating input: `EDM_GATEGUARD_STATE_DIR` set to a read-only directory -- the marker still resolves through `edm_data_dir()` so the gated path is reached, and every Edit is denied for the rest of the session. `gg_mark_checked`'s own docstring at `:250-251` claims the gate "never denies again for lack of a mark (AC7)", which the code does not implement | Treat an unresolvable/unwritable state store as budget-exhausted after the first denial in the process (or count denials in a store independent of the checked-file), and emit the stderr advisory on the empty-`GG_CHECKED_FILE` branch too |
| L1-009 | P2 | plugins/edm/bin/edm-check-grants:658-659 | `_cite_files="$(ls ... )"` followed by `for _cf in $_cite_files` word-splits on IFS, so any path component containing a space produces fragments. `PLUGIN_ROOT` is derived from `SCRIPT_DIR` (the user's checkout location), so a checkout under e.g. `~/My Projects/marketplace` yields fragments, every `[[ -f "$_cf" ]] \|\| continue` at `:660` skips, the loop body never runs, and the EDMV4-T04 AC11 orphan/unanchored check reports zero violations and exits 0. That is a check that cannot fail rather than one that reports a problem -- the same class D32 records this check being rebuilt to escape | Iterate with a NUL-delimited `find ... -print0 \| while IFS= read -r -d "" _cf`, the idiom `bin/edm-hookify:127-129` and `bin/edm-lint-artifacts` already use |
| L1-010 | P2 | plugins/edm/bin/edm-state:1725,1728,1729,1733,1735,1736 | `_l13_applies` mixes two matching disciplines: the file-name markers use `grep -qx` (whole-line, so only a marker at the REPOSITORY ROOT counts) while the extension markers at `:1726-1732` use suffix matches that hit at any depth. Demonstrating input: a TypeScript monorepo whose only TypeScript config files are `packages/app/tsconfig.json` and `packages/api/tsconfig.json` and which has no root `tsconfig.json` -- `detect-conditional-lenses` reports `L13` N/A, so the Type Design lens is dropped from a fully typed codebase. That is the coverage-loss-disguised-as-N/A outcome guard D2 names, arriving through the applicability predicate rather than through a cost decision. Secondary: the `.` in `tsconfig.json`, `Cargo.toml`, `go.mod`, `pyproject.toml`, `mypy.ini`, `pyrightconfig.json` is an unescaped regex metacharacter, so `tsconfigXjson` also matches | Anchor each file-name marker on a basename instead of the whole line and escape the dots: `grep -qE '(^\|/)tsconfig\.json$'`, and apply the same to the pyproject.toml content probe's path resolution |
| L1-011 | P2 | plugins/edm/bin/tests/wave8-smoke.sh:5356-5360 | `t52_ascii_scan`'s two branches do not implement the same predicate. The PCRE branch matches `[^\x00-\x7F]` (non-ASCII only); the fallback branch matches `grep -nv '^[[:print:][:space:]]*$'`, which under `LC_ALL=C` additionally selects any line containing an ASCII control byte outside the whitespace set (e.g. 0x1B ESC, 0x07 BEL). Since the fallback is the branch that runs on BSD grep -- macOS, this plugin's stated primary platform and its bash 3.2 floor per C1 -- the AC2 assertion enforces a strictly different rule there than on GNU grep, and a shell script legitimately embedding an ESC in a colour escape would fail the suite for a reason AC2 does not claim to police | Make the fallback match the PCRE branch's predicate exactly (e.g. `LC_ALL=C grep -n '[^'$'\t\r\n'' -~]'`), or state the deliberate divergence at the assertion so the two hosts' meanings are not silently different |
| L1-012 | P2 | plugins/edm/bin/tests/wave8-smoke.sh:4749-4763,4810-4817 | The EDMV4-T28 house-contract enforcement never asserts that the fourteen lens files carry fourteen DISTINCT IDs covering `L1`..`L14`. `t28_contract_violations` derives `n` from each file's own opening frame at `:4810` and checks that file against itself; the only cross-check is a COUNT comparison at `:4761`. Two lens files both declaring `L12` (or one declaring `L15`) would pass every assertion in the band while a real lens ID had no agent -- and `bin/edm-state`'s `ALL_LENS_IDS` would then require a `lens-L{N}.jsonl` from an agent that does not exist, downgrading every full round to `partial` at `audit-round-complete`. Verified against the live tree: L1-L14 each appear exactly once today, so this is an enforcement completeness gap, not a live defect | Accumulate each file's resolved `n` into a list and assert `printf '%s\n' "${ids[@]}" \| sort -u \| wc -l` equals the count AND that the sorted `L{n}` set equals `ALL_LENS_IDS` |

## Noted / Not Actionable

| ID | File:Line | Rationale |
|----|-----------|-----------|
| L1-014 | plugins/edm/bin/edm-check-grants:654,676 | `CITATION_EXEMPT_SECTIONS="Verifier completion sentinel"` is tested with the space-joined `case " $list " in *" $item "*` idiom, but its only entry itself contains spaces, so the idiom stops discriminating the instant a second entry is added, and today already substring-matches a hypothetical section named exactly `completion sentinel`. Correct for the one entry that exists; the pattern is used consistently elsewhere in `bin/edm-state` for genuinely space-free token lists |
| L1-015 | plugins/edm/bin/edm-check-grants:668-670 | A bare `Sec."X"` continuation whose `CLAUDE.md` prefix sits on the PREVIOUS line is not detected. Explicitly recorded as a known gap in the code comment at the check itself and in decisions.md D32 -- documented as a deliberate non-closure rather than an oversight |
| L1-016 | plugins/edm/bin/edm-state:4936-4939,5008-5021 | Completeness check (3) applies `read_round_lenses($all)` first, so a legacy round record carrying `lenses: []` with `round_type: "full"` is substituted with the CURRENT `ALL_LENS_IDS` and the union always covers -- meaning the "`ALL_LENS_IDS` grew between round-start and completion" guard can never fire for exactly the historical shape where it would matter. This is the AC10(a) C-4 substitution behaving as specified, and check (1) still requires a landed JSONL for all fourteen on that same record, which is the pass-7 case the backstop exists for |
| L1-017 | plugins/edm/bin/edm-state:5886-5895,5916-5923 | `pattern_seed_file_for` and `_pattern_heading4_for` have no `*)` default arm and print nothing for an unrecognized audit type (yielding a confusing "pattern file not found at  (skipping)" message, or a stub missing its fourth heading). Both call sites validate against `PATTERN_AUDIT_TYPE_ENUM_LIST` before calling, so neither is reachable today |
| L1-018 | plugins/edm/bin/edm-hookify:216 | `($payload_raw \| try fromjson catch {})` turns an unparseable stdin payload into `{}`, so every `not_contains` condition matches vacuously and a `block` rule can fire on a parse failure rather than a real match. Unreachable through either shipped consumer: `edm-gateguard:394` and `edm-bash-gate:63` both build the payload with `jq -c` and bail before invoking hookify if that fails |
| L1-019 | plugins/edm/bin/tests/timing.sh:499-512 | Branch 2 ("marker present") measures a heterogeneous sample set: sample 1 is a real first-touch denial (the path is unmarked), samples 2-20 take the already-checked allow path because `gg_mark_checked` recorded the path during sample 1. The quoted p95 is therefore the allow path with one denial outlier, which matches what D34 records ("38 ms marker-present"); recorded so a later reader does not read the figure as twenty full gate-path samples |
| L1-020 | plugins/edm/bin/_edm-datadir-lib.sh:102-106 | With `HOME` unset, the third candidate resolves to `/.local/share/edm`, which passes the `/*` test; `_edm_datadir_creatable` then walks to `/` and, for a root caller only, finds it writable -- placing the plugin data root at the filesystem root. Every non-root caller gets the intended empty-string degradation, and the plugin's documented platforms do not run hooks as root |

## Coverage and gaps for this lens

Covered: all nine primary `bin/` scripts named in the round scope, read end to end; the
EDMV4-marked regions of `bin/edm-state`; `hooks/hooks.json` in full; `skills/code-audit`,
`skills/orchestrator` Step 1b.5, `skills/plan` Step 6; all 14 lens agent frontmatters and lens-ID
frames; the EDMV4-T28 and EDMV4-T52 bands of `wave8-smoke.sh`; `timing.sh --gateguard`.

Not covered (recorded, not silently absent): the non-EDMV4 majority of `bin/edm-state` (approx.
6,800 lines, only the EDMV4-touched regions were read); `bin/tests/wave6-smoke.sh` and
`wave7-smoke.sh` in full (only the sites decisions.md D31/D39 name were spot-checked);
`bin/_edm-lint-lib.sh`, `bin/edm-lint-artifacts`, `bin/edm-check-vocabulary`,
`bin/edm-check-verifier-sentinel` and `evals/`; the 13 changed `SKILL.md` files other than the
three above; `bin/tests/fixtures/**`. No Bash tool was available to this agent, so no finding was
reproduced by execution and no `git diff` was read -- new code was identified by `EDMV4-T##`
markers, which will miss any edit to pre-existing code that carries no marker.

<!-- L1-COMPLETE round=1 lenses=L1 findings=20 actionable=13 noted=7 -->
