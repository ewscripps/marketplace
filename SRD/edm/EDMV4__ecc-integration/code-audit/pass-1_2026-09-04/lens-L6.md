# Lens L6 -- Documentation Accuracy (EDMV4 ECC Integration, round 1, full)

Scope: `git diff main..HEAD -- plugins/edm/`. Mandate is comment, docstring and operator-message
accuracy only. Severities per `CLAUDE.md Sec."Severity vocabulary"` (read via
`plugins/edm/docs/canonical-sections.md`).

## Findings (L6: Documentation Accuracy)

| ID | File:Line | Doc Says | Code Does | Fix |
|----|-----------|----------|-----------|-----|
| L6-001 | plugins/edm/CLAUDE.md:626 | Do-NOT-adopt guard D1: "its independent-agent auditing (writer/verifier separation across `edm-implementer` -> `edm-qc-auditor` -> the 11 code-audit lenses)" | `ALL_LENS_IDS` in `bin/edm-state:1668-1675` enumerates L1-L14 and self-checks `-eq 14`; `agents/edm-audit-*.md` minus the synthesizer is 14 files | Change "the 11 code-audit lenses" to "the 14 code-audit lenses". `EDMV4-T31` AC (f) named this exact string as in-scope and wave-3 QC (`qc/qc-shard-pass-w03-02.md:224-229`) re-flagged that the sweep entry omitted it; the sweep still did not land |
| L6-002 | plugins/edm/CLAUDE.md:629 | Do-NOT-adopt guard D2: "Do not reduce the 11-lens or 2-auditor fan-out to keep spawn counts low. Those counts are already deterministic" | The deterministic fan-out this guard protects is fourteen lenses. Guard D2 is cited by name as authoritative in three live places (`CLAUDE.md:265`, `CLAUDE.md:1251`, `skills/code-audit/SKILL.md`), so the wrong number is load-bearing, not decorative | Change "11-lens" to "14-lens". Same `EDMV4-T31` AC (f) sweep entry as L6-001 |
| L6-003 | plugins/edm/agents/edm-implementer.md:31 | "The code pair -- apply the pre-flight checklist; avoid the top anti-patterns flagged by the 11 code-audit lenses." | 14 lenses now flag anti-patterns; L12 (silent failures), L13 (type design) and L14 (behavioral test coverage) are invisible to the implementer's pre-emption instruction | Change to "the 14 code-audit lenses". This agent file was outside every T29/T31/T33 Target Components list, so no sweep reached it |
| L6-004 | plugins/edm/bin/edm-state:2553 | `--accept-p2-debt` comment: "it means this flag does NOT imply \"a full eleven-lens round was run\"" | The same fact is stated in `CLAUDE.md:265` as "does not itself require that a full fourteen-lens round was ever recorded (CA-426)". The two copies of one claim disagree on the count | Change "eleven-lens" to "fourteen-lens" so the code comment and its `CLAUDE.md` mirror agree |
| L6-005 | plugins/edm/CLAUDE.md:1309 | "the `ratio=UNMEASURABLE` refusal at `timing.sh:419-423` (G37/CA-197) is the extreme case of exactly this" | `timing.sh:419-423` is the "Add one small mermaid fence per file" loop. The `ratio=UNMEASURABLE` refusal is the `if [[ "$p95_base" -le 0 ...` block at `timing.sh:429-433`, with the `echo` at `:430` | Replace the line range with a by-name anchor ("the `ratio=UNMEASURABLE` refusal in `timing.sh`'s `--mermaid-ratio` arm"), matching the G39/CA-315 remediation pattern. Note the `G10/CA-340` ban cannot see this: its regex omits `timing.sh` and its second filter only matches lines beginning with `#`, so no citation in `.md` prose is ever scanned |
| L6-006 | plugins/edm/CLAUDE.md:1257 | `bin/` table: "`edm-sync-canonical-sections` \| Regenerate `docs/canonical-sections.md` from this file's \"Severity vocabulary\" and \"Mermaid diagram conventions\" sections" | `bin/edm-sync-canonical-sections:110-122` extracts **seven** sections; `docs/canonical-sections.md` carries seven `##` headings. `CLAUDE.md:496-504` states "seven sections total" in the same file | Update the row to say seven sections (or to defer to the generator's own block rather than restate a count). `EDMV4-T04` grew the set from three to seven and did not sweep this row, leaving `CLAUDE.md` self-contradictory |
| L6-007 | plugins/edm/bin/edm-sync-canonical-sections:15 | `EDM-HELP` block (printed by `--help`): "This script deterministically extracts **the two** by-name-referenced canonical sections out of plugins/edm/CLAUDE.md"; `:21` "Re-run it (no arguments) after editing **either** source section" | Seven sections are extracted (`:110-122`). An operator following this help edits one of the five undocumented sections and does not know to re-run, then `--check` fails with a drift diff it was told could not happen | Restate as seven, or point at the generation block. This is operator-facing `--help` output, not an internal comment |
| L6-008 | plugins/edm/CLAUDE.md:1496 | `userConfig` reference: "`qc_shard_threshold` -- ticket count above which QC spawns multiple `edm-qc-auditor` shards (default `20`)" | `plugins/edm/.claude-plugin/plugin.json:132` ships `"default": 6`, lowered from 20 by this initiative's own wave-1 remediation (decisions.md D30). The plugin.json description additionally says "Keep this at or below 6 unless maxTurns is raised with it" | Change `20` to `6`. The same section's preamble says "See `.claude-plugin/plugin.json` for the live schema", so a reader trusting the prose gets a sharding threshold more than 3x the shipped one |
| L6-009 | plugins/edm/README.md:149 | Agents table row: `edm-audit-{logic,dead-code,edge-cases,test-quality,runtime,docs,consistency,security,spec,dry,wiring}` -- eleven names | Three shipped lens agents are absent from the list: `edm-audit-silent-failures.md`, `edm-audit-type-design.md`, `edm-audit-behavioral-tests.md`. The same README says "14-lens exhaustive audit" at `:123` and "full fourteen lenses" at `:269-272` | Add the three names to the brace expansion. `EDMV4-T33`'s Target Components named `README.md:123,204-205,269,272` but not `:149`, so the sweep passed over the one row that enumerates agents |
| L6-010 | plugins/edm/CHANGELOG.md:31-33 | "[3.3.0] ... `skills/code-audit/SKILL.md`, `README.md`, `plugin.json`, `marketplace.json` and the other user-facing surfaces are updated **throughout** to read 14 lenses / 18 contested agents in place of 11 lenses / 15 agents." | `.claude-plugin/marketplace.json`'s `edm` entry has an updated `description` and `version`, but its `agents` array (`:52-83`) still enumerates 30 files -- only the 11 pre-EDMV4 lenses plus the synthesizer. `plugins/edm/agents/` holds 33 `.md` files. `README.md:149` is likewise unswept (L6-009) | Either land the three agent paths in `marketplace.json` and the README row, or narrow the CHANGELOG claim to the surfaces that were actually updated. `EDMV4-T33` scoped marketplace.json to its description string and version only, never its `agents` array |
| L6-011 | plugins/edm/CHANGELOG.md:9-77 | The `[3.3.0]` entry describes exactly three changes: the 11->14 lens growth, the Mermaid budget conditional, and the Phase 5 `audit-tickets` gate deadlock | The release also ships five new `bin/` executables (`edm-gateguard`, `edm-hookify`, `edm-stop-gate`, `edm-bash-gate`, `edm-repo-readiness`), one new sourced library (`_edm-datadir-lib.sh`), a new canonical rule format (`.claude/edm-hookify/*.json`), two new `PreToolUse` hook blocks and a second `Stop` hook entry, plus a six-member `EDM_GATEGUARD_*` env-var family. The strings "gateguard", "hookify", "stop-gate", "bash-gate" and "repo-readiness" appear **nowhere** in CHANGELOG.md | Add Added/Changed entries for the gateguard/hookify/readiness surfaces. An operator upgrading to 3.3.0 gets three new blocking hooks and six new environment variables with no changelog record of either |
| L6-012 | plugins/edm/bin/edm-gateguard:250-251 | `gg_mark_checked` docstring: "A write failure warns on stderr naming EDM_GATEGUARD_STATE_DIR but never changes the decision already made and **never denies again for lack of a mark** (AC7)." | `gg_mark_checked` is best-effort and returns 0 on failure (`:255-258`, `:265-268`). The path is therefore not recorded, `gg_is_checked` (`:243-247`) returns false on the next call, and `gg_maybe_deny` denies the same path again -- exactly "denying again for lack of a mark" -- until `EDM_GATEGUARD_MAX_DENIALS` is spent. The second half of the sentence is also loose: `gg_mark_checked` runs at `:352`, *before* `emit_decision deny` at `:354`, so no decision has yet been made | Restate as what the code guarantees: a write failure never itself produces a denial and never changes this call's decision; a subsequent call to the same path may be denied again, bounded by the denial budget |
| L6-013 | plugins/edm/bin/tests/wave7-smoke.sh:8930-8932 | "every new stale-citation instance was a citation of this plugin's own bin/tests/evals script by relative name plus digits, inside a comment line -- a shape narrow enough to ban directly. **Scoped to exactly that shape (this plugin's own bin/tests/evals scripts).**" | `g10_citation_regex` names only `edm-state\|edm-init\|edm-lint-artifacts\|edm-check-*\|run-eval.sh\|score-artifacts.sh\|_edm-*.sh\|wave*-smoke.sh\|run-all.sh`. Twelve shipped scripts are outside it, including all five new EDMV4 binaries plus `edm-compare-eval`, `edm-lint-staged-artifacts`, `edm-sync-canonical-sections`, `edm-validate-prefix`, `timing.sh`, `harness-smoke.sh`, `_harness.sh`. A live citation of exactly the banned shape sits unflagged at `bin/edm-repo-readiness:48` ("matches edm-compare-eval:44-48") | Either widen the alternation to `edm-[a-z-]+` plus the test-harness names, or reword the comment to say the ban covers a named subset. As written, a reader adding a `bin/edm-gateguard:412` citation is told a tripwire will catch it, and nothing will |
| L6-014 | plugins/edm/docs/ecc-integration-analysis.md:1026 (also :364, :372) | Part 8.2 item 8: "**This corrects both**: the **current-tree** citations are `:5607` (target-directory computation) and `:5640` (the read-only skip message)". Part 4.2's body carries the same two numbers at `:364` and `:372` | `local patterns_dir="${SCRIPT_DIR}/../docs/audit-patterns"` is at `bin/edm-state:6070` and the read-only skip `echo` is at `:6113` -- 463 and 473 lines off. The correction that fixed the previous stale pair (`:5577`/`:5624`) is itself stale, and it claims currency explicitly, so a reader cannot tell it is a snapshot | Replace all four sites with by-name anchors (`patterns_dir` assignment in `cmd_update_patterns`; the read-only skip `echo`), per the D21 remediation rule "locate by symbol name at edit time". This is the third recurrence of the stale-anchor pattern decisions.md D21 names, on a surface no scan covers |
| L6-015 | plugins/edm/docs/ecc-integration-analysis.md:374-383 | Part 4.2 quotes the skip message as ``echo "update-patterns: pattern directory is not writable at ${pattern_dir} (plugin may be installed read-only; skipping)"`` and concludes, in the present tense: "On any plugin-cache install that directory is not writable, so **this is the path taken every time**. The consequence: **EDM's pattern library only ever grows for people running the plugin from a checkout of this repository.** For every installed user, the learning loop is a no-op" | Two things changed underneath it. (1) The message at `bin/edm-state:6113` now reads `...not writable at ${patterns_dir} **and no writable plugin data directory is available** (plugin may be installed read-only; skipping)` -- different variable name, extra clause. (2) `EDMV4-T18` inserted branch (a) at `:6088-6104`: an installed user with a resolvable `edm_data_dir()` now writes a delta to `${data}/patterns/{type}-audit.md`, so the skip is no longer "the path taken every time" and the learning loop is no longer a no-op for installed users. This initiative fixed the defect its own shipped analysis still describes as open | Amend Part 4.2 in the Part 8.2 self-correction style the document already uses twice: mark 4.2 as implemented by `EDMV4-T18`/`EDMV4-T20`, re-quote the current message, and retire the "no-op for every installed user" conclusion. Leaving it reads as a live P1 defect to the next person who opens the document |

## Noted / Not Actionable

| ID | File:Line | Rationale |
|----|-----------|-----------|
| L6-016 | plugins/edm/docs/audit-patterns/README.md:137 | "\| code-audit.md \| 11-lens code audit \| EDMV2 seed (2026-06-08) \|" -- retained deliberately per decisions.md D33: the row's own third column dates it to the EDMV2 seed corpus, so bumping the count would misdate a historical snapshot. Correctly framed as history |
| L6-017 | plugins/edm/bin/tests/wave7-smoke.sh:1898-1902 | "AC8 (the eleven lens '## False Alarm Filter' sections...)" is an EDMV3-era batch-scope note recording what a past batch's boundary was, not a claim about today's set. The assertion it introduces (`:2002-2011`) correctly checks all fourteen against `WAVE7_LENS_COUNT`. Worth a wording touch-up if the block is edited for another reason, not on its own |
| L6-018 | plugins/edm/CHANGELOG.md:143, 164, 279, 484, 651, 654, 689, 712, 751, 774, 994, 1247, 1254 | Pre-`[3.3.0]` entries carrying 11-lens / eleven-lens counts. CHANGELOG historical entries are never edited by this project's own policy; each is accurate for the release it documents |
| L6-019 | plugins/edm/docs/ecc-integration-analysis.md:215, 483, 530-532, 926, 935 | "EDM runs 11 lenses", "EDM's eleven", "hardcodes eleven lens IDs" -- these describe the pre-EDMV4 baseline that motivated the growth to 14, in a document whose header dates it 2026-08-30 and whose Part 8 carries an explicit self-correction protocol (decisions.md D12). Distinct from L6-014/L6-015, which are claims the document asserts as *current* |
| L6-020 | plugins/edm/agents/edm-explorer.md:89 | `generate.ts:225-231` cites a file in the external ECC checkout, not this repository. Unverifiable from here and consistent with decisions.md D12's recorded correction of that exact range. Out of reach of any in-repo scan by construction |
| L6-021 | plugins/edm/CLAUDE.md:1522-1540 | `EDMV4-T56` plugin-distribution section verified against the real filesystem, not accepted as prose: the working tree is `plugins/edm/`; `~/.claude/plugins/marketplaces/stg-marketplace/.claude-plugin/marketplace.json` exists; `~/.claude/plugins/cache/stg-marketplace/edm/<version>/` exists with `2.0.0`, `3.2.0`, `3.2.1` and `3.2.2` subdirectories, confirming the "directory per installed version, read as a pattern" wording. No inaccuracy found |
| L6-022 | plugins/edm/CLAUDE.md:307 and docs/canonical-sections.md | "referenced by name from the eleven touch points inventoried in `architecture.md`" -- a Mermaid-convention touch-point count unrelated to the lens count. Deliberately excluded from the 11 -> 14 sweep and correct as written |

## Coverage note

Areas swept this round: (1) `file:line` citations in doc and agent prose -- complete across
`plugins/edm/**/*.md`, `agents/`, `skills/`, `CLAUDE.md`, `README.md`, with every in-repo citation
resolved against the cited file; (2) comments asserting a guarantee the code no longer provides --
partial (`edm-gateguard`, `edm-hookify`, `edm-stop-gate`, `edm-bash-gate`, `edm-sync-canonical-sections`,
the `G10/CA-340` band in `wave7-smoke.sh`) but **not** an exhaustive pass over `wave6-smoke.sh`'s
or `wave8-smoke.sh`'s assertion comments; (3) stale lens counts -- complete across the plugin tree;
(4) operator-facing messages -- covered for the five new `bin/` binaries and
`edm-sync-canonical-sections`, not for `edm-state`'s full message surface; (5) the `EDMV4-T56`
distribution section -- complete, verified against the filesystem.
