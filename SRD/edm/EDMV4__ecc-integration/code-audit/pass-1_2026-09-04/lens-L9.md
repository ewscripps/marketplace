# Lens L9: Spec & Ticket Compliance -- EDMV4 ECC Integration, Round 1 (full)

Inputs: `srd.md` v1.5.0 (63 requirement IDs, 61 substantive), `tickets/README.md` + 8 epic files
(55 tickets), `decisions.md` D1-D44, `qc/qc-summary.md` + 10 shard files, `.edm-state.json`.
Diff under audit: `git diff main..HEAD -- plugins/edm/`.

Method: every requirement/AC traced to a landed artifact by reading the artifact, not the record.
Where record and tree disagree, the tree is cited.

**Coverage statement** (budget-bounded, stated so gaps are recorded rather than silent):

| Area | Covered? |
|---|---|
| 1. Six runtime-unverified tickets (`T06`, `T07`, `T34`-`T37`), static ACs | Yes |
| 2. Four mid-flight scope additions (`T54`-`T57`) | Yes |
| 3. `T18`/`T20` mutual-assumption pattern, search for siblings | Yes -- one sibling found (L9-007/L9-008) |
| 4. Cross-cutting AC across all 55 | Partial -- smoke coverage checked by banded-section presence per ticket ID across `wave6`/`wave7`/`wave8`; the doc-update half checked on the surfaces the tickets name plus a CHANGELOG sweep. Not checked: "no new linter warnings" (belongs to L4/L6) |

**Verified genuinely delivered** (spot-checked in the tree, not from the record), so a later reader
can tell "checked and clean" from "never checked": `T04` (all 14 files carry the
`Read docs/canonical-sections.md` anchor), `T08` (`plugin.json` 3.3.0, `*opus-5*` arm priced and
asserted), `T09`, `T21` (`ALL_LENS_IDS` = 14 + `CONDITIONAL_LENS_IDS` = L13), `T24`
(`detect-conditional-lenses` called from `skills/code-audit/SKILL.md` Step 1), `T26`
(`agents/edm-audit-type-design.md`, conditional carve-out present), `T34`-`T37` (orchestrator
Step 1b.5, `skills/orchestrator/SKILL.md:102-127`, 26 lines, three signals, three pairs,
security tie-breaker, compliance pre-select, guard-D6 clause), `T38`-`T41`
(`bin/edm-repo-readiness`, six categories, `READINESS_RUBRIC_VERSION`), `T45` (both consumers
wired: `edm-gateguard:390-406`, `edm-stop-gate:158-166`), `T48` (explorer instruction), `T53` AC2
(`run-all.sh:41` and `:102`), `T54` (`bin/edm-state:4151-4156`), `T55` AC1/AC4 (`maxTurns` 200,
namespaced `SubagentStop` matcher), `T56` (three-location section in `plugins/edm/CLAUDE.md`),
`T57` (wave7 assertions retargeted at the delta, resolved via the real `get-patterns` read path).

---

## Findings (L9: Spec & Ticket Compliance)

### Missing Implementations (P1)

| ID | Requirement | Ticket | What's Missing | Evidence (search results) |
|----|-------------|--------|-----------------|------------------------------|
| L9-001 | SRD Sec.3.4 DoD item 1 ("all of its acceptance criteria satisfied **and evidenced**") | 16 tickets, listed at right | No QC verdict of any kind exists for 16 of the 55 tickets. `qc/qc-summary.md` covers waves 1-3 only (15 + 14 + 10 = 39). Never audited: `T11`, `T14`, `T15`, `T26`, `T28`, `T39`, `T40`, `T45`, `T50`, `T51`, `T52`, `T53`, `T54`, `T55`, `T56`, `T57`. That set contains **all four Phase-5 cross-cutting verification tickets that gate the Definition of Done** (`T50`/`T51`/`T52`/`T53`) and **all four mid-flight scope additions** (`T54`/`T55`/`T56`/`T57`). `decisions.md` D42/D43/D44 are labelled "Wave 5", so at least waves 4 and 5 drained with no shard written at all -- the same silent-absence failure `EDMV4-61`/`T55` AC5 exists to make impossible, recurring after the ticket that fixed it. The premise that "all 55 tickets are recorded as landed and every QC FAIL as remediated" does not hold for these 16: they are unrecorded, not FAIL-and-remediated. | `qc/qc-summary.md:169` -- `<!-- QC-SUMMARY-COMPLETE wave=03 shards=10 tickets=39 -->`; `Glob SRD/edm/EDMV4__ecc-integration/qc/*` returns only `qc-shard-pass-w01-01..04`, `w02-01..04`, `w03-01..02` plus `qc-summary.md` -- zero `qc-shard-impl-*.md`, zero wave-4-or-later shards |
| L9-007 | SRD Sec.3.4 DoD item 8 -- "`plugins/edm/CHANGELOG.md` carries a new entry documenting the 11-to-14 lens change **and every other user-visible change in this initiative**" | Unowned (see L9-008) | The `[3.3.0]` entry documents exactly three things: the 11-to-14 lens growth, `EDMV4-T01`'s Mermaid budget, and `EDMV4-T54`'s gate-mapping fix. It documents **none** of the initiative's other user-visible surfaces: three new blocking hooks (`PreToolUse Edit\|Write\|MultiEdit` -> `edm-gateguard`, which can DENY an edit; `PreToolUse Bash` -> `edm-bash-gate`; a second `Stop` entry -> `edm-stop-gate`), two new CLIs (`edm-repo-readiness`, `edm-hookify`), a new project-owned config surface (`.claude/edm-hookify/*.json`), six new `EDM_GATEGUARD_*` environment knobs, a new `edm-state` subcommand (`detect-conditional-lenses`), the orchestrator Step 1b.5 size-classifier dialog, the `SRD/.codemap.md` artifact slot, and a changed shipped default (`qc_shard_threshold` 20 -> 6). A user upgrading to 3.3.0 gets a hook that can refuse their edits with no changelog line saying so. | `grep -in 'gateguard\|hookify\|stop-gate\|repo-readiness\|bash-gate\|classifier\|Step 1b.5\|codemap' plugins/edm/CHANGELOG.md` returns exactly **one** hit, `:729`, inside the historical EDMV3 section and unrelated (a Mermaid line classifier). `grep -o 'EDMV4-T[0-9]+'` over the `[3.3.0]` block (`:9-78`) returns only `:17` (T21 group), `:37` (T01), `:56` (T54) |

### Partial Implementations (P1)

| ID | Ticket | AC | What the Spec Requires | What Code Does | File:Line |
|----|--------|----|--------------------------|------------------|-----------|
| L9-002 | `EDMV4-T55` | AC1, AC2 + the pack's cross-cutting Documentation AC | `edm-implementer` `maxTurns` raised to 200 and `edm-qc-auditor` to 150; project conventions doc updated when conventions change | Both raises landed in the agent frontmatter, but `plugins/edm/CLAUDE.md`'s `Verifier completion sentinel (canonical)` -> `Turn budget parity` subsection still asserts the four read-only verifiers "run at `maxTurns: 50`, at parity with the producer agents ... (`edm-srd-writer`, `edm-ticket-writer`, `edm-implementer`)". Both halves are now false: `edm-qc-auditor.md:11` is 150, `edm-implementer.md:12` is 200. The passage is a standing instruction -- "Do not 'tidy' any of these four back down" -- so a contributor obeying it restores the exact `50` the wave-1 P0 remediation moved away from. `decisions.md` D31 shows the smoke assertions were made floor-based/value-agnostic; the prose was never swept. | `plugins/edm/CLAUDE.md:437-447` vs `plugins/edm/agents/edm-qc-auditor.md:11`, `plugins/edm/agents/edm-implementer.md:12` |
| L9-003 | `EDMV4-T57` | AC6 | "`wave7-smoke.sh` reports zero failures except those owned by **a named, still-open ticket**, and every remaining failure names its owning ticket in its message or an adjacent comment" | Two assertion sites name their owner as the literal string `NEEDS-NEW-TICKET`, which is not a named ticket. Both guard the four-heading Living-Library contract against the live `docs/audit-patterns/` tree and both record that a `CONTRACT-FAIL` there would be "a genuine, currently-shipped defect ... out of `EDMV4-T57`'s Target Components". No follow-on identifier was reserved, unlike this initiative's four other boundary records (`EVALB` D9, `CAMGAP` D40, `LINUXV` D43, `EDMRT` D44), and `decisions.md` carries no row for it. The contract currently holds against `docs/audit-patterns/code-audit.md` (four `##` headings at `:8`, `:70`, `:235`, `:257`; no `###` after the fourth), so the assertions pass today -- the defect is the unowned, unnamed obligation, not a red suite. | `plugins/edm/bin/tests/wave7-smoke.sh:3664-3671` and `:4675-4678` |

### Scope Creep (P2)

| ID | File / Feature | Not Specified In | Recommendation |
|----|------------------|---------------------|-------------------|
| L9-004 | `plugins/edm/bin/edm-bash-gate` (new top-level `bin/` script, 76 lines) and the third `PreToolUse` matcher block it registers | It is a *justified* consequence of `EDMV4-T45` AC3/AC9 (Spike A cleared, so `bash`-event rules ship) -- but no ticket names it as a deliverable, and three cross-cutting ACs enumerate the new `bin/` set by name and exclude it: `EDMV4-T50` AC1 lists exactly five (`_edm-datadir-lib.sh`, `edm-gateguard`, `edm-hookify`, `edm-stop-gate`, `edm-repo-readiness`); `EDMV4-T52` AC4's coverage assignment says "the four new `bin/` scripts, the shared library and the two JSON config files"; `EDMV4-T53` AC3 requires three smoke cases for "every new `bin/` script (`edm-gateguard`, `edm-hookify`, `edm-stop-gate`, `edm-repo-readiness`)". `EDMV4-T45`'s Target Components list four files and no fifth script. | Do not remove it -- it is the only route by which `EDMV4-43`'s `bash` event ships without touching the `git commit` block `T45` AC5 pins byte-identical. Amend the three name lists (`T50` AC1, `T52` AC4, `T53` AC3) from four/five to five/six, or file a follow-on ticket owning `edm-bash-gate` explicitly. It does carry smoke coverage (12 references in `wave8-smoke.sh`), so the exposure is the enumerations, not the tests |
| L9-005 | `edm-gateguard`'s `MultiEdit` dispatch arm, and the `MultiEdit` token in the `PreToolUse` matcher | Shipped against an evidence precondition this initiative itself recorded as unmet. `decisions.md` D26 (Spike B, `EDMV4-T07`) closes: "`MultiEdit` remains genuinely unverified and **should be re-tested before GateGuard's `MultiEdit` arm ships** against a host where the tool is confirmed present." No re-test happened -- D44 closes `T07` `NOT RUNTIME-VERIFIED` -- yet the arm shipped. `EDMV4-T11`'s AC requires the `Edit`/`Write`/`MultiEdit` matcher block, so the pack and the decision ledger genuinely contradict each other on a shipping precondition. | Reconcile the two records in one place: either amend D26's closing sentence to record that the arm ships unverified under `EDMV4-T11`'s AC and is carried by `EDMRT`, or add `MultiEdit`-arm re-verification explicitly to `EDMRT`'s scope. As shipped the arm is inert on this host and harmless; the defect is two authoritative records disagreeing |
| L9-008 | The pack's Target-Components model versus its cross-cutting Documentation AC ("Changelog entry written if initiative has a CHANGELOG") | This is the `T18`/`T20` mutual-assumption pattern recurring on a second surface, and it is the mechanism behind L9-007. Exactly three tickets list `plugins/edm/CHANGELOG.md` in Target Components -- `T01`, `T33` (AC12) and `T54` (AC9) -- and exactly those three produced `[3.3.0]` content. Every epic-02/03/05/06 ticket carries the cross-cutting Changelog AC but names no CHANGELOG target, so its implementer could not satisfy it without writing outside its assigned scope -- which `decisions.md` D34 records as a contract violation, and correctly praises an implementer for refusing. D34 resolved that instance by having the coordinator write the missing figure; the identical structural gap on the CHANGELOG was never resolved the same way. | Assign CHANGELOG ownership explicitly rather than relying on a cross-cutting AC no ticket can execute: either add `plugins/edm/CHANGELOG.md` to `EDMV4-T53`'s Target Components (it is already the Definition-of-Done pass and already owns DoD evidence) with an AC naming DoD item 8, or make the CHANGELOG a coordinator-level obligation at wave close on the D34 precedent. The general lesson worth recording as a pattern: a cross-cutting AC that no ticket's Target Components can reach is unassignable by construction, and reads as satisfied because no single ticket fails on it |

## Noted / Not Actionable

| ID | File:Line | Rationale |
|----|-----------|-----------|
| L9-006 | `SRD/.codemap.md` (absent) | `EDMV4-T48` lists `SRD/.codemap.md (new, project-resident)` in Target Components and the file does not exist. Not actionable: all nine `T48` ACs bind the *instruction* in `agents/edm-explorer.md` and `CLAUDE.md`, not the artifact, and AC6 explicitly forbids a generator. EDMV4's own four explorers ran in Phase 1, long before `T48` landed in Phase 4, so no explorer in this initiative was ever subject to the instruction. Verified delivered: `agents/edm-explorer.md:71-111` carries the full `## Codemap` section covering AC1-AC5, AC7, AC8; `plugins/edm/CLAUDE.md:146` documents the layout slot (AC9) |
| L9-009 | `SRD/edm/EDMV4__ecc-integration/decisions.md:57` (D44) | The six `NOT RUNTIME-VERIFIED (D44)` closures were checked against the code for their **static** ACs, which is the half the qualifier does not cover. All six are genuinely satisfied statically: `T06`/`T07` are fully recorded as D25/D26 with the `claude --version` string verbatim, the per-tool row split, the positive control and the `hooks.json`-untouched assertion; `T34`-`T37` are all present in `skills/orchestrator/SKILL.md:102-127` and asserted in `wave8-smoke.sh` (banded sections at `:113`, `:1351`, `:1485`, `:1565`). No static AC among the six is unmet. Recorded so a later reader can distinguish "verified static-clean" from "never checked", per the D36 discipline |
| L9-010 | `plugins/edm/bin/edm-gateguard` (413 lines) vs `EDMV4-T11` AC1's original 200-400 band | `decisions.md` D42 raised the ceiling to 500 with the rationale recorded at the assertion, after five later tickets each added code their own ACs required. This is a properly escalated and recorded bound change, not a silent widening -- the implementer refused to edit another ticket's assertion and flagged the two options instead. No action |
