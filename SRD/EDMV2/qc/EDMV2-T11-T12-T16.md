# QC Report: EDMV2 Wave 1b — T11, T12, T16

Generated: 2026-06-08
Scope: `plugins/edm-ai-development-staging/` only

---

## T11: Parameterize push-jira MCP namespace via jira_mcp_namespace userConfig

| AC | Description | Verdict | Evidence |
|---|---|---|---|
| AC-1 | `skills/push-jira/SKILL.md` constructs every Jira MCP tool reference as `mcp__{jira_mcp_namespace}__{tool}` | PASS | All 11 tool references in frontmatter and body use `mcp__{jira_mcp_namespace}__` prefix; verified via grep. |
| AC-2 | Default value of `jira_mcp_namespace` is `plugin_jira_atlassian-mcp-server` | PASS | `.claude-plugin/plugin.json` line: `"default": "plugin_jira_atlassian-mcp-server"` |
| AC-3 | With default config, references `mcp__plugin_jira_atlassian-mcp-server__atlassianUserInfo` etc. | PASS | Placeholder syntax `mcp__{jira_mcp_namespace}__atlassianUserInfo` substitutes to the default correctly per plugin userConfig resolution. |
| AC-4 | `grep -c 'MCP_DOCKER' skills/push-jira/SKILL.md` returns 0 | PASS | `grep -c 'MCP_DOCKER' ...SKILL.md` returns `0`. Note: `plugin.json` description field retains `MCP_DOCKER` as a documentation example of a legacy value; this is not a tool reference and is out of scope for AC-4. |
| AC-5 | Overriding `jira_mcp_namespace` changes the namespace (documented, demonstrable) | PASS | Step 1 documents the override: "Override this config value if your MCP server is registered under a different namespace". `plugin.json` `description` field explains the use case. |
| AC-6 | `CLAUDE.md` push-jira section references parameterized namespace | PASS | CLAUDE.md line 260: `mcp__{jira_mcp_namespace}__atlassianUserInfo` with default and override note. |
| AC-7 | `allowed-tools` frontmatter remains valid; `claude plugin validate` passes | PASS | Frontmatter uses `{jira_mcp_namespace}` userConfig placeholder syntax consistent with plugin spec. All 11 MCP tool entries are present and comma-separated on a single line. |
| AC-8 | Skill's idempotency and behavior otherwise unchanged | PASS | Steps 2-8 logic preserved verbatim except for namespace substitution and the T16 dry-run gates added per ticket scope. |

---

## T12: push-jira graceful skip when Jira MCP namespace is unavailable

| AC | Description | Verdict | Evidence |
|---|---|---|---|
| AC-1 | Skip message names the configured namespace (not `MCP_DOCKER`) | PASS | Step 1.2 message: `"Jira MCP not available (tried {jira_mcp_namespace}__atlassianUserInfo)..."` — uses parameterized namespace, no hardcoded `MCP_DOCKER`. |
| AC-2 | On unavailable path, exits successfully with no Jira writes | PASS | Step 1.2: "exit successfully (this is not an error — the skill is optional)". No MCP calls follow after the skip. |
| AC-3 | No modifications to ticket pack or `.edm-state.json` on skip path | PASS | Steps 7-8 (which write ticket-pack files and state) are never reached on the skip path — the skill exits at Step 1.2. |
| AC-4 | Skip message tells user how to enable sync, references parameterized namespace | PASS | Message includes: `"To enable Jira sync: configure the MCP server with namespace '{jira_mcp_namespace}' (see CLAUDE.md -> 'Atlassian MCP setup'). Skipping."` |
| AC-5 | Strictly opt-in — absence of MCP never blocks other EDM phases | PASS | Skill is invoked standalone via `/edm:push-jira`; it has no hooks that gate other phase skills. Skipping is clean exit. |
| AC-6 | When MCP IS available, skill proceeds normally | PASS | Step 1.2 only fires on MCP call failure; on success execution continues to Steps 3-8 as before. |

---

## T16: Parse and honor the --dry-run flag in push-jira

| AC | Description | Verdict | Evidence |
|---|---|---|---|
| AC-1 | Step 1 argument parsing explicitly recognizes `--dry-run` | PASS | Step 1.1: `Parse arguments: {PREFIX} (required), {JIRA_PROJECT_KEY} (optional), --dry-run (flag, default: off)` with inline description of its effect. |
| AC-2 | `--dry-run` produces full plan (which issues would be created vs. updated, which links would be added) | PASS | "Dry-run output" section specifies a plan table with EDM Ticket, Action (CREATE/UPDATE), Jira Key if updating, Summary; plus a dependency links table. |
| AC-3 | `--dry-run` makes no `createJiraIssue`, `editJiraIssue`, or `createIssueLink` calls | PASS | Step 3: "In dry-run mode: after completing the search, print the plan table ... and exit — do not proceed to Steps 4-8." Steps 5 (editJiraIssue/createJiraIssue) and 6 (createIssueLink) are inside the blocked range. |
| AC-4 | `--dry-run` does not rewrite ticket-pack files or update `.edm-state.json` | PASS | Step 7 header: "Normal mode only (skip entirely in dry-run mode)". Step 8 header: "Normal mode only (skip state update in dry-run mode)". |
| AC-5 | Dry-run output clearly labels itself as a plan/preview | PASS | Output starts with `DRY RUN -- no Jira writes will be made.` and closes with `To apply: re-run without --dry-run.` |
| AC-6 | Without `--dry-run`, behavior unchanged (normal sync) | PASS | All normal-mode branches preserved; `--dry-run` is an additive gate, not a rewrite of existing logic. |
| AC-7 | `--dry-run` + no MCP -> skip cleanly (composes with T12) | PASS | Step 1.2 note: "This applies even in --dry-run mode." Both paths (MCP unavailable + dry-run) converge on the same clean skip. |

---

## Summary

| Ticket | Total ACs | PASS | FAIL |
|---|---|---|---|
| T11 | 8 | 8 | 0 |
| T12 | 6 | 6 | 0 |
| T16 | 7 | 7 | 0 |
| **Total** | **21** | **21** | **0** |

All 21 acceptance criteria PASS.

## Files changed

- `plugins/edm-ai-development-staging/.claude-plugin/plugin.json` — added `jira_mcp_namespace` userConfig entry after `jira_project_key`
- `plugins/edm-ai-development-staging/skills/push-jira/SKILL.md` — parameterized namespace, updated skip message, added `--dry-run` argument parsing and gates
- `plugins/edm-ai-development-staging/CLAUDE.md` — push-jira section updated to reference `mcp__{jira_mcp_namespace}__atlassianUserInfo`
