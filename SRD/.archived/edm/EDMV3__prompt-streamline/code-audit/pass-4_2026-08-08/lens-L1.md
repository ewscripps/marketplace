# Lens L1: Logic, Correctness & Completeness -- Round 4

Scope: full `plugins/edm/` tree plus repository-root `CLAUDE.md`, `.gitlab-ci.yml`, `.gitignore`.
Method: (a) re-verification of every ledger entry whose Lens(es) column includes L1 and whose
Status is `open`; (b) a fresh scan for L1 defects introduced by the Wave 7 (7a-7g) remediation.

## Cross-round verification of open L1 ledger entries

| Ledger ID | Prior sev | Verdict | Evidence |
|-----------|-----------|---------|----------|
| CA-182 | P0 | **FIXED** | `bin/edm-state:1838-1846`. The precheck is now unconditional: `conv_out="$(cmd_audit_converged "$prefix" 2>&1)" || conv_ec=$?` runs on every path, and only the exit-3 arm degrades, and only when `ag_schema_class2 != "2"` (`:1840-1843`). Every other non-zero status reaches `[[ $conv_ec -eq 0 ]] || die` at `:1845`. At `schema_version: 1` with a blocking finding the gate now refuses. CA-183's sibling refusal sits correctly ahead of it at `:1817-1821`, so fast-track/fix-pack cannot route through `convergence_exempt()`. Rationale recorded in place at `:1828-1834`. |
| CA-019 | P1 | **FIXED** | `bin/edm-lint-artifacts:192` -- `mermaid_scan_awk` now runs `awk -f "$MERMAID_RULES_AWK" ... -f <(cat <<'AWK_MAIN' ...)` and calls the shared `mermaid_is_violation` at `:208`; the private `trim`/`strip_entities`/`is_violation` triplet is gone, and `:84` fails fast if the shared file is missing. The blocking consumer, the commit hook and the nightly eval now enforce one rule body. The ignore-marker half is closed the other way: `evals/score-artifacts.sh:98-105` now states the carve-out and its reason explicitly, which satisfies False-Alarm-Filter criterion 2. |
| CA-184 | P1 | **FIXED** | `bin/edm-state:1141-1147`. `local ec=0` is declared before the fork and the capture is on the same statement: `( "$@" ) || ec=$?`. `_EDM_TRAP_DEPTH=0`, `rm -rf "$lockdir"`, `_restore_traps`, `return $ec` all now run on a non-zero locked body. Rationale at `:1135-1140`. |
| CA-185 | P1 | **FIXED** | `attribution_mode` is a strict two-value enum again (`bin/edm-state:361`, `:403`, and both absent-path literals at `:314`/`:406`); the torn-line count moved to a new `unparseable_lines` field (`:362`, `:404`) threaded through both consumers (`:2053`/`:2078` phase-complete, `:3672`/`:3688` audit-round-complete) and surfaced as an informational `TORN_TOKEN_LINES` anomaly for both `phase_durations` (`:1318-1324`) and `audit_rounds` (`:1326-1337`). Contract documented at `CLAUDE.md:434-438`. |
| CA-186 | P1 | **FIXED** | `hooks/hooks.json:86`. All three named defects closed: trailing slashes stripped by a `while [ "${srd_root%/}" != "$srd_root" ]` loop; an absolute value is refused loudly with a named diagnostic and `exit 0`; the root reaches awk through the environment (`root="$srd_root" awk '... ENVIRON["root"] ...'`) rather than `-v`, so backslash escape processing no longer mangles it. Prefix derivation `substr($0,1,rl)==(ENVIRON["root"] "/")` is correct for both flat and product-scoped layouts. |
| CA-187 | P1 | **FIXED** | `bin/edm-lint-artifacts:42-47` now states the real contract ("exit 1 (a real violation) makes the hook exit 2 ... exit 2 (a setup/usage error) makes the hook exit 0"), and the internal contradiction at the old `:42` is gone. `:56-60` now describes the derived `srd_root` and names the absolute-path refusal. The `plugins/edm/CLAUDE.md` `bin/` row was changed to defer to `--help` rather than restate a count. |
| CA-017 | P2 | **FIXED** | `bin/edm-lint-artifacts:24-39` enumerates all seven classes, including `unreadable` (emitted at `:246`) and `scan-error` (`:381`). That set exactly matches the seven class strings reaching `report_violation` (`:246`, `:286`, `:305`, `:319`/`:332`, `:350`, `:381`, `:392`). The CLAUDE.md `bin/` row no longer hardcodes "four violation classes". |
| CA-196 | P2 | **PARTIALLY FIXED** | See finding L1-002 below. |
| CA-197 | P2 | **FIXED** | `bin/tests/timing.sh:320-324`. A zero or negative p95 on either side now prints `ratio=UNMEASURABLE` naming both remedies and exits 3 instead of emitting `b/1` as if it were a multiplier. |
| CA-198 | P2 | **FIXED** | New `_print_line` renderer at `bin/edm-state:658-665` (with an explicit note that `_print_literal` must stay unchanged for the pattern-library splice), used at `:4835`. HANDOFF.md now ends with exactly one newline: the command substitution at `:4738-4834` strips all trailing newlines and `_print_line` adds exactly one back. |
| CA-199 | P2 | **FIXED** | `bin/edm-state:4351-4355`. The `$0`-based re-derivation is deleted; `patterns_dir="${SCRIPT_DIR}/../docs/audit-patterns"` uses the `BASH_SOURCE`-derived global from `:62`, so a sourced `edm-state` resolves inside the plugin. |

Ten of eleven open L1 entries are genuinely closed, including the round's only P0. One is partially
closed. Three new L1 defects were introduced by Wave 7 itself, two of them in the same function.

## Findings (L1: Logic, Correctness & Completeness)

| ID | Severity | File:Line | What's Wrong | Fix |
|----|----------|-----------|--------------|-----|
| L1-001 | P1 | plugins/edm/bin/edm-state:3381 | NEW, introduced by this round's CA-203 remediation. The replacement liveness probe `holder_evidence="$(pgrep -f -- "$git_dir" ...)"` is wrong in three compounding ways, and the comment at :3368-3375 asserts the opposite of what it does. (1) `git_dir` comes from `git rev-parse --git-dir` at :3338, which prints the RELATIVE string `.git` from the top of a work tree -- the normal invocation point -- so the pattern is four characters, not a repository-identifying path. (2) `pgrep -f` takes an extended regex, so `.` is a wildcard: `.git` matches any command line containing any character followed by `git` (`/usr/lib/git-core/...`, `digit`, `legit`, an unrelated repository's git). The probe is therefore host-wide, which is exactly the "MATCHES an unrelated repo's git process anywhere else on the host" failure the comment claims to have eliminated. (3) It self-matches: the invoking process's own command line ends in ` git-lock-check`, and pgrep excludes only itself, not its ancestors -- so ` git` matches and `holder_evidence` is non-empty. Net effect: whenever `lsof` reports nothing (a genuinely stale lock -- the only case this command exists for), the pgrep fallback fires, :3384-3389 prints "possible holder evidence was found", and the removal branch at :3391-3404 is unreachable. The subcommand can no longer perform its documented remediation. Invisible to the suite: wave7-smoke.sh:5804-5811 greps the function body for the literal source text `pgrep -f -- "$git_dir"` and never executes the branch (:5821 runs the command only in a repo with no lock file). | Resolve the git dir to an absolute path before using it as a scope (`git rev-parse --absolute-git-dir`, or `git_dir="$(cd "$git_dir" && pwd)"`), anchor the match as a fixed string rather than an ERE, and exclude this process tree. Concretely: keep `lsof` as the primary oracle; for the fallback use `pgrep -F`-style literal matching or filter `pgrep -af -- "$abs_git_dir"` output through `grep -F -- "$abs_git_dir"` and drop any PID in this process's own ancestry (`$$`, `$PPID`). If neither `lsof` nor a literal-capable `pgrep` is available, print "liveness could not be determined" and refuse, rather than guessing. Then add an executing test: create a lockdir-free scratch repo, `touch .git/index.lock` with a backdated mtime, and assert the removal path is actually taken. |
| L1-002 | P2 | plugins/edm/bin/tests/timing.sh:67-78 | CA-196 PARTIALLY FIXED. The rounding half is genuinely closed -- `idx = int(0.95 * NR); if (idx < 0.95 * NR) idx = idx + 1` at :72-73 is a correct nearest-rank ceiling (N=10 -> 10, N=5 -> 5, N=3 -> 3). But the consequence the finding also named is now live in the opposite direction and undischarged: for every sample count this file actually uses, ceil(0.95*N) equals N, so `_p95` returns the MAXIMUM sample in all six measuring modes while the emitted key is still `p95_ms` (:139, :169, :207, :216, :247, :274, :327). The comment at :58-66 defers the sample-count half to a follow-up and CHANGELOG.md:211-217 records that the T67 table figures at :196-202 predate the fix and are still the biased-low pre-fix numbers -- so the published 3,000 ms and 60,000 ms budget evidence in plugins/edm/CLAUDE.md and the T67 AC rows remain figures no current run of this harness would reproduce, under a harness header at :7 asserting every mode is a real measurement. Deferral is not a resolution state in this methodology, and no decisions.md entry carries the deferral. | Pick one and land it in this pass: either raise the sample counts to 20 in all six modes so `p95` is a real p95, or rename the emitted key to `max_ms` and update the four consuming documents (CHANGELOG.md's T67 table, plugins/edm/CLAUDE.md's two budget statements, epics/11 T67 ACs). Either way re-run every mode and re-record the published figures, and record the choice in decisions.md so the CHANGELOG note at :211-217 can be replaced by a result rather than a caveat. |
| L1-003 | P2 | plugins/edm/bin/edm-state:3327 | NEW, same function as L1-001, same remediation. `_git_lock_age_bucket`'s bucket list ends at `1`, and `find -mmin +1` truncates the fractional part, so it is true only at age >= 2 minutes. The action gate 34 lines below at :3361 uses `find "$lock_file" -mmin +0`, true at age >= 1 minute. The two thresholds disagree over the whole [60s, 120s) window: a 90-second-old lock is described as `age: less than 1 minute` at :3354, then passes the gate whose own refusal message at :3364 reads "is less than a minute old; leaving in place", and is removed at :3391 with the same contradictory age string embedded in the removal message. The operator-facing evidence trail on a destructive path directly contradicts the decision taken. | Add `0` to the bucket list at :3327 and give it its own label (e.g. `more than 1 minute`), so the coarsest reported bucket lines up exactly with the `-mmin +0` gate; or change the fallback at :3333 to `less than 2 minutes` and restate the guard message at :3364 in the same units. Assert the alignment with a case that backdates a lock file by 90 seconds and checks the printed age against the action taken. |
| L1-004 | P2 | plugins/edm/evals/run-eval.sh:209-231 | NEW/aggravated by this round's CA-214 remediation. `cleanup()` is installed on `EXIT INT TERM` at :231, but only one of its two terminal branches actually terminates. When `STARTED=true` and `COMPLETE!=true` it calls `exit 4` at :226; on every other path it falls through to `prune_old_runs; return "$ec"` at :228-229. A returning trap handler resumes the interrupted script, and `set -e` is deliberately off (:63), so a Ctrl-C before `STARTED=true` (set at :401 -- i.e. during the bounded auth probe at :300 or `provision_scratch` at :324) kills the child, deletes `SCRATCH_DIR` at :220-222, and then lets execution continue against a directory that no longer exists. The observable result is `exit 2` with "no working Claude auth" or "failed to initialize scratch git repository" reported for what was a user interrupt. Compounding: `local ec=$?` at :210 captures the last command's status, not `130`/`143`, so even the exiting branch cannot report a signal-conventional code. This is the same missing-terminal-exit class the CA-143 remediation fixed in bin/edm-state's trap arms (:1116-1118, :623-625) and did not sweep here. | Split the handler: keep `cleanup` as the EXIT body, and install a separate `trap 'cleanup; exit 130' INT` / `trap 'cleanup; exit 143' TERM` so a signal always terminates. Move the `exit 4` decision into the EXIT body only, and leave the `CLEANUP_DONE` guard as-is so the second entry is a no-op. |

## Noted / Not Actionable

| ID | File:Line | Rationale |
|----|-----------|-----------|
| L1-005 | plugins/edm/evals/run-eval.sh:248-252 | `--provision-only` exits at :251, which is after the trap install at :231, so `cleanup` -> `prune_old_runs` now runs in a mode documented as self-contained provisioning. Bounded and harmless in practice: the default `OUT_ROOT` is the gitignored `evals/runs/`, the ownership filter at :186 touches only run-ID-shaped directories, and an explicit `--out` root is opt-in only. Behaviour, not a defect. |
| L1-006 | plugins/edm/bin/edm-compare-eval:108-119 | The per-dimension delta table joins candidate to baseline POSITIONALLY (`$bd[$e.key]` against `to_entries` over an array), not by name, so a reordering would silently compare unrelated dimensions under the candidate's label. Safe by construction today: `score-artifacts.sh`'s `build_dims_json` (:484-499) always emits all five entries in fixed `DIM_NAMES` order with `"score":null` for skipped ones, and `dimensions_scored` equality is enforced at :83-88 before this block runs. |
| L1-007 | plugins/edm/hooks/hooks.json:86 | `srd_root="${srd_root#./}"` strips only one leading `./`, so a pathological `././SRD` would still fail the awk prefix test and disable commit-time enforcement. All three defects CA-186 actually named are fixed, and no documented workflow produces that value. |
| L1-008 | plugins/edm/bin/edm-state:1009-1016 | The CA-209 flock-timeout redesign introduces a new on-disk path shape, `${lockfile}.timeout.$$`, created only on the timeout branch and removed by the parent immediately afterwards. Logically correct (`$$` is the parent's PID even inside the subshell, and a locked body that exits 99 for its own reasons is no longer misreported). The gitignore-coverage question this raises is the CA-212 class and belongs to L5, not L1. |
| L1-009 | plugins/edm/bin/tests/timing.sh:72-73 | The ceiling arithmetic itself is correct nearest-rank p95 for every N. Only the sample count and the emitted key name remain at issue, and both are folded into L1-002 rather than filed separately. |
| L1-010 | plugins/edm/bin/edm-compare-eval:41-44 | Accepts `-h|--help` but not the bare `help` token eleven sibling scripts accept. A family-consistency question for L7; no logic defect. |

## Meta

`Write` was absent from this lens's delivered runtime tool set despite the frontmatter grant
(ledger CA-130). Both `lens-L1.md` and `lens-L1.jsonl` were transcribed by the orchestrator from
the agent's returned text.
