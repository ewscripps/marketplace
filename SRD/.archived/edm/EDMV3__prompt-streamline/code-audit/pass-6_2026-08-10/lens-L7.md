# Lens L7 -- Cross-File Consistency (pass 6, 2026-08-10)

**Tooling note (CA-130's class):** Write/Edit/Bash absent from this lens's delivered
runtime tool set (Read, Grep, Glob, WebFetch, WebSearch, TaskStop only). This report
was transcribed by the orchestrator from the lens agent's final message.

Scope swept: `plugins/edm/{bin,bin/tests,hooks,monitors,evals,skills,agents,docs}`, plugins/edm/CLAUDE.md, and the edm-scoped jobs of the repository-root `.gitlab-ci.yml`. Prior-round L7 IDs checked for reopening: CA-006, CA-009, CA-010, CA-016, CA-032, CA-045, CA-046, CA-048, CA-075, CA-076, CA-077, CA-078, CA-079, CA-081, CA-225 through CA-231, CA-298, CA-309, CA-317, CA-318.

## Findings (L7: Cross-File Consistency)

| ID | File A | File B | What Differs | Why It Matters | Fix |
|----|--------|--------|--------------|----------------|-----|
| L7-001 | `plugins/edm/bin/edm-state:1185-1193` (`with_state_lock`, invalid-PID reclaim) | `plugins/edm/bin/edm-state:3587-3589` + `:3597-3602` (`cmd_git_lock_check`) | **CA-318 is fixed on the happy path only.** Both reclaimers now have an age gate, but they take opposite directions when the age cannot be computed. `cmd_git_lock_check` reads the age through the shared helper and treats failure as "unknown": `lock_age_s="$(_git_lock_age_seconds "$lock_file")" || lock_age_s=""`, then gates on `[[ -n "$lock_age_s" ]] && (( lock_age_s >= 60 ))` -- unknown age falls to the `else` and **refuses** (`return 1`). The sibling hand-rolls the same computation with the inverse default: `_lockdir_created_s="$(date -r "$lockdir" +%s 2>/dev/null || echo 0)"; _lockdir_age_s=$(( $(date +%s) - _lockdir_created_s ))` -- an unreadable mtime yields `created_s=0`, an age of ~1.7e9 seconds, and an unconditional **reclaim**. It also bypasses `_git_lock_age_seconds` (`:3538-3543`), whose own docstring states every age gate in the file derives from one call to it and that it is the sanctioned portable primitive (`date -r`, never `stat`, never `find -mmin` -- the CA-303 lesson). | The two in-code comments now assert the asymmetry is closed in both directions (`:1173-1176` "age-gate this reclaim so it agrees in direction with cmd_git_lock_check's own undetermined-liveness reclaim (which refuses rather than assumes dead)"; `:3636-3638` "with_state_lock's own invalid-pidfile reclaim path ... now agrees with the refusal direction here"). Both claims are false on the error path, which is the CA-227/CA-228 "PARTIALLY FIXED, and the comment says otherwise" shape. Behaviourally: on any host where `date -r <dir>` does not work, the new gate silently degrades to *no gate at all* (always reclaim -- exactly pre-CA-318 behaviour, with no diagnostic), while the sibling degrades to maximum caution. Reclaiming is the direction that can delete a live lock. | Replace the inline computation with `_lockdir_age_s="$(_git_lock_age_seconds "$lockdir")" || _lockdir_age_s=""`, then gate on `[[ -n "$_lockdir_age_s" ]] && (( _lockdir_age_s >= 1 ))`, with the existing not-reclaiming-yet stderr line covering the empty case (naming "age could not be determined" so the two refusal reasons are distinguishable). One primitive, one error contract, and the two comments become true as written. |
| L7-002 | `plugins/edm/bin/edm-state:1120` + `:1124` + `:1128` (flock branch) | `plugins/edm/bin/edm-state:1024-1031` (`_lock_retry_or_die`, consumed by the mkdir branch) | Two implementations of one operation -- acquire the state lock -- with **different total wait budgets and no stated rationale**. The flock branch waits `flock -w 10` (10 s); the mkdir branch waits `50` tries x `sleep 0.1` = ~5 s. The mkdir branch is macOS's path (`:1037`: "falls back to a mkdir-based spin-lock on `<lockbase>.lockd` for macOS portability"; macOS ships no `flock(1)`), the flock branch is Linux/CI's. Second half: the two `die` messages for the identical failure quote different units -- "state lock timeout after 10s on `<lockfile>`" versus "state lock timeout after 50 tries on `<lockdir>`". | The platform CLAUDE.md names as the primary development platform gets **half** the contention budget of CI, and nothing in the file, CLAUDE.md, the SRD or the ticket pack says that is deliberate -- so the next contributor tuning one number has no way to know whether the other is intentionally different. `with_state_lock` has ~30 bare callers via `rmw_state`, and the `Stop`/`PreCompact`/`SessionStart` hooks plus the `edm-impl-progress` monitor make concurrent invocation ordinary, so the shorter budget is the one most likely to produce a hard `die` on a developer's machine. The mismatched units make the two failures uncorrelatable in a log and leak a loop-internal count to the operator; `:1184` already calls it "the existing 50-try/5s total budget" in a comment, proving the duration is known and simply not surfaced. | Pick one budget and state it once: either raise the mkdir bound to 100 tries for 10 s parity, or lower `flock -w` to 5, with a one-line comment naming the chosen number as shared. Make both messages quote the same unit, e.g. `state lock timeout after ~5s (50 tries x 0.1s) on ...`. |
| L7-003 | `.gitlab-ci.yml:124-128`, `:131-135`, `:144-148` (`lint:bash-syntax`); `:501-502` (`validate:manifest`); `:376-377` (`test:smoke`); `:411-416` (`test:smoke-bash32`); `:659-666` (`eval:nightly`) | `.gitlab-ci.yml:118-121`, `:166-170`, `:184-189`, `:200-207`, `:253-256`, `:275-280`, `:294-301`, `:355-358`, `:472-475`, `:581-585`, `:615-625`, `:677-680` (the conforming paths) | **The job-named `FAILED` convention is now uniform for 9 of 13 jobs, with four holes left.** (a) `lint:bash-syntax` has four terminal exit paths; only the first prints `lint:bash-syntax: FAILED`. The three grep-ban exits print a job-named sentence but **no `FAILED` token**, so `grep FAILED` over a pipeline log misses them -- inside the very job that demonstrates the convention. (b) `validate:manifest`'s manifest-parse early exit prints `FAIL: marketplace.json does not parse, or no plugin named 'edm' is declared` and exits 1 with no job name and the un-standardized `FAIL` token, where the same job's terminal path at `:581-585` prints `validate:manifest: FAILED`. (c) `test:smoke` and `test:smoke-bash32` print **no job-named failure line anywhere** -- both are bare `bash .../run-all.sh` invocations, and `:415`'s `bash --version | head -1 | grep -q 'version 3\.2'` fails with no message at all. (d) `eval:nightly` names itself only in the comparison branch (`:679`); the `run-eval.sh` and `score-artifacts.sh` steps do not. | This is the same shape CA-317 raised, at the sibling sets the round-5 sweep did not check -- and CA-317's own text calls that out: "the THIRD consecutive round in which an L7 sweep closed the named sites without checking the rest of the file, which is itself the finding." The durability half CA-317 prescribed ("add one suite assertion pinning the convention so the next sweep does not have to be exhaustive by hand") landed as a **hand-enumerated list over exactly the sites the sweep fixed**: `plugins/edm/bin/tests/wave7-smoke.sh:7329-7343` loops over `lint:artifacts lint:grants lint:vocabulary`, adds one eval:nightly check, and screens for bare-`FAIL` with `[a-z][a-z0-9:_-]*: FAIL[^E]|[a-z][a-z0-9:_-]*: FAIL$` -- a regex that by construction only sees the `<job>: FAIL` shape, so `:502`'s `FAIL:`-prefixed exit is invisible to it, and nothing asserts that every job containing `exit 1` carries a named line. So round 7 would have to sweep by hand again. | Add the missing lines at the four sites; wrap the two `test:` jobs so a non-zero suite prints `test:smoke: FAILED` / `test:smoke-bash32: FAILED` (and give `:415`'s version assertion its own message). Then replace the hand list at `wave7-smoke.sh:7329` with a whole-file sweep: enumerate every top-level job key (`^[a-z][a-z0-9:_-]*:$`), and for each body containing `exit 1`, assert a `<job>: FAILED` occurrence -- which makes the convention self-enforcing for jobs added later. |
| L7-004 | `plugins/edm/bin/edm-state:5186` | `plugins/edm/bin/edm-lint-artifacts:76`, `edm-check-grants:79`, `edm-check-vocabulary:73`, `edm-check-skill-sync:40`, `edm-init:43`, `edm-validate-prefix:34`, `edm-sync-canonical-sections:61`, `edm-compare-eval:48`, `evals/run-eval.sh:89`, `evals/score-artifacts.sh:144`, `evals/tiering-matrix.sh:82` | `edm-state` is the only one of the twelve `print_help` call sites that passes `$0`; the other eleven pass `${BASH_SOURCE[0]:-$0}`, which is the form `bin/_edm-cli-lib.sh:25-28` documents as the caller convention (with the reason the library cannot derive the path itself). | No live defect: the dispatch is guarded by `if [[ "${BASH_SOURCE[0]}" == "${0}" ]]` at `:5142`, so `$0` is provably the script path on the only reachable path -- stated plainly so the reviewer can disposition this as NOTED if preferred. It is filed because `edm-state` is the largest and most-read member of the family, so it is the shape a contributor copies, and the suite's CA-005/CA-154 loop (`wave7-smoke.sh:5513-5528`) asserts only that each file *sources* the library, never the call form -- so the divergence has no guard in either direction. | Change `:5186` to `print_help "${BASH_SOURCE[0]:-$0}"`, and extend the existing CA-005/CA-154 loop to assert the call literal alongside the source literal, the way the G66 evals loop already asserts an exact literal rather than a substring. |
| L7-005 | `plugins/edm/hooks/hooks.json:8` (`SessionStart`) | `plugins/edm/hooks/hooks.json:96` (`Stop`), `:106` (`PreCompact`) | Three sibling fire-and-forget hooks, two error-handling shapes. `Stop` and `PreCompact` are byte-identical (`command -v edm-state >/dev/null 2>&1 && edm-state checkpoint-if-active || true`); `SessionStart` adds `2>/dev/null` to the inner command (`... && edm-state session-start 2>/dev/null || true`), so it is the one hook in the file that discards its own diagnostics. | The trailing `|| true` already guarantees non-blocking, so the extra redirect buys silence only. `session-start` is the hook that globs **every** state file under the SRD root on every session, i.e. exactly the surface where a torn or malformed state file, an unresolvable path or a lock timeout would announce itself -- and it is the one place that announcement is thrown away. CLAUDE.md's Hooks-behavior table documents both rows (`:664`, `:667`) with no mention of the difference, and CA-298's lesson was precisely that hooks in one family must degrade the same way *and say so*. | Drop `2>/dev/null` so all three siblings degrade identically; or, if quiet session start is deliberate, keep it and add the rationale to the `SessionStart` row of CLAUDE.md's Hooks-behavior table so the divergence is intentional on the page. |

## Noted / Not Actionable

| ID | File:Line | Rationale |
|----|-----------|-----------|
| L7-N01 | `bin/edm-state:1188` (1 s) vs `:3597` (60 s) | The two age *thresholds* legitimately differ and the divergence is documented at `:1176-1184` -- 1 s is 10x the mkdir loop's own 0.1 s poll interval, 60 s covers a real git operation mid-flight. Only the error-path direction is actionable (L7-001). |
| L7-N02 | `bin/edm-state:3700-3715` vs `:1005-1012` | `cmd_git_lock_check` hand-rolls the mv-aside-then-remove instead of calling `_edm_reclaim_stale_lockdir`, and logs to stdout where the helper logs to stderr. Both are justified on the page: the comment names it as the same idiom deliberately, and `git-lock-check` is a user-facing report (stdout) where the lockdir reclaim is a background event during another command (stderr). Duplication axis belongs to L10. |
| L7-N03 | all 11 `agents/edm-audit-*.md` frontmatter | Verified byte-uniform this round: identical `tools` list, `model: opus`, `effort: max`, `maxTurns: 30`, `color: cyan`, `disallowedTools: Edit, NotebookEdit`. All 11 also carry all seven house-style boilerplate strings (scope statement, confidence clause, ASCII rule, JSONL-authoritative clause, fixed-schema clause, residual-risk clause, write-path contract) and the identical D34 `docs/canonical-sections.md` anchor sentence. No divergence. |
| L7-N04 | `agents/edm-audit-*.md:8` (KillShell, BashOutput without Bash) | Already dispositioned NOTED as CA-179 -- uniform across all eleven and inert. The synthesizer's omission of the two is the absence of an inert grant, not a divergence. |
| L7-N05 | eight EDMV3-54 prompt-surface touch points carrying the bare `CLAUDE.md Sec."..."` form | Documented as intentional residual scope in `plugins/edm/CLAUDE.md:306-322` (decisions.md D34), named as follow-on ticket `EDMV4-T04`. False-alarm filter (1). |
| L7-N06 | `evals/run-eval.sh:63`, `score-artifacts.sh:120`, `tiering-matrix.sh:65`, `bin/tests/run-all.sh:17` use `set -uo pipefail` where the nine `bin/` helpers use `set -euo pipefail` | Consistent within its own family: every long-running driver/aggregator that must survive a failing sub-run omits `-e`; every short-lived helper keeps it. False-alarm filter (3). |
| L7-N07 | `bin/edm-state`/`edm-init`/`edm-validate-prefix` `die()` default exit 1 vs 2 in the other nine | Documented as deliberate at `bin/edm-state:70-73` and asserted per-script by `wave7-smoke.sh:7271-7290` (G21/CA-074) -- only the two-argument *shape* was unified, not the numeric defaults. False-alarm filter (1). |
| L7-N08 | `hooks/hooks.json:19,32,45,58,71` (five gate command hooks) | CA-298 and CA-229 verified closed: all five are identical modulo the gate token, each passes its own token (`srd`, `audit-srd`, `tickets`, `audit-tickets`, `implement`), and all five now probe `resolve-dir ... || exit 0` before `gate-check`, so a setup condition exits 0 and only a real refusal exits 2 -- matching the PreToolUse hook's idiom and the five prompt bodies' first-invocation clause. |
| L7-N09 | `.gitlab-ci.yml:108`, `:243` (extension exclusions) | CA-233's L8 residual: both the `lint:bash-syntax` and `lint:shellcheck` loops now carry the identical `*.awk|*.txt` exclusion. Verified consistent; the in-suite twin's exclusion set is L8's to close. |
| L7-N10 | `skills/{plan,srd,tickets,audit-srd,audit-tickets,implement,code-audit,verify-runtime}/SKILL.md` | All eight gate sites reference `skills/orchestrator/SKILL.md Sec."Gate PROTOCOL"` by name and none restates it, per CLAUDE.md's intent-to-file index; asserted at `wave7-smoke.sh:2813`, `:2870`, `:3063`. No divergence. |
| L7-N11 | `argument-hint` quoting across the 14 `SKILL.md` files (`<PREFIX>` bare in six, `'<PREFIX>'` quoted in `verify-runtime:7`) | Cosmetic YAML-scalar quoting only; both parse identically and `validate:manifest`'s frontmatter checker accepts both. Below the L7 threshold. |

### Coverage note for the synthesizer

Two sibling sets in scope were **not** exhaustively diffed this round for want of a Bash tool (no ability to run the suite or diff bodies mechanically): the seven `bin/tests/*-smoke.sh` suites beyond their header/`SCRIPT_DIR`/`set` lines (asserted uniform by `wave7-smoke.sh:7253-7259`), and the nine `agents/edm-test-*.md` N/A-exit-token strings, whose last two divergences were closed as CA-081/CA-230 in round 4. Neither is claimed clean here.
</content>
