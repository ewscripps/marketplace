# Code Audit Lens L5: Runtime Hygiene

- **Date**: 2026-07-28 | **Round**: pass-1 (full, 11 lenses) | **Branch**: `edm/edmv3-prompt-streamline`
- **Note**: the lens agent had no Write tool at runtime despite its frontmatter granting one, so this file was transcribed by the orchestrator from the agent report. Findings, sites, severities and fixes are unaltered. The agent also had no Bash, so `git diff main...HEAD` could not be run and provenance is inferred from CHANGELOG plus the file tree.

Artifacts under `SRD/` including `.edm-state.json` are deliberately tracked and are not findings. This lens hunts the opposite: transient files that should never be committed and are not excluded.

| ID | Sev | Site | Defect | Fix |
|---|---|---|---|---|
| L5-01 | P1 | `bin/edm-state:431,692-694,705` + `.gitignore:10-11` | `lockbase="${f%.json}"` makes the artifacts `.edm-state.lock` and `.edm-state.lockd/`, but `.gitignore` lists `.edm-state.json.lock` and `.edm-state.json.lockd/`. Neither pattern has a wildcard, so **neither matches** -- a remediation that looks applied and is inert (the archived EDMV2 L5 table recorded the two rows mutually inconsistently and REMEDIATION.md carried the error into the `.gitignore` edit). The flock file is created by the `200>` redirect and **never unlinked** on any path, so one permanent untracked lock per initiative on every Linux box and CI runner. The Stop and PreCompact hooks take the lock every turn, and it sits beside the file the methodology tells the user to `git add` | Correct the two patterns to `.edm-state.lock` and `.edm-state.lockd/`; add `rm -f "${lockfile}"` after the flock FD scope closes; assert the `.gitignore` names the `lockbase`-derived forms |
| L5-02 | P1 | `bin/edm-state:3565-3583` | `${pattern_file}.tmp.$$` is written **inside the tracked** `docs/audit-patterns/`, with no trap anywhere in the script. Leaks on any signal in the loop body (recurring once per novel finding) and on any `set -e` abort from `head`, the `printf` block or `tail` -- including disk-full and a read-only *directory*. The read-only guard tests `[[ -w "$pattern_file" ]]`, the file not the directory, so a writable file in a non-writable directory produces a raw redirect abort instead of the documented graceful skip. The in-code comment covers the CI consequence but not `git status` | Trap EXIT/INT/TERM around the loop; add `plugins/edm/docs/audit-patterns/*.tmp.*` to `.gitignore`; test the directory in the read-only guard |
| L5-03 | P1 | `bin/edm-state:2813-2821` | `findings-ledger.md.tmp.<PID>` inside the tracked initiative `code-audit/` directory, no trap. Worse-placed than L5-02: it lands in exactly the directory the code-audit skill tells the operator to stage and commit, so it is likely to be swept in alongside the real ledger. Called once per audit round | Local trap before the write, cleared after the `mv`; add `SRD/**/*.md.tmp.*` to `.gitignore`. A shared `write_atomic <dest>` helper would cover all four hand-rolled `.tmp.$$`+`mv` sites (`:416`, `:1202`, `:2813`, `:3565`), three of them untrapped |
| L5-04 | P1 | `bin/tests/wave6-smoke.sh:3101-3112` | The test appends to the **tracked** `docs/canonical-sections.md` and restores it with no trap -- the suite's only trap covers `$TMP`. Any interrupt in the window (the suite has multiple `sleep 1`s and runs tens of seconds) or a `set -e` abort leaves a stray `hand-edited, not regenerated` line in a tracked file plus an orphaned backup. The next `--check` then fails with a confusing out-of-sync diff, and the line is a plausible accidental commit. `run-all.sh` runs this from CI and every local pre-push | Back up inside `$TMP` and trap the restore; better, copy `CLAUDE.md` + `docs/` into `$TMP` and run the generator against the copy so the test never touches the real tree |
| L5-05 | P2 | `bin/edm-sync-canonical-sections:63-64, 91-92` | The staging `mktemp` is correctly trapped, but the tracked destination is written with a non-atomic `cp` -- truncate-then-write. A signal or ENOSPC leaves the generated file half-written. Every other writer in the codebase stages beside the target and `mv`s, and this is the one whose target is byte-compared by both `--check` and a smoke case | `mktemp "${DST}.tmp.XXXXXX"`, trap, `mv -f`; add `plugins/edm/docs/*.tmp.*` to `.gitignore` |
| L5-06 | P2 | `evals/run-eval.sh:59,193-194` + `.gitlab-ci.yml:174-179` | `evals/runs/` is correctly gitignored, but has **no retention policy at all** -- every invocation mints a directory with three full `claude -p` JSON payloads plus stderr logs, and nothing prunes. CI's `expire_in` bounds the pipeline artifact, not the disk. And it sits **inside the path the blocking 100KB `du` gate measures**, a budget documented as covering the fixture -- so one local eval run makes the local reproduction of the gate fail with a message about a fixture budget. It survives in CI only because `GIT_CLEAN_FLAGS=-ffdx` wipes ignored files between pipelines: correct by accident of runner config, not construction | Scope the `du` to tracked bytes (which is what the documented budget means); keep the N most recent run directories and state the retention rule in `evals/README.md` |
| L5-07 | P2 | `bin/tests/wave7-smoke.sh` (19 sites) | Nineteen hardcoded `/tmp` scratch trees and **no `trap` at any line in the file**; cleanup is an inline `rm` on the happy path only. Several trees are megabytes (`cp -R` of `skills`, `agents`, `hooks`, `bin`). Hardcoding `/tmp` rather than `${TMPDIR:-/tmp}` also puts concurrent agents and worktrees in one namespace -- the same collision class that already produced a flake the harness fixed by scoping assertions while leaving the shared namespace | One suite-level `TMP` + `trap ... EXIT INT TERM` and route all nineteen through it; replace literal `/tmp` with `${TMPDIR:-/tmp}` in `_harness.sh:54` and `timing.sh` |
| L5-08 | P2 | `bin/tests/wave6-smoke.sh:818-819, 3237-3238, 3290-3291, 3413-3414, 3460-3461` | Nine `mktemp -d` fake-HOME/CWD trees created **outside** the trap-covered `$TMP`, with no INT/TERM trap. A leak leaves a directory shaped exactly like a real `~/.claude/projects/<encoded-cwd>/` containing synthetic token JSONL. The suite documents the correct pattern for itself at `:394-396` and these nine did not follow it. Positive note, verified at every call site: `HOME` is overridden before and restored after every `stage_session_jsonl`, so no synthetic data ever reaches the real `~/.claude/` | Create all nine inside `$TMP`; delete the five now-redundant inline `rm -rf` lines |
| L5-09 | P2 | `bin/tests/timing.sh:79` | `--generate-fixture` is the only mode with no `rm -rf` -- deliberately persistent, since `--subcommands` and `--all-lint` need `--dir`, but neither the script nor any doc tells the operator to delete the 50-initiative tree. Every certification pass leaves another. No trap on any of the six modes, so the five that do clean up leak on signal | Print an explicit teardown line; add per-mode traps; use `${TMPDIR:-/tmp}` |
| L5-10 | P2 | `evals/tiering-matrix.sh:130-131` | The `mktemp` template puts `.json` **after** the `X`s, which GNU accepts and BSD/macOS rejects (`mkstemp(3)` requires trailing `X`s), so `--self-test` takes the `die "mktemp failed"` branch on the platform this codebase declares as its hard target -- and the AC11 divergence grep that exists to catch this scans only `bin/` and does not include `mktemp` forms. Separately `trap ... RETURN` fires on function return only, so a `die` or SIGINT leaks the file | Drop the suffix and use `${TMPDIR:-/tmp}`; `trap ... RETURN EXIT INT TERM`; extend the AC11 grep to `evals/` and to `mktemp` templates |
| L5-11 | P2 | `bin/edm-lint-artifacts:343-355` | The attribution pattern file has no trap, and `scan_md_files` runs **inside the per-initiative loop** in `--all` mode, so the window is entered N times per run. This is the hot commit-hook path | Hoist to script scope, create once beside the PCRE probe, trap EXIT/INT/TERM |

## Noted / Not Actionable
- `.edm-state.json.bak` and `.edm-state.json.tmp.<PID>` -- both correctly matched by `.gitignore`, and the `.bak` is overwritten in place (one per state file, bounded). Contrast L5-02/L5-03, where the same idiom uses a base name the pattern does not cover.
- `edm-check-grants:114-115` -- `mktemp -d "${TMPDIR:-/tmp}/..."` with the trap on the very next line and honouring `$TMPDIR`. The reference implementation the others should copy.
- `wave3/4a/5/6-smoke.sh` preambles -- `TMP` + `trap ... EXIT` correct in all four (wave6's gap is the nine trees outside `$TMP`).
- `_harness.sh:51-101` `with_scratch_repo` -- trap installed before anything that can fail, caller's traps restored, and `harness-smoke.sh` proves cleanup on both the failing-function and SIGINT paths. Only the hardcoded `/tmp` is carried into L5-07.
- `run-eval.sh` scratch tree -- `trap cleanup EXIT INT TERM` installed before provisioning, with a re-entrancy guard and orphaned-child kill. Thorough. `--provision-only` non-cleanup is intentional and documented.
- `score-artifacts.sh:227` process substitution -- a `/dev/fd` fifo, never a persistent file.
- `wave7:263`, `:1374` `sed -i.bak` -- both targets are scratch copies, never the real tree.
- `edm-init:145`'s generated per-initiative `.gitignore` -- an intentional committed artifact under `commit_state_file=false`; also a natural second home for L5-01's corrected lock lines.
- `edm-state watch-impl` -- writes no PID file and no log; output goes to the monitor's stdout. No runtime file.
- `.gitlab-ci.yml:318/338` and `:366-367` -- one trapped, one not, but both run in a per-job ephemeral container whose filesystem is discarded.
- `edm-check-vocabulary`, `edm-compare-eval`, `edm-check-skill-sync`, `edm-validate-prefix` -- grepped for every creation idiom; no runtime file creation at all. Read-only.
- `hooks/hooks.json` -- all five command hooks are read-only probes; they write no files, but they amplify L5-01 and L5-11 by invoking those writers frequently.

## Not traced
`wave4a/4b/3/5-smoke.sh` bodies beyond their trap preambles -- recommend the same grep (a `mktemp` not containing `$TMP`, plus any write whose path starts `$REPO_ROOT`/`$PLUGIN_DIR`) across those four. `skills/**` and `agents/**` prompt bodies -- all runtime paths seen land under `SRD/`, but not every write instruction across 13 skills and 25 agents was enumerated. `edm-state`'s remaining ~3,000 unread lines: the four `.tmp.$$` sites and the lock/pid site are the complete set matched by a broad temp-file grep, so a fifth writer is unlikely but a temp path built without `.tmp` or `$$` would be missed.
