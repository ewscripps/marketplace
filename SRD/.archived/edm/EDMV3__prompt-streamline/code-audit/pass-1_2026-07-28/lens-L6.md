# Code Audit Lens L6: Documentation Accuracy

- **Date**: 2026-07-28 | **Round**: pass-1 (full, 11 lenses) | **Branch**: `edm/edmv3-prompt-streamline`
- **Note**: the lens agent had no Write tool at runtime despite its frontmatter granting one, so this file was transcribed by the orchestrator from the agent report. Findings, sites, severities and fixes are unaltered. The agent also had no Bash, so `git diff main...HEAD` could not be run and provenance is inferred from CHANGELOG plus the file tree.

| ID | Sev | Site | Defect | Fix |
|---|---|---|---|---|
| L6-01 | P1 | `CLAUDE.md:448-464` | The entire "gap that leaves" subsection describes the `compute_cost_usd` family-wildcard behaviour that was removed hours ago. There are no bare family wildcards; the arms are explicit generations and `edm-state:321-328` documents the removal. It tells the reader no warning fires in exactly the case where one now does, and directs hand-cross-checking because "nothing marks it as suspect" -- the mechanism now marks it automatically. The stated arm order is also wrong | Rewrite to the eight actual arms; keep the spirit as "a warned figure is a placeholder, not a measurement" |
| L6-02 | P1 | `evals/README.md:74-75` | Both documented eval defaults are wrong: 900 vs the real 2700, and $2 vs the real $15. The same file contradicts itself at `:233-234`, and its own measured figure (a 913s phase) is impossible under a 900s cap. An operator setting a budget from this line caps a real run at 1/7 the intended spend and kills it before the audit phase can finish | 900 -> 2700, 2 -> 15 |
| L6-03 | P1 | `bin/edm-lint-artifacts:118-123` | The `build_line_classes` header enumerates two of the **three** emitted classes -- `marker` is consumed as a first-class set at `:317` and passed to `mermaid_scan_awk` at `:425`, and is documented nowhere; the block's own follow-on comment at `:149` says "the three emitted classes", contradicting itself four lines earlier. It also claims the 40% budget is "met by construction" by the shared pass, which the recorded history disproves: after the shared pass the ratio was 3.40x, *worse* than before, and reached 1.19x only after the separate class-4 rewrite | Document `marker`; replace the budget clause with the truth -- the shared pass removes three redundant reads, and class 4's own cost was fixed separately |
| L6-04 | P1 | `hooks/hooks.json:86` + `bin/edm-lint-artifacts:31-34` | The help block says exit 2 is kept distinct "so the PreToolUse git-commit hook can tell a misinvocation from a dirty tree". The hook branches on any non-zero, so an exit 2 (edm-state absent, unresolvable prefix, missing directory) blocks the commit with "Fix artifact violations before committing" when there are no violations | Capture the status and branch; whatever blocking policy is chosen for exit 2, the two messages must differ |
| L6-05 | P1 | `bin/edm-lint-artifacts:435,436,471` | Three `die` messages lost their interpolations. Line 435 instructs `edm-state init` with **no argument**, which `cmd_init` refuses -- so the instruction cannot fix the problem it reports. The other two promise a path and print nothing | Restore `${PREFIX}`, `${INIT_DIR}`, `${PATH_ARG}` |
| L6-06 | P2 | `CHANGELOG.md:203` | The AC8 row still presents `git diff --stat shows zero changes` as the proof of PASS, while the same file at `:75-76` declares exactly that evidence invalid and records its replacement | Name the seven scoping assertions as the evidence; drop the `git diff --stat` claim |
| L6-07 | P2 | `CHANGELOG.md:325-358` duplicated at `:387-420` | Four `Added` bullets appear **twice, byte-identically**, inside one version entry, separated by an unnecessary `### Added (continued)` split. The two-copies-drift shape this initiative exists to close, in its own changelog | Delete the second copy; fold the continuation into the single `### Added` |
| L6-08 | P2 | `bin/edm-state:1947-1948` | The convergence-waiver `[warn]` names `mode` as the cause, but `convergence_exempt` returns true for two reasons and the second leaves `mode` as `standard` -- so it prints "standard mode -- skipping code-audit convergence check", which is false, since standard mode requires one. The very next line gets it right, so two adjacent lines disagree | Name the actual trigger |
| L6-09 | P2 | `bin/edm-state:2031` | The archive refusal says "remediate the blocking findings" for all four exit-1 causes, two of which (invalid JSONL, out-of-enum status) have no findings and need the ledger repaired -- re-running the audit will not fix malformed JSONL. A usage error lands in the same arm | Make the guidance conditional, or generic enough to cover ledger repair |
| L6-10 | P2 | `bin/edm-check-grants:67` | `usage() { sed -n '2,45p' "$0"; }` over a header running to line 57, so `--help` never prints the Output format, the Exit codes contract, or the bash-3.2 note -- the exact failure the sentinel convention was introduced to remove, in a file that shipped after the fix | Sentinels plus the shared awk extractor |
| L6-11 | P2 | `CLAUDE.md:411` | Documents `tokens.cache_write`, a field nothing writes -- the code writes `cache_write_5m`/`cache_write_1h`, `:466` of the same file explains the TTL split, and the CHANGELOG records the rename. A jq query for it returns null | List the two real fields |
| L6-12 | P2 | `.gitlab-ci.yml:10-11` | "Every `image:` entry below is pinned to a `@sha256:` digest" is false for `test:smoke-bash32` at `:277`. The exception is documented 260 lines later, so a reader auditing supply-chain posture from the header gets the wrong answer | Amend the header to name the one exception |
| L6-13 | P2 | `CLAUDE.md` model/effort paragraph | Omits `verify-runtime` (sonnet/high) from the phase-skill inventory -- it names seven of the eight and stops, and `verify-runtime` is the mandatory Phase 6 closure step. The three `test*` skills are also unaccounted for, so the paragraph covers 10 of 14 | Add `verify-runtime` and the test skills, or state that the testing layer is covered by its own section |
| L6-14 | P2 | `bin/edm-state:1249` | `approve-gate`'s usage message names one of three legal argument forms, while the help block and the implementation both carry all three. An operator who mistyped `code-audit` is told only a gate number is legal | `<gate-num>\|3.5\|code-audit` |
| L6-15 | P2 | `bin/edm-lint-artifacts:145-149`, `:338-341` | Two comments each claim sole authorship of the same 39,872 ms measurement; the CHANGELOG attributes it to both loops and records the re-profiled 70,168 ms. "This loop was the cause" is the sharper error | Say "one of the two per-line fork loops" in both, and cross-reference |
| L6-16 | P2 | `bin/tests/timing.sh:301` | `--all-lint` prints an initiative count it never measured, using the default 50, in a harness whose header states "no numbers are invented". Run against a 10-initiative fixture it asserts 50 -- and the CHANGELOG quotes this mode as the AC7 evidence | Derive the count from the fixture at measurement time |
| L6-17 | P2 | `bin/tests/timing.sh:254-258` | The comment says the ratio "should stay near 1.0x" because files with no fence short-circuit, but the mode appends a fence to **every** file, so the cited mechanism is inactive for the number being produced | State the fixture honestly: one diagram per file, the worst realistic case, short-circuit deliberately not exercised |
| L6-18 | P2 | `.gitlab-ci.yml:177` | Failure message ends in a colon promising a list, then exits without printing one. The parallel branch above it earns its colon | Drop the colon or print the offenders |
| L6-19 | P2 | `CLAUDE.md` schema_version contract vs `bin/edm-state:1168` | Doc says `cmd_init` writes "the wave the running plugin version belongs to"; it writes the literal `1`. So a brand-new initiative created by 3.1.0 starts at 1 and warn-and-proceeds through every wave-B check by name, on an initiative that never had a legacy shape | Document the real behaviour and why (`migrate-schema` is the only writer of 2). If the intent was otherwise, that is a code change for a correctness lens |

## Noted / Not Actionable
- `bin/edm-state:3030` -- the "expected: open\|fixed\|noted" list omits `deferred`, which the filter tolerates. Deliberate: advertising abolished vocabulary in an operator-facing list would contradict the policy `edm-check-vocabulary` enforces.
- `bin/edm-state:343-352` -- the `unknown` arm's "tokens are necessarily 0" is false in one narrow case (a message with no `.message.model` but non-zero tokens). Pricing is correct either way; intentional approximation.
- `.gitlab-ci.yml:47-56` -- the lint-stage header says "the four checks below" while seven jobs follow; the paragraph is explicitly scoped to the four AC10 checks and the CLAUDE.md table carries the accurate count.
- `bin/edm-state:2152`, `:3601` -- `metrics-report`'s usage omits an optional flag, and `update-patterns` says "appended to" for a heading-targeted insertion; "append" is the contract document's own vocabulary.
- `.gitlab-ci.yml:234` -- "all five library docs" hardcodes a count, verified correct today.
- `CHANGELOG.md` 1.x/2.0.0 entries use non-ASCII glyphs -- pre-existing, outside this diff, and CHANGELOG is out of the vocabulary checker's scope.
- Repository-root `CLAUDE.md` still records edm at v2.1.0 with a 13-skill list (14 exist, `verify-runtime` absent) -- outside this lens's file scope; flagged for routing.

## Not examined
No Bash, so scope was reconstructed from the CHANGELOG and the file tree; provenance as "changed by this initiative" is inferred. Not examined: `edm-check-grants:120-end`, `edm-init`, `edm-validate-prefix`, `edm-compare-eval`, `edm-check-skill-sync`, the `*-smoke.sh` bodies, `run-eval.sh` beyond the two constants, `score-artifacts.sh`, `tiering-matrix.sh`, `evals/baseline/README.md`, `monitors/monitors.json`, the 14 SKILL.md bodies and the 30 agent bodies. The CHANGELOG's "1401 assertions" figure was not verified -- not statically countable without a shell.
