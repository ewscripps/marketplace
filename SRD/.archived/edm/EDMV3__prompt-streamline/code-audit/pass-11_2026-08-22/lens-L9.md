# Lens L9 -- Spec & Ticket Compliance (round 11, 2026-08-22)

Scope this round: verification verdicts on the five round-10 L9 findings assigned to this lens
(CA-106, CA-509, CA-510, CA-514, CA-536), plus the spec-compliance residue those remediations left.
Inputs read: `srd.md` (header, revision history, EDMV3-28, EDMV3-90), `decisions.md` D62,
`tickets/epics/03-ci-and-fixture-eval.md`, `tickets/epics/04-structured-findings.md`,
`tickets/epics/08-economics-honesty.md`, `tickets/README.md` (grep), `plugins/edm/CLAUDE.md`,
`plugins/edm/bin/edm-state`, `plugins/edm/bin/tests/wave6-smoke.sh`, `wave7-smoke.sh`.

## Round-10 carry-in verdicts

| ID | Round-10 sev | Verdict | Evidence |
|---|---|---|---|
| CA-509 | P1 open | **FIXED** | `srd.md:9` is now Version `1.4.0`; `srd.md:26` adds the CR8 revision-history row naming D57/D58 and CA-509 by ID; EDMV3-90 AC1 carries the carve-out at `srd.md:3838-3841` (`approve-gate code-audit --accept-p2-debt`, scoped to that one subcommand pairing, still P0/P1-non-waivable) and AC4 the matching recorded-exemption carve-out at `srd.md:3855-3858`. The pack's own CR route was taken, as the finding prescribed. |
| CA-510 | P1 open | **FIXED** | T51 gained AC11 at `tickets/epics/08-economics-honesty.md:187-201` with all three prescribed branches -- (i) POSITIVE `full` stays `full`, (ii) NEGATIVE downgrade to `partial` naming every affected lens with nothing else mutating, (iii) C-4 no-pass-dir/no-manifest leaves the round unchanged -- and an updated AC-band note at `:202-204` ("11 acceptance criteria"). The mutual disclaimer is gone: T51's Out of Scope at `:221-224` now explicitly claims completion-time ownership ("This ticket owns the completion-time completeness downgrade (AC11 above)") while T27's Out of Scope at `tickets/epics/04-structured-findings.md:445` disclaims to T51, which is now consistent rather than circular. Named smoke cases exist: `plugins/edm/bin/tests/wave6-smoke.sh:921-938` (`CA477FULL`) and `:946-970` (`CA477CLASS`). Behaviour is documented in `plugins/edm/CLAUDE.md`'s `audit_rounds.<type>.rounds[].round_type` row, including the irreversibility clause. |
| CA-514 | P1 open | **FIXED (ticket half); two residues filed below** | T28 gained AC14 at `tickets/epics/04-structured-findings.md:587-612`: the three sanctioned `spec_swept` values, (i) POSITIVE non-blocking for `yes`/`n/a` including under `--accept-p2-debt`, (ii) NEGATIVE `no` refuses `audit_converged` and `approve-gate code-audit` with `--accept-p2-debt` explicitly non-waiving, (iii) C-4 absent-field grandfathering, plus explicit ownership of the `SPEC_SWEEP_PENDING` informational `validate` anomaly in EDMV3-T05 AC2's four-field shape. The doc half is present: `plugins/edm/CLAUDE.md`'s state/ledger field table carries a `spec_swept` row with the enum, the writer (`edm-audit-synthesizer`), the two blocking consumers and the C-4 absence rule. Code exists and is exercised (`bin/edm-state:1665-1682`, `:1904-1922`, `:2422-2440`, `:4850-4916`; `wave7-smoke.sh:9407-9505+`). Residues: L9-R11-02 (stale Verify line range) and L9-R11-03 (contradicting AC-band notes). |
| CA-536 | P2 open | **STILL OPEN -- see L9-R11-01** | The ticket half landed (`tickets/epics/04-structured-findings.md:577-586`: AC13 amended, new Verify clauses). The code obligation the amendment creates did not: `edm-state --help` still does not name the refusal reason. |
| CA-106 | P1 open | **FIXED per the sanctioned route (b); one residue filed below** | D62 (`decisions.md:71`) records the human's route-(b) choice at the round-10 convergence gate and enumerates exactly what it covers: T23 AC8/AC13 amended, the `tickets/README.md` coverage-map row, and the `.gitlab-ci.yml` / `plugins/edm/CLAUDE.md` comment qualifications. All four landed -- `tickets/epics/03-ci-and-fixture-eval.md:579-583` and `:616-628` record the boundary and name **EDMV4-T05** as the owning follow-on rather than leaving a "pending" status with no owner; `tickets/README.md:638` names the follow-on in the EDMV3-28 coverage row; `plugins/edm/CLAUDE.md`'s `eval:nightly` row now reads "D62/CA-106: no baseline exists yet, but that no longer preempts this refusal". This is a recorded scope boundary under D15 route (2), not a deferral. Residue: the boundary is absent from `srd.md` itself (L9-R11-04). |

## Findings (L9: Spec & Ticket Compliance)

### Missing Implementations (P1)

| Requirement | Ticket | What's Missing | Evidence (search results) |
|---|---|---|---|
| (none new this round) | -- | -- | -- |

### Partial Implementations (P1)

| Ticket | AC | What the Spec Requires | What Code Does | File:Line |
|---|---|---|---|---|
| EDMV3-T28 | AC13 (as amended by CA-536) | The `--help` block "enumerate[s] every refusal reason `audit_converged` can return, including the `spec_swept` sweep-debt refusal added by AC14", with Verify `edm-state --help | grep -n 'spec_swept\|sweep-debt'`. | **L9-R11-01.** The `EDM-HELP` sentinel block that `print_help` emits runs from `bin/edm-state:2` to the `# EDM-HELP-END` marker at `:53`. Its `audit-converged` line (`:42`) reads only "query code-audit convergence over findings-ledger.jsonl (read-only)" -- it enumerates no refusal reason at all. The earliest occurrence of `spec_swept` or `sweep-debt` anywhere in the file is `:1665`, i.e. 1,600 lines past the end of the help block, so the amended Verify command returns nothing and AC13 cannot be closed. Secondary divergence in the same AC: the requirement text says the **`CLAUDE.md` `bin/` table** enumerates the refusal reasons, but that table (`plugins/edm/CLAUDE.md`, `edm-state` row) only lists 39 subcommand names; the refusal reasons live in the state/ledger field table's `spec_swept` row instead. The amended Verify (`grep -n 'audit-converged' plugins/edm/CLAUDE.md`) passes without testing the clause it is attached to, so the doc half is green by a weaker check than its own AC text. Fix is small: add the refusal reasons to the `audit-converged` help line (or a `refusal reasons:` sub-line) and either point AC13's doc clause at the field table or move the enumeration into the `bin/` table. | `plugins/edm/bin/edm-state:42`, `:53`, `:1665`; `tickets/epics/04-structured-findings.md:577-586`; `plugins/edm/CLAUDE.md` (`bin/` table `edm-state` row) |

### Scope Creep (P2)

| File / Feature | Not Specified In | Recommendation |
|---|---|---|
| (none new this round) | -- | This round's scope was the five carry-in IDs; no new unticketed endpoint, model or config option was found in the files read. A full scope-creep sweep over the whole tree was not re-run this round -- see "Coverage gaps" below. |

### Other spec-compliance defects (P2)

| ID | Finding | Evidence |
|---|---|---|
| L9-R11-02 | **T28 AC14's Verify clause cites a line range that does not contain the evidence.** AC14 says "Verify: `bash plugins/edm/bin/tests/wave7-smoke.sh:8755-8910` -- the existing case block covers all three branches verbatim". The `spec_swept` enforcement block in that suite actually begins at `:9339` (banner at `:9420`, "=== spec_swept enforcement (audit-converged, approve-gate, validate) ==="), with the branch cases at `:9446` (fixed+`no` refuses and names the ID), `:9457` (`yes` and `n/a` both pass), `:9473` (absent field never blocks), `:9484` (open+`no` does not block), `:9496-9505` (multi-ID naming). There is no `spec_swept` token anywhere in `8755-8910`. A reviewer following the Verify clause literally lands on unrelated assertions and would conclude the AC is unproven. Same defect class the initiative's own D15 rule exists to prevent: a Verify clause must be runnable as written. Fix: re-cite the real range (or, better, cite the banner string rather than line numbers, which drift on every insertion). | `tickets/epics/04-structured-findings.md:608-612`; `plugins/edm/bin/tests/wave7-smoke.sh:9339`, `:9420`, `:9446-9505` |
| L9-R11-03 | **T28 now carries two contradicting AC-band notes.** The AC list ends with "**AC-band note.** 14 acceptance criteria against the 6-12 band ... AC14 is the `spec_swept` refusal's own three branches" (`:613-615`), while Technical Notes still ends with "**AC-band note.** 13 acceptance criteria against the 6-12 band. Six of them (AC1-AC6) ... Recorded in the README sizing section" (`:627-629`). One ticket cannot have both 13 and 14 ACs. The stale copy additionally asserts the count is "Recorded in the README sizing section", so `tickets/README.md`'s sizing row is a likely third stale site (not verified this round -- see coverage gaps). This is precisely the `spec_swept`-class sweep debt AC14 itself exists to enforce, left inside AC14's own landing commit. Fix: delete the Technical-Notes duplicate (the AC-list copy is the one adjacent to the criteria it counts) and sweep the README sizing row in the same change. | `tickets/epics/04-structured-findings.md:613-615` vs `:627-629` |
| L9-R11-04 | **CA-106's route-(b) boundary is recorded everywhere except `srd.md`.** `srd.md:1749-1774` EDMV3-28 ("Baseline captured on wave-A code and committed") is byte-unchanged: `- **Priority**: Must Have`, six unchecked ACs, and no reference to D62, to EDMV4-T05, or to any out-of-scope boundary. The v1.4.0 revision row (`srd.md:26`) covers CR8/EDMV3-90 only and states "No requirement added or removed". So the artifact a reviewer reads as the requirements baseline still asserts an in-scope, undelivered **Must Have** whose capture the ticket pack, `tickets/README.md` and `decisions.md` all now say is out of scope and owned by another initiative. This is the same defect shape as CA-509 (ticket pack asserts a boundary `srd.md` does not carry), which was closed by taking the CR route -- the CR route was not taken here. Mitigating and the reason this is P2 rather than P1: D62 is an explicit human gate decision that enumerated its own remediation scope and did not include an `srd.md` edit, so the divergence is documented rather than silent. Fix: a CR9 row plus a one-line boundary note under EDMV3-28 naming D62 and EDMV4-T05. | `srd.md:1749-1774`, `:26`; `decisions.md:71`; `tickets/epics/03-ci-and-fixture-eval.md:579-583`, `:616-628`; `tickets/README.md:638` |
| L9-R11-05 | **CR8's own document sweep is incomplete.** `srd.md:12` still reads `Last Updated | 2026-07-26` although the revision history's newest row is 1.4.0 dated 2026-08-21, and `srd.md:16` still describes the companion `decisions.md` as "(D1-D16)" when that file now runs to D62. Both sites name the very facts CR8 changed, so under the same-commit sweep convention this initiative now enforces on itself (`spec_swept`), CR8 shipped with sweep debt in its own document. | `srd.md:12`, `:16`, `:26`; `decisions.md:71` |
| L9-R11-06 | **D61's six-dimension sweep missed EDMV3-28 AC5.** `srd.md:1769-1771` still requires `evals/baseline/README.md` to state "that the **five** dimensions are proxies". D61 (`decisions.md:70`) authorized six scorer dimensions (`scorer_version` 1.1.0) and swept T23's ACs, `evals/baseline/README.md`, `score-artifacts.sh` (CA-484) and `wave7-smoke.sh`, but this SRD AC still states the old count as a number. A wave-A capture scores five of six, so the sentence is coincidentally near-true for a capture -- but the clause is about the dimension *set* being proxies, and the set is six. Medium confidence on severity, high on the textual fact. Fix in the same CR that closes L9-R11-04, since both edit EDMV3-28. | `srd.md:1769-1771`; `decisions.md:70` |

## Noted / Not Actionable

- CA-509, CA-510, CA-514 and CA-106 are confirmed remediated as described in the carry-in table above; no re-open. Their residues are filed as separate new findings rather than re-opened under the old IDs, because each is a distinct site with its own fix.
- T51 AC11's Verify clause names real smoke cases (`CA477FULL`, `CA477CLASS` at `wave6-smoke.sh:921-970`), so it does not share L9-R11-02's stale-citation defect. Not verified this round: that the case asserts the exact warn substring `for: L2 L3 L4` the AC quotes.
- `spec_swept` being a `findings-ledger.jsonl` field rather than a state field, while documented inside `CLAUDE.md`'s state-field table, is explicitly labelled as such in its own row ("**not a state field** -- ... listed here because the same enforcement kernel reads it"). Deliberate placement, not a misfiling.
- The `--accept-p2-debt` flag itself is no longer scope creep against `srd.md`: CR8 authorizes it. That was CA-509's entire content.

## Coverage gaps (declared, not silently omitted)

This round's L9 pass was scoped by the launching agent to the five carry-in IDs plus the residue they left. The following were **not** re-derived and carry no verdict from this round:

1. Full requirement-by-requirement SRD sweep (120 requirements) and full AC-by-AC ticket sweep (all 11 epic files). Last full sweep: round 10.
2. Repo-wide scope-creep sweep (new endpoints / models / config options with no ticket).
3. `tickets/README.md`'s sizing row for T28 (the likely third site of L9-R11-03) and its AC-count table generally.
4. T22/T23 eval-baseline and T51 economics ACs beyond the clauses the five carry-in IDs touch.
