I have completed both the priority-1 verification and the whole-scope sweep. I lack Write (CA-130, tenth consecutive round for L10), so both halves follow inline.

---

# Findings (L10: DRY & Redundancy) -- Round 9

**Round type**: full. **Delivery**: Write, Edit and Bash all absent from the delivered tool set (CA-130, tenth consecutive round for this lens). No command was executed; every verdict below is from reading the tree at HEAD. Two findings carry hand-derived arithmetic and are marked medium confidence accordingly.

## Part 1 -- Post-remediation verification of the round-8 L10 set

| Finding | Verdict | Evidence |
|---|---|---|
| CA-344 (1) edm-init prefix regex | **LANDED COMPLETE** | `/Users/darryl.porter/projects/marketplace/plugins/edm/bin/edm-init:53-56` -- the local `^[A-Z][A-Z0-9]{2,5}$` copy is gone, replaced by a comment naming `edm-validate-prefix` as the validator; `:87` now delegates by sibling path **unconditionally** (the inconsistent `command -v` guard the finding named is gone too). One regex survives, at `edm-validate-prefix:42`. |
| CA-344 (2) `agent_grants_class` | **LANDED COMPLETE** | `edm-check-grants:260-267` defines it; both directions call it -- `:278` (positive/`assert_agent_grant`) and `:544` (AC3 negative pass). No third hand-rolled predicate survives. |
| CA-344 (3) `_collect_md_files_into` / `_summarize_and_exit` | **LANDED COMPLETE** | `edm-lint-artifacts:435-441` and `:447-455`; three call sites each (`:466`/`:480`/`:518`, `:474`/`:495`/`:528`). The 0-clean/1-violations contract is now encoded once. |
| CA-344 (4) `_scan_product_dirs_for_prefix` | **LANDED COMPLETE** | `edm-validate-prefix:57-70`; both callers converted (`:81` die-path, `:92` warn-path). The diverged guards are reconciled -- the root `-d` test and `.archived` skip now live in the single walker. |
| CA-344 (5) `_lookback_window` | **LANDED COMPLETE** | `edm-check-grants:378-383`; `resolve_targets_for_line:399` captures the window **once** and passes the text to both `nearby_agent_name` and `window_mentions_lens`. The double `sed` execution per candidate line is gone. |
| CA-417 `skipped_phases_str` | **LANDED COMPLETE, PINNED** | Helper at `edm-state:870-872`; both survivors converted (`:2432` cmd_phase_complete, `:5328` `_write_handoff_body`) with in-line CA-417 markers; four callers total (`:902`, `:2432`, `:2997`, `:5328`). Docstring at `:866-869` now states the correct history. The missing durability pin landed: `wave6-smoke.sh:1067-1077` asserts exactly **one** non-comment literal copy of the jq derivation and **>=4** call sites. |
| CA-418 coverage headers | **LANDED PARTIAL** | The DRY half landed: `edm-state:1116-1118` defines three shared header constants, referenced from all four sites (`:2705`, `:2717`, `:3559`, `:3568`); metrics-report's inline `printf` width specs are gone, so one mechanism now owns the widths. **Residual filed as #4 below**: the Coverage-column width is still specified twice and the two specs disagree, and `wave5-smoke.sh:111-125` was not extended to the header rows as prescribed. |
| CA-419 `_cmd_set_provenance_field` | **LANDED PARTIAL** | The structural half landed: `edm-state:4903-4912` with two one-line wrappers at `:4914`/`:4916`. **The divergence half did not land** -- filed as #3 below. |
| CA-420 `archived_state_file_for` | **LANDED COMPLETE** | `edm-state:157-168`, defined directly beside `list_state_files` as prescribed; `cmd_migrate_schema:2810` is its sole consumer and carries no local `case` patterns. The overclaiming comment is corrected at `:2805-2808`. |
| CA-467 twelve-copy schema identity check | **LANDED COMPLETE, WITH POSITIVE CONTROL** | `wave7-smoke.sh:1655-1686` extracts the schema line from all 11 agents + `skills/code-audit/SKILL.md`, normalises the lens token, asserts 12/12 extracted and exactly 1 deduplicated line, and includes a real positive control (`:1682-1686`) that mutates a copy and requires 2 distinct lines. I independently verified all twelve copies are byte-identical modulo the lens token. The self-contradictory docstring is corrected at `agents/edm-audit-dry.md:107`. |

**Net**: 8 of 10 complete, 2 partial. No re-introduced copies, no new divergence introduced by the remediation itself. The two partials are residuals of the same shape flagged in prior rounds -- a structural extraction that landed while the behavioural divergence it sat next to did not.

## Part 2 -- New findings

| # | Type | File A | File B | Canonical | Recommendation |
|---|---|---|---|---|---|
| 1 | Diverged parallel enumeration | `.gitlab-ci.yml:106` | `.gitlab-ci.yml:242` + `bin/tests/wave7-smoke.sh:1081` | `.gitlab-ci.yml:106` | Single-source the glob triple; extend the CA-233 pin to cover the file set, not only the exclusion arm |
| 2 | Duplicate enum literal | `bin/edm-state:4358-4359` | `bin/edm-state:4506-4507` | neither -- add `AUDIT_TYPE_ENUM_LIST` | Follow the file's own documented single-source enum convention |
| 3 | Diverged parallel implementation | `bin/edm-state:4903-4916` | `bin/edm-state:4938`, `:4951` | `cmd_set_parent` | Add `write_handoff_internal` to `_cmd_set_provenance_field` (CA-419's undelivered half) |
| 4 | Duplicated constant, already diverged | `bin/edm-state:1097`, `:1106` | `bin/edm-state:1116`, `:1118` | the row defs | Derive header widths from the row pad ladders; extend `wave5-smoke.sh:112`'s pin to the header rows |
| 5 | Copy-pasted block x3 | `bin/_edm-lint-lib.sh:103-107` | `bin/edm-lint-artifacts:207-208`, `evals/score-artifacts.sh:325-326` | `_edm-lint-lib.sh` | Extract one `awk`-with-rules helper; also single-source the rules-file existence precondition |

### Details

#### Finding 1: the plugin's "what is a bash source file" definition is written three times; the exclusion half is pinned, the file-set half is not and has already diverged

- **File A**: `/Users/darryl.porter/projects/marketplace/.gitlab-ci.yml:104-124` (`lint:bash-syntax`) -- `for f in plugins/edm/bin/* plugins/edm/bin/tests/*.sh plugins/edm/evals/*.sh`, `*.awk|*.txt) continue ;;`, then a `FAIL`/`COUNT` loop.
- **File B**: `/Users/darryl.porter/projects/marketplace/.gitlab-ci.yml:240-263` (`lint:shellcheck`) -- the **same 20-line block, verbatim**, differing only in the tool invoked, the job name in the two verdict strings, and the trailing verb ("parsed" vs "clean").
- **File C**: `/Users/darryl.porter/projects/marketplace/plugins/edm/bin/tests/wave7-smoke.sh:1081-1087` (T61 AC10 in-suite twin) -- `for t61_f in "$PLUGIN_DIR"/bin/* "$PLUGIN_DIR"/bin/tests/*.sh`, same exclusion arm.
- **Divergence**: copy C's file set **omits `evals/*.sh`**. Copies A and B cover the three eval drivers; C covers two of the three globs. So a syntax error introduced into `evals/run-eval.sh`, `evals/score-artifacts.sh` or `evals/tiering-matrix.sh` is caught by CI but **not** by a local `bin/tests/run-all.sh` -- which `plugins/edm/CLAUDE.md` Sec."Testing changes" step 5 presents as "the same command CI runs and the fastest way to catch a regression before opening an MR". It is not the same for this class.
- **Why this is not a false alarm**: this exact enumeration has diverged before and shipped -- CA-162 (round 2) filed `lint:shellcheck` covering only `bin/*` while `lint:bash-syntax` covered more, and CA-233 spent four rounds resynchronising the *exclusion* arm across the same three loops. CA-233's fix added a real three-way consistency assertion at `wave7-smoke.sh:1099-1115`, but it checks **only** the `*.awk|*.txt) continue ;;` string -- `:1101-1104` counts occurrences in `.gitlab-ci.yml` and `:1112-1115` extracts the twin's loop block. Nothing compares the `for f in ...` glob lists. The comment at `:1092-1096` states the intent as "so a future edit to just one of them cannot silently re-diverge the other two", which is true of the exclusion and false of the file set sitting one line above it.
- **Fix**: (a) hold the glob triple in one place -- a YAML anchor or `!reference`d snippet consumed by both CI jobs, and have the in-suite twin read the same list (it already reads `.gitlab-ci.yml` at `:1100` for the exclusion check, so the machinery exists); (b) extend the CA-233 assertion to require the three `for` lines' glob lists to be identical, with a positive control; (c) either add `evals/*.sh` to the twin or state in its label that it is deliberately bin-only, so a reader is not misled about local/CI parity.

#### Finding 2: the audit-type enum is the one enum in `bin/edm-state` that does not follow the file's own documented single-source convention

- **File A**: `/Users/darryl.porter/projects/marketplace/plugins/edm/bin/edm-state:4357-4360` (`cmd_audit_round_start`) -- `case "$audit_type" in code|srd|tickets) ;; *) die "... (expected: code|srd|tickets)" ;; esac`.
- **File B**: same file, `:4505-4508` (`cmd_audit_round_complete`) -- byte-identical apart from the subcommand name in the message.
- **Divergence**: none today. That is the finding, per the CA-343 framing -- the file explicitly documents the opposite convention and then breaks it here.
- **Why this matters**: `:799-803` states the rule in the file's own words: enums live in one space-separated constant consumed by the `case " $LIST " in *" $item "*)` idiom "so no caller re-encodes the enum as a second literal". Four constants follow it -- `MODE_ENUM_LIST` (`:804`), `LIFECYCLE_MODE_ENUM_LIST` (`:805`), `ALL_LENS_IDS` (`:1528`, with a self-check at `:1530`), `SETTABLE_KEYS` (`:1923`) -- and every one of their consumers derives its diagnostic from the constant via `${LIST// /|}` rather than retyping it (`:827`, `:835`, `:947`, `:1970`, `:4823`, `:4845`). The audit-type enum is spelled out **six** times instead: two `case` patterns, two die-message literals, and two usage strings (`:4353`, `:4502`), plus two help-header lines (`:39-40`).
- **Cost if a fourth audit type is ever added** (plausible -- this plugin already has a testing layer and a `test-coverage` skill): six in-code edits. Miss the `complete` half and `audit-round-start <PREFIX> <newtype>` succeeds while `audit-round-complete <PREFIX> <newtype>` dies -- leaving a round that can be opened and never closed, which surfaces only as the informational `OPEN_AUDIT_ROUND` anomaly.
- **Fix**: add `AUDIT_TYPE_ENUM_LIST="code srd tickets"` beside the other four constants, convert both validators to the word-membership idiom with `${AUDIT_TYPE_ENUM_LIST// /|}` in the diagnostic, and derive the two usage strings from it. A `count_matches_strict` single-definition pin in the shape `wave6-smoke.sh:1067-1077` already uses would make it durable.

#### Finding 3: CA-419's structural half landed; the behavioural divergence it named did not -- two of four cross-initiative-prefix writers still leave HANDOFF.md stale

- **File A**: `/Users/darryl.porter/projects/marketplace/plugins/edm/bin/edm-state:4903-4916` -- `_cmd_set_provenance_field` plus its two wrappers. Writes the state key via `rmw_state`, echoes a confirmation, and **returns**.
- **File B**: same file, `:4938-4948` (`cmd_set_parent`) and `:4951` onward (`cmd_add_related`) -- both validate the target initiative exists **and** call `write_handoff_internal "$prefix"` (`:4947`).
- **Divergence**: four subcommands write a cross-initiative prefix link into state; only two refresh the committed handoff document. `_write_handoff_body` reads both of the un-refreshed fields into the rendered artifact -- `supersedes` at `:5318` and `forked_from` at `:5319` -- so after `edm-state set-supersedes` or `set-forked-from`, the committed cross-user resume doc keeps showing the **stale** value until some unrelated command happens to rewrite it. The validation asymmetry is at least consistent with `plugins/edm/CLAUDE.md`'s state-field table (which claims existence validation for `parent_prefix` only); the HANDOFF-refresh asymmetry is documented nowhere.
- **Why this is not a false alarm**: this is CA-419's own prescription verbatim -- "collapse both into one `_cmd_set_provenance_link` **and bring the pair in line with `cmd_set_parent`'s `write_handoff_internal` call in the same edit**". The first clause shipped; the second did not. CA-419's text called the divergence "the more interesting half".
- **Fix**: add `write_handoff_internal "$prefix"` as the last line of `_cmd_set_provenance_field` (one line, since the pair is now a single function -- which is precisely what the extraction was supposed to make cheap). Decide the validation half explicitly: either add the `state_file_for`/`-f` existence check to match the sibling pair, or record in `CLAUDE.md`'s state-field table why provenance links are deliberately allowed to name a not-yet-created initiative.

#### Finding 4: CA-418 residual -- the Coverage column width is declared twice and the two declarations disagree by one column; no pin covers the headers

- **File A**: `/Users/darryl.porter/projects/marketplace/plugins/edm/bin/edm-state:1097` (`COVERAGE_LAYER_ROW_JQ_DEF`) and `:1106` (`COVERAGE_EPIC_ROW_JQ_DEF`) -- the Coverage field width is expressed as a jq pad ladder: `if .value.pct < 10 then <9 sp> elif < 100 then <8 sp> else <7 sp>` (layer) and `8/7/6` (epic).
- **File B**: same file, `:1116` and `:1118` -- the same width is expressed again as `printf` field specs: `%-10s` (layer) and `%-9s` (epic-with-measured).
- **Divergence**: for an integer `pct` the row emits an **11**-column Coverage field (layer: `"82"+"%"` = 3 chars + 8 pad) where the header allocates **10**; and a **10**-column field (epic: 3 + 7) where the header allocates **9**. Both are off by one in the same direction, so the trailing `Measured At` header sits one column left of its data. For the fractional `pct` the state schema actually documents (`plugins/edm/CLAUDE.md` shows `"pct": 82.4`), `"82.4%"` is 5 chars + 8 pad = 13, and the offset widens to three columns. `COVERAGE_EPIC_HEADER` (`:1117`) is correct because Coverage is its last column and needs no width.
- **Confidence: medium.** Derived by hand from the format strings; no Bash was delivered this round, so the exact rendered offsets were not observed. CA-418 flagged the same limitation and asked the next round to confirm by running both commands -- that request is still outstanding.
- **Why this is L10 and not merely cosmetic**: the extraction's own comment at `:1108-1115` asserts "Widths derive from the row clamps". The `14`/`15`/`14` layer and epic clamps genuinely do (and match). The Coverage width does not -- it is a hand-chosen second specification of a number the row def already owns, and it is wrong. That is the CA-343/CA-420 class (a comment asserting single-sourcing over a site that is not single-sourced) at a fourth site, introduced by the fix for the finding that named the class.
- **Fix**: emit each header from the same constants the row def pads with (or at minimum widen `%-10s`/`%-9s` to `%-11s`/`%-10s` and add a comment naming the row def as the number's owner). Then extend `wave5-smoke.sh:111-125` -- which today compares only the **data** row byte-for-byte between `get-coverage` and `metrics-report` -- to compare the header/underline pair too, which is the assertion CA-418 prescribed and that did not land.

#### Finding 5: the CA-472 awk-program caching wrapper exists in three copies, and the precondition it depends on is enforced in two of five consumers

- **File A**: `/Users/darryl.porter/projects/marketplace/plugins/edm/bin/_edm-lint-lib.sh:103-107` and `:189` -- `_BLC_PROG_CACHE`.
- **File B**: `/Users/darryl.porter/projects/marketplace/plugins/edm/bin/edm-lint-artifacts:207-208` and `:229` -- `_MSA_PROG_CACHE`.
- **File C**: `/Users/darryl.porter/projects/marketplace/plugins/edm/evals/score-artifacts.sh:325-326` and `:364` -- `_SMB_PROG_CACHE`.
- **Shared body**: `if [[ -z "${VAR:-}" ]]; then VAR="$(cat "$MERMAID_RULES_AWK")"$'\n'"$(cat <<'AWK_MAIN' ... )"; fi` then `awk ... "$VAR" "$file"`. Only the per-site main program and the cache-variable name differ.
- **Divergence**: cosmetic only -- A pre-initialises the variable at file scope and tests `[[ -z "$VAR" ]]`; B and C use `${VAR:-}` with no initialisation. No behavioural difference.
- **Secondary observation, same class**: the precondition this idiom silently depends on -- that `$MERMAID_RULES_AWK` exists, since a failed `cat` yields a program with no function definitions and awk then aborts on an undefined-function call -- is hand-enforced twice (`edm-lint-artifacts:84`, `score-artifacts.sh:187`, both `[[ -f ... ]] || die`) and **absent from the file that defines the path**. The three consumers that reach `build_line_classes` only by sourcing `_edm-lint-lib.sh` (`edm-check-grants`, `edm-check-vocabulary`, `edm-state`'s pattern machinery) carry no guard at all.
- **Confidence: low-to-medium**, and the false-alarm filter is only partly cleared: `score-artifacts.sh`'s help header (`:90-92`, `:119-121`) declares an AC6 constraint of "nothing beyond bash 3.2 and jq, no `bin/` executable on the PATH" -- but it already *sources* `bin/_edm-cli-lib.sh` at `:133`, so sourcing a shared function is within its existing posture. The genuine friction is that a shared helper needs a per-caller cache key, and bash 3.2 has no associative arrays, so the helper would take the cache-variable name and use `printf -v` / indirect expansion.
- **Cost already paid**: CA-472's fix required three synchronised edits across three files in one round. They agree today because that sweep was done correctly -- the same "and that is the finding" posture as CA-244 and CA-343(2).
- **Fix**: add `_edm_awk_with_rules <cache-varname> <main-program> <file...>` to `_edm-lint-lib.sh`, route all three through it, and move the `[[ -f "$MERMAID_RULES_AWK" ]]` precondition into the library beside the assignment at `:90` so all five consumers inherit it instead of two hand-copying it and three going without.

## Noted / Not Actionable

**Confirmed still present, already filed -- not re-filed:**
- **CA-459** (open, tagged as documented debt D60): re-confirmed present; roughly seventeen hand-rolled positive-control blocks alongside `assert_tree_absent` (`bin/tests/_harness.sh:237-256`). Explicitly excluded from re-filing per instruction.
- **CA-471** (open, co-filed by L10 in round 8): the completeness gate itself has landed at `bin/edm-state:4438-4472` (`_cmd_audit_round_complete_body` now downgrades a code round to `partial` when a lens named in `lenses-run.txt` has no non-empty, parseable `lens-L{N}.jsonl`) and is documented in `plugins/edm/CLAUDE.md`'s `round_type` row. The `skills/code-audit/SKILL.md` half CA-471 also prescribed -- a check between fan-out and synthesis -- I did not find. Not a DRY finding; recorded here for the synthesizer.
- **CA-128** (lens JSONL skeleton, ~36 lines x 11): unchanged, zero drift, disposition holds.
- **CA-129** (`lint:pattern-library-contract` vs its in-suite twin): unchanged; CI copy at `.gitlab-ci.yml:384-430` remains the authoritative fast-fail.
- **CA-282** (`cmd_metrics_report`'s forked renderer arms): re-checked, no drift.
- **CA-283** (`evals/run-eval.sh`'s three phase blocks): re-checked at `:500-583`. The structural drift CA-283 named is unchanged -- Phase 1 assigns `PHASE1_OK` in both arms with no pre-initialisation, while Phases 2 and 3 pre-initialise to `false` at `:516`/`:548`. Still semantically equivalent; the three artifact-copy blocks are still copy-pasted, and CA-283's prescribed `run_phase` extraction is still the right fix. No escalation.
- **CA-376** bundle items re-verified with no change: the five byte-identical `UserPromptExpansion` command hooks (`hooks/hooks.json:19`, `:32`, `:45`, `:58`, `:71`) and the deliberate prompt/command two-layer duplication; `_unpack_token_fields`' marshalling residual; `_lock_retry_or_die`'s documented dynamic scoping; `edm-init`'s `.gitignore` heredoc mirrored in `README.md`; `score-artifacts.sh`'s `_scan_mermaid_blocks` vs `build_line_classes` (the *rule body* is shared via CA-019; my finding 5 is about the newer caching wrapper, which post-dates that disposition by two rounds).
- **CA-115**, **CA-116**, **CA-249**: re-checked, unchanged, dispositions hold.

**One stale disposition premise worth recording (not a re-file):** CA-376 dispositions the `SRD_ROOT` duplication as "the four independent SRD_ROOT derivations have no shared library they could source first". Both halves of that rationale are now false. There are **five** byte-identical copies of `SRD_ROOT="${EDM_SRD_ROOT:-${CLAUDE_PLUGIN_OPTION_SRD_ROOT:-./SRD}}"` -- `bin/edm-init:18`, `bin/edm-validate-prefix:17`, `bin/edm-lint-artifacts:64`, `bin/edm-state:65`, and `bin/edm-lint-staged-artifacts:43` (added by CA-436's extraction after the disposition was written) -- and **all five scripts source `bin/_edm-cli-lib.sh`**, so a shared library they can source demonstrably exists; the derivation simply sits above the `source` line. I am not re-filing the duplication (five copies of a one-line env-var default chain is low value, and the do-not-re-file instruction stands), but the disposition's stated reason should be corrected so a future round does not keep skipping it on a premise that no longer holds.

**Swept and cleared, no finding:**
- `bin/tests/run-all.sh` -- CA-096's `_standalone_check` (`:166-182`) is used at all three sites (`:192`, `:201`, `:207`); no residual hand-rolled block.
- `bin/tests/timing.sh` -- CA-280/CA-309 fully converted; all thirteen measurement sites route through `_measure_p95` and no hand-rolled `_now`/`_ms_between` timing loop survives.
- `skills/*/SKILL.md` Step 0 preflight -- correctly single-sourced: one canonical body in `skills/plan/SKILL.md:20`, seven by-name references, pinned at `wave7-smoke.sh:3063-3124`.
- `hooks/hooks.json` `Stop` (`:96`) and `PreCompact` (`:106`) are byte-identical two-copy commands. Same rationale as CA-376's five-command-hook item -- JSON has no include mechanism. Not actionable.
- `bin/tests/_harness.sh` -- `check_fails:168-175` and `check_refuses_and_leaves_state:330-336` carry the same lowercase-compare block, and `check_state_unchanged:284-293` and `check_refuses_and_leaves_state:312-323` the same hash-before/absent-guard/hash-after block. Both pairs are semantically identical with no drift, sit within one file, and differ in control flow (`return` vs fall-through) in a way that makes bash extraction awkward. Mechanical, not semantic. Not filed.
- The `INIT_DIR` resolution clause appears in eleven agent files in three prose shapes -- eight byte-identical (`agents/edm-test-unit.md:23-25` and siblings), two adding "(handles both flat and product-scoped layouts)" (`edm-test-planner.md:27-30`, `edm-test-coverage-auditor.md:32-35`), one with a different rationale (`edm-ticket-writer.md:18-21`, citing its own missing `Bash` grant). No behavioural divergence; the variation is additive detail. Same deliberate per-context duplication rationale as CA-128/CA-467. Not filed.
- `.gitlab-ci.yml:749` and `:759` each recompute `RUN_DIR="$(ls -td plugins/edm/evals/runs/*/ ...)"`. GitLab concatenates `script:` entries into one shell, so the second derivation is redundant rather than necessary -- but it is two lines, harmless, and arguably clearer per-block. Not filed.

**Tooling caveat for the synthesizer:** CA-130 reproduced for a **tenth** consecutive round on this lens -- Write, Edit and Bash all absent; delivered set was Read, Grep, Glob, WebFetch, WebSearch, TaskStop. My agent definition *did* include the `## JSONL Line Format` section this round. The full smoke suite result quoted in my brief (2182/0) is orchestrator-supplied and was **not** independently verified by this lens. Findings 4 and 5 are marked medium/low confidence specifically because they would have been settled by one command each.

---

