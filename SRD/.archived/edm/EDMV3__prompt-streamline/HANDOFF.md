# EDMV3 - Session Handoff

> **Last updated**: 2026-08-23T06:35:56Z by darryl.porter  
> **To resume**: `/edm:orchestrator EDMV3`

## Current Status

- **Phase**: Phase 6 - Implementation
- **Gates approved**: 3 of 3
- **Last gate**: Gate 3 - approved 2026-07-27T01:43:50Z by darryl.porter
- **Product**: edm
- **Description**: prompt-streamline
- **Next action**: Ready to archive - run `edm-state archive EDMV3`

## Resume Point

- **Phase**: Phase 6 - Implementation
- **Step**: 6
- **Last decision**: Stage C complete: 13 P2s fixed (CA-401 false-pass site, 408, 418, 419, 454, 458, 463, 464, 465/D59, 466, 468, 469, 470; CA-407 found already fixed). Nine test-infrastructure P2s tagged as candidate Stage E debt set (D60): CA-401 remainder, 402, 403, 404, 405, 453, 455, 456, 457, 459. Suite 2182/0. Next: Stage D round-9 full audit under remediation freeze.

**Pending artifacts for Phase 6 - Implementation**:

_(implementation in progress -- track individual ticket status)_

> Copy-paste to resume: `/edm:orchestrator EDMV3`

## Lifecycle & Mode

- **Mode**: standard
- **Lifecycle mode**: standard
- **Compliance**: false
- **Implementation mode**: standard

## Open Code-Audit Findings

not converged: 39 blocking finding(s) for EDMV3 (P0=0 P1=0 P2=39) of 562 considered, 70 NOTED excluded:
  CA-171 P2 ROUND 11: UPGRADED FROM NOTED TO P2 AFTER THREE ROUNDS HELD (L5-004). Original text: three mktemp files with inline rm -f and no trap anywhere in the file, all under ${TMPDIR:-/tmp} and tiny, on a path whose assertion helpers never exit early. WHAT CHANGED: the count is five, not three (:45, :63, :115, :139, :191, each with only a tail-position rm -f at :56, :94, :133, :180, :224), and the round-10 holding rationale ('a path whose assertion helpers never exit early') is a statement about the ERREXIT path only -- correct on that axis under :5's set -euo pipefail -- and says nothing about the SIGNAL path. The upgrade trigger is :64: _ac2_sleep_fn() { pwd > "$_ac2_pathfile"; sleep 5; }, polled by :73 for up to 100 iterations. That is a deliberate multi-second window -- the widest signal-deliverable window anywhere in the test tree -- with _ac2_pathfile unhandled across it, so a Ctrl-C there leaks with near-certainty rather than by unlucky timing. The file's FIRST trap is at :265 and its body removes only $CA146_SCRATCH; all five files predate it. CONSISTENCY ARGUMENT FOR FILING: CA-481(b) was filed and fixed over a ONE-STATEMENT window and CA-495(b) over subshells that DID carry tail removals, so holding a 5-second window with the same cleanup shape is not defensible against those two precedents. NOT the same site as the correctly-trapped CA-146 scratch tree (:263 + :265-268 four-arm, nested subdirs at :283-386, dispositions cleared at :397) -- L5 recorded that separately so the two are not conflated. FIX: install one four-arm trap immediately after :45 covering all five paths by variable name with ${var:-} guards, or (simpler, and matching the rest of the tree) mint a single suite-level $TMP scratch root at the top of the file with the canonical four-arm trap and root all five files inside it, keeping the tail rm -f's as a fast path.
  CA-401 P2 NEW in round 7 (L4-03 and L4-04 merged -- they overlap at :1186 and one edit fixes both): two defects in the same count-extraction block. (a) Unguarded TERMINAL grep in a command substitution under set -euo pipefail: in t66ac3_claude_count=$({ grep -oE '[0-9]+ subcommands' FILE || true; } | head -1 | grep -oE '^[0-9]+') and t40_heading_order at :1911 the FIRST grep is || true-guarded and the terminal one is not, so rewording the 'N subcommands' phrase or the 'Severity vocabulary' heading makes the assignment exit 1 and kills the suite at line 1186, discarding ~1,400 later assertions in the largest suite. Same class with no || true anywhere on the pipeline at :2018, :2019, :358, :2392, :2393 (grep piped to sort -u to wc -l, and ls piped to wc -l -- a zero-match grep poisons the pipeline under pipefail even though wc -l exits 0). The file documents the correct convention itself at :1236-1238 and violates it two lines of context away. (b) Guard asymmetry: [[ dispatch_count == claude_count ]] compares two independently-extracted values with neither validated numeric or non-empty, so the pass message renders as 'count () matches the dispatch table ()' in the both-empty case; the assertion has no floor. The dispatch regex also undercounts by design (an alternation arm counts as one subcommand while --help lists two; an arm whose cmd_ body wraps counts as zero), so merging two subcommands into one arm and decrementing CLAUDE.md keeps it green. Fix: || true on every terminal grep, ^[0-9]+$ floors on both sides before comparing, and count arms by parsing cmd_ function definitions rather than case arms. ROUND 8: STILL OPEN, and now CORROBORATED INDEPENDENTLY BY L1 (L1-004), which extends the class to a SECOND suite -- multi-lens, higher confidence. L4 re-verified 15 unguarded sites in wave7-smoke.sh (:1195, :1937, :358, :373, :1322, :1501, :1504, :2044, :2045, :3621, :3628, :3696, :4296, :4300, :4394) plus the unfloored comparison at :1196. L1 independently found the same class in a SECOND SHAPE -- a bare capture with no guard at all -- at wave6-smoke.sh:285, :291, :892, :3248, :3255, :3261, :3267 and wave7-smoke.sh:3621, :3628, :3696, :4296, :4300. grep -c always prints a count so the VALUE is fine; the exit status is 1 on zero matches, the assignment inherits it, and both suites run under set -euo pipefail, so deleting or renaming BLOCKING_FILTER or schema_at_least kills the suite at that line and discards every later assertion instead of producing the one-line fail count_matches exists to guarantee. Ten of L1's twelve can genuinely reach zero; wave6:291 and wave6:3261 are synthetic controls with guaranteed matches (same shape, benign). wave7-smoke.sh:6875 is NOT in this set -- its capture sits inside a double-bracket test where errexit does not apply. The file documents the correct convention itself at wave7:1253-1254 and violates it. Fix, now covering both suites: route every count capture through count_matches or count_matches_strict, using the strict form and asserting its exit status wherever a fixed path's disappearance must not read as a passing zero (the shape wave7:4319 already uses after the CA-392 fix), and add numeric floors on both sides of :1196. See CA-459 for the false-pass direction of the same bare-capture class. ROUND 11: STILL OPEN, PARTIALLY REMEDIATED, corroborated by two lenses (L1, L4). THE FIXED HALF, recorded so it is not re-derived: the terminal-grep-in-a-pipeline shape this finding named first ($({ grep -oE ... || true; } | head -1 | grep -oE '^[0-9]+')) is GONE -- a repo-wide search for 'head -1 | grep -oE' and 'head -1 | grep -o ' across plugins/edm/bin/tests/ returns zero matches, the two originally-cited sites carry a fix comment at wave7-smoke.sh:1432 ('CA-401(b): terminal grep guarded'), and count_matches is now used 68 times across four suite files. THE SURVIVING HALF is the bare command-substitution capture of grep -c with neither || true nor count_matches, under set -euo pipefail: eleven sites, wave6-smoke.sh:296, :1205, :1222, :3638, :3645, :3657 and wave7-smoke.sh:3946, :3953, :4021, :5149, :5153. grep -c always PRINTS a count so the value is fine; the exit status is 1 on zero matches and the assignment inherits it. Six of the wave6 sites grep the live bin/edm-state for a symbol, so a rename kills the suite at that line and discards every later assertion instead of producing the one-line fail the assertion exists to give. ESCALATION L4 NAMES: :5149/:5153 are the G25/CA-342 durability pins for the schema_at_least call-site count, so the exact regression they exist to catch -- removal of the last schema_at_least " call site -- makes grep -c exit 1 and ABORTS the suite rather than failing the pin, and a pin that dies instead of failing reports nothing to run-all.sh beyond a crash. FIX: route all eleven through count_matches / count_matches_strict (the convention the same file documents at :1253-1254), using count_matches_strict plus an explicit exit-status assertion at the six $EDM_STATE sites where a symbol's disappearance must not read as a passing zero.
  CA-402 P2 NEW in round 7 (L4-05): repo-wide grep -r needles that match the assertion's own source, plus three exclusion vocabularies for one check class. All three sites walk ${PLUGIN_DIR}/, which contains bin/tests/wave7-smoke.sh where the needle literal is defined. :2742 and :2798 are 'this string must be gone' assertions whose baseline is therefore permanently at least 1 -- either the threshold was tuned to absorb the self-hit (fragile: it silently allows one real violation) or it accounts for it some way not confirmed; :2018's distinct-form count is inflated by its own regex literal. The correct exclusions exist elsewhere in the same file and diverge from each other: :1444 uses grep -v tests/ (unanchored), :1817 uses grep -v ${PLUGIN_DIR}/bin/tests/ (absolute), and :2742, :2798 and :2018 use none. Fix: one shared harness_grep_plugin_excluding_tests() helper in _harness.sh, called by every repo-wide scan. Folded sub-item from L4's sweep: :2098 uses grep -qi for the Mermaid heading reference where the eight sibling assertions on the same reference (:1980, :1993, :2003, :2108) are case-sensitive, which would accept a lowercased heading; one character, same consistency class. ROUND 8 (L4): STILL OPEN, all three halves unchanged, and L4 supplied the CONCRETE ONE-LINE CORE of the self-match half (raised as L4-11 and folded in here rather than given its own ID): wave7-smoke.sh:2001 defines MERMAID_QUOTED, and grepping the whole tree finds exactly one occurrence of that identifier -- its own definition. It has NO readers. Its only observable effect is that its literal value is precisely what the repo-wide scan 43 lines below (:2044, :2045, scanning the plugin directory) searches for, making it a guaranteed self-hit inside both the raw floor of 11 and the distinct-form count of 1. Deleting :2001 is the fix for that half. The exclusion-vocabulary half is unchanged: five divergent live forms and still no shared harness helper, with none at all at :2044/:2045. The case-sensitivity outlier at :2124 is unchanged. L4 CLEARED :2768 and :2824 as already handled -- both build their needles from two runtime halves so the contiguous phrase never appears in this file's own source and cannot self-match; the self-match half is live only at :2044/:2045.
  CA-403 P2 NEW in round 7 (L4-06): six positive controls were derived from the pattern rather than from the real regression. The CA-037 control convention itself is good and merits credit, but in these six the control string was written to satisfy the regex (for example 'synthetic control: code_audit_converged true' against the flag-leak pattern, and 'a row mentioning missing version header' against the wrong-classes pattern), so each proves only 'the regex compiles and matches a string invented for it' -- not 'the regex matches the shape the real violation had'. A regex loosened during a later edit keeps both the control and the live scan green while no longer detecting anything specific. :2826's control (reproducing real prose, 'synthetic control: Ask: Do you approve of this plan?') is the counter-example done right. Fix: copy control strings verbatim from the pre-fix text each finding was raised against (git history has it) and say so in a comment beside each control. ROUND 8 (L4): STILL OPEN. All six cited controls are unchanged and none carries a provenance comment: wave7-smoke.sh:963, :1137, :1284, :1294, :1304, :1442. Each proves only that the regex matches a string invented to match it, not that it matches the shape the real violation had, so a regex loosened in a later edit keeps both the control and the live scan green. :2852's control remains the in-file counter-example done right, reproducing real retired prose. Fix unchanged. Distinguish from CA-457, which is the ARM-COVERAGE axis of the same control-quality class rather than the PROVENANCE axis.
  CA-404 P2 NEW in round 7 (L4-07): the eval scorer's exit status is discarded and two failed runs prove determinism. The four dimension extractions run bash "$SCORE_ARTIFACTS" run-dir > out-dimN.json 2>/dev/null and then jq the file with 2>/dev/null, so a crash yields an empty JSON file and a jq failure swallowed into an empty dN -- the scorer's exit code is never checked and its stderr is thrown away. The same file demonstrates the right pattern at :563 (jq -e with an explicit ERR sentinel) and does not apply it to the four extractions. Related, :556: 'if diff -q out-a.json out-b.json' establishes run-to-run determinism, but two identically EMPTY outputs from two crashed runs also satisfy diff -q. Partially mitigated by the separate dim_count assertion at :563; a non-empty and valid-JSON precondition on both files before the diff would close it outright. Fix: check the scorer's exit status at all five sites, capture stderr into the failure message, and add the validity precondition before the determinism diff. ROUND 8 (L4): PARTIALLY FIXED, NARROWED. The determinism half is CLOSED -- wave7-smoke.sh:548-554 captures rc_a and rc_b on their own statements and asserts both are 0 before the diff at :556, and :563-580 additionally require 5 dimensions and non-null scores for dimensions 1-4, so two identically-empty crashed outputs now fail three assertions before reaching the diff. WHAT REMAINS OPEN is diagnosis at the four scorer-dimension extraction sites (:607, :635, :648, :678), which all run the scorer with stderr discarded and no rc capture, then read the result with jq, also stderr-discarded. Because each site asserts a literal expected value (50/100/0/80) a crash does FAIL, so the residual is diagnosis rather than a false pass: the failure message reads 'dimension 2 scored , expected the hand-computed value 50' with the scorer's own stderr thrown away. Fix: capture rc on the same statement at all four sites and interpolate the captured stderr into the failure message, using the shape :548-554 and :563 already demonstrate in this same file.
  CA-405 P2 NEW in round 7 (L4-08): three meta-assertions count test scaffolding or markdown shape rather than behavior, using grep -c, which counts LINES and not occurrences -- so two hits on one line count once and any 'expect N' threshold is off-by-one-per-shared-line the moment formatting changes. :2521 counts check_fails or check-quote occurrences inside an extracted block; :1693 counts markdown bullets of the form dash-backtick in an Output section. :1701's grep -ci 'defer' against the synthesizer agent is additionally unanchored and case-insensitive: it fires on 'deferred', 'deferral' and any future word containing the trigraph, which is conservative for an absence check but wrong for any threshold other than zero. Fix: assert on behavior or on named anchors instead of counts where possible; where a count is genuinely wanted, use grep -o piped to wc -l for occurrences and anchor the needle. ROUND 8 (L4): NARROWED to ONE surviving sub-item; the other two are FALSE ALARMS and are recorded here so they are never re-investigated. (a) SURVIVING: the T44 AC8 meta-assertion, now at wave7-smoke.sh:2547, counts harness call sites inside an extracted block against a greater-than-zero threshold, so it passes with 39 of 40 assertions deleted -- a presence-only assertion over test scaffolding. It also counts lines rather than occurrences, though at a zero-adjacent threshold that is immaterial. Fix: drop it (T44's real cases already prove harness use by running) or pin an exact expected count derived from the block. (b) FALSE ALARM, :1710: the pattern is anchored at line start so at most one match per line -- a line count and an occurrence count are identical here and no off-by-one is possible. (c) FALSE ALARM, :1718: the grep is unanchored and case-insensitive but the threshold is exactly zero, so over-matching can only produce a false FAILURE, never a false pass, and the target file's existence is proven by the positive assertion at :1721.
  CA-453 P2 NEW in round 8 (L1-001): CA-389 and CA-390 shipped their CODE HALVES and NONE of their prescribed tests. CA-389 prescribed three wave6-smoke.sh assertions and CA-390 prescribed one; NONE OF THE FOUR EXIST. Verified three ways: a repo-wide grep for CA-389 and CA-390 outside SRD/ returns exactly two hits, both source comments in bin/edm-state; a grep for the rendered string 'Gates approved' across bin/tests/ returns ONE hit and it is a COMMENT (wave6-smoke.sh:644), not an assertion; and required_gate_count appears NOWHERE in any test file. So the rendered gates-approved line emitted at bin/edm-state:5531 -- the line BOTH findings are about -- has NO ASSERTION ANYWHERE IN THE SUITE, exactly as CA-389's own closing sentence warned ('No test anywhere asserts this line today, so the whole CA-335 fix is uncovered'). Both fixes are one careless edit away from silently regressing, and THE CLASS HAS NOW REGRESSED TWICE (CA-335 to CA-389/CA-390). This is the COMPLETENESS HALF of two findings whose code halves are correct and verified, not a re-flag of the code halves; contrast CA-391, which landed code AND check_refuses_and_leaves_state cases in the SAME COMMIT and is the model. Fix: add the four prescribed cases beside the existing G13/CA-391 block -- (i) a prototype initiative renders a denominator of 1; (ii) a fast-track initiative with phases 1, 2, 3 and 5 recorded skipped renders a denominator of 0, NOT 3; (iii) a gate re-approved after approve-gate 1, 2, 1 renders a numerator of 2 of 3, counting DISTINCT gates rather than approval events; (iv) the reconciling note text '3 approval event(s) recorded above for 2 distinct gate(s)' appears in the same HANDOFF. ROUND 11: STILL OPEN, UNCHANGED SINCE ROUND 8, re-verified three ways at HEAD (L1). (a) required_gate_count -- the denominator variable, at bin/edm-state:5754/:5772 and rendered at :6110 as '- **Gates approved**: ${gates_count} of ${required_gate_count}' -- appears in NO file under bin/tests/. (b) The string 'Gates approved' appears in bin/tests/ exactly once, at wave6-smoke.sh:655, and it is a COMMENT, not an assertion. (c) The reconciling note text built at bin/edm-state:5938 has no assertion anywhere. So both the CA-389 and CA-390 code halves remain uncovered and the class has regressed twice (CA-335 -> CA-389/CA-390). LEDGER COMPONENT WAS STALE: the rendered line moved from :5531 to :6110. Fix unchanged: add the four cases beside the existing G13/CA-391 block at wave6-smoke.sh:660-666.
  CA-455 P2 NEW in round 8 (L4-09): NINE conditional SKIP paths print a SKIP line and increment NEITHER PASS NOR FAIL. The suite summary at :7954 carries no skip term, and run-all.sh:108 parses exactly a two-number Results line, so a SKIPPED CASE IS INDISTINGUISHABLE FROM A CASE THAT NEVER EXISTED. This is not hypothetical: three of the nine (:6589, :7649, :7737) are gated on the presence of flock(1), and macOS -- THE DOCUMENTED PRIMARY DEVELOPMENT PLATFORM per CLAUDE.md Sec.'Testing changes' -- ships none. So on a developer's own machine the G12/CA-345 lock-contention hook contract, the G49 live-timeout case and the G13/CA-347 unremovable-marker case NEVER EXECUTE, and run-all.sh still prints ALL SUITES PASSED. Two more (:7055, :7085) vanish under root, the default user in most container images. Fix: add a SKIP counter and a skip() function to _harness.sh, call it at all nine sites, emit a three-term Results line, and extend run-all.sh's summary regex and its table with a SKIP column, so a silently degraded environment is VISIBLE IN THE AGGREGATE rather than invisible. Note this compounds CA-471: a round can currently be reported green while both a lens's authoritative artifact and three lock-contract test cases are missing.
  CA-456 P2 NEW in round 8 (L4-10): four assertions run against a fixture THE TEST ITSELF WROTE THREE LINES EARLIER. t25_ledger_case writes three literal JSONL lines at :1663-1667, then asserts against them: every line is valid JSON (:1670-1675); every id matches the CA-NNN shape and carries confidence and lenses (:1677); ids are unique (:1681); every status is in the open/fixed/noted enum (:1685). NO PLUGIN FILE IS READ; nothing under test produces or consumes this fixture. These four assertions CANNOT FAIL for any change to any EDM source file -- only if jq itself broke -- and they inflate the pass count without covering anything. The block header at :1626-1632 explains why the synthesizer has no directly-invokable entry point but does not acknowledge that the resulting assertions are vacuous. REAL COVERAGE IS AVAILABLE AND ALREADY PRECEDENTED: edm-state render-ledger consumes exactly this file shape, and wave6-smoke.sh:2875-2906 already drives it. Fix: pipe this fixture through edm-state render-ledger and assert the RENDERED MARKDOWN -- ID ordering, severity grouping, status rendering -- converting four tautologies into real assertions about shipped code. While there, add the CA-378 durability case the round-6 fix never got: a title containing a pipe character must still render a row with exactly eight cells.
  CA-457 P2 NEW in round 8 (L4-12): a SIX-ARM detection regex is proven by a control exercising ONE ARM. T61_BASH4_RE (:951) has six alternation arms guarding the bash-3.2 compatibility constraint -- the associative-array declaration, two array-read builtins, the uppercase and lowercase parameter-expansion forms, and the named-file-descriptor form. Its positive control at :963 feeds a single synthetic line exercising ONLY the first arm. The parameter-expansion and named-fd arms carry NON-TRIVIAL ESCAPING that no control validates; if any of them were mis-escaped, :967's 'zero bash-4-only constructs found' assertion would stay PERMANENTLY GREEN over a live violation while the control still passed on the first arm. This is ADJACENT TO CA-403 BUT A DISTINCT AXIS: CA-403 is about control PROVENANCE (controls authored to satisfy the regex rather than copied from real pre-fix text), this is about control ARM COVERAGE. Fix: loop the control over one synthetic line PER ARM and require EVERY arm to match its own line, failing BY ARM NAME so a mis-escaped arm is named rather than silently tolerated.
  CA-459 P2 NEW in round 8 (L10-206), CA-037's L10 half VERIFIED FOR THE FIRST TIME IN THREE ROUNDS: the positive-control idiom has a canonical harness helper and roughly 17 hand-rolled copies, and they HAVE DIVERGED on the exact defect the helper exists to close. CANONICAL: assert_tree_absent (_harness.sh:235-254) routes BOTH the real and the control haystack through count_matches_strict (:198-207), asserts count_matches_strict's own EXIT STATUS alongside its printed value, and additionally guards EVERY SCAN PATH with an existence test before the needle check (:239-241); 41 call sites (wave7 33, wave6 3, harness-smoke 5). COPIES: wave6-smoke.sh:291, :995, :2805, :3261 and wave7-smoke.sh:409, :940, :963, :1137, :1284, :1294, :1304, :1442, :2852, :3007, :4149, :4206, :7924; eleven of these are the SAME FIVE-LINE SHAPE to within the needle. EXACTLY ONE (wave7-smoke.sh:4204-4208, T47 AC5) uses count_matches_strict and captures its exit code, matching the canonical contract. THE OTHER SIXTEEN use a bare grep count with a true fallback (or grep -l / grep -n variants) and no path-existence guard, violating the harness's own docstring rule at :179-185 verbatim -- 'Any caller whose expected count is 0 MUST pair this with a positive control ... OR use count_matches_strict' -- and, critically, THE POSITIVE CONTROL DOES NOT SUBSTITUTE FOR THE SECOND PROTECTION: it proves the pattern CAN match, not that the path was READ. CONCRETE LIVE FALSE-PASS INSTANCE at wave7-smoke.sh:1438: a wrong plugin directory, a renamed skills layout, or a glob expanding to nothing yields grep exit 2, the stderr redirect discards the diagnostic, the true fallback swallows the status, awk prints 0, and the assertion passes as 'the leak is absent' -- while the control at :1442 still passes, because it is built from a synthetic string rather than from the tree. False-alarm filter: the sixteen differ from the one only by not having been converted; the genuine structural gap is that assert_tree_absent expresses only 'expect zero' while several sites assert 'expect exactly 1', which argues for a SECOND HELPER rather than sixteen hand-rolls; both sides are test code, but prior rounds have consistently treated _harness.sh duplication as in scope (CA-042, CA-145, CA-037 are all test-side); every one of these files already sources _harness.sh. Fix: (1) add assert_count_with_control beside assert_tree_absent sharing its count_matches_strict plus path-existence contract, so the expect-exactly-N family has a canonical form at all; (2) convert the eleven identical-shape sites, and make the remaining non-count variants (:4149, :7924, :7863, :6621) at minimum stop discarding grep's exit 2; (3) add a durability pin of the shape wave6-smoke.sh:781-784 already uses, asserting the bare-grep-with-true-fallback shape adjacent to a control assignment occurs ZERO times in bin/tests/, using count_matches_strict rather than count_matches. RELATED TO CA-401, which is the SUITE-ABORT direction of the same bare-capture class; this is the FALSE-PASS direction. Fix both in one test-hygiene pass. ROUND 11: STILL OPEN, UNCHANGED, corroborated by two lenses (L4, L10) -- and CARRIED AS DOCUMENTED DEBT D60, excluded from re-filing per the standing instruction. assert_count_with_control (the second helper this finding prescribed for the expect-exactly-N family) still does not exist anywhere in plugins/edm (zero grep hits); the canonical assert_tree_absent / count_matches_strict family remains intact in bin/tests/_harness.sh (11 occurrences) and expresses only 'expect zero'; the hand-rolled 'grep -c ... || true' shape survives at 66 sites (wave7-smoke.sh 56, wave6-smoke.sh 9, timing.sh 1). The false-pass direction is unchanged: a wrong plugin directory or renamed layout yields grep exit 2, the stderr redirect eats the diagnostic, || true eats the status, the count reads 0, and the assertion passes as 'the leak is absent' while its control still passes off a synthetic string. Recorded as confirmation only; D60 is the disposition of record.
  CA-491 P2 NEW in round 9 (L4 P2 with two L1 noted items folded in), covering bin/edm-check-grants:506-517: the CA-441 missing-task-grant rule shipped with NO must-fail case, and its only coverage is an assertion the rule cannot influence. The established must-fail shape sits 130 lines above at t03_ac6_case (:364-396) -- copy the tree to scratch, strip the grant from one skill's allowed-tools, then assert exit 1 plus the file name and the class name at :388-391 -- and it exercises the AskUserQuestion rule ONLY; nothing was added for Task. Because CA-441's own fix granted Task to all twelve skills that need it, the rule's trigger condition at :515 is false everywhere and mark_and_maybe_report is NEVER REACHED on the live tree, so the family of 'edm-check-grants exits 0 against the live tree' assertions (wave7:339, :2860, :3034, :3171) passes identically whether the rule works or does not exist. A mis-escaped detection regex at :509 (two alternatives, a case-insensitive extended grep, non-trivial escaping) or a has_tool prefix bug would leave the rule silently protecting nothing, permanently, and the next skill added would reproduce exactly the CA-441 defect it was built to prevent. This is CA-441's own prescription landing its code half without its test half -- the CA-453 shape at a new site. TWO KNOWN LIMITS OF THE RULE, folded in from L1's noted set rather than filed separately so the test written for this finding covers them: (a) :511 derives the reported line from a whole-file grep while the trigger is derived from the body, so a reported line can fall in frontmatter -- byte-identical to the pre-existing AskUserQuestion rule at :492-494, hence a shared quirk rather than a new defect; (b) :509's spawn alternative requires the spawn verb and the agent-name prefix on ONE line, so a multi-line spawn instruction escapes the rule -- no live instance today, since metrics and verify-runtime, the only two skills without Task, contain no spawn instruction in any form; and the line-anchored agent-name alternative is currently carried entirely by the first alternative, with no control proving it can match at all. Fix: clone t03_ac6_case for the Task rule (strip the Task grant from a scratch copy of one testing skill, assert exit 1 and the reported class name), plus one synthetic control line per regex alternative, each failing by alternative name. ROUND 11: STILL OPEN, UNCHANGED SINCE ROUND 9, corroborated by two lenses (L1, L4). The rule is intact and has MOVED to bin/edm-check-grants:512-523 (this entry said :506-517): trigger at :515 is grep -qiE '(^|[^a-zA-Z])spawn[^a-zA-Z].*edm-|^Agent: edm-' and the report call at :522 is mark_and_maybe_report skill "$skillname" Task "$f" "$task_ln". Coverage at HEAD: t03_ac6_case (wave7-smoke.sh:370-401) asserts only missing-askuserquestion-grant at :397, and a repo-wide search of bin/tests/ for CA-441, 'Task grant', task_grant, missing-task and MISSING_TASK returns nothing. Because CA-441's own fix granted Task to every skill that needs it, :521's condition is false everywhere on the live tree, so mark_and_maybe_report is never reached and the whole 'edm-check-grants exits 0 against the live tree' family (:345, :3028, :3202, :3339, :3447, :3488) passes identically whether the rule works or does not exist. Both folded-in limits also still hold verbatim: :517 derives the reported line from a whole-file grep -niE while the trigger is derived from $body, so a reported line can land in frontmatter; and :515's spawn alternative requires the verb and the edm- prefix on ONE line, so a multi-line spawn instruction escapes it. The ^Agent: edm- alternative still has no control proving it can match at all.
  CA-492 P2 NEW in round 9 (L4 P2): the CA-426 stream-separation regression fixture POSITIVELY PINS that an initiative with zero recorded audit rounds converges via --accept-p2-debt, and no case asserts any boundary on it. PDEBT6 (:800-815) is init-ed and given a ledger, then approve-gate with --accept-p2-debt is run with NO audit-round-start and NO audit-round-complete at all -- which is the point, it reproduces the zero-recorded-rounds stderr warn CA-426 was about. But cmd_audit_converged's zero-rounds arm warns on stderr and PROCEEDS, so stdout carries the not-converged prefix, the override engages, :812 asserts exit 0 and :814-815 asserts the success message. The suite now positively certifies that an initiative which has never run a code-audit round can be converged. The test-quality defect is the ASYMMETRY: CA-425's negative set added in the same batch covers a partial round (:774), an invalid ledger (:786), the wrong gate (:792) and an unrecognized argument (:795) but has NO zero-round member, even though zero-round is the one class the CA-426 fix deliberately RE-ENABLED; the narrowing comment at edm-state:2236 enumerates a partial round, invalid JSONL and an invalid status line and silently omits the arm that proceeds; and CA-471 landed in the same batch specifically to make round completeness verifiable. Whether zero-round convergence is INTENDED is an L9 or spec question this finding does not decide -- what L4 owns is that the behaviour is now locked in by a passing assertion with no companion case stating the boundary either way. Fix: rebuild the CA-426 regression on a fixture with a real completed round plus an independently-induced stderr warn, so the regression no longer depends on the zero-round shape; then add an explicit zero-round case asserting whichever behaviour is decided, with a comment naming the decision and its authorizing artifact.
  CA-496 P2 NEW in round 9 (L7 P2-1 + L11 N2): the CA-005/CA-154/CA-365 CLI-family durability loop hardcodes NINE bin/ helpers and omits edm-lint-staged-artifacts, the tenth -- created by CA-436's own remediation and now the delegate for the plugin's most privileged hook. The comment at :5924-5926 still claims all 12 print_help call sites in the plugin are covered; there are 13. The new file IS compliant with both contracts today (:29 sources the shared library, :32 uses the caller-name convention), so there is no live defect -- the finding is that the one property this loop exists to provide, DURABILITY, is exactly what the newest and most privileged member lacks. Third instance of a single shape: CA-231 (library header undercounted its consumers by one), CA-270 (boundary loop undercounted by one), now the CLI-family loop. Note the asymmetry that makes it invisible: lint:bash-syntax's CA-005 ban at .gitlab-ci.yml:126 DERIVES its file set from the tree, so the negative rule covers the new file while the positive assertion does not. L11 additionally records that the G21 die-map at :7978 was not extended either. Fix: add edm-lint-staged-artifacts at :5900 and correct :5925's count to 13; better, derive the list from the bin/edm-* glob minus the underscore-prefixed libraries, the way .gitlab-ci.yml:106 derives its sweep, so the eleventh helper cannot repeat this. ROUND 11: CONFIRMED STILL OPEN, UNCHANGED, FOR A FOURTH CONSECUTIVE ROUND -- corroborated independently by L7 (Part A) and L11 (Finding 3), both with file:line evidence, and both explicitly instructing the synthesizer to reconcile against this ID rather than mint a new one. The loop MOVED but the content did not: it is now at wave7-smoke.sh:6733 (was :5900 when this entry was written, :6328 in round 10), still enumerating the same nine helpers -- edm-state edm-lint-artifacts edm-validate-prefix edm-init edm-check-vocabulary edm-check-grants edm-compare-eval edm-check-skill-sync edm-sync-canonical-sections -- and still omitting edm-lint-staged-artifacts. The stale prose count also moved and survived: the comment is now at :6757-6759 and still claims 'all 12 print_help call sites ... not just the 9 bin/ helpers'; the true counts are 13 and 10. No live defect: the omitted file conforms today (bin/edm-lint-staged-artifacts:47 sources the shared library with the standard literal, :50 uses print_help "${BASH_SOURCE[0]:-$0}"), so the exposure is durability-only. AGGRAVATING CONTEXT: this is now the ONLY member of the round-10 L7 assignment still open, in a round that closed BOTH other undercount-by-one loop findings of the same class (CA-502 and CA-531). FIX (unchanged, and now with a better in-tree precedent): add edm-lint-staged-artifacts to :6733 and correct :6758's counts to 13/10; better, derive the list the way wave7-smoke.sh:5472-5479 already derives the CA-501 sweep -- glob ${PLUGIN_DIR}/bin/*, skip _*, *.awk, *.txt -- which makes the loop self-extending and retires the class.
  CA-518 P2 NEW in round 10, RESIDUAL OF THE CA-474 FIX (L1-004): lint:hooks-shell writes the four-byte string null to its temp file for a command-type hook whose .command key is absent, and both checks pass it -- so the blocking gate over the plugin's most privileged shell prints OK for a hook it never checked. :328 is jq -r --argjson n "$JQ_IDX" '[.hooks | to_entries[] | .value[] | .hooks[] | select(.type=="command")][$n].command' plugins/edm/hooks/hooks.json > "$cmdfile". select(.type=="command") filters on the TYPE tag; nothing requires the object to carry a .command. For an entry with {"type":"command"} and no command key (or an explicit null), jq -r emits the literal text null. bash -n accepts null as a well-formed single-word command and shellcheck --shell=sh accepts it too, so :330 prints OK: bash -n hook #N, :337 prints OK: shellcheck hook #N, COUNT reaches EXPECTED_COUNT by construction, and :351 prints the job's clean summary. This is the exact failure direction the job exists to prevent, one level up: CA-437 was a command string being SPLIT so malformedness escaped detection; this is a command string being ABSENT so malformedness escapes detection. The job is needs: [] with no allow_failure, and per its own header at :265-268 it covers 'the most privileged shell in the plugin (it runs automatically with no permission prompt on every commit)'. No sibling backstops it -- edm-check-grants parses the same file but reads "prompt": entries at :466, not .command. NOT CLEARED BY THE FILTERS: the header's stated contract at :270 is 'Extracts every command-type hook's .command string via jq and runs both checks over each', and an entry with no .command string is a case that contract does not cover and the code does not detect. Confidence medium: the gap is certain from reading, and reachability requires a malformed hooks.json, which is precisely the input class a lint job exists for. RELATED, DELIBERATELY NOT FILED: the COUNT vs EXPECTED_COUNT cross-check at :343 is now a tautology because COUNT is incremented in lockstep with IDX at :325-326 before any work -- Filter 2, the comment at :310-311 states this deliberately as a structural rather than post-hoc invariant. FIX: validate the extraction instead of trusting it. Derive EXPECTED_COUNT as today, then add WELLFORMED="$(jq '[.hooks | to_entries[] | .value[] | .hooks[] | select(.type=="command") | select(.command | type=="string" and length > 0)] | length' plugins/edm/hooks/hooks.json)" and fail the job when WELLFORMED != EXPECTED_COUNT, naming both numbers. Pin it with a wave7 case that copies hooks.json to scratch, deletes one .command key, and asserts the job body fails -- there is no assertion today that would turn red. ROUND 11: HALF-LANDED, STAYS OPEN AT P2. THE CODE HALF IS FIXED and matches the prescription exactly -- .gitlab-ci.yml:329 now computes WELLFORMED with the prescribed select(.command | type=="string" and length > 0) narrowing and :330-333 fails the job naming both numbers, so a hook declaring {"type":"command"} with no .command key can no longer reach bash -n / shellcheck as the four-byte string null (L1 and L8 both verified this half). THE TEST HALF DID NOT LAND: no file under plugins/edm/bin/tests/ references WELLFORMED or EXPECTED_COUNT, so deleting :329-333 tomorrow turns nothing red -- exactly the condition this finding's own fix text called out. This is the CA-453 shape at a new site, on a needs: [] job with no allow_failure guarding the plugin's most privileged shell. NOTE THE SPLIT VERDICT: L8 recorded CA-518 'VERIFIED FIXED' on the code half alone; L1 recorded it open on the missing pin. Adjudicated OPEN because the prescribed fix was code AND pin, and L1's evidence (a repo-wide grep of bin/tests/ returning zero references) is not contradicted by L8, which did not examine the pin. FIX (unchanged): add the prescribed wave7 case -- copy hooks/hooks.json to scratch, delete one .command key, run the same two jq expressions the job body uses, and assert WELLFORMED != EXPECTED_COUNT. The suite already has the pattern for asserting against $GITLAB_CI_YML (wave7-smoke.sh:1378) and for the indexed jq hook-command walk (:5520, :7109).
  CA-538 P2 NEW in round 11 (L2-002): the if's condition decides nothing for the statement it guards -- :146 and :148 are BYTE-IDENTICAL, printf '%-28s %8s %8s %8s\n' "$_suite" "$_s_pass" "$_s_fail" "$_suite_status" on both arms, same format string and same four arguments. Only the EXTRA output at :149-152 differs. This is the CA-054 class, which this project filed under L2 and fixed ('the prototype) case arm emits output byte-identical to the *) fallback immediately below it -- pure duplication with no behavioural difference'). Nothing is unreachable in the strict sense -- both arms execute -- which is why it is P2 and not higher: it is the 'condition that cannot change the outcome' half of the class. Adjacent and deliberately not merged: bin/edm-init:146-159's standard|iac|data-ml) and mini-srd) arms also have byte-identical bodies, but both arms execute and each comment carries distinct mode documentation, so L2 recorded that as L4/L10's call rather than a finding (see L2-N21). FIX (2 lines): hoist the printf above the if, then reduce the block to a one-armed if [[ "$_suite_status" != "PASS" ]]; then _failed_suites+=(...); echo ...; fi. Output is unchanged byte-for-byte, so no test or fixture changes.
  CA-539 P2 NEW in round 11 (L2-003): if (trimmed == "classDef" || trimmed == "style" || trimmed == "linkStyle") return 0 is provably inert -- it can never change the function's result. Any line whose trimmed form is exactly one of those three bare keywords contains no ';', no '['/'('/'{'/'|'/'"' span character and no '->', so it falls through the sub(/;[[:space:]]*$/...) at :102, all five span regexes at :105-109 and the sequence-message check at :112-116, and returns 0 at :117 regardless. DISTINGUISH FROM :95 (^(classDef|style|linkStyle)[[:space:]]), which IS load-bearing and has a regression fixture behind it (bin/tests/fixtures/mermaid/valid/v04-classdef-style-linkstyle.md). FALSE ALARM FILTER: (1) reachable only if the span-check set were widened to flag something a bare keyword contains -- implausible; (2) :96 carries no comment of its own, and the :93-94 rationale it inherits ('their trailing ; is a statement terminator') is INAPPLICABLE to a bare keyword that has no ';', so it is not documented as a deliberate net; (3) no test environment differs. Confidence medium because the harm is nil -- this is an inert line, not a wrong one -- but the project has filed and resolved this exact preempted-guard class five times (CA-140, CA-202, CA-260, CA-309, CA-362) and once more in round 10 (CA-519), which establishes the project's position rather than an accepted trade-off. FIX BY MERGING, not by deleting outright: relax :95 to ^(classDef|style|linkStyle)([[:space:]]|$) and delete :96. That expresses the carve-out once, keeps the (contrived but harmless) bare-keyword case covered by the same rule, and removes a line that reads as a guard while guarding nothing. Plain deletion is the acceptable alternative and matches the six precedents.
  CA-540 P2 NEW in round 11, RESIDUAL OF THE CA-511 FIX (L3 Finding 1): the timeout-nesting guard hardcodes the phase count, so adding a fourth eval phase silently re-inverts the nesting WITH A GREEN SUITE. inner_worst_case_secs=$((PHASE_TIMEOUT_SECONDS * 3 + 60)) -- the 3 is a literal. The driver's actual phase count is the number of invoke_claude calls (three today: :542 plan, :575 srd, :613 audit-srd) and nothing couples the two. THE DURABILITY PIN MAKES THIS WORSE RATHER THAN BETTER: wave7-smoke.sh:8975-8976 asserts the LITERAL SOURCE STRING inner_worst_case_secs=$((PHASE_TIMEOUT_SECONDS * 3 + 60)) is present, and :8982-8986 re-implements the same formula with its own hardcoded * 3, so the fix's own test certifies the stale constant. TRIGGERING SCENARIO: a contributor adds a fourth phase (invoke_claude tickets ...) -- exactly the change .gitlab-ci.yml:778-780 warns about in prose ('Adding a fourth phase ... inverts the nesting -- raise this timeout in the same change'). The guard's worst case still reads 8160s against CI_JOB_TIMEOUT=9000 so it proceeds; the real worst case is 10860s. GitLab kills the job at 150m (.gitlab-ci.yml:781) and the trailing script: steps never run -- scoring, edm-compare-eval, and CA-452's partial-run-always-fails-the-job handshake are ALL bypassed precisely when the run was partial, which is the one case that handshake exists for. Both wave7 assertions stay green because neither reads the phase count. This is the identical shape CA-511 was filed for, reachable through one plausible edit. FIX: derive the multiplier instead of writing it -- add a single enumerable source (e.g. EVAL_PHASES="plan srd audit-srd") that both the invocation sites and the arithmetic read, and change the wave7 case from a literal-string grep to a computed assertion: the invoke_claude call-site count must EQUAL the multiplier the guard uses. Positive control: add a stub fourth invoke_claude line in a scratch copy and confirm the assertion turns red.
  CA-541 P2 NEW in round 11, RESIDUAL OF THE CA-481(c) FIX -- TWO LENSES INDEPENDENTLY, MERGED (L3 Finding 2 + L5-002; same root cause, same file, one fix). The cross-file trap sweep that finally landed after failing to land across three rounds is STRUCTURALLY BLIND to shapes it exists to catch. THE PARSER BUG (both lenses): :9643-9648 derives the signal list as the text after the LAST QUOTE CHARACTER on the line -- nq = split(line, sq, "'"); sig = (nq >= 3) ? sq[nq] : "", then the double-quote fallback. For a trap whose handler is a BARE FUNCTION NAME (trap cleanup EXIT) there is no quote of either kind, so nq == 1 and ndq == 1, sig stays empty, and has_exit/has_int/has_term/has_hup are all false: the line matches the regex, is processed, and contributes NOTHING -- not flagged by combined mode, and never registered in file_has_exit so no-hup mode cannot report the file either. TWO LIVE SITES ARE IN THAT FORM TODAY: evals/run-eval.sh:276 (trap cleanup EXIT) and bin/tests/wave6-smoke.sh:37 (trap cleanup_wave6 EXIT); both pass only because their OTHER three arms happen to be quoted. DECISIVE EVIDENCE THIS MATTERS: the historical CA-482 regression at wave6-smoke.sh WAS the unquoted form -- :34's own comment describes the pre-fix line as trap cleanup_wave6 EXIT INT TERM HUP -- so re-introducing it today passes silently, while the SAME BUG written with quotes is caught. A one-character difference decides whether the tripwire fires. SECOND BLIND SPOT, SAME ROOT CAUSE: a RETURN-only trap yields sig = ' RETURN', has_exit false, invisible -- which is precisely why CA-544 (bin/edm-state:5341) survived a green sweep that names bin/* in scope. THIRD CLAUSE NEVER PINNED (L3): the convention has a third clause, stated file-wide at bin/edm-state:687-691 and plugin-wide in bin/edm-check-grants' sibling comment block -- INT/TERM/HUP must actually TERMINATE the process after cleanup -- and the awk program never inspects a trap BODY for exit. So the cleanup-then-resume bug itself still passes when split across two statements (trap 'rm -rf "$scratch"' EXIT then trap 'rm -rf "$scratch"' INT TERM HUP with no exit): clause 1 does not fire (no single statement combines EXIT with a real signal) and clause 2 does not fire (the file traps HUP). That is exactly the CA-446 shape. FOURTH: clause 2 is PER-FILE, not per-resource -- a file with two cleanup resources where only one is HUP-covered passes, because file_has_hup[FILENAME] is set by any HUP trap anywhere in the file. POSITIVE-CONTROL GAP: both controls (:9687, :9694) use the single-quoted handler form, so neither exercises any of the above; they prove the checks are not vacuous, not that they are complete. SEVERITY: P2, not P1 -- no live violation exists at HEAD, all thirteen sites CA-481/CA-482 named are canonical. This is a durability gap, and CA-481(c) is on record as the reason the class survived four rounds. FIX: (1) derive the signal list POSITIONALLY rather than by quote position -- strip the trap keyword, drop a quoted handler if the next token opens a quote, otherwise drop exactly one whitespace-delimited token, then parse the remainder; (2) add a third mode asserting that every trap statement naming INT, TERM or HUP has a body containing exit (allowing an indirected handler only when the named function itself contains exit); (3) treat a RETURN disposition as EXIT-class for no-hup registration; (4) add one positive control per shape -- unquoted-combined (trap cleanup EXIT INT TERM HUP), RETURN-only, and a two-statement EXIT + INT/TERM/HUP-without-exit file.
  CA-542 P2 NEW in round 11 (L3 Finding 3): edm-init has no unwind for a partial scaffold, and its own existence guard then blocks the retry with a diagnostic that CANNOT WORK. edm-init runs set -euo pipefail (:16) and installs NO trap anywhere, but its scaffold is a multi-step non-atomic sequence: mkdir -p "$DIR" (:130), mkdir -p "$DIR/explorers" (:133), a heredoc into decisions.md (:134-144), a mode-dependent mkdir -p "$DIR/code-audit" (:146-159), then edm-state init "$PREFIX" (:165), then the .gitignore write (:175-189), then branch creation (:191+). TRIGGERING SCENARIO: edm-state init at :165 fails -- jq absent (require_jq dies), a state lock timeout (with_state_lock dies after EDM_STATE_LOCK_WAIT_S), an out-of-band mutator return, a read-only or full filesystem, or a Ctrl-C. Under set -e with no trap, edm-init aborts having already created $DIR, $DIR/explorers, $DIR/decisions.md and (in three of five modes) $DIR/code-audit, and having written NO state file and no .gitignore. Re-running the identical command now hits if [[ -d "$DIR" ]] at :125 and dies with 'initiative directory already exists at $DIR (run edm-state get $PREFIX to inspect)' -- and THAT HINT CANNOT WORK, because the state file the guard implies exists was never written, so edm-state get fails too. The operator's only route out is a manual rm -rf of a directory the tool created and refuses to acknowledge as its own leftover. The SIGNAL case is sharper: a Ctrl-C anywhere between :130 and :189 produces the same wedge with no diagnostic at all. SEVERITY: P2 -- recoverable by hand, no data loss (the scaffold is template content), and it requires a failure or signal mid-scaffold. Filed rather than noted because the recovery path is actively MISLEADING and one trap closes it. EXPLICITLY NOT A REOPEN OF THE DISPOSITIONED TOCTOU: the :125-to-:130 check-then-create race was adjudicated Not Actionable in round 10's Decisions/Non-Findings item 9 and L3 honoured that; this finding is scoped to the PARTIAL-FAILURE axis and L3 states the scoping explicitly (its N2). FIX: record the directory in a variable, install the canonical four-arm trap layer immediately after mkdir -p "$DIR" at :130 (EXIT cleanup-only, removing $DIR ONLY if this invocation created it and has not yet reached the success point; INT/TERM/HUP cleanup-then-exit 130/143/129), and clear the disposition once edm-state init returns successfully. ALTERNATIVE, if leaving the partial tree is preferred: make :126's diagnostic distinguish the two cases -- test for the state file and, when absent, say the directory is an incomplete scaffold from a failed run and name the rm -rf that clears it.
  CA-543 P2 NEW in round 11 (L4): the CA-426 stream-separation regression pin DISCARDS THE VERY STREAM IT EXISTS TO TEST. pdebt6_out="$("$EDM_STATE" approve-gate PDEBT6 code-audit --accept-p2-debt 2>/dev/null)" || pdebt6_ec=$?. This case exists -- per its own banner at :806, 'a warn-and-proceed stderr line must NOT disable the override (stream separation)' -- precisely to prove that the zero-rounds warn goes to STDERR while the refusal goes to STDOUT. It throws stderr away and asserts only stdout and the exit code. NOTHING asserts the warn appears on stderr at all. Delete the zero-rounds warn from cmd_audit_converged entirely, or move it to stdout in a way that still leaves the override engaged, and this regression pin STAYS GREEN -- it proves half of a two-stream contract. ADJACENT TO BUT DISTINCT FROM CA-492 (which is still open on the same fixture): CA-492 is the MISSING BOUNDARY CASE (nothing pins that a zero-round initiative should not converge); this is the DISCARDED STREAM in the case that does exist. FIX: capture stderr to its own variable (2>"$errfile" or a separate capture) and add one assertion that the zero-rounds warn text is PRESENT on stderr and ABSENT from stdout. Land with CA-492, which is the other half of the same fixture's weakness.
  CA-544 P2 NEW in round 11 -- TWO LENSES INDEPENDENTLY, MERGED (L5-001 at high confidence + L7-11-002 at medium; same line, same root cause, same fix). bin/edm-state:5341 is the LAST RETURN-scoped cleanup trap in the plugin with no signal arms, and it sits on the binary every hook fires. _pxt_ignfile is created at :5340 (mktemp "${TMPDIR:-/tmp}/edm-pxt-ignored.XXXXXX" || return 1) inside pattern_extract_titles and armed at :5341 with trap 'rm -f "$_pxt_ignfile"' RETURN -- RETURN ONLY. bin/edm-state:54 is set -euo pipefail, so an errexit-triggered abort inside the function (the three awk invocations at :5349, :5360, :5382 are unguarded) terminates the process WITHOUT firing the function's RETURN trap, because bash routes errexit to exit and runs EXIT traps only; an INT/TERM/HUP delivered while awk is parsing a large findings report does the same. _pxt_ignfile is declared local at :5339, so even a hypothetical process-wide EXIT trap could not reference it -- the exact scoping problem the sibling solved with "${tmp:-}". THE SIBLING IS THE PROOF: whole-tree grep finds exactly two RETURN traps, this one and evals/tiering-matrix.sh:158, and the latter is the FIXED form (RETURN sharing a four-arm EXIT/INT/TERM/HUP set) whose own rationale at :148 states verbatim 'RETURN-only leaked $tmp on a die (exit 2, no RETURN) or a SIGINT mid-self-test' -- a description of THIS site. CA-014 identified the shape, CA-481(a) converted the other site this round, and this one was never swept. THE CONSISTENCY CONCERN IS THE PIN, NOT THE LEAK (L7): CA-481(c)'s new sweep at wave7-smoke.sh:9636-9682 cannot see this site in either arm -- the combined arm only flags EXIT paired with a real signal in ONE statement and a bare RETURN trap pairs nothing; the no-hup arm is FILE-granular (:9656-9661, keyed on FILENAME) and bin/edm-state installs HUP traps at :695, :1510 and :6081, so the file is green regardless. The sweep was landed because the convention was 'stated in prose in one file and enforced nowhere', and the one remaining non-conforming site is the one its chosen granularity exempts -- the same all-but-one loop shape as CA-496, CA-231, CA-270 and CA-531. That parser/granularity gap is filed separately as CA-541. FALSE ALARM FILTER: fails all three clauses -- not gitignore-coverable (TMPDIR), not deleted on the abort paths, not tmpfs-backed. SEVERITY: P2, honestly scoped -- pattern_extract_titles is on the edm-state update-patterns path (a low-frequency operator command reached from /edm:code-audit), not on every hook fire, and the leak is one small file in TMPDIR. FIX: mirror the sibling -- trap 'rm -f "${_pxt_ignfile:-}"' RETURN EXIT plus trap 'rm -f "${_pxt_ignfile:-}"; exit 130' INT, TERM/143, HUP/129 -- using the ${_pxt_ignfile:-} guard for the reason tiering-matrix.sh:150-153 documents (the trap is process-wide, so a second firing at EXIT can occur after local _pxt_ignfile has left scope and a bare expansion would trip set -u). Both call sites run the function in a SUBSHELL (:5541 process substitution, :5654 command substitution) so an EXIT arm is correctly scoped and the sibling's fix applies directly; check the :5554 call path for with_state_lock nesting (_save_traps/_restore_traps bracketing) before applying. Then add the RETURN-names-EXIT predicate to _ca481_sweep as part of CA-541.
  CA-545 P2 NEW in round 11, RESIDUE OF THE CA-495(b) REMEDIATION IN THE SIBLING SUITE (L5-003): the last two scratch files in wave6-smoke.sh are rooted OUTSIDE the suite's trap-covered root. :4929 and :4930 use mktemp "${TMPDIR:-/tmp}/edm-ca059-1.XXXXXX" / -2 inside ca059_case(), instead of ${TMP} (created :21, owned by the four-arm trap at :37-40), with a single tail-position rm -f at :4960 as their only cleanup. Every other mktemp site in the file interpolates ${TMP}. THE INTERRUPT WINDOW IS REAL, NOT THEORETICAL: :4932-4937 launches two background edm-state record-partial-verdict processes that DELIBERATELY CONTEND for the same state lock and then waits on both, so the case spends real wall-clock time in a signal-deliverable state; and :4945-4946 runs cat on one of the files under set -euo pipefail (:6) BEFORE the tail removal, so an unexpected read failure aborts ahead of :4960. FALSE ALARM FILTER: the consistent-project-pattern defence fails outright -- this same file states the nest-under-$TMP rule at :463-464 ('trap rm -rf "$TMP" EXIT (set at the top of this file) already covers cleanup on every exit path ... without a second trap layer') -- and this is the exact class just remediated across eleven sites in wave7-smoke.sh under CA-495(b). FIX (one line each): change both templates to "${TMP}/edm-ca059-1.XXXXXX" / -2 and keep :4960 as a fast path.
  CA-546 P2 NEW in round 11 (L5-005): --generate-fixture's SELF-MINTED 50-initiative tree is never removed on any path and never pruned across invocations. :243 is : "${DIR:=$(mktemp -d "${TMPDIR:-/tmp}/edm-timing-fixture.XXXXXX")}", populated by the :247-251 loop into 50 state files, 50 per-initiative .gitignore files and 50 approved gates. _timing_cleanup (:42-48) sweeps exactly TMP_PC / TMP_LG / TMP_SS / TMP_LINT / TMP_MR; DIR is excluded, and the exclusion rationale at :40-41 ('DIR ... is deliberately NOT swept: it may be a caller-supplied --dir the caller expects to keep') covers ONLY the caller-supplied case. But ':=' means the mktemp fires ONLY WHEN --dir WAS OMITTED -- i.e. precisely the branch where the tree is minted by the script and owned by nobody. THE RATIONALE THEREFORE DOCUMENTS THE BRANCH THAT CREATES NOTHING AND NOT THE BRANCH THAT DOES. Nothing prunes across invocations, and :252-253 prints the path for reuse with no removal instruction, so a developer iterating on latency work accumulates one 50-initiative tree per --generate-fixture call indefinitely; on macOS the system temp directory is per-user and survives reboots. FALSE ALARM FILTER: clause 1 N/A (not in a worktree), clause 2 fails (never deleted), clause 3 fails (persistent temp dir). SEVERITY: held at P2 because timing.sh is explicitly NOT discovered by run-all.sh (:29-30), so this is operator-invoked only and never fires in CI. FIX (preferred, keeps the documented carve-out honest): set a _TIMING_OWNS_DIR=true flag in the ':=' branch and add DIR to _timing_cleanup's loop guarded on it, preserving the caller-supplied carve-out exactly. Alternative, if handoff-to-caller is intentional for both branches: say so at :40-41 and add a removal instruction to the :252 message.
  CA-547 P2 NEW in round 11, TWO L6 FINDINGS MERGED (L6 P2-1 + P2-3 -- same file:line, same root cause: a measured verdict and a measured quantity hand-maintained in prose with nothing single-sourcing or pinning them). (a) THE ENFORCED 100KB EVAL BUDGET HAS THREE DOCS AND TWO MUTUALLY EXCLUSIVE VERDICTS. plugins/edm/CLAUDE.md:1057 states the lint:file-type-ban job 'sums all of plugins/edm/evals/, which is ~134KB and therefore OVER BUDGET at HEAD'; the same table row marks the job 'Yes (blocking since EDMV3-T57 AC10 -- carries no allow_failure)'; and .gitlab-ci.yml:404-411 does if [ "$evals_kb" -gt 100 ]; then ... exit 1. So CLAUDE.md asserts a BLOCKING lint job is red on every MR that touches the plugin. But plugins/edm/evals/fixtures/tiny-svc/README.md:38-46 says the tree 'stays under the 100KB budget for plugins/edm/evals/ ... and fails the blocking lint stage above 100KB' (i.e. green), and plugins/edm/evals/README.md:294 says 'The fixture plus expected.json stays under 100KB'. AT MOST ONE OF THESE IS TRUE, and a contributor's belief about whether the plugin currently merges depends on which file they opened. (b) THE ~134KB FIGURE IS A HAND-MAINTAINED MEASURED QUANTITY WITH NOTHING PINNING IT -- it changes with any fixture or baseline edit and no test asserts it. This is the exact failure mode the SAME FILE refuses two hundred lines earlier: the edm-lint-artifacts row at plugins/edm/CLAUDE.md:803 tells readers to run --help 'rather than a count hardcoded here (a count drifts as classes are added)', and the schema-comment counts are pinned by computed assertions under the G25/CA-342 banner in wave7-smoke.sh. CONFIDENCE MEDIUM ON (a)'s DIRECTION ONLY: no lens had Bash this round (CA-130) so the byte total was not measured independently -- but the finding rests on the docs contradicting EACH OTHER, which holds whichever number is right. FIX: single-source the verdict on the job's own emitted line ('file-type-ban: tracked plugins/edm/evals/ size OK (${evals_kb}KB, documented budget is 100KB)') and delete the standing pass/fail assertion from all three prose sites; replace the ~134KB figure with the reproduction command already given at plugins/edm/evals/README.md:297-298 (git ls-files -- plugins/edm/evals, never du), or pin it the way G25/CA-342 pins its counts. If the sum really does exceed 100KB, say so plainly in CLAUDE.md AND name the resolution (narrow the sum to plugins/edm/evals/fixtures/ per EDMV3-T22 AC3, or raise the constant at .gitlab-ci.yml:404).
  CA-548 P2 NEW in round 11 (L6 P2-2): scope conflation INSIDE the bullet that exists to explain the scope. :38-39 reads 'The fixture plus expected.json stays under the 100KB budget for plugins/edm/evals/' -- a FIXTURE-TREE measurement asserted against the WHOLE-TREE budget. Lines :41-43 of the same bullet then correctly describe the enforcement as git ls-files -- plugins/edm/evals (driver and scorer included), so ONE BULLET BOTH DISTINGUISHES AND COLLAPSES the two scopes that plugins/edm/CLAUDE.md:1057 and plugins/edm/evals/README.md:294-298 call an unresolved divergence (CA-463). Filed separately from CA-547 because it is a different file and a different defect: it survives a CLAUDE.md-only fix, and its fix is a rewording rather than a single-sourcing. FIX (one sentence): 'The fixture plus expected.json is ~11KB. The enforced ceiling is 100KB over ALL git-tracked bytes under plugins/edm/evals/ -- a strictly larger set that includes the driver and scorer (CA-463).'
  CA-549 P2 NEW in round 11, RESIDUAL OF CA-535's REMEDIATION (L7-11-001) -- and a self-correction: the false claim originated in round 10's own L7-003 write-up, propagated into ledger entry CA-535, and was copied VERBATIM from there into shipped prompt-surface text. The new quoting-rationale comment at orchestrator:7 reads: argument-hint: '<initiative description | /path/to/file | PROJ-123>'  # quoted: the | here is a literal pipe, not the YAML block-scalar indicator -- unquoted it would be parsed as one. TWO PROBLEMS. (1) THE RULE IS FOLLOWED AT 1 OF 3 SITES. Three of the fourteen plugins/edm/skills/*/SKILL.md argument-hint values contain a literal | -- orchestrator:7 (quoted, with the comment), test:7 (<PREFIX> [--fill-gaps | --skip-scaffold], UNQUOTED) and metrics:7 (<PREFIX|--all|--calibrate> [--with-human-baseline], UNQUOTED) -- so the rule is asserted in one file and violated in two, and the comment added to make the exception 'read as an exception' instead makes two CONFORMING siblings read as bugs. (2) THE RULE IS WRONG ON THE MERITS. | is a YAML block-scalar indicator only in the FIRST CHARACTER POSITION of a value; all three of these values begin with '<', which is not an indicator character, so all three are ordinary plain scalars in which a mid-value | is just a pipe character. orchestrator:7's quoting is therefore OPTIONAL, not required. LIVE PROOF IN THE SAME DIRECTORY: test:7 and metrics:7 have shipped unquoted through eleven audit rounds and through validate:manifest's frontmatter parse and neither has ever been flagged. CONTRAST the genuinely-forced case elsewhere in this repository -- plugins/git/skills/commit/SKILL.md:5 must be quoted, and the reason is the ': ' in 'optional type override: feat|fix|...' (a colon-space is illegal in a plain scalar), NOT the pipes. WHY IT MATTERS: this is shipped prompt-surface text in a plugin whose entire thesis is that a documented convention is a contract. A contributor who reads orchestrator:7 will either add unnecessary quotes to the next skill or 'fix' test:7 and metrics:7 as latent parse bugs, and the file gives them no way to discover that the stated reason is not the real one. FIX, preferred (a): DROP the quotes at orchestrator:7 so all three |-containing siblings agree -- 12 of 14 sites are already unquoted -- keeping a comment that names the REAL constraint (quote only when the value starts with a YAML indicator character or contains ': '). Alternative (b): keep the quotes, correct the comment to state the actual rule plus that this value is quoted as a readability choice, and quote test:7 and metrics:7 to match. CA-535's ledger entry has already been amended with the retraction. OPTIONAL PIN: assert that the set of argument-hint values containing | is uniform in quoting, positive control = one perturbed value.
  CA-550 P2 NEW in round 11 (L8-11-01): EDM_EVAL_MAX_BUDGET_USD is the ONLY EDM_EVAL_* numeric knob in this driver with NO VALIDATION AT ALL, and the value flows unchecked to claude -p --max-budget-usd "$PHASE_MAX_BUDGET_USD" at :492. :460 is PHASE_MAX_BUDGET_USD="${EDM_EVAL_MAX_BUDGET_USD:-15}". The contrast is inside the same file twenty-five lines up: EDM_EVAL_PHASE_TIMEOUT_SECONDS at :423-433 IS validated with an explicit case that exits 2 on anything that is not a positive whole number, precisely because 'the driver runs without set -e, so an unvalidated value silently disabled the phase timeout instead of aborting' (CA-444); CI_JOB_TIMEOUT at :434-459 gets the same treatment; EDM_EVAL_KEEP_RUNS is validated and clamped (CA-443). The budget knob -- the one that bounds MONEY rather than time -- is the sole exception, and the omission is DOCUMENTED AS A KNOWN STATE RATHER THAN FIXED: plugins/edm/CLAUDE.md:1033 reads 'per-run spend ceiling, default 15. NOT validated -- unlike the timeout knob above, a non-numeric value here is still taken as-is.' FALSE ALARM FILTER: clause 1 does NOT clear it -- documenting that a validation is absent is not the same as documenting a decision that it should be absent, no rationale is given anywhere, and the CA-160 'validate beside the default' rule was applied to the sibling knob in the same file and explicitly declined here. FAILURE SCENARIO (not an escalation): an operator or CI variable typo, a shell-mangled value, or a locale decimal comma ('15,00') reaches the CLI unchecked in eval:nightly -- the one job holding ANTHROPIC_API_KEY (.gitlab-ci.yml:783), running --permission-mode acceptEdits with no human in the loop across three phases. THE DIRECTION OF FAILURE IS UNDETERMINED AND THAT IS THE FINDING: either the CLI rejects the flag (loud, acceptable) or it ignores an unparseable value and the per-phase ceiling is simply not applied, leaving three unbounded-spend phases whose only remaining bound is the 2700s wall clock. Nothing in this tree establishes which, and nothing asserts the shape. Note ${...:-15} DOES cover the empty case, so the exposure is non-numeric / negative / multi-dot values only. FIX: apply the CA-444 pattern verbatim at :460 -- case "$PHASE_MAX_BUDGET_USD" in ''|*[!0-9.]*|*.*.*|.) echo 'run-eval.sh: EDM_EVAL_MAX_BUDGET_USD must be a positive decimal number (got ...)' >&2; exit 2 ;; esac -- sited beside the default the way the timeout's is, and add one smoke case asserting exit 2 on a non-numeric value. SECONDARY, cross-referenced to L6's axis rather than filed separately: run-eval.sh:36 and :406 call this a PER-PHASE ceiling while plugins/edm/CLAUDE.md:1033 calls it a PER-RUN ceiling -- for a spend control that is a 3x difference in what an operator thinks they capped.
  CA-551 P2 NEW in round 11 (L8-11-02): incomplete process containment on the timeout and interrupt paths -- the driver signals a SINGLE PID, never the process GROUP, so credential-bearing grandchildren outlive the boundary the driver asserts. run_with_timeout (:309-330) backgrounds the invocation ('"$@" >"$outfile" 2>"$errfile" &' then CURRENT_CHILD_PID=$!) and both it (:316-318) and cleanup (:256-264) signal exactly $CURRENT_CHILD_PID -- the claude process itself -- with kill -TERM then sleep 2 then kill -KILL. NEITHER EVER SIGNALS THE GROUP. The DIRECT-child leak WAS analysed and closed: the comment at :465-470 explains that invoke_claude deliberately avoids a (...) subshell so CURRENT_CHILD_PID stays visible to the top-level trap. The GRANDCHILD case is addressed NOWHERE. claude -p runs here with Bash(edm-state *), Bash(edm-init *), Bash(edm-validate-prefix *) and Bash(jq *) granted (:421), so at any instant it may have a Bash-tool child of its own, and those children inherit the driver's entire environment INCLUDING ANTHROPIC_API_KEY. FAILURE SCENARIO: on the 124-timeout path or any INT/TERM/HUP, claude dies and every subprocess it had open is reparented and keeps running -- with the eval's grants and the API key still in its environment, past the containment boundary evals/README.md claims. cleanup then rm -rf's $SCRATCH_DIR (:265-267) out from under whatever is still writing into it, so the scratch tree can be deleted mid-write by a process the driver no longer knows about. On a CI runner container teardown masks this; the DOCUMENTED DEVELOPER PATH is bash plugins/edm/evals/run-eval.sh on a workstation, where the orphan persists for the rest of the login session. This is the only place in the plugin where a process holding a live credential can survive its supervisor. FIX (bash-3.2-safe): make the backgrounded invocation lead its own process group and signal the group, keeping the current single-PID kill as fallback -- set -m around the & at :311 (set +m immediately after capturing $!), then kill -TERM -"$pid" 2>/dev/null || kill -TERM "$pid" 2>/dev/null at :316, the same for -KILL at :318, and the same pair in cleanup at :261/:263. Add one smoke case: push a bash -c 'sleep 300 & wait' stub through run_with_timeout with a 1s bound, capture the grandchild's PID from the stub, and assert kill -0 on it FAILS after the timeout returns 124.
  CA-552 P2 NEW in round 11 (L8-11-03) -- A RE-RAISE OF A ROUND-8 L8 ITEM THAT WAS FILED WITH id:null AND NEVER REACHED THE LEDGER, so it has no disposition either way; filed now with an ID specifically so it cannot be lost a third time. eval:nightly is, by its own comment at :794-797, 'the one job in the pipeline holding ANTHROPIC_API_KEY'. Its artifacts block publishes the ENTIRE plugins/edm/evals/runs/ tree with when: always (so failed and partial runs upload too) and expire_in: 30 days, with NO exclude: and NO redaction step. What lands there includes raw/<phase>.stderr.log and raw/<phase>.json for every claude -p invocation (run-eval.sh:480) -- raw stderr and the full JSON result of each phase. GitLab's variable masking applies to JOB LOGS ONLY; artifacts are NEVER masked. So any diagnostic that echoes the environment (a CLI stack trace including argv/env, a Node crash dump, a future set -x added for debugging) is published to everyone who can read the pipeline's artifacts, for a month, with no masking layer between. BOUNDARY STATED EXPLICITLY, WHICH IS WHY CONFIDENCE IS LOW: this is NOT a demonstrated leak. L8 traced every write into the run directory (:157, :162, :480, :725) and found NO SITE THAT WRITES THE KEY TO A FILE TODAY, and the fixture content is synthetic. This is an EXPOSURE-SURFACE finding: the redaction control that would contain a future stderr regression does not exist, in the one job where it matters, and when: always guarantees the failure paths -- the ones most likely to dump diagnostics -- are the ones that upload. FIX: narrow paths: to what the pipeline actually consumes downstream (run.json and scores.json) and either drop raw/ or add exclude: ["plugins/edm/evals/runs/*/raw/*.stderr.log"]. If the raw stderr is genuinely wanted for triage, add a redaction pass before upload (sed-scrub sk-ant-[A-Za-z0-9_-]* and the literal $ANTHROPIC_API_KEY value across runs/*/raw/) and say so in the job comment.
  CA-553 P2 NEW in round 11, TWO L9 RESIDUES MERGED (L9-R11-02 + L9-R11-03): T28's AC14 landing commit -- the commit whose entire purpose was to give the spec_swept refusal its missing ACs -- SHIPPED WITH TWO PIECES OF ITS OWN SWEEP DEBT, which is precisely the class AC14 exists to enforce. (a) AC14's VERIFY CLAUSE CITES A LINE RANGE THAT DOES NOT CONTAIN THE EVIDENCE. AC14 says 'Verify: bash plugins/edm/bin/tests/wave7-smoke.sh:8755-8910 -- the existing case block covers all three branches verbatim'. The spec_swept enforcement block in that suite actually begins at :9339 (banner at :9420, '=== spec_swept enforcement (audit-converged, approve-gate, validate) ==='), with the branch cases at :9446 (fixed+no refuses and names the ID), :9457 (yes and n/a both pass), :9473 (absent field never blocks), :9484 (open+no does not block) and :9496-9505 (multi-ID naming). There is NO spec_swept token anywhere in 8755-8910. A reviewer following the Verify clause literally lands on unrelated assertions and would conclude the AC is unproven -- the same defect class the initiative's own D15 rule exists to prevent: a Verify clause must be runnable AS WRITTEN. (b) T28 NOW CARRIES TWO CONTRADICTING AC-BAND NOTES. The AC list ends with 'AC-band note. 14 acceptance criteria against the 6-12 band ... AC14 is the spec_swept refusal's own three branches' (:613-615), while Technical Notes still ends with 'AC-band note. 13 acceptance criteria against the 6-12 band. Six of them (AC1-AC6) ... Recorded in the README sizing section' (:627-629). One ticket cannot have both 13 and 14 ACs. The stale copy additionally asserts the count is 'Recorded in the README sizing section', so tickets/README.md's sizing row is a LIKELY THIRD STALE SITE -- explicitly NOT verified this round (L9 declared it a coverage gap). FIX (one commit): re-cite AC14's Verify range, or better cite the BANNER STRING rather than line numbers, which drift on every insertion; delete the Technical-Notes duplicate AC-band note (the AC-list copy is the one adjacent to the criteria it counts); and sweep tickets/README.md's T28 sizing row in the same change, verifying whether it is the third stale site.
  CA-554 P2 NEW in round 11, RESIDUE OF THE CA-106 FIX (L9-R11-04): CA-106's route-(b) boundary is recorded EVERYWHERE EXCEPT srd.md. srd.md:1749-1774 (EDMV3-28, 'Baseline captured on wave-A code and committed') is BYTE-UNCHANGED: '- **Priority**: Must Have', six unchecked ACs, and NO reference to D62, to EDMV4-T05, or to any out-of-scope boundary. The v1.4.0 revision row at srd.md:26 covers CR8/EDMV3-90 only and states 'No requirement added or removed'. So the artifact a reviewer reads as THE REQUIREMENTS BASELINE still asserts an in-scope, undelivered MUST HAVE whose capture the ticket pack (epics/03:579-583, :616-628), tickets/README.md:638 and decisions.md D62 all now say is out of scope and owned by another initiative. THIS IS THE SAME DEFECT SHAPE AS CA-509 -- the ticket pack asserting a boundary srd.md does not carry -- and CA-509 was closed by taking the CR route; the CR route was NOT taken here. MITIGATING, AND THE REASON THIS IS P2 RATHER THAN P1: D62 is an explicit HUMAN GATE DECISION that enumerated its own remediation scope and did not include an srd.md edit, so the divergence is documented rather than silent. FIX: a CR9 revision row plus a one-line boundary note under EDMV3-28 naming D62 and EDMV4-T05. Land in the same CR as CA-555's EDMV3-28 AC5 correction, since both edit the same requirement.
  CA-555 P2 NEW in round 11, TWO L9 RESIDUES MERGED (L9-R11-05 + L9-R11-06 -- same file, same class: a landing commit whose own document sweep was incomplete, one editing pass to fix). (a) CR8's OWN DOCUMENT SWEEP IS INCOMPLETE. srd.md:12 still reads 'Last Updated | 2026-07-26' although the revision history's newest row is 1.4.0 dated 2026-08-21, and srd.md:16 still describes the companion decisions.md as '(D1-D16)' when that file now runs to D62. Both sites name the very facts CR8 changed, so under the same-commit sweep convention this initiative now ENFORCES ON ITSELF (spec_swept, CA-416/CA-514), CR8 shipped with sweep debt in its own document. (b) D61's SIX-DIMENSION SWEEP MISSED EDMV3-28 AC5. srd.md:1769-1771 still requires evals/baseline/README.md to state 'that the FIVE dimensions are proxies'. D61 (decisions.md:70) authorized SIX scorer dimensions (scorer_version 1.1.0) and swept T23's ACs, evals/baseline/README.md, score-artifacts.sh (CA-484) and wave7-smoke.sh -- but this SRD AC still states the old count as a number. A wave-A capture scores five of six, so the sentence is coincidentally near-true FOR A CAPTURE, but the clause is about the dimension SET being proxies and the set is six. Medium confidence on (b)'s severity, high on the textual fact. FIX: one editing pass over srd.md -- refresh :12's Last Updated to match the newest revision row, correct :16's decisions.md range (or better, drop the numeric range and name the file, so it cannot go stale again), and change :1769-1771's 'five' to 'six' citing D61. Land (b) in the same CR as CA-554, since both edit EDMV3-28.
  CA-556 P2 NEW in round 11, RESIDUAL OF THE CA-513 FIX (L10 Finding 2). WHAT CA-513 CORRECTLY FIXED, stated first so this is not misread as a reopen: the fence CLASSIFICATION is now genuinely single-sourced -- ignored_line_set is called exactly once, at :5342, and the shared de-indenting run-length-aware classifier in bin/_edm-lint-lib.sh / bin/edm-mermaid-rules.awk is the only fence authority in the function. That was the whole of the P1. WHAT REMAINS is the skip-set CONSUMPTION boilerplate: the two-line prelude 'BEGIN { while ((getline _pxt_ln < ignfile) > 0) if (_pxt_ln != "") skip[_pxt_ln] = 1 }' plus '(NR in skip) { next }' is copy-pasted BYTE-IDENTICALLY at :5350-5351, :5361-5362 and :5383-5384 -- three copies, 34 lines apart, inside one function. DIVERGENCE TODAY: none; all three are identical. The finding is the extension cost and the untested surface. WHY P2 RATHER THAN NOTED: adding a sixth audit type is explicitly contemplated in two places (bin/edm-state:5313 'adding the matching arm here and the matching row to the README'; docs/audit-patterns/README.md:71). A new arm that copies the awk skeleton but DROPS '(NR in skip) { next }' silently re-opens CA-476/CA-513 FOR THAT AUDIT TYPE ONLY, and nothing in the suite would catch it: the CA-513 regression block (wave7-smoke.sh:4277-4315) drives _ca476_extract code ONLY. The srd arm (:5360-5369) and qc arm (:5382-5391) each carry their own independent copy of the prelude and have NO indented-fence and NO long-fence case at all -- so the property 'every arm honours the shared ignore set' is currently verified for ONE ARM OUT OF THREE. FALSE ALARM FILTER: (1) the three copies are identical so no per-arm requirement is being expressed; (2) not test-only; (3) not a circular-dependency workaround -- the shared library is already sourced at bin/edm-state:63. FIX (either is sufficient; the second is cheaper): extract a one-line helper that prepends the prelude to an arm-specific program body, so the skip contract exists once; OR add a count_matches_strict pin asserting the number of '(NR in skip) { next }' occurrences in pattern_extract_titles equals PATTERN_AUDIT_TYPE_ENUM_LIST's word count minus the two names in the ticket|test-coverage early-return arm -- wave7-smoke.sh:1217-1228 is already exactly this shape, so it is a copy of an established local idiom. INDEPENDENTLY OF EITHER: extend the CA-513 fixture to drive the srd and qc arms with an indented fence, so the untested two-thirds becomes tested.
  CA-557 P2 NEW in round 11 (L10 Finding 3): the eval scorer's six dimension names are a SECOND instance of the CA-503/CA-533 one-list-many-literals class. DIM_NAMES at :147 is canonical (requirement-id-coverage ac-testability mermaid-parse-success coverage-map-bidirectionality lens-jsonl-prose-agreement known-gap-recall) and build_dims_json:582 / build_skipped_json:599 correctly derive from it -- but --describe at :163-181 RE-TYPES ALL SIX AS PROSE LITERALS (the one in-code second copy), the header docstring at :44-58 re-lists all six, and DIM_SCORES / DIM_REASONS at :670-671 are two further six-element arrays that must stay index-aligned with DIM_NAMES by hand; evals/README.md:211-225 and evals/baseline/README.md:89-113 re-list them again. So a seventh dimension costs edits at eight sites, while bin/edm-state:799-803 documents the OPPOSITE convention and six constants there now obey it (CA-503, CA-533, both fixed this round). DIVERGENCE TODAY: none. WHAT GENUINELY REDUCES THIS FINDING'S WEIGHT, stated plainly: the COUNT is machine-pinned at six (wave7-smoke.sh:648, :686), --describe's six names are individually pinned (:858, :863) so a silent name drift between DIM_NAMES and --describe fails a test, and the index alignment is indirectly protected because dimensions_scored is READ FROM THE DATA rather than assumed (score-artifacts.sh:684-692, documented at evals/README.md:234) and a malformed array fails a jq parse immediately. NO LIVE BUG IS REACHABLE -- filed at P2 for convention consistency only. FALSE ALARM FILTER, scoping the fix tightly: the six per-dimension scorer functions and their D{N}_SCORE / D{N}_REASON skip idioms are per-dimension DATA AND LOGIC, not duplicated logic -- score_from_ratio is already correctly shared by all six and _scan_mermaid_blocks correctly consumes the shared edm-mermaid-rules.awk -- so those are NOT the finding; the prose re-listings in the two READMEs are documentation and should stay readable. The finding is NARROWLY the --describe block at :163-181. FIX: interpolate DIM_NAMES members into --describe's numbered paragraphs so the one in-code re-encoding disappears; leave the docstring and both READMEs as prose. Optionally add a pin asserting ${#DIM_NAMES[@]} == ${#DIM_SCORES[@]} == ${#DIM_REASONS[@]}.
  CA-558 P2 NEW in round 11 (L11 Finding 2): two REAL, COMMITTED QC shards are invisible to the only step that consumes them, and two consumers of one namespace disagree about the shape. WHAT EXISTS: SRD/edm/EDMV3__prompt-streamline/qc/qc-shard-01.md and qc-shard-02.md. These are not hypothetical -- qc/qc-summary.md:5 names both ('Shard reports: qc-shard-01.md (T01-T14), qc-shard-02.md (T15-T64)') so they were produced and merged once, under the pre-CA-473 bare convention. THE BREAK, ON THE MERGE SIDE: skills/implement/SKILL.md:122 merges all qc-shard-impl-*.md AND all qc-shard-pass-*.md. A bare qc-shard-01.md matches NEITHER glob. Any future re-run of the merge step on this initiative regenerates qc-summary.md from ZERO matching shards and silently drops both files' verdicts. THE DISAGREEMENT: bin/edm-state:2638 still globs the WIDER qc/qc-shard-*.md for phase-6 completion, so these same two files DO satisfy the phase gate -- one consumer accepts the shape, the other cannot see it, and edm-state's own die text at :2642 enumerates only the two post-CA-473 prefixes. Round 10's L11-N3 recorded the glob breadth as a documented choice; this is its CONCRETE CONSEQUENCE, on real committed artifacts rather than a fixture, which is why it is filed now and was not then. FIX: rename the two committed shards to qc-shard-impl-01.md / qc-shard-impl-15.md (they are per-range reports, matching the impl semantics) so they rejoin the merge glob, and note the rename in qc-summary.md:5. Then EITHER narrow edm-state:2638 to the two documented prefixes so it agrees with its own die text at :2642, OR amend :2642 to admit the bare form is still accepted. Leaving both as-is keeps a shape that passes the gate and is dropped by the merge. Sequence with CA-515, which governs the pass- key shape.
  CA-559 P2 NEW in round 11 (L11 Finding 4): edm-check-grants checks grant-without-instruction for AGENTS ONLY, so skill over-grants are structurally invisible -- and two live instances exist today. The checker's own help block documents the asymmetry precisely: :26-34 states that two directions are checked 'for sources 1-3 (agents only)', including the negative 'warning: grant-without-instruction' direction that the comment credits with catching edm-audit-synthesizer's unexplained Edit grant; :35-37 then states 'Source 4 (skills) only has a positive direction' -- a skill MISSING AskUserQuestion is caught; a skill GRANTING a tool it never uses is not. Nothing else scans skill allowed-tools for over-grants: the CI validate:manifest job checks that declared tool names are well-formed, not that they are used or that they exist. TWO LIVE INSTANCES FOUND BY HAND THIS ROUND: (1) skills/verify-runtime/SKILL.md:8 grants Bash(mkdir *) and the body contains no mkdir instruction -- and it arguably NEEDS one, filed separately as CA-560; (2) skills/implement/SKILL.md:8 grants TodoRead, the only occurrence of that token anywhere in the plugin -- cross-lens note: L1 already carries this one (pass-10 lens-L1.md:154), so it is cited here as EVIDENCE for the structural gap and is NOT double-counted as a separate finding. FIX: extend the negative direction to source 4 -- for each skills/*/SKILL.md, warn when allowed-tools grants a Bash(<cmd> *) matcher or a named tool whose token appears nowhere in that skill's body. Keep it WARNING-ONLY, exactly as the agent-side negative direction is (:31-32), so it never changes the exit code; the value is the visible line, not a block. Fixing CA-560 removes one of the two instances from the list at the same time.
  CA-560 P2 NEW in round 11 (L11 Finding 5): the post-deploy write has no directory-creation step, though the grant for one is held. skills/verify-runtime/SKILL.md:80 instructs writing ${INIT_DIR}/post-deploy/verification.md, and :8 grants Bash(mkdir *) -- but there is NO mkdir instruction anywhere in the body. The single place a directory is mentioned is :49-50 and it is a PROHIBITION ('do not create post-deploy/verification.md or its directory') governing the empty-map early exit, not the write path. WHY THIS IS A BREAK AND NOT JUST AN UNUSED PERMISSION: the plugin's own convention is to instruct the mkdir explicitly before writing into a subdirectory that may not exist, and BOTH sibling write-into-new-subdir surfaces do it -- agents/edm-qc-auditor.md:87 ('Run mkdir -p <initiative-dir>/qc before writing.') and skills/code-audit/SKILL.md:62 (mkdir -p "${OUTPUT_DIR}", backed by the same Bash(mkdir *) grant). verify-runtime holds the IDENTICAL grant and skips the step, so its post-deploy/ write depends on the host's Write tool creating parent directories -- an UNSTATED dependency the two sibling surfaces deliberately do not take. FIX: add mkdir -p "${INIT_DIR}/post-deploy" as the first action of step 4(g) at :80, phrased the way edm-qc-auditor.md:87 phrases it. This uses the existing grant and removes this file from CA-559's dead-grant list at the same time.
  CA-561 P2 NEW in round 11 (L11 Finding 6): the tiny-svc fixture registers a route no dispatcher can reach, and DOES NOT ANNOTATE IT -- so deliberate cannot be told from incomplete. routes.js:8-11 declares a two-entry route table including { method: 'GET', path: '/health', handler: 'handleHealth' }, :13-15 defines handleHealth and :17 exports it. Nothing reaches it: server.js:16 imports only routes (not handleHealth), and its single handler at :19-24 returns 404 unless route.handler === 'handleWebhook'. There is no dispatcher in the fixture that can route to handleHealth, and grep confirms zero callers anywhere under fixtures/tiny-svc/. WHY IT MATTERS HERE SPECIFICALLY: this fixture has an EXPLICIT CONVENTION of annotating deliberate dead or uncounted wiring so a reader can tell design from omission -- queue.js:5-7 names its no-persistence gap and says it is deliberately NOT one of the six counted, and processor.js:21-22 names its failure branch as 'present-but-unreachable' on purpose. The unreachable /health route is the ONE piece of dead wiring carrying no such note, and it is not among expected.json's six gaps. SCORING IMPACT IS NIL in the penalising direction (dimension 6 known-gap-recall measures recall of the six; extra findings are not penalised), so this is FIXTURE MAINTAINABILITY, not a scoring defect -- which is why it is P2 at medium confidence. FIX: add a two-line comment to routes.js in the established form, stating that /health is intentionally unreachable (there is no dispatcher) and that this is deliberately not one of the six counted gaps, mirroring queue.js:5-7. IF INSTEAD the intent was a reachable health endpoint, add the dispatch branch to server.js and leave expected.json untouched. Either resolution requires the fixture_version discipline at fixtures/tiny-svc/README.md:30 only if the GAP SET changes; a comment-only change does not.

## Gates

- Gate 1 - approved 2026-07-25T20:40:25Z by darryl.porter
- Gate 2 - approved 2026-07-25T22:30:47Z by darryl.porter
- Gate 3 - approved 2026-07-27T01:43:50Z by darryl.porter
- Gate code-audit - approved 2026-08-23T06:35:49Z by darryl.porter [enforcement: permission-ask] [39 P2 debt accepted, round 11, by darryl.porter at 2026-08-23T06:35:49Z]

## Artifact Checklist

| Artifact | Status |
|----------|--------|
| `./SRD/edm/EDMV3__prompt-streamline/planning.md` | [present] |
| `./SRD/edm/EDMV3__prompt-streamline/srd.md` | [present] |
| `./SRD/edm/EDMV3__prompt-streamline/audit-srd.md` | [present] |
| `./SRD/edm/EDMV3__prompt-streamline/tickets/README.md` | [present] |
| `./SRD/edm/EDMV3__prompt-streamline/tickets/audit.md` | [present] |
| `./SRD/edm/EDMV3__prompt-streamline/architecture.md` | [present] |
| `./SRD/edm/EDMV3__prompt-streamline/decisions.md` | [present] |
| `./SRD/edm/EDMV3__prompt-streamline/ROLLBACK.md` | [absent] (on-demand) |
| `./SRD/edm/EDMV3__prompt-streamline/exec-report.md` | [absent] (on-demand) |

## Key Decisions Made

_(none recorded yet - decisions are captured at Gate 1)_

-> See full decision ledger: `./SRD/edm/EDMV3__prompt-streamline/decisions.md`

## How to Resume

1. Pull the latest branch - all EDM artifacts are committed
2. Open Claude Code in the project root
3. Run: `/edm:orchestrator EDMV3`
4. The orchestrator detects the existing initiative and resumes from **Phase 6 - Implementation**

## Notes

_(Add anything a teammate should know before resuming - context, blockers, preferences)_
