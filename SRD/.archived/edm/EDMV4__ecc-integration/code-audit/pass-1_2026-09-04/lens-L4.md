# Lens L4 -- Test Quality (round 1, full)

Scope swept: `plugins/edm/bin/tests/wave8-smoke.sh` (5896 lines, read end to end),
`_harness.sh`, `run-all.sh`, `harness-smoke.sh`, plus targeted sweeps of `wave7-smoke.sh` and
`wave6-smoke.sh` for the five named defect classes and the fixture trees under
`bin/tests/fixtures/`.

Every finding below names the concrete input or one-line mutation that should fail the assertion
and does not.

### Sweep coverage by defect class

| Class | Coverage | Result |
|---|---|---|
| 1. Tautological comparisons | Complete across all 8 suites -- every `-eq "$VAR"` comparison enumerated by grep, each variable's provenance traced by hand | One live instance (L4-012). The two `EDMV4-T30` instances named in the brief are genuinely fixed: `wave7-smoke.sh:1665-1678` and `:5626` now anchor to `bin/edm-state`'s `ALL_LENS_IDS`, an independent definition in another file. `wave7:4957`, `:9672`, `:9774`, `:1783` all compare operands with genuinely different provenance (a loop counter incremented only on success vs. a list length) and are not tautologies. |
| 2. Positive controls that cannot fire | Complete for wave8's own controls; sampled in wave7 | The `awk -v` backslash hazard named in the brief is handled CORRECTLY at `wave8-smoke.sh:5652` and `:5677` -- the shell passes `\\t` and awk's own `-v` escape processing collapses it to the two literal characters the fixture contains. Findings raised: L4-012 (a control that never varies its operand) and L4-024 (a preamble claiming universal control pairing the section does not have). |
| 3. `set -e` aborts | Complete in wave8; wave7's known sites re-read and confirmed guarded | Five live instances in wave8 (L4-006, L4-007). `wave7-smoke.sh:1160-1174`, `:1772` and `:1801` carry explicit guards whose comments record the ~500-assertion incident. |
| 4. Self-matching scans / unnormalized path exclusions | Complete -- every `grep -v '/tests/'` and every `${SCRIPT_DIR}/..` interpolation enumerated | Clean. All four `grep -v '/tests/'` filters in wave8 (`:5012, 5093, 5158, 5193`) and all three in wave7 (`:1223, 6622, 6630`) are applied to `$PLUGIN_DIR` -- `_HARNESS_PLUGIN_DIR`, a `cd .. && pwd`-normalized absolute path with no `..` component. Runtime-assembled needles are used correctly at `:740`, `:3232-3237`, `:5875-5876`. |
| 5. Premise fails silently | Complete for both SIGINT cases and for wave8's marker/spy-based premises | One live instance (L4-009, `harness-smoke.sh`). `wave7-smoke.sh:6215`'s sibling is demoted (L4-023) because it does assert the child's exit code. The spy- and marker-based premises in wave8 (`:3402-3415`, `:4306-4316`, `:4633-4661`) each carry a real paired control. |

Not swept, and worth a later pass: `wave3-smoke.sh`, `wave4a/4b-smoke.sh` and `wave5-smoke.sh`
were grepped for the five classes above but not read line by line. They are largely pre-EDMV4 and
outside this initiative's diff, but the `set -e`/`$?` and substring-`check` classes are shared
harness idioms and could recur there.

## Findings (L4: Test Quality)

| ID | Test File:Line | What Passes That Shouldn't | Fix |
|----|-----------------|------------------------------|-----|
| L4-001 | plugins/edm/bin/tests/wave8-smoke.sh:251 | `check_absent` is handed an invented sentinel needle, `MISSING_REFRESHED_NOTE_SENTINEL_NEVER_PRESENT`, which by construction never appears in `agents/edm-explorer.md`. The assertion therefore always passes, while its own label claims it verifies that the `"Refreshed"` string is PRESENT -- the opposite property. Mutation that should fail it and does not: delete the entire `append a short "Refreshed" note naming which sections you touched and this initiative's prefix` sentence from `agents/edm-explorer.md:84-85`; this assertion still passes. | Delete the assertion (line 254's `check ... 'short "Refreshed" note'` already covers the real property), or convert it to `check` against the real literal. `check_absent` with a made-up needle can never fail and only inflates the pass count. |
| L4-002 | plugins/edm/bin/tests/wave8-smoke.sh:3667 | `check "<label>" "" "$T13_EXIT_ALLOW_OUT"` -- `check()` tests `[[ "$actual" == *"$expected"* ]]`, and with an empty expected the pattern is `**`, which matches every string including a non-empty one. The assertion "exit-code-mode allow prints nothing to stdout" passes for any stdout at all. Mutation: add `printf 'LEAKED\n'` to `emit_decision`'s allow arm in `bin/edm-gateguard`; this assertion still passes (`:3657`'s `-z "$T13_EXIT_OUT"` covers the deny path only, not the allow path). | Use `[[ -z "$T13_EXIT_ALLOW_OUT" ]] && pass ... \|\| fail ...`, the idiom the same file already uses at `:3645` and `:3390`. |
| L4-003 | plugins/edm/bin/tests/wave8-smoke.sh:4502 | Identical empty-needle defect: `check "EDMV4-T15 AC10 -- an unparseable stdin payload's stdout is empty" "" "$T15_AC10_OUT"`. Always true. Mutation: make `bin/edm-gateguard`'s unparseable-payload arm emit its JSON deny payload to stdout before exiting 1; the assertion still passes, so the "never blocks" half of AC10 is untested on the stdout channel. | Same fix: an emptiness test, not `check`. |
| L4-004 | plugins/edm/bin/tests/wave8-smoke.sh:1338 | `t32_nonascii="$(LC_ALL=C grep -l -P '[^\x00-\x7F]' ... 2>/dev/null \|\| true)"`. BSD `grep` on macOS -- the stated bash-3.2 floor platform -- has no working `-P`; it exits 2 printing nothing, `2>/dev/null` hides the diagnostic and `\|\| true` swallows the exit code, so `t32_nonascii` is unconditionally empty and the assertion at `:1341-1343` ("new .jsonl fixtures and lenses-run.txt contain no non-ASCII bytes") always passes. No positive control. Input that should fail it and does not: write a real UTF-8 byte (e.g. `\xc3\xa9`) into `bin/tests/fixtures/code-audit/lens-L12.jsonl`. This exact failure mode is documented as a hazard in this same file at `:5342-5346` and correctly guarded there by the `T52_HAS_PCRE` probe (`:5347-5360`), and again in `wave7-smoke.sh:2238` and `:5729`; the T32 site was never converted. | Route this scan through `t52_ascii_scan` (already defined in the same file) or replicate its `T52_HAS_PCRE` detect-and-fall-back split, and add a positive control on a scratch file carrying a real non-ASCII byte, as `:5398-5406` already does. |
| L4-005 | plugins/edm/bin/tests/wave8-smoke.sh:3691 | An unconditional, top-level `pass` making a substantive claim nothing computes: "the only two nonzero exits observed above are 2 (exit-code deny, AC3) and 1 (setup error, AC4) -- no policy refusal reuses exit 1". No variable is read, no comparison is made. Mutation: change `emit_decision`'s exit-code deny arm in `bin/edm-gateguard` to `exit 3`; AC3's own check at `:3657` goes red, but this AC5 "behavioral half" still reports PASS, so the claim it exists to make is never tested. | Collect the observed RCs into one string and loop over them for `{1,2}` membership, exactly as the sibling T44 block already does at `:2489-2501`. |
| L4-006 | plugins/edm/bin/tests/wave8-smoke.sh:940 | `t25_lint_out="$(bash .../edm-lint-artifacts --path ... 2>&1)"` on line 940, then `t25_lint_exit=$?` on line 941, under `set -euo pipefail`. A simple assignment whose command substitution fails carries that non-zero status, so `set -e` aborts the suite AT line 940; line 941 is only ever reached with `$? == 0`. The `[[ "$t25_lint_exit" -eq 0 ]] && pass` at `:942` is therefore unconditional. Input that should produce a named FAIL and does not: put one non-ASCII byte in any `agents/edm-audit-*.md` -- `edm-lint-artifacts` exits 1, wave8 dies at line 940 with no `Results:` line, and the ~4950 assertions after it never run (`run-all.sh` reports CRASH, not a named failing assertion). Same shape at `:1058-1061`, `:1154-1157`, `:1330-1334`. The correct guarded form is already used in the same file at `:4982-4985` and `:5419-5421`. | Convert all four to `rc=0; out="$(cmd 2>&1)" \|\| rc=$?`. |
| L4-007 | plugins/edm/bin/tests/wave8-smoke.sh:444 | Same `set -e` abort class in compound-command form: the `( ... exit 90..97 ... )` subshell at `:444-467` is a plain command, so any non-zero exit aborts the suite before `T17_AC5_RC=$?` on line 468 can read it. `T17_AC5_RC` can only ever be 0, making the `pass` at `:470` unconditional and the entire documented exit taxonomy in the `fail` message at `:472` (`90=source,91=data_dir,...,97=not-rooted-at-data-dir`) unreachable. Mutation: rename `edm_marker_path` in `bin/_edm-datadir-lib.sh` -- the subshell exits 93, and instead of "EDMV4-T17 AC5 -- sourced-library check failed (rc=93)" the suite dies silently at line 467. Note `set +e` on line 445 governs only the inside of the subshell; it has no effect on the parent's `set -e` reaction to the subshell's exit status. This is the same AC the wave-1 QC already had to rewrite once ("never sourced the library"). | `T17_AC5_RC=0; ( ... ) \|\| T17_AC5_RC=$?`. |
| L4-008 | plugins/edm/bin/tests/wave8-smoke.sh:3349 | Numeric equality routed through the substring helper `check`, so a count of `10`, `20`, `100` satisfies an expected `"0"`: `check "... invokes the state binary zero times" "0" "$t11_ac5_count"` passes when `grep -c 'edm-state' bin/edm-gateguard` returns `10`. Input that should fail and does not: add ten `edm-state` references to `bin/edm-gateguard`. Same defeat at `:3353` and `:3357` (AC6 destructive/heredoc/subshell detection, AC7 node/python/npx/pip -- a real reintroduction of shell-command inspection would plausibly exceed nine hits), `:3199` (`no check has a null or empty fix string`, expected `"0"` -- `edm-repo-readiness` emits well over ten checks), `:1844` (`marker holds exactly one line`, expected `"1"` -- a 10-line marker passes), `:1969`, `:2713`, `:2892`, `:3024`, `:3028`, `:3157`, `:3159`, `:4406` (expected `"500"`, satisfied by `1500`). `wave7-smoke.sh:6229-6231` records this exact hazard as G28/CA-263 and converted its own site to `[[ -eq ]]`; the wave8 sites were not. | Use the file's own numeric idiom `[[ "$count" -eq 0 ]] && pass ... \|\| fail ...` for every count assertion, or add a `check_eq` helper to `_harness.sh` and convert all sites. |
| L4-009 | plugins/edm/bin/tests/harness-smoke.sh:78 | The SIGINT cleanup case's premise fails silently and the assertion still passes. `kill -INT "$_child_pid" 2>/dev/null \|\| true` discards every failure. `wait` on `:79` then blocks until the child completes `sleep 5` NORMALLY, and `with_scratch_repo`'s normal-return path (`_harness.sh:150`, `rm -rf "$dir"`) removes the scratch directory anyway -- so the `pass "scratch dir removed after SIGINT"` at `:92` is produced by ordinary cleanup, not by the INT trap it claims to test. Mutation that should fail it and does not: change `kill -INT` to `kill -CONT` (a no-op for this child); the test still passes. Second mutation: delete `_harness.sh:123`'s `trap 'rm -rf "$dir"; exit 130' INT` line entirely; the test still passes. Nothing anywhere asserts the child died from the signal. `wave7-smoke.sh:6227` shows the missing half (`check "... actually died from the signal (exit 128+2=130)" "130" "$child_ec"`). | Capture the child's status (`child_ec=0; wait "$_child_pid" \|\| child_ec=$?`) and assert `child_ec -eq 130` before asserting cleanup; fail loudly if `kill` itself returns non-zero rather than `\|\| true`. |
| L4-010 | plugins/edm/bin/tests/wave8-smoke.sh:3253 | Three branches print an uncounted `echo "  NOTE: ..."` where the sibling branch calls `pass`, so the condition is silently unenforced and the suite's assertion total varies run to run. `:3253` is the damaging one: `T40_REAL_STATE` is hardcoded to `${REPO_ROOT}/SRD/edm/EDMV4__ecc-integration/.edm-state.json` -- the initiative currently under audit. `edm-state archive EDMV4` relocates that file to `SRD/.archived/edm/...`, after which EDMV4-T40 AC8's "edm-repo-readiness never writes to .edm-state.json" check prints a NOTE and never runs again, forever, with no failing assertion. Mutation: `mv SRD/edm/EDMV4__ecc-integration SRD/.archived/edm/` (i.e. archive this initiative, which is imminent) -- the check silently stops testing. Same shape at `:3194` (EDMV4-T39 AC4/AC5) and `:107` (EDMV4-T05 AC3, now dead but still uncountable). This file's own comment at `:3114-3124` names this exact shape as "the exact vacuous-assertion class wave-3 QC caught twice already in EDMV4-T30". | Resolve the state file by globbing `SRD/*/*/.edm-state.json` (plus `SRD/.archived/**`) and `fail` when none is found, rather than pinning one initiative's path and skipping; convert `:3194` to a `fail` (T39 AC4/AC5 has a synthetic no-initiatives fixture at `:3260` that guarantees an inapplicable category). |
| L4-011 | plugins/edm/bin/edm-bash-gate:38 | Missing coverage on a new hook that fires on every `Bash` tool call. `bin/edm-bash-gate` is registered in `hooks/hooks.json`'s `PreToolUse` array under matcher `Bash` (asserted at `wave8-smoke.sh:4583-4585`), yet every assertion naming it (`wave8-smoke.sh:4566, 4584, 4711, 4726, 5113, 5125, 5297`) checks only that the file exists, is executable, parses under `/bin/bash -n`, prints a non-empty `--help`, and is named in `CLAUDE.md`. Not one of the six exit paths its own `EDM-HELP` block documents as a contract is executed: allow (exit 0) when `edm-hookify` is off PATH (`:55`), when `jq` is off PATH (`:56`), on empty stdin (`:60`), on an unparseable payload (`:63`), on `edm-hookify` exit 1 (`:71`, the never-escalate clause); and block (exit 2) on a matched `"action": "block"` bash rule (`:71-74`) -- the only blocking path in the file. Mutation: invert `:71` to `-ne 2`; every Bash tool call in a Phase-6 session is refused and the suite stays green. The `block-rm-rf-bash.json` fixture built for exactly this event exists and is exercised only against `edm-hookify` directly, never through this consumer. Compare `edm-gateguard` (~150 behavioral assertions) and `edm-stop-gate` (~30). | Add a T45 behavioral block mirroring `t46_ac9_case` (`:2334-2354`): drive `edm-bash-gate` with the `block-rm-rf-bash.json` fixture and a matching `{"tool_input":{"command":"rm -rf /"}}` payload for exit 2, then the same payload with `edm-hookify` off PATH, with a broken `jq` shim, with empty stdin, and with `not json` on stdin, for exit 0 each time. |
| L4-012 | plugins/edm/bin/tests/wave7-smoke.sh:5630 | A "positive control" that cannot fire. `[[ "$T48_CONTESTED_TOTAL" -ne $((T48_EXPECTED_TOTAL + 1)) ]]` claims to prove "a `T48_CONTESTED_AGENTS` one name longer than the anchor would be caught", but it never constructs such a list -- it compares the real list's own member count against `anchor + 1`. Whenever the main assertion two lines above passes (`t48_contested_count == T48_EXPECTED_TOTAL`, and `T48_CONTESTED_TOTAL` is the member count of the same string), this reduces to `n != n + 1`: a tautology no input can fail. There is no mutation a real control would catch that this one does, because it never varies the operand it claims to vary. Contrast the correct form 3950 lines earlier at `:1674-1678`, which builds a synthetic 15-member probe string. | `t48_probe=$((T48_CONTESTED_TOTAL + 1)); [[ "$t48_probe" -ne "$T48_EXPECTED_TOTAL" ]]` -- vary the list length, not the anchor. |
| L4-013 | plugins/edm/bin/tests/wave8-smoke.sh:311 | `harness_scratch_dir` is called 14 times in one process (`:311, 2537, 3370, 3618, 3740, 4280, 4479, 4506, 4522, 4596, 4667, 4908, 5398, 5484`) against `_harness.sh:70-71`'s explicit contract: "do not call it more than once per process without capturing and restoring the prior trap yourself". Each call overwrites the single non-local `_HARNESS_SCRATCH_DIR` and re-installs the EXIT trap, so only the LAST directory is ever removed; the other 13 leak into `${TMPDIR}` on every run, including `$TMP`, which by then holds two full `cp -R "${PLUGIN_DIR}/."` copies of the plugin tree (`:2902`, `:2945`). It also silently voids the remediation `:946-951` claims to have applied: `T05_TMP`'s EXIT trap (`:45`) was replaced by the `harness_scratch_dir TMP` call on line 311, 641 lines BEFORE the `${T05_TMP}/t25-grants.err` write on `:952` that the comment says is protected by it. Finally `:5808`'s `trap - EXIT INT TERM HUP` leaves the last ~90 lines of the suite with no cleanup trap at all. | Call `harness_scratch_dir` once and derive per-section subdirectories under it, or extend `_harness.sh` to append each directory to a list the single EXIT trap iterates. Correct the `:946-951` comment, which is factually wrong as written. |
| L4-014 | plugins/edm/bin/tests/wave8-smoke.sh:1052 | Three of four `edm-check-grants` stderr captures still redirect into the repository working tree -- `2>"${SCRIPT_DIR}/.t27-grants.err"` (`:1052`), `.t26-grants.err` (`:1148`), `.t28-grants.err` (`:4976`) -- removed only on the line after the `fi`. Any exit between the two (a turn ceiling, an interrupt, or the `set -e` aborts of L4-006/L4-007, several of which fire between these lines) leaves an untracked file in `plugins/edm/bin/tests/`. The identical site at `:952` was moved out of `SCRIPT_DIR` for precisely this reason, with the comment "Found live in a wave-5 worktree" -- one of four was fixed. The suite itself asserts a clean `git status --porcelain` at `:518-525` and `:3129-3134`. Mutation: send SIGINT during the `edm-check-grants` run at `:1052`; `.t27-grants.err` persists in the tracked tree. | Redirect all three into the EXIT-trapped scratch dir, as `:952` intends to (and see L4-013 for why `:952`'s own trap is currently absent). |
| L4-015 | plugins/edm/bin/tests/wave8-smoke.sh:4553 | The suite hard-codes this initiative's own live artifact paths, so it is not runnable outside this repository and changes behaviour the moment the initiative is archived. `BASH_DECISIONS_MD="${PLUGIN_DIR}/../../SRD/edm/EDMV4__ecc-integration/decisions.md"` gates EDMV4-T45 AC3's entire Spike-A precondition on a file that exists in no other consuming project and moves to `SRD/.archived/` on archive -- the `else` at `:4564` then hard-FAILs. Same coupling at `:5438` (`edm-lint-artifacts EDMV4` against the live initiative). Input that should not fail and does: run `bash plugins/edm/bin/tests/run-all.sh` from an installed plugin cache in any other repository -- wave8 reports a failing assertion for a condition that has nothing to do with the plugin's correctness. | Copy the two `D25` rows into a committed fixture under `bin/tests/fixtures/` and assert against that, keeping any live-tree read as an optional cross-check. |
| L4-016 | plugins/edm/bin/tests/wave8-smoke.sh:5139 | Two more unconditional top-level `pass` calls used as log lines: `:5139` (`/bin/bash --version recorded: ...`) and `:5553` (`(b) recorded as Not Applicable to edm-stop-gate ...`). Both are honest about being records rather than claims, but both increment `PASS` and are folded by `run-all.sh:125` into the aggregate total, so the suite reports two assertions that no input can fail. `:5139` is worse than it looks: the ticket text it implements (`tickets/epics/08-cross-cutting.md:89`) asks only that a non-3.2 `/bin/bash` be "visible in the log rather than silently vacuous", which a bare `echo` achieves. Mutation: run on a host whose `/bin/bash` is 5.2 -- the bash-3.2-floor claim the whole T50 section rests on is void, and this line still reports PASS. | `echo` the version instead of `pass`-ing it; optionally add a real assertion that `/bin/bash --version` reports 3.2 when `EDM_REQUIRE_BASH32=1`. For `:5553`, `echo` the N/A rationale. |
| L4-017 | plugins/edm/bin/tests/wave8-smoke.sh:2699 | `"$(grep -c ... 2>/dev/null \|\| echo 0)"` emits TWO lines on a no-match: `grep -c` prints `0` on stdout AND exits 1, then `echo 0` prints a second `0`, so the variable holds `0\n0`. The subsequent `[[ "$T18_DELTA_HEADINGS" -eq 1 ]]` is then a bash arithmetic syntax error printed to stderr rather than the intended diagnostic. Worse at `:3027-3028`, where the expected value IS `0`: `check "... the fresh delta still carries zero ### entries" "0" "0\n0"` passes -- and passes identically when `$T20_AC4_DELTA` does not exist at all (grep exit 2), so a broken path reads as a clean result. Mutation: point `T20_AC4_DELTA` at a nonexistent path; the assertion still passes. Same construction at `:2712` and `:2890`. The correct form (`\|\| true`) is already used at `:1312` and `:2684` in the same file. | Replace `\|\| echo 0` with `\|\| true` at all four sites, and prefer `count_matches_strict` (documented in `_harness.sh:205-220` for exactly this exit-2 collapse) wherever the expected count is zero. |
| L4-018 | plugins/edm/bin/tests/wave8-smoke.sh:248 | Incomplete assertion: `check "EDMV4-T48 AC4 -- instruction requires a \"Refreshed\" note naming touched sections and the prefix" 'append a' "$(cat "$EXPLORER_AGENT")"` asserts only the substring `append a` anywhere in the whole agent file. It tests none of the three properties its label names (a note, the touched sections, the prefix). Mutation: rewrite `agents/edm-explorer.md:84-85` to `... rather than rewriting it from scratch, and append a paragraph of your choosing.` -- the "Refreshed"-note requirement is gone and this assertion still passes. (Line `:254` catches that specific rewrite; this line contributes a pass regardless.) | Assert the full clause `append a short "Refreshed" note naming which sections you touched and this initiative's prefix`, or delete the line as redundant with `:254`. |
| L4-019 | plugins/edm/bin/tests/wave8-smoke.sh:4742 | The EDMV4-T28 section's preamble states that "every zero-count-style assertion below is paired with a positive control: a scratch fixture that corrupts exactly one contract element and proves the checker actually flags it". `t28_contract_violations` (`:4767-4885`) emits 33 distinct violation tags; `t28_neg_case` (`:4927-4955`) supplies negative fixtures for 15 of them. Eighteen tags -- `DISALLOWED_TOOLS`, `EFFORT`, `OPENING_FRAME`, `OUTPUT_JSONL_PATH`, `SCHEMA_LENS_ID`, `LENS_ID_UNRESOLVABLE`, `ASCII_REMINDER`, `SEVERITY_CITATION`, `ANCHOR_QUALIFIER`, `ANCHOR_NEVER_CWD`, `JSONL_ID_RULE`, `JSONL_ROUND_RULE`, `JSONL_SEV_RULE`, `JSONL_STATUS_RULE`, `RESIDUAL_RISK_PARAGRAPH`, `NA_STANDARD_SENTENCE`, the three `NA_CONDITIONAL_*` tags, and `MISSING_OR_EMPTY_FILE` -- have no fixture proving their `grep -qF` discriminates. Mutation that should be caught and is not: widen `:4832`'s needle from `never the caller's cwd` to the substring `cwd` -- the `ANCHOR_NEVER_CWD` check then passes on any file mentioning `cwd` for any reason, and no fixture exists to notice. (A checker needle mistyped in the tightening direction is loud, since all 14 files then fail; only a loosened needle is silent, and that is what a control catches.) The preamble's universal claim is what makes this actionable rather than a bare coverage note. | Either add `t28_neg_case` entries for the eighteen uncovered tags (each is a one-line `sed` delete against the L1 reference, matching the existing fifteen), or soften the preamble to state exactly which contract elements are control-backed. |

## Noted / Not Actionable

| ID | File:Line | Rationale |
|----|-----------|-----------|
| L4-020 | plugins/edm/bin/tests/wave8-smoke.sh:2073 | `t43_ac9_snapshot` uses `stat -f '%N %z %m'`, which is BSD-only; GNU `stat -f` means `--file-system` and takes no format argument, so on Linux the before/after snapshots would degrade to identical filesystem metadata and the AC9 "wrote no file" check would compare equal even for a script that did write. This is NOT a silent false pass: the positive control at `:2096-2101` runs the identical snapshot around a script that provably writes and would report the detector broken, so the failure is loud. Linux is recorded as untested under decisions.md D43, making this a known-platform gap rather than an unguarded assertion. Worth fixing when D43 is closed (a `stat -c` / `stat -f` pair, as `bin/edm-gateguard` already carries per `wave8-smoke.sh:5248-5260`). |
| L4-021 | plugins/edm/bin/tests/wave8-smoke.sh:3305 | `EDMV4-T11 AC1`'s line-count band on `bin/edm-gateguard` was widened from `200-400` to `200-500`, which is on its face an assertion weakened to let a ticket pass. Not actionable: the widening is recorded as decisions.md D42, the comment at `:3291-3303` names each of the five later tickets whose AC-mandated code grew the file, states explicitly that the bound is "widened, not removed", and requires the next ticket needing more to justify it in place. The band remains closed on both ends, preserving the property it was written to protect. |
| L4-022 | plugins/edm/bin/tests/wave6-smoke.sh:4079 | `check "... cites the Step-1-vs-agent-behaviour contract-violation shape" "agents/edm-test-integration.md:21-25" "$t23ac4_out"` pins a file:line-range citation embedded in an `edm-state` message. The citation is accurate against the current tree (verified: `agents/edm-test-integration.md:21-25` is exactly the N/A carve-out paragraph), and the assertion does test that the message still carries the pointer. It cannot detect the pointer going stale, since it compares against `edm-state`'s own literal rather than the target file. `wave8-smoke.sh:1107-1113` explicitly refuses to pin a line range for the same citation on G10/CA-340 grounds. Demoted: the assertion is not false today, and the drift risk is a documentation-accuracy concern (L6), not a false pass. |
| L4-023 | plugins/edm/bin/tests/wave8-smoke.sh:2685 | `[[ "$T18_SEED_HEADINGS" -gt 0 ]] \|\| fail "... the disjointness assertion below would pass vacuously"` is a guard that calls `fail` on breakage but never calls `pass` on success, so it contributes to the FAIL count but not the PASS count. This is deliberate and correct (a precondition, not an assertion); noted only because it makes the suite's assertion total asymmetric between the healthy and broken cases, which matters if anyone ever pins a total. Same shape at `:1322`. |
| L4-024 | plugins/edm/bin/tests/wave7-smoke.sh:6215 | `kill -INT -- "-${child_pid}" \|\| true` has the same swallowed-failure shape as L4-009, and the `set -m` process-group allocation it depends on does not work without a controlling terminal (e.g. under `nohup`). Demoted rather than reported as a false pass: unlike `harness-smoke.sh`, this case DOES assert the child's exit status against `130` at `:6227`, so a failed signal delivery surfaces as a named failing assertion rather than a silent green. The remaining gap is diagnosis quality (the failure reads as a cleanup regression rather than "the signal was never delivered"), not correctness. |
| L4-025 | plugins/edm/bin/tests/wave8-smoke.sh:5652 | `awk -v marker='..._mmessage="${_mrest#*$'"'"'\\t'"'"'}"'` was checked specifically against the `awk -v` escape-processing hazard named in this round's brief, and is CORRECT: the shell's single quotes pass `\\t` through literally, awk's own `-v` assignment processing then collapses `\\` to `\`, yielding the two literal characters `\t` that `bin/edm-hookify` actually contains -- not a real tab. The sibling at `:5677` is correct for the same reason. Recorded as a non-finding so a later reader does not re-flag it; also note that if either marker ever stopped matching, the paired controls at `:5656` and `:5681` would fail loudly rather than pass silently. |
