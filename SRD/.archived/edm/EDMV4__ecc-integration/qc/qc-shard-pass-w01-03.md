# QC Audit Report: EDMV4 Phase 6 wave 1 [Shard 3/3]

**Date**: 2026-09-02
**Tickets audited**: EDMV4-T21, EDMV4-T34, EDMV4-T48, EDMV4-T49
**Audited against**: `edm/edmv4-ecc-integration` @ `d43cacc` (T21/T34/T48), `ef1122e` (T49)
**Implementation mode**: `standard` (`.edm-state.json.implementation_mode`) -- the TDD compliance pass does not run.

**Merge-state caveat (read before merging).** `ef1122e` (EDMV4-T49) is **not** an ancestor of
`edm/edmv4-ecc-integration`. `git merge-base --is-ancestor ef1122e edm/edmv4-ecc-integration`
returns false; the commit currently lives only on `worktree-agent-a5210de8f14565c8e`. T49 below is
graded against `ef1122e`'s own tree. T21/T34/T48 are all ancestors of the integration branch and
were graded there.

## Summary

| Ticket | Title | Verdict |
|---|---|---|
| EDMV4-T21 | Grow ALL_LENS_IDS to fourteen and add the CONDITIONAL_LENS_IDS sibling | PASS |
| EDMV4-T34 | Add a size-classifier pre-step and a lifecycle_mode write path to the orchestrator dialog | PARTIAL |
| EDMV4-T48 | Have the first explorer write and refresh `SRD/.codemap.md` | FAIL |
| EDMV4-T49 | Write the eleven verified corrections back into `ecc-integration-analysis.md` | FAIL |

## Detailed Findings

### EDMV4-T21: Grow ALL_LENS_IDS to fourteen and add the CONDITIONAL_LENS_IDS sibling -- PASS

All 9 acceptance criteria verified statically. Two NOTED observations, neither a defect in this ticket.

- [x] AC1: `ALL_LENS_IDS="L1 L2 L3 L4 L5 L6 L7 L8 L9 L10 L11 L12 L13 L14"` -- `plugins/edm/bin/edm-state:1614`. Fourteen IDs, ascending.
- [x] AC2: `plugins/edm/bin/edm-state:1616` tests `-eq 14` and dies with `internal: ALL_LENS_IDS must enumerate exactly 14 lenses`. Neither the test nor the message retains `11`.
- [x] AC3: `grep -c 'eleven canonical' plugins/edm/bin/edm-state` returns **0** (executed). The comment at `:1608` now reads "the fourteen canonical code-audit lens IDs".
- [x] AC4: `CONDITIONAL_LENS_IDS="L13"` at `edm-state:1626`, immediately beside `ALL_LENS_IDS`; `wc -w`-based self-check `-eq 1` with `die` at `:1628`. Uses the same `echo $LIST | wc -w | tr -d ' '` bash-3.2 idiom (constraint C1 honoured -- no arrays).
- [x] AC5 (**guard D2 framing verified**): `edm-state:1618-1625` states "'Conditional' means a lens that may be auto-N/A when it is genuinely inapplicable to the target stack -- it is NEVER a lens excluded to save audit cost", names **guard D2** and `CLAUDE.md Sec."Prompt conventions (house style)"` at `:1619-1620`, and records D2's stated cost ("coverage loss disguised as an efficiency gain") at `:1622-1624`. Framed as genuine inapplicability, never as licence to skip for cost.
- [x] AC6 (**`die` verified**): load-time subset assertion at `edm-state:1633-1638` uses the required `case " $ALL_LENS_IDS " in *" $_cond_lens_id "*)` idiom (`:1634-1635`) and dies naming the offending ID at `:1636`: `internal: CONDITIONAL_LENS_IDS member '${_cond_lens_id}' is not a member of ALL_LENS_IDS`. `unset _cond_lens_id` at `:1639` leaves no loop residue.
- [x] AC7: **The tests/ scoping is legitimate; no second encoding is hidden.** Recursive scan of `plugins/edm/bin/` finds the literal `L1 L2 L3` in exactly one non-test source line -- the `ALL_LENS_IDS` declaration at `edm-state:1614`. The four `bin/tests/` matches are: `wave6-smoke.sh:957` and `:3572-3573` (pre-existing/explanatory comment text about the "for: L1 L2 L3 L4" over-reporting hazard -- example strings, not encodings), `:3577` (the scan itself), and `:3530`, which is the new smoke assertion pinning `ALL_LENS_IDS`' literal value. `:3530` is not a "caller re-encoding the list": an assertion that derived its expected value from the constant would be a tautology asserting nothing. Excluding `bin/tests/` targets what AC7 says it cares about ("no **caller** re-encodes"), and the exclusion matches the convention `wave6-smoke.sh:3249` (T62 AC10) already uses.
- [x] AC8: `# shellcheck disable=SC2086` preserved for `ALL_LENS_IDS` at `edm-state:1615`, and matching directives added at `:1627` (the `CONDITIONAL_LENS_IDS` length check) and `:1632` (the subset for-loop). No new shellcheck warning: `shellcheck -f gcc` output is byte-identical between `d292bee~1` and the branch tip (3 findings both sides, all at `:128`/`:132`, all pre-existing).
- [x] AC9: **Executed.** `bash plugins/edm/bin/edm-state --help` exits **0** and prints the usage block; neither self-check trips at source time.

**Deliberate-regression check (as instructed) -- CONFIRMED CORRECT.** `git diff d292bee~1 <branch> -- plugins/edm/bin/tests/wave6-smoke.sh` is **purely additive** around the T27 AC1 site. `wave6-smoke.sh:3511-3514` (the `--lenses L1..L11` / `round_type == "full"` assertion) appear as unchanged **context lines** in the diff -- genuinely left intact, not weakened, not silently patched. The explanatory comment sits *after* it at `:3515-3520` and correctly attributes the rewrite to EDMV4-T30. The failure is real and mechanically explained: `edm-state:4602-4608` derives `round_type` by comparing the sorted `--lenses` argument against sorted `$ALL_LENS_IDS`, so an 11-of-14 listing now yields `partial`.

**Findings**:
```
[NOTED] EDMV4-T21 | tickets/epics/04-audit-lenses.md:87 | AC#7: grep -n 'L1 L2 L3' plugins/edm/bin/ | The AC's literal command is ill-formed -- it lacks -r, so run unaliased it emits "grep: plugins/edm/bin/: Is a directory" and exits 2, returning zero lines rather than "exactly one line". Spec defect in the AC, not in the implementation; the substantive requirement is met (see AC7 above). No remediation needed in code.
[NOTED] EDMV4-T21 | plugins/edm/bin/edm-state:132 | AC#8: shellcheck reports no new warning | Pre-existing (identical at d292bee~1): shellcheck cannot parse the SC2086 directive at :132 and emits SC1072/SC1073, aborting analysis of the rest of the file. AC8's "no new warning" clause is therefore trivially true but vacuous -- shellcheck never reaches lines 1614-1639. Not introduced by T21 and outside its Out of Scope; flagged so nobody later reads AC8's green as real coverage of the new lines.
[NOTED] EDMV4-T21 | plugins/edm/bin/edm-state | Guard D2 runtime half | No --na-lenses / lenses_na token exists anywhere in bin/edm-state yet, so the "a caller naming any lens other than L13 as N/A must hard-die" behaviour the Description motivates at :58-63 is currently declarative only. Correct per this ticket's Out of Scope ("Any consumer of the two constants"); it becomes real when EDMV4-T22/T24 land. Flagged as a dependency risk, not a T21 defect.
```

---

### EDMV4-T34: Add a size-classifier pre-step and a lifecycle_mode write path -- PARTIAL

11 of 12 ACs verified statically. One AC carries a clause that requires a live suite run.

- [x] AC1: **Step 1b.5** at `plugins/edm/skills/orchestrator/SKILL.md:102`, positioned after Step 1b (`:76`) and before Step 1c (`:131`). Carries the identical "Skipped on resume (Step 1b already read a recorded non-default mode)." line at `:104` that Step 1c carries at `:133`.
- [x] AC2: `SKILL.md:106-108` -- "Scores exactly three signals -- files touched, new dependency or contract, and design ambiguity -- and takes the **highest** tier any single signal reaches; this is not an average." All three signals named; highest-tier-wins stated; "not an average" explicit.
- [x] AC3: `SKILL.md:110-114` table gives exactly three pairs, both members always stated: trivial `(standard, fix-pack)`, small `(mini-srd, standard)`, full `(standard, standard)`. `:116` -- "These are the only three pairs emitted."
- [x] AC4: `SKILL.md:116-119` -- "`fast-track` is never recommended: it shares one row of the mode matrix with `fix-pack` and the two are documented as behaviourally identical (`CLAUDE.md Sec."EDM mode matrix"`), so distinguishing them would require splitting that row, which is out of scope here." One sentence (soft-wrapped across four physical lines).
- [x] AC5: `SKILL.md:121-123` -- "State the computed recommendation in one line inside Step 1c's `AskUserQuestion` body, naming both members of the pair and which signal scored what tier". Reasoning requirement and both-members requirement both present.
- [x] AC6: `SKILL.md:137-139` -- `AskUserQuestion` header `"Lifecycle"` (9 chars, within `<=12`), options **Standard** / **fast-track** / **fix-pack**, same form as the mode question at `:135-136`; "(Recommended)" annotation is moved by Step 1b.5 per `:138-139`.
- [x] AC7: `SKILL.md:142-144` -- recording step now reads `edm-state set-mode <PREFIX> mode <value>`; `edm-state set-mode <PREFIX> lifecycle_mode <value>` **(only if not the `standard` default)**; `edm-state set-mode <PREFIX> compliance_enabled true` (only if On). Matches the `compliance_enabled` conditional-write pattern exactly.
- [x] AC8: Grep of `skills/orchestrator/SKILL.md` finds **no** `jq` write, **no** bare `edm-state set`, and no mode/lifecycle_mode setter other than `set-mode` (`:142`, `:143`, plus the `:128-129` prose reference). Both writes land in `cmd_set_mode` (`bin/edm-state:5099`), whose `lifecycle_mode)` arm at `:5128-5131` validates against `LIFECYCLE_MODE_ENUM_LIST` (`:808`, `standard fast-track fix-pack`) and dies on an invalid value. Exactly one write mechanism per field.
- [x] AC9: `SKILL.md:125-127` -- "This step only marks which existing Step 1c option carries "(Recommended)". It never auto-applies a mode and never calls `set-mode` itself".
- [x] AC10: `SKILL.md:126-129` -- "the classifier is a default, not an enforcement. The user always confirms or overrides via Step 1c, and whatever is selected (recommended or not) is recorded through the same `edm-state set-mode <PREFIX> mode|lifecycle_mode <value>` path".
- [x] AC11: The lifecycle question is item 2 of Step 1c, which carries "Skipped on resume" at `:133`. The resume branch is **untouched** -- `git show aee1eed -- .../SKILL.md` shows the first changed hunk starts at `@@ -100,17 +100,50 @@`, entirely below `:90-95`.
- [ ] AC12: **split verdict.**
  - "Step 1b.5's prose is under 30 lines" -- **PASS**: `SKILL.md:102-129` is 28 lines.
  - "`edm-check-skill-sync` ... asserts the dispatcher holds no phase procedure body" -- **PASS, executed**: `bash plugins/edm/bin/edm-check-skill-sync` exits **0**, printing `CLEAN (dispatcher holds no phase procedure; every phase skill owns its own; no skill blocks Skill-tool dispatch)`. Step 1b.5 is dispatch-and-gate prose only -- scoring instructions, a destination table, and a "(Recommended)" annotation rule; it runs no phase.
  - "`bash plugins/edm/bin/tests/run-all.sh` passes" -- **PARTIAL** (runtime-only).

**Guard D6 check (as instructed) -- CLEAN.** Step 1b.5 cites the mode matrix by section reference twice (`:118`, `:123`) and never restates what any mode does. `:122-123` makes the prohibition explicit in the instruction itself: "never the destination's behaviour (`CLAUDE.md Sec."EDM mode matrix"` covers that; consult it, do not restate it here)". The one adjacent statement -- `:117-118`, that `fast-track` and `fix-pack` "are documented as behaviourally identical" -- is mandated verbatim by AC4 and is a claim about the matrix's row structure, not a restatement of either mode's behaviour. `aee1eed` also touched no file other than `SKILL.md`, honouring the ticket's "Any change to `bin/edm-state`" exclusion.

**Finding**:
```
[PARTIAL] EDMV4-T34 | AC#12: `bash plugins/edm/bin/tests/run-all.sh` passes | runtime-check: run the full aggregator from a real git checkout of the integration branch and assert exit 0. It cannot be settled statically, and it cannot currently be green: EDMV4-T21 deliberately left wave6-smoke.sh:3511-3514 red (an 11-of-14 --lenses listing now derives round_type=partial via edm-state:4602-4608), and wave7-smoke.sh carries nine analogous -eq 11 assertions. Both belong to EDMV4-T30. Expect this to close only after T30 lands; re-run then. (An exported-tree run from /tmp aborted at exit 128 partway through wave6 -- the suite needs a real git worktree, so no aggregate result is available from this audit.)
```

---

### EDMV4-T48: Have the first explorer write and refresh `SRD/.codemap.md` -- FAIL

7 of 9 ACs pass. Two documentation ACs are partially met.

- [x] AC1: `plugins/edm/agents/edm-explorer.md:71-90` instructs the **first** explorer to write `SRD/.codemap.md` if absent or refresh it if present. Critically, it **amends** rather than contradicts the existing single-write-path sentence: `:65` now reads "This and the codemap path below (`SRD/.codemap.md`) are the only two permitted write paths -- writing anywhere else is a contract violation", and `:74-75` calls it "a second, permitted write path". This is exactly what the ticket's Technical Notes required.
- [x] AC2: `edm-explorer.md:77-80` -- "A codemap describes the repository's **current** architecture; `architecture.md` ... describes that initiative's **target** architecture -- the two never duplicate or contradict each other because they answer different questions." One sentence.
- [x] AC3: `edm-explorer.md:74-77` -- "`${user_config.srd_root}/.codemap.md` ... It lives at the `srd_root` root, not inside this initiative's own directory, because it is shared and reused across every initiative rather than scoped to one." Location and reason both stated.
- [x] AC4: `edm-explorer.md:82-86` -- "If it already exists, `Read` it first and **refresh** it ... rather than rewriting it from scratch, and append a short "Refreshed" note naming which sections you touched and this initiative's prefix." Also correctly warns at `:85-86` that a refresh must be a full `Write` because `Edit` is denied in frontmatter (`:10`).
- [ ] AC5: **FAIL (partial).** The operative rule is present and correct at `edm-explorer.md:87-88` -- "Omit any section you have nothing real to say about -- never fill it with a placeholder or an instruction to the reader". But AC5's second clause ("That is the exact failure ECC's generator ships and **is named as the reason**") is unmet: the strings `ECC` and `generate.ts` appear nowhere in `edm-explorer.md`. The reason given is generic ("an omitted section is a correct, honest codemap, not an incomplete one"). `:89` mentions "No generator script produces, refreshes, or validates this file", but that is AC6's content -- it does not name ECC's unconditional-template-string failure as the origin of the omit rule.
- [x] AC6: **No generator exists anywhere.** `grep -rn -i 'codemap' plugins/edm/bin/` returns matches only in `bin/tests/wave8-smoke.sh` -- and those are the assertions that a generator does *not* exist (`:266-276`). `grep -i codemap plugins/edm/bin/edm-state` returns nothing: no subcommand. A tree-wide scan across `*.md`/`*.json`/`*.sh`/`*.ts`/`*.js`/`*.py` finds codemap references only in `edm-explorer.md`, `CLAUDE.md`, `wave8-smoke.sh` and the analysis/SRD documents. `0c8ac40` touched exactly two files, both markdown (`CLAUDE.md`, `edm-explorer.md`). D11 honoured.
- [ ] AC7: **FAIL (partial).** `edm-explorer.md:92-95` delivers the ASCII-only requirement and three of the seven facts AC7 enumerates: that no automatic lint pass reaches `SRD/.codemap.md`, that prefix mode resolves one initiative directory, and that `--all` iterates initiative directories. **Three required facts are absent**: (a) that the git-commit hook runs prefix mode; (b) that only the manual `--path` mode reaches the `SRD/` root and nothing invokes it automatically; (c) that the codemap is covered by **`EDMV4-57`'s manual `--path` sweep and by nothing else**. Omitting (b) and (c) leaves a reader with the wrong fact -- that the codemap has no lint coverage at all, rather than that it has exactly one, manual, named source of coverage. Nothing in `plugins/edm/CLAUDE.md:92-94` or `:142-147` supplies the missing three either.
- [x] AC8: `edm-explorer.md:92-93` states the ASCII rule inline: "The file is ASCII-only: no em dashes (use `--`), no arrows (use `->`), straight quotes only, no emoji -- hold yourself to this explicitly".
- [x] AC9: `plugins/edm/CLAUDE.md:92-94` adds `+-- .codemap.md   <- current-architecture map (Should/on-demand); written and refreshed by the first explorer of each initiative (agents/edm-explorer.md); shared across every initiative` to the layout tree at the `SRD/` root level. The current-versus-target distinction is stated in the annotations beneath, under **Canonical artifact homes** at `:142-147`.

**Findings**:
```
[P1] EDMV4-T48 | plugins/edm/agents/edm-explorer.md:92-95 | AC#7: the gap in its automatic coverage is stated as fact | Three of the seven required facts are missing. The text stops at "no automatic lint pass reaches SRD/.codemap.md ... neither reaches the SRD/ root itself" and never states that the git-commit hook runs prefix mode, that only the manual --path mode reaches the SRD/ root with nothing invoking it automatically, or that EDMV4-57's manual --path sweep is the codemap's sole coverage. As written the reader concludes there is no coverage at all, which is the wrong fact.
[P2] EDMV4-T48 | plugins/edm/agents/edm-explorer.md:87-88 | AC#5: ... never filled with a placeholder ... That is the exact failure ECC's generator ships and is named as the reason | The omit-never-placeholder rule is present and operative, but ECC's generator is not named as the reason -- "ECC" and "generate.ts" do not appear in the file. The generic justification at :88 does not satisfy the AC's second clause. Prose-only; no behavioural impact, which is why this is P2 rather than P1.
```

---

### EDMV4-T49: Write the eleven verified corrections back into `ecc-integration-analysis.md` -- FAIL

10 of 12 ACs pass. One P1 correctness defect and one P2 completeness defect.

- [x] AC1 (pre-flight): The file pre-existed. `git show ef1122e~1:plugins/edm/docs/ecc-integration-analysis.md | wc -l` = **1024**, matching the ticket's Technical Note exactly; the commit is a 79-insert/13-delete modification, not a creation. No newly-invented file at that path.
- [x] AC2: All eleven corrections landed, and the commit message carries the correction-number -> Part checklist. I re-derived the mapping independently from the section header offsets (Part 1.2 `:26`, 1.6 `:116`, 4.1 `:256`, 4.2 `:356`, 4.3 `:430`, 4.4 `:483`, 5.1 `:567`, 5.2 `:620`, 5.3 `:685`, 5.4 `:747`, 8.2 `:982`) and **every one of the eleven checklist rows is accurate**: 1 -> `:37`+`:118`; 2 -> `:454`; 3 -> `:739`; 4 -> `:589`; 5 -> `:761`; 6 -> `:520`; 7 -> `:654`; 8 -> `:364`+`:372`; 9 -> `:322`; 10 -> `:296`; 11 -> `:989`. Eleven, not ten.
- [x] AC3: The Part 8.2 exemplar shape is a bolded self-correction clause ("**This corrects an earlier assessment of mine that treated it as a neutral readiness rubric.**"). All eleven new entries at `:996-1035` match it -- "**This corrects that summary total**", "**This corrects a citation the document repeated from ECC without checking**", "**This corrects that assumption**", "**This corrects that listing**", "**This corrects an incomplete table**" (x2), "**This corrects that figure**", "**This corrects that count**", "**This corrects both**", "**This corrects the omission**", "**This corrects that line range**". Each names the old claim, the new claim and why, so no correction is a silent overwrite.
- [x] AC4: `:37` (`| Hook registrations | 23 |`) and `:118` ("registers 23 hooks across seven events"). The per-event table at `:120-129` is **unchanged by the diff** and is internally consistent with the corrected total: seven rows summing to 8+2+2+7+2+1+1 = **23**. Correction 1 at `:998-1000` says explicitly "The per-event table's own rows were already correct; only the summary total and event-type count were wrong."
- [x] AC5: Located by grep, not by the SRD's line numbers. The **pre-image had exactly one** live citation of `rules/common/security.md` for the seven triggers (`ef1122e~1:449`), and it is corrected at `:450-456` to `orch-pipeline/SKILL.md:100-104`. The only two post-image occurrences of that path (`:454`, `:1002`) are inside the correction text itself. The ECC-origin note is recorded at `:1004-1006`: "`orch-pipeline/SKILL.md:100-104`, which itself miscites `security.md` -- that miscitation is how the error propagated here."
- [x] AC6: `:739-744` -- the Part 5.3 effort estimate is annotated in place: "**Effort**: medium -- and this estimate assumes no evaluator exists to adapt. **ECC has no rule evaluator at all** ... EDM therefore builds the evaluator entirely from scratch; only the JSON rule-file *format* is reusable." Both halves of AC6 (no evaluator + explicit estimate annotation) present.
- [ ] AC7: **FAIL.** The caller-count half is correct and verified: `:363-364` says "called mid-phase by six skills (`implement`, `code-audit`, `audit-tickets`, `audit-srd`, `test`, `test-coverage`)", and `grep -rln update-patterns plugins/edm/skills/` returns exactly those **six** files. The replacement citations are also correct against the tree -- I confirmed `bin/edm-state:5607` is `local patterns_dir="${SCRIPT_DIR}/../docs/audit-patterns"` and `:5640` is the `pattern directory is not writable` echo, so the AC's own `:5595`/`:5627-5629` are the stale numbers and the implementer's re-verification (which AC7 itself mandates) was right. **But correction 8 was not applied at every site**: `:984` still reads ``- The `update-patterns` read-only skip (4.2) -- confirmed at `bin/edm-state:5577` and `:5624`.`` The stale pair survives inside Part 8.2 itself, 39 lines above correction 8 which declares those exact numbers wrong. The sibling bullet at `:989` *was* updated for correction 11 (`:200-240` -> `:225-231`), so the bullet list was edited and this line was missed.
- [x] AC8: All seven figures present with their citations -- `GAN_EVAL_CRITERIA` dead code, `scripts/gan-harness.sh` never reads it (`:589-591`); `delivery-gate`'s undocumented 30GB tier between 50GB and 15GB (`:761`); `silent-failure-hunter` body 44 lines not "~30", others 35 and 39 (`:520-522`); `harness-audit.js` consumer mode 16 checks / 39 points once the unconditionally-appended GitHub checks are counted (`:654-655`); `GATEGUARD_DISABLED=1` as a second independent kill switch recognizing only the literal `'1'`, `gateguard-fact-force.js:732-734` (`:322`); `MultiEdit` one retry **per still-unchecked file**, `gateguard-fact-force.js:1234-1256` (`:296-299`); codemaps placeholders at `generate.ts:225-231` (`:989`).
- [x] AC9: Part 8.3 is updated at `:1050-1054`, recording the `zunoworks/gateguard` licence as verified MIT with the quoted `LICENSE` text and the `SRD/edm/EDMV4__ecc-integration/decisions.md` D13 citation. **The AC's premise does not hold** and I graded it on that basis rather than on its literal wording: the pre-image Part 8.3 list (`ef1122e~1`) contains four bullets -- the GateGuard +2.25 result, the harness-audit rubric weights, the `orch-*` wrappers, and the test suite -- and **no licence entry at all**. Nothing could "move out of the unverified list" because nothing was in it. The implementer's handling (record the verified fact rather than fabricate a bullet to delete) is the correct response to a false premise per `CLAUDE.md Sec."Unverifiable acceptance criteria (D15)"`, so AC9 passes on its satisfiable content. The delivered wording overstates it, which is filed separately below.
- [ ] AC10: **FAIL (partial).** The "left intact" half passes cleanly: `ef1122e` touches **only** `plugins/edm/docs/ecc-integration-analysis.md`, so `planning.md Sec."Corrections to the source document"` (`planning.md:45`) is untouched and no deletion occurred. The "cross-reference each other" half is one-directional: `planning.md:3` and `:278` point at the analysis, but the amended analysis never points back at `planning.md`'s correction table. Its Part 8.2 block cites only "EDMV4 Phase 1 (2026-09-02)" (`:996`) and `decisions.md` D13 (`:1053`). The one `planning.md` mention in the document, at `:677`, is pre-existing prose about `/edm:audit-harness` findings and is unrelated.
- [x] AC11: **Executed.** `LC_ALL=C grep -c '[^ -~\t]'` on the amended document returns **0** -- fully ASCII, normalized in place. `grep -n edm-lint-ignore` returns nothing: no marker was added, so the exemption route the AC forbids was not taken.
- [x] AC12: **Executed.** `bash plugins/edm/bin/edm-lint-artifacts --path plugins/edm/docs/` exits **0**, printing `--path plugins/edm/docs/ -- CLEAN (no violations found)`.

**Findings**:
```
[P1] EDMV4-T49 | plugins/edm/docs/ecc-integration-analysis.md:984 | AC#7: Correction 8 updates the update-patterns line citations | The stale pair survives. Line 984 still asserts, as a claim "verified against source", "confirmed at `bin/edm-state:5577` and `:5624`" -- the exact numbers correction 8 at :1023-1026 declares wrong. Verified against the tree: :5607 is the patterns_dir computation and :5640 is the read-only skip message; :5577 and :5624 are neither. The document now contradicts itself inside the very section that exists to correct it. Fix: change :984 to "confirmed at `bin/edm-state:5607` and `:5640`".
[P2] EDMV4-T49 | plugins/edm/docs/ecc-integration-analysis.md:996-1035 | AC#10: the two documents cross-reference each other rather than one replacing the other | One-directional. planning.md:3 and :278 reference the analysis, but the analysis's new Part 8.2 block never names planning.md's correction table as the evidence trail -- it cites only "EDMV4 Phase 1" and decisions.md D13. Fix: add one pointer to `SRD/edm/EDMV4__ecc-integration/planning.md Sec."Corrections to the source document"` in the Part 8.2 preamble at :996-997.
[P2] EDMV4-T49 | plugins/edm/docs/ecc-integration-analysis.md:1050-1051 | AC#9: the licence ... moves out of the unverified list | The delivered heading reads "**Resolved and moved out of this list (2026-09-02):**" and closes "It no longer belongs among the claims not verified." Nothing moved: the pre-image Part 8.3 list at ef1122e~1 never contained a licence bullet. In a section whose entire purpose is verification honesty, asserting a removal that never happened is the class of claim this ticket exists to eliminate. Fix: reword to record the verification without implying a prior listing, e.g. "**Verified since this list was written (2026-09-02):** ... This claim was not previously enumerated above; it is recorded here for completeness."
[NOTED] EDMV4-T49 | tickets/epics/07-inherited-tickets.md AC5/AC7 | Two ticket-spec inaccuracies, no code impact | (a) AC5 names "Part 4.2 at :360,368" as a security-trigger site; those lines are correction 8's update-patterns citations, and the only security-trigger site was :449. (b) AC7's replacement citations :5595/:5627-5629 are themselves stale against the tree; the implementer's :5607/:5640 are correct, consistent with the epic's own advisory banner ("Locate every site by symbol name or by the literal string the AC quotes, at edit time"). Neither is a defect in the delivered work.
[NOTED] EDMV4-T49 | branch state | ef1122e is not an ancestor of edm/edmv4-ecc-integration | The commit sits on worktree-agent-a5210de8f14565c8e and has not been merged. Graded against its own tree; the merge must pick it up or T49 ships nothing.
```

## Remediation Required

Four FAIL findings, in priority order. PARTIAL findings are not remediated here -- EDMV4-T34 AC12 is closed by `/edm:verify-runtime` before archive.

1. **[P1] EDMV4-T49** -- `plugins/edm/docs/ecc-integration-analysis.md:984`. Replace `` `bin/edm-state:5577` and `:5624` `` with `` `bin/edm-state:5607` and `:5640` ``. This is the same correction 8 the ticket already applied at `:364` and `:372`; the Part 8.2 bullet was missed while its neighbour at `:989` was updated. Leaving it makes the corrections section self-contradictory.
2. **[P1] EDMV4-T48** -- `plugins/edm/agents/edm-explorer.md:92-95`. Extend the coverage-gap paragraph with the three missing facts: the git-commit hook runs prefix mode; only the manual `--path` mode reaches the `SRD/` root and nothing invokes it automatically; `EDMV4-57`'s manual `--path` sweep is the codemap's only coverage. Roughly two sentences.
3. **[P2] EDMV4-T49** -- `ecc-integration-analysis.md:996-997`, add a cross-reference to `SRD/edm/EDMV4__ecc-integration/planning.md Sec."Corrections to the source document"`; and `:1050-1051`, reword the licence note so it does not claim a removal from a list that never held the entry.
4. **[P2] EDMV4-T48** -- `plugins/edm/agents/edm-explorer.md:87-88`. Add the reason clause naming ECC's generator: its Data Flow and External Dependencies sections ship as literal unconditional template strings, which is the exact failure the omit rule prevents.

<!-- QC-SHARD-COMPLETE range=EDMV4-T21..EDMV4-T49 assigned=4 audited=4 -->
