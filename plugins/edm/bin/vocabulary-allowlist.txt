# EDMV3-T30: allowlist for bin/edm-check-vocabulary.
#
# Format: a one-line "#" justification comment, immediately followed by one
# "<path>|<substring>" data line. A violation is suppressed only when BOTH the reported
# file matches <path> (an exact repo-relative file path, or a "/"-suffixed directory used
# as a prefix match) AND the reported snippet contains <substring> verbatim. An empty
# <substring> (nothing after the "|") allowlists every match under that path unconditionally.
# Blank lines are ignored; every non-comment line must be a "<path>|..." data line.
#
# Class 1: the NOTED-versus-deferral clarification in CLAUDE.md is intentional -- the
# Severity vocabulary section must name the abolished words in order to state that they no
# longer apply (EDMV3-T29 AC2).
plugins/edm/CLAUDE.md|is distinct from deferral
# Class 1 (continued): second line of the same clarification sentence pair.
plugins/edm/CLAUDE.md|deferral does not exist in this methodology
# Class 2: the checker's own prohibited-token list is data describing the banned words, not
# an instance of the policy violation itself.
plugins/edm/bin/vocabulary-prohibited.txt|
# Class 2 (continued): the checker's own allowlist file (this file) names the same tokens in
# its justification comments and path/substring columns -- also data, not policy prose.
plugins/edm/bin/vocabulary-allowlist.txt|
# Class 3: CHANGELOG.md is project history. Past release notes describe the vocabulary that
# was canonical at release time and are not rewritten retroactively.
plugins/edm/CHANGELOG.md|
# Class 4: bin/tests/ negative-test cases must contain the literal flags/phrases verbatim to
# assert that they are rejected (EDMV3-16 AC10, EDMV3-17); without this carve-out the checker
# and the required negative tests would be mutually unsatisfiable.
plugins/edm/bin/tests/|
# Class 5: agents/edm-audit-spec.md's False-Alarm-Filter criterion asks whether the AUDITED
# project's own tickets mark a requirement deferred/out of scope -- a legitimate, substantive
# filter criterion about the project under audit, not this methodology's own vocabulary
# (EDMV3-32 forbids deleting a filter criterion, and editing that file's wording is outside
# this ticket's file boundary -- EDMV3-T29's agent owns it).
plugins/edm/agents/edm-audit-spec.md|deferred

# Class 6: bin/edm-state's legacy-status coercion. This is the code that ABOLISHES the
# `deferred` status -- the blocking predicate deliberately treats a legacy `deferred` line as
# open, and coerce_status rewrites it to open on read (EDMV3-T25 AC4 / EDMV3-T28 AC5). The
# token must appear here precisely because the mechanism exists to neutralize it; removing the
# word would remove the neutralization. Every other occurrence in bin/ remains a violation.
plugins/edm/bin/edm-state|deferred
